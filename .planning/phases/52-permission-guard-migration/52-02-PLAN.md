---
phase: 52-permission-guard-migration
plan: 02
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - trade-flow-api/src/subscription/guards/subscription.guard.ts
  - trade-flow-api/src/subscription/test/guards/subscription.guard.spec.ts
  - trade-flow-api/src/business/policies/business.policy.ts
  - trade-flow-api/src/customer/policies/customer.policy.ts
  - trade-flow-api/src/job/policies/job.policy.ts
  - trade-flow-api/src/job/policies/job-type.policy.ts
  - trade-flow-api/src/quote/policies/quote.policy.ts
  - trade-flow-api/src/quote/policies/quote-line-item.policy.ts
  - trade-flow-api/src/quote-settings/policies/quote-settings.policy.ts
  - trade-flow-api/src/schedule/policies/schedule.policy.ts
  - trade-flow-api/src/visit-type/policies/visit-type.policy.ts
  - trade-flow-api/src/tax-rate/policies/tax-rate.policy.ts
  - trade-flow-api/src/item/policies/item.policy.ts
  - trade-flow-api/src/estimate/policies/estimate.policy.ts
  - trade-flow-api/src/estimate/policies/estimate-line-item.policy.ts
  - trade-flow-api/src/estimate-settings/policies/estimate-settings.policy.ts
  - trade-flow-api/src/user/policies/support-role.policy.ts
  - trade-flow-api/src/migration/policies/migration.policy.ts
  - trade-flow-api/src/user/utilities/is-support-user.utility.ts
  - trade-flow-api/src/user/utilities/is-super-user.utility.ts
autonomous: true
requirements:
  - RBAC-09
  - RBAC-10

must_haves:
  truths:
    - "SubscriptionGuard uses hasPermission(user, 'bypass_subscription') instead of supportRoles.length > 0"
    - "All 15 policy files use hasPermission(authUser, 'view_support_dashboard') instead of isSupportUser(authUser)"
    - "MigrationPolicy uses hasPermission(authUser, 'manage_migrations') instead of isSuperUser(authUser)"
    - "isSupportUser() and isSuperUser() utility files are deleted from the codebase"
    - "No file in the codebase imports from is-support-user.utility or is-super-user.utility"
    - "All existing tests pass after migration with updated mocks"
    - "Solo business users experience zero change in behavior (RBAC-10)"
  artifacts:
    - path: "trade-flow-api/src/subscription/guards/subscription.guard.ts"
      provides: "Permission-based support bypass"
      contains: "hasPermission(user, \"bypass_subscription\")"
    - path: "trade-flow-api/src/business/policies/business.policy.ts"
      provides: "Permission-based support check in canRead/canUpdate"
      contains: "hasPermission(authUser, \"view_support_dashboard\")"
    - path: "trade-flow-api/src/migration/policies/migration.policy.ts"
      provides: "Permission-based super user check in all CRUD methods"
      contains: "hasPermission(authUser, \"manage_migrations\")"
  key_links:
    - from: "trade-flow-api/src/subscription/guards/subscription.guard.ts"
      to: "trade-flow-api/src/auth/utilities/has-permission.utility.ts"
      via: "hasPermission import"
      pattern: "import.*hasPermission.*from.*@auth/utilities/has-permission"
    - from: "trade-flow-api/src/business/policies/business.policy.ts"
      to: "trade-flow-api/src/auth/utilities/has-permission.utility.ts"
      via: "hasPermission import"
      pattern: "import.*hasPermission.*from.*@auth/utilities/has-permission"
---

<objective>
Migrate all hardcoded role checks to the permission system and delete the old utilities.

Purpose: Every `isSupportUser()` and `isSuperUser()` call site across the entire codebase is replaced with `hasPermission()` checks, and the SubscriptionGuard support bypass uses `bypass_subscription` permission. This completes the RBAC migration -- role-name-based authorization no longer exists in the codebase.
Output: 17 migrated files (1 guard + 15 isSupportUser policies + 1 isSuperUser policy), 2 deleted utility files, updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-permission-guard-migration/52-CONTEXT.md
@.planning/phases/52-permission-guard-migration/52-RESEARCH.md
@.planning/phases/52-permission-guard-migration/52-PATTERNS.md
@.planning/phases/52-permission-guard-migration/52-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From trade-flow-api/src/auth/utilities/has-permission.utility.ts:
```typescript
export function hasPermission(user: IUserDto, permission: string): boolean;
```

From trade-flow-api/src/auth/utilities/has-any-permission.utility.ts:
```typescript
export function hasAnyPermission(user: IUserDto, permissions: string[]): boolean;
```

<!-- Existing code being migrated -->

From trade-flow-api/src/user/utilities/is-support-user.utility.ts:
```typescript
export function isSupportUser(authUser: IUserDto): boolean {
  return authUser.supportRoles.some((role) =>
    [SupportRoleName.SUPER_USER, SupportRoleName.SUPPORT_ADMINISTRATOR].includes(role.roleName as SupportRoleName),
  );
}
```

From trade-flow-api/src/user/utilities/is-super-user.utility.ts:
```typescript
export function isSuperUser(authUser: IUserDto): boolean {
  return authUser.supportRoles.some((role) => role.roleName === SupportRoleName.SUPER_USER);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate SubscriptionGuard and all 15 isSupportUser policy files</name>
  <files>
    trade-flow-api/src/subscription/guards/subscription.guard.ts,
    trade-flow-api/src/business/policies/business.policy.ts,
    trade-flow-api/src/customer/policies/customer.policy.ts,
    trade-flow-api/src/job/policies/job.policy.ts,
    trade-flow-api/src/job/policies/job-type.policy.ts,
    trade-flow-api/src/quote/policies/quote.policy.ts,
    trade-flow-api/src/quote/policies/quote-line-item.policy.ts,
    trade-flow-api/src/quote-settings/policies/quote-settings.policy.ts,
    trade-flow-api/src/schedule/policies/schedule.policy.ts,
    trade-flow-api/src/visit-type/policies/visit-type.policy.ts,
    trade-flow-api/src/tax-rate/policies/tax-rate.policy.ts,
    trade-flow-api/src/item/policies/item.policy.ts,
    trade-flow-api/src/estimate/policies/estimate.policy.ts,
    trade-flow-api/src/estimate/policies/estimate-line-item.policy.ts,
    trade-flow-api/src/estimate-settings/policies/estimate-settings.policy.ts,
    trade-flow-api/src/user/policies/support-role.policy.ts
  </files>
  <read_first>
    trade-flow-api/src/subscription/guards/subscription.guard.ts,
    trade-flow-api/src/business/policies/business.policy.ts,
    trade-flow-api/src/customer/policies/customer.policy.ts,
    trade-flow-api/src/user/policies/support-role.policy.ts,
    trade-flow-api/src/user/utilities/is-support-user.utility.ts
  </read_first>
  <action>
**SubscriptionGuard migration (per D-10):**
In `src/subscription/guards/subscription.guard.ts`:
1. Remove the import of `isSupportUser` (if present) or any direct role-length check
2. Add import: `import { hasPermission } from "@auth/utilities/has-permission.utility";`
3. Replace the support bypass block (approximately lines 47-50):
   - BEFORE: `if (user.supportRoles && user.supportRoles.length > 0) { return true; }`
   - AFTER: `if (hasPermission(user, "bypass_subscription")) { return true; }`
4. No other changes to SubscriptionGuard logic

**Policy migration -- 15 files using isSupportUser() (per D-04, D-06):**
Each policy file gets the same mechanical change:
1. Remove: `import { isSupportUser } from "@user/utilities/is-support-user.utility";`
2. Add: `import { hasPermission } from "@auth/utilities/has-permission.utility";`
3. Replace every `isSupportUser(authUser)` call with `hasPermission(authUser, "view_support_dashboard")`
4. No other logic changes -- the surrounding if-block and return pattern stays identical

The 15 files and their `isSupportUser()` call counts:
- `src/business/policies/business.policy.ts` -- 2 calls (canRead, canUpdate)
- `src/customer/policies/customer.policy.ts` -- 3 calls (canCreate, canRead, canUpdate)
- `src/job/policies/job.policy.ts` -- 3 calls (canCreate, canRead, canUpdate)
- `src/job/policies/job-type.policy.ts` -- 3 calls (canCreate, canRead, canUpdate)
- `src/quote/policies/quote.policy.ts` -- 3 calls (canCreate, canRead, canUpdate)
- `src/quote/policies/quote-line-item.policy.ts` -- 2 calls (canCreate, canRead)
- `src/quote-settings/policies/quote-settings.policy.ts` -- 2 calls (canRead, canUpdate)
- `src/schedule/policies/schedule.policy.ts` -- 3 calls (canCreate, canRead, canUpdate)
- `src/visit-type/policies/visit-type.policy.ts` -- 3 calls (canCreate, canRead, canUpdate)
- `src/tax-rate/policies/tax-rate.policy.ts` -- 3 calls (canCreate, canRead, canUpdate)
- `src/item/policies/item.policy.ts` -- 3 calls (canCreate, canRead, canUpdate)
- `src/estimate/policies/estimate.policy.ts` -- 3 calls (canCreate, canRead, canUpdate)
- `src/estimate/policies/estimate-line-item.policy.ts` -- 2 calls (canCreate, canRead)
- `src/estimate-settings/policies/estimate-settings.policy.ts` -- 2 calls (canRead, canUpdate)
- `src/user/policies/support-role.policy.ts` -- 1 call (canRead)

After modifying all 16 files (1 guard + 15 policies), grep the entire codebase to confirm zero remaining imports of `is-support-user.utility`:
```bash
grep -r "is-support-user" src/ --include="*.ts" | grep -v ".spec.ts" | grep -v "test/"
```
This should return zero results (excluding test files which are handled in Task 2).
  </action>
  <verify>
    <automated>cd trade-flow-api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/subscription/guards/subscription.guard.ts` contains `hasPermission(user, "bypass_subscription")`
    - `src/subscription/guards/subscription.guard.ts` does NOT contain `supportRoles.length > 0` or `supportRoles && user.supportRoles.length`
    - `src/subscription/guards/subscription.guard.ts` imports from `@auth/utilities/has-permission.utility`
    - All 15 policy files contain `import { hasPermission } from "@auth/utilities/has-permission.utility"`
    - All 15 policy files contain `hasPermission(authUser, "view_support_dashboard")`
    - No policy file contains `import.*isSupportUser.*from.*is-support-user`
    - No non-test `.ts` file imports from `@user/utilities/is-support-user.utility`
    - TypeScript compilation succeeds (`npx tsc --noEmit` exits 0)
  </acceptance_criteria>
  <done>SubscriptionGuard uses bypass_subscription permission, all 15 policy files use view_support_dashboard permission, zero remaining isSupportUser imports in production code</done>
</task>

<task type="auto">
  <name>Task 2: Migrate MigrationPolicy, delete old utilities, and update all tests</name>
  <files>
    trade-flow-api/src/migration/policies/migration.policy.ts,
    trade-flow-api/src/user/utilities/is-support-user.utility.ts,
    trade-flow-api/src/user/utilities/is-super-user.utility.ts,
    trade-flow-api/src/subscription/test/guards/subscription.guard.spec.ts
  </files>
  <read_first>
    trade-flow-api/src/migration/policies/migration.policy.ts,
    trade-flow-api/src/user/utilities/is-super-user.utility.ts,
    trade-flow-api/src/subscription/test/guards/subscription.guard.spec.ts,
    trade-flow-api/src/business/test/policies/business.policy.spec.ts,
    trade-flow-api/src/business/test/mocks/business-mock-generator.ts,
    trade-flow-api/src/user/data-transfer-objects/permission.dto.ts
  </read_first>
  <action>
**MigrationPolicy migration (per D-04, D-06):**
In `src/migration/policies/migration.policy.ts`:
1. Remove: `import { isSuperUser } from "@user/utilities/is-super-user.utility";`
2. Add: `import { hasPermission } from "@auth/utilities/has-permission.utility";`
3. Replace all 4 `isSuperUser(authUser)` calls with `hasPermission(authUser, "manage_migrations")`
4. The 4 methods are: `canCreate`, `canRead`, `canUpdate`, `canDelete` (all return the same check)

**Delete old utilities (per D-04):**
Delete these two files entirely:
- `src/user/utilities/is-support-user.utility.ts`
- `src/user/utilities/is-super-user.utility.ts`

After deletion, grep to confirm zero remaining imports anywhere (including tests):
```bash
grep -r "is-support-user.utility\|is-super-user.utility" src/ --include="*.ts"
```
This MUST return zero results. If any test files still import the old utilities, update them.

**Update SubscriptionGuard test:**
In `src/subscription/test/guards/subscription.guard.spec.ts`:
- Update the test case that verifies support user bypass
- The mock user must now have a support role with `permissions: DtoCollection.create([{ id: "perm-1", name: "bypass_subscription", description: "Bypass subscription checks", category: "billing" }])`
- Previously the test may have checked `supportRoles.length > 0` -- now the guard checks `hasPermission(user, "bypass_subscription")` so the mock must carry that permission
- Add a negative test: user with supportRoles but WITHOUT `bypass_subscription` permission should NOT bypass subscription check

**Update policy tests:**
Search for ALL policy spec files that reference `isSupportUser` or mock support users:
```bash
grep -rl "isSupportUser\|is-support-user" src/ --include="*.spec.ts"
```
For each found test file:
- Remove imports of `isSupportUser` or `is-support-user.utility`
- Update support user mocks to include `permissions: DtoCollection.create([{ id: "perm-1", name: "view_support_dashboard", description: "View support dashboard", category: "support" }])` on the support role DTO
- The test assertions stay the same (support user can access all resources) -- only the mock structure changes

For migration policy tests (if they exist):
```bash
grep -rl "isSuperUser\|is-super-user" src/ --include="*.spec.ts"
```
Update super user mocks to include `permissions: DtoCollection.create([{ id: "perm-1", name: "manage_migrations", description: "Manage migrations", category: "support" }])` on the support role DTO

**Final verification:**
Run full CI gate to confirm no regressions:
```bash
cd trade-flow-api && npm run ci
```
  </action>
  <verify>
    <automated>cd trade-flow-api && npm run ci</automated>
  </verify>
  <acceptance_criteria>
    - `src/migration/policies/migration.policy.ts` contains `hasPermission(authUser, "manage_migrations")`
    - `src/migration/policies/migration.policy.ts` does NOT contain `isSuperUser`
    - `src/migration/policies/migration.policy.ts` imports from `@auth/utilities/has-permission.utility`
    - `src/user/utilities/is-support-user.utility.ts` does NOT exist (deleted)
    - `src/user/utilities/is-super-user.utility.ts` does NOT exist (deleted)
    - `grep -r "is-support-user.utility\|is-super-user.utility" src/ --include="*.ts"` returns zero results
    - `src/subscription/test/guards/subscription.guard.spec.ts` contains `bypass_subscription`
    - `npm run ci` exits 0
  </acceptance_criteria>
  <done>MigrationPolicy migrated, old utility files deleted, all tests updated with permission-carrying mocks, full CI passes with zero regressions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Policy canRead/canWrite | Authorization decision points where support bypass logic changed |
| SubscriptionGuard | Global guard where support bypass logic changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-05 | Elevation of Privilege | Policy migration | mitigate | Every `isSupportUser()` replaced with `hasPermission(user, "view_support_dashboard")` -- semantically equivalent because Phase 51 seeds both Super User and Admin roles with this permission. Verified by existing policy tests updated with permission mocks |
| T-52-06 | Elevation of Privilege | MigrationPolicy | mitigate | `isSuperUser()` replaced with `hasPermission(user, "manage_migrations")` -- only Super User role has this permission (seeded in Phase 51). Admin role does NOT get manage_migrations, preserving the restricted access |
| T-52-07 | Denial of Service | Subscription bypass | mitigate | `supportRoles.length > 0` replaced with `hasPermission(user, "bypass_subscription")` -- both Super User and Admin roles have this permission. If permissions fail to hydrate, `hasPermission` returns `false` (fail-closed) |
| T-52-08 | Elevation of Privilege | Deleted utilities | mitigate | Old utilities deleted only after ALL call sites migrated. CI compilation would fail immediately if any import remains, preventing deployment of broken code |
</threat_model>

<verification>
```bash
# Confirm no remaining old utility imports
cd trade-flow-api && grep -r "is-support-user.utility\|is-super-user.utility\|isSupportUser\|isSuperUser" src/ --include="*.ts"
# Should return ZERO results

# Confirm new permission checks are in place
cd trade-flow-api && grep -r "hasPermission" src/ --include="*.ts" | grep -v "test/" | grep -v ".spec.ts" | wc -l
# Should return 18+ results (1 SubscriptionGuard + 15 policies + 1 MigrationPolicy + utility definitions)

# Full CI gate
cd trade-flow-api && npm run ci
```
</verification>

<success_criteria>
- SubscriptionGuard bypasses subscription check via hasPermission(user, "bypass_subscription")
- All 15 policy files check hasPermission(authUser, "view_support_dashboard") instead of isSupportUser()
- MigrationPolicy checks hasPermission(authUser, "manage_migrations") instead of isSuperUser()
- is-support-user.utility.ts and is-super-user.utility.ts are deleted
- Zero remaining imports or references to old utilities in any .ts file
- All existing tests pass with updated permission-carrying mocks
- npm run ci passes clean
- No frontend changes (RBAC-10) -- all changes are backend-only
</success_criteria>

<output>
After completion, create `.planning/phases/52-permission-guard-migration/52-02-SUMMARY.md`
</output>
