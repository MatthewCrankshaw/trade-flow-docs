---
phase: 08-job-detail-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-ui/src/pages/JobDetailPage.tsx
  - trade-flow-ui/src/features/jobs/components/JobOverviewSection.tsx
autonomous: true
requirements:
  - INTG-01
  - INTG-02

must_haves:
  truths:
    - "The hardcoded getNextVisitHint() function is removed and replaced with schedule-derived logic"
    - "The sticky header shows real next visit date/time for active jobs with upcoming schedules"
    - "The sticky header shows 'Not scheduled yet' for active jobs with no upcoming schedules"
    - "The sticky header shows 'On hold' for on-hold jobs"
    - "The sticky header shows last visit date for completed/closed jobs with schedules"
    - "The sticky header shows 'No visits recorded' for completed/closed jobs without schedules"
    - "The ScheduleSummaryCard displays '{N} visits' total count"
    - "The ScheduleSummaryCard shows color-coded badges per status (only statuses with count > 0)"
  artifacts:
    - path: "trade-flow-ui/src/pages/JobDetailPage.tsx"
      provides: "deriveNextVisitHint function replacing getNextVisitHint"
      contains: "deriveNextVisitHint"
    - path: "trade-flow-ui/src/features/jobs/components/JobOverviewSection.tsx"
      provides: "Enhanced ScheduleSummaryCard with per-status badges"
      contains: "STATUS_CONFIG"
  key_links:
    - from: "trade-flow-ui/src/pages/JobDetailPage.tsx"
      to: "JobDetailHeader"
      via: "deriveNextVisitHint(job.status, schedules) passed as nextVisitHint prop"
      pattern: "deriveNextVisitHint"
    - from: "trade-flow-ui/src/features/jobs/components/JobOverviewSection.tsx"
      to: "schedule-utils.ts"
      via: "import STATUS_CONFIG"
      pattern: "STATUS_CONFIG"
---

<objective>
Replace mock schedule data in the job detail page with real API-driven schedule information.

Purpose: Complete the scheduling milestone integration -- the job detail page should show real schedule summaries and next-visit hints derived from actual schedule data.
Output: Enhanced JobDetailPage with schedule-aware header hints and per-status badge breakdown in the summary card.
</objective>

<execution_context>
@/Users/mcrankshaw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mcrankshaw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/api.types.ts:
```typescript
export type JobStatus =
  | "planned"
  | "scheduled"
  | "in_progress"
  | "completed"
  | "closed"
  | "on_hold";

export type ScheduleStatus =
  | "scheduled"
  | "confirmed"
  | "completed"
  | "canceled"
  | "no_show";

export interface Schedule {
  id: string;
  jobId: string;
  visitTypeId: string | null;
  // ... includes startDateTime: string, status: ScheduleStatus
}
```

From src/features/schedules/components/schedule-utils.ts:
```typescript
export const STATUS_CONFIG: Record<ScheduleStatus, { label: string; className: string }>;
```
Note: STATUS_CONFIG is NOT exported from the schedules barrel (`@/features/schedules`). Import directly from `@/features/schedules/components/schedule-utils`.

From src/features/jobs/components/JobDetailHeader.tsx:
```typescript
interface JobDetailHeaderProps {
  title: string;
  status: JobStatus;
  customerName: string;
  address: string | null;
  nextVisitHint: string;  // <-- string prop, no interface change needed
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace getNextVisitHint with schedule-derived logic</name>
  <files>trade-flow-ui/src/pages/JobDetailPage.tsx</files>
  <action>
In JobDetailPage.tsx:

1. Delete the hardcoded `getNextVisitHint(status: JobStatus)` function (lines 57-72).

2. Add `import { format } from "date-fns"` at the top (date-fns is already a dependency).

3. Create a new function `deriveNextVisitHint(status: JobStatus, schedules?: Schedule[])` that returns a string:
   - If status === "on_hold": return "On hold"
   - If status is "completed" or "closed":
     - If no schedules or empty: return "No visits recorded"
     - Sort all schedules by startDateTime descending, take first. Return `Last visit: ${format(new Date(sorted[0].startDateTime), "EEE d MMM, h:mm a")}`
   - For all other statuses (active jobs: planned, scheduled, in_progress):
     - If no schedules or empty: return "Not scheduled yet"
     - Filter to schedules with status "scheduled" or "confirmed" AND startDateTime > now
     - Sort ascending by startDateTime
     - If no upcoming found: return "Not scheduled yet"
     - Return `Next: ${format(new Date(upcoming[0].startDateTime), "EEE d MMM, h:mm a")}`

4. Update the JSX call site (line 161) from `getNextVisitHint(job.status)` to `deriveNextVisitHint(job.status, schedules)`.

5. Add `import type { Schedule } from "@/types"` if not already imported (Schedule type is needed for the function parameter -- check if it comes from the schedules import already; if `useGetSchedulesByJobQuery` returns `Schedule[]` the type may already be available, but add explicit import if needed).

Do NOT modify any other mock data (MOCK_CUSTOMER, MOCK_JOB_TYPE, MOCK_ACCESS_NOTES, MOCK_COMMERCIAL) per user decision -- MOCK_ prefix is self-documenting.
  </action>
  <verify>
    <automated>cd /Users/mcrankshaw/PersonalProjects/trade-flow-ui && npm run typecheck && npm run lint</automated>
  </verify>
  <done>getNextVisitHint is removed. deriveNextVisitHint computes the header hint from real schedule data and job status. Typecheck and lint pass.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance ScheduleSummaryCard with per-status badge breakdown</name>
  <files>trade-flow-ui/src/features/jobs/components/JobOverviewSection.tsx</files>
  <action>
In JobOverviewSection.tsx:

1. Add import: `import { STATUS_CONFIG } from "@/features/schedules/components/schedule-utils"` (direct import, not via barrel -- STATUS_CONFIG is not re-exported from the barrel).

2. Add import: `import { cn } from "@/lib/utils"` if not already present.

3. Add import: `import type { ScheduleStatus } from "@/types"` if not already present.

4. Rewrite the `ScheduleSummaryCard` component body:

   a. Keep the existing `now`, `upcomingSchedule`, and `nextVisitLabel` logic (lines 174-194) -- it correctly computes the next visit date already.

   b. Replace the bottom stats section (lines 212-221, the "Completed / Total Visits" two-stat display) with:
      - A line showing `{totalVisits} visits` as the total count (use `text-sm font-medium text-muted-foreground`)
      - A flex-wrap container of Badge components for each status with count > 0:
        - Compute `statusCounts` by reducing `schedules` into a `Partial<Record<ScheduleStatus, number>>`
        - Map over `Object.entries(statusCounts)` and render a Badge for each entry:
          ```
          <Badge
            key={status}
            variant="outline"
            className={cn("text-xs", STATUS_CONFIG[status as ScheduleStatus].className)}
          >
            {count} {STATUS_CONFIG[status as ScheduleStatus].label}
          </Badge>
          ```
        - Wrap badges in a `div` with `className="flex flex-wrap gap-1.5"`

   c. Keep the card structure: CardHeader with Calendar icon, CardContent with Next Visit section, Separator, then the new badge breakdown below.

5. Remove the `completedVisits` const (no longer needed separately -- it is part of the badge breakdown now).

Do NOT change any other components in this file (JobDetailsCard, AccessNotesCard, CommercialSnapshotCard, JobOverviewSection).
  </action>
  <verify>
    <automated>cd /Users/mcrankshaw/PersonalProjects/trade-flow-ui && npm run typecheck && npm run lint</automated>
  </verify>
  <done>ScheduleSummaryCard shows "{N} visits" total count and color-coded badges per status (only non-zero statuses). Typecheck and lint pass.</done>
</task>

</tasks>

<verification>
1. `cd trade-flow-ui && npm run typecheck` -- no type errors
2. `cd trade-flow-ui && npm run lint` -- no lint errors
3. `cd trade-flow-ui && npm run build` -- production build succeeds
4. Manual: Job detail page header shows schedule-derived next visit hint
5. Manual: ScheduleSummaryCard shows total count + per-status badges
</verification>

<success_criteria>
- getNextVisitHint() hardcoded function is completely removed from JobDetailPage.tsx
- deriveNextVisitHint() uses real schedule data to compute header hint
- ScheduleSummaryCard displays "{N} visits" total and color-coded status badges
- Only statuses with count > 0 appear as badges
- All non-schedule mock data (MOCK_CUSTOMER, etc.) remains untouched
- TypeScript typecheck and ESLint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-job-detail-integration/08-01-SUMMARY.md`
</output>
