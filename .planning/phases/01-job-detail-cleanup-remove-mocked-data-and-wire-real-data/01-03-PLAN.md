---
phase: 01-job-detail-cleanup-remove-mocked-data-and-wire-real-data
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - trade-flow-api/src/quote/services/quote-creator.service.ts
  - trade-flow-api/src/quote/services/quote-transition.service.ts
  - trade-flow-api/src/quote/quote.module.ts
  - trade-flow-api/src/schedule/services/schedule-creator.service.ts
  - trade-flow-api/src/schedule/services/schedule-updater.service.ts
  - trade-flow-api/src/schedule/schedule.module.ts
  - trade-flow-api/src/job/services/job-updater.service.ts
  - trade-flow-api/src/job/job.module.ts
  - trade-flow-api/src/estimate/services/estimate-creator.service.ts
  - trade-flow-api/src/estimate/services/estimate-transition.service.ts
  - trade-flow-api/src/estimate/services/estimate-to-quote-converter.service.ts
  - trade-flow-api/src/estimate/services/estimate-lost-marker.service.ts
  - trade-flow-api/src/estimate/estimate.module.ts
  - trade-flow-api/src/quote/test/services/quote-creator.service.spec.ts
  - trade-flow-api/src/schedule/test/services/schedule-creator.service.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Creating a quote produces a QUOTE_CREATED event in the job_events collection"
    - "Sending/accepting/rejecting a quote produces QUOTE_SENT/QUOTE_ACCEPTED/QUOTE_REJECTED events"
    - "Creating a schedule produces a SCHEDULE_CREATED event"
    - "Changing a schedule status produces a SCHEDULE_STATUS_CHANGED event"
    - "Changing job status produces a JOB_STATUS_CHANGED event"
    - "Creating an estimate produces an ESTIMATE_CREATED event"
    - "Sending an estimate produces an ESTIMATE_SENT event (via EstimateTransitionService)"
    - "Responding to an estimate produces an ESTIMATE_RESPONDED event (via EstimateTransitionService)"
    - "Converting an estimate to quote produces an ESTIMATE_CONVERTED event (via EstimateToQuoteConverter)"
    - "Marking an estimate as lost produces an ESTIMATE_LOST event (via EstimateLostMarker)"
  artifacts:
    - path: "trade-flow-api/src/quote/services/quote-creator.service.ts"
      provides: "Inline QUOTE_CREATED event write"
      contains: "jobEventCreator.create"
    - path: "trade-flow-api/src/quote/services/quote-transition.service.ts"
      provides: "Inline QUOTE_SENT/ACCEPTED/REJECTED event writes"
      contains: "jobEventCreator.create"
    - path: "trade-flow-api/src/estimate/services/estimate-transition.service.ts"
      provides: "Inline ESTIMATE_SENT/RESPONDED event writes"
      contains: "jobEventCreator.create"
    - path: "trade-flow-api/src/estimate/services/estimate-to-quote-converter.service.ts"
      provides: "Inline ESTIMATE_CONVERTED event write"
      contains: "jobEventCreator.create"
    - path: "trade-flow-api/src/estimate/services/estimate-lost-marker.service.ts"
      provides: "Inline ESTIMATE_LOST event write"
      contains: "jobEventCreator.create"
  key_links:
    - from: "quote-creator.service.ts"
      to: "job-event-creator.service.ts"
      via: "constructor injection of JobEventCreator"
      pattern: "this.jobEventCreator.create"
    - from: "estimate-transition.service.ts"
      to: "job-event-creator.service.ts"
      via: "constructor injection of JobEventCreator"
      pattern: "this.jobEventCreator.create"
    - from: "estimate-to-quote-converter.service.ts"
      to: "job-event-creator.service.ts"
      via: "constructor injection of JobEventCreator"
      pattern: "this.jobEventCreator.create"
    - from: "estimate-lost-marker.service.ts"
      to: "job-event-creator.service.ts"
      via: "constructor injection of JobEventCreator"
      pattern: "this.jobEventCreator.create"
---

<objective>
Wire inline job event writes into all existing backend services that perform job-related actions (per D-06, D-07). This covers quote services (creator + transition), schedule services (creator + updater), job updater, and all four estimate services (creator, transition, converter, lost marker). After this plan, every backend action on quotes, schedules, estimates, and job status writes a corresponding event to the job_events collection.

Purpose: Populates the job_events collection with real activity data so the timeline (wired in Plan 04) has events to display.
Output: 9 existing services modified with inline event writes, 4 module imports updated, existing tests updated with mock JobEventCreator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-job-detail-cleanup-remove-mocked-data-and-wire-real-data/01-CONTEXT.md
@.planning/phases/01-job-detail-cleanup-remove-mocked-data-and-wire-real-data/01-RESEARCH.md
@.planning/phases/01-job-detail-cleanup-remove-mocked-data-and-wire-real-data/01-PATTERNS.md
@.planning/phases/01-job-detail-cleanup-remove-mocked-data-and-wire-real-data/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- the JobEventCreator service that will be injected into existing services -->
<!-- Executor: read the actual SUMMARY from Plan 01 for precise exports -->

Expected exports from Plan 01:
```typescript
// From trade-flow-api/src/job-event/services/job-event-creator.service.ts
@Injectable()
export class JobEventCreator {
  create(dto: Omit<IJobEventDto, "id" | "createdAt" | "updatedAt">): Promise<IJobEventDto>;
}

// From trade-flow-api/src/job-event/enums/job-event-type.enum.ts
export enum JobEventType {
  JOB_STATUS_CHANGED = "job_status_changed",
  SCHEDULE_CREATED = "schedule_created",
  SCHEDULE_STATUS_CHANGED = "schedule_status_changed",
  QUOTE_CREATED = "quote_created",
  QUOTE_SENT = "quote_sent",
  QUOTE_ACCEPTED = "quote_accepted",
  QUOTE_REJECTED = "quote_rejected",
  ESTIMATE_CREATED = "estimate_created",
  ESTIMATE_SENT = "estimate_sent",
  ESTIMATE_RESPONDED = "estimate_responded",
  ESTIMATE_CONVERTED = "estimate_converted",
  ESTIMATE_LOST = "estimate_lost",
}

// From trade-flow-api/src/job-event/job-event.module.ts
@Module({ exports: [JobEventCreator] })
export class JobEventModule {}
```

Estimate module service names (RESOLVED from v1.8 milestone artifacts):
```
src/estimate/services/
  estimate-creator.service.ts        (class: EstimateCreator)
  estimate-transition.service.ts     (class: EstimateTransitionService) -- handles SENT, RESPONDED
  estimate-to-quote-converter.service.ts (class: EstimateToQuoteConverter) -- handles CONVERTED
  estimate-lost-marker.service.ts    (class: EstimateLostMarker) -- handles LOST
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inline event writes to quote, schedule, and job services</name>
  <files>
    trade-flow-api/src/quote/services/quote-creator.service.ts,
    trade-flow-api/src/quote/services/quote-transition.service.ts,
    trade-flow-api/src/quote/quote.module.ts,
    trade-flow-api/src/schedule/services/schedule-creator.service.ts,
    trade-flow-api/src/schedule/services/schedule-updater.service.ts,
    trade-flow-api/src/schedule/schedule.module.ts,
    trade-flow-api/src/job/services/job-updater.service.ts,
    trade-flow-api/src/job/job.module.ts,
    trade-flow-api/src/quote/test/services/quote-creator.service.spec.ts,
    trade-flow-api/src/schedule/test/services/schedule-creator.service.spec.ts
  </files>
  <read_first>
    trade-flow-api/src/quote/services/quote-creator.service.ts,
    trade-flow-api/src/quote/services/quote-transition.service.ts,
    trade-flow-api/src/quote/quote.module.ts,
    trade-flow-api/src/schedule/services/schedule-creator.service.ts,
    trade-flow-api/src/schedule/services/schedule-updater.service.ts,
    trade-flow-api/src/schedule/schedule.module.ts,
    trade-flow-api/src/job/services/job-updater.service.ts,
    trade-flow-api/src/job/job.module.ts,
    trade-flow-api/src/job-event/services/job-event-creator.service.ts,
    trade-flow-api/src/job-event/enums/job-event-type.enum.ts,
    trade-flow-api/src/job-event/job-event.module.ts,
    trade-flow-api/src/job-event/data-transfer-objects/job-event.dto.ts,
    trade-flow-api/src/quote/test/services/quote-creator.service.spec.ts,
    trade-flow-api/src/schedule/test/services/schedule-creator.service.spec.ts
  </read_first>
  <action>
For each service below, apply this pattern (per D-07 -- inline event writes, no observer pattern):

1. Add `JobEventCreator` to the constructor injection
2. Import `JobEventType` from `@job-event/enums/job-event-type.enum.ts`
3. Import `IJobEventDto` from `@job-event/data-transfer-objects/job-event.dto.ts`
4. After the primary operation succeeds, call `this.jobEventCreator.create(...)` with the appropriate event data
5. Use `DateTime.now()` for `occurredAt`

**Check if `@job-event/*` path alias exists in tsconfig.json. If not, add `"@job-event/*": ["src/job-event/*"]` to paths. Also add `"@job-event-test/*": ["src/job-event/test/*"]` for test mocks.**

**Services to modify:**

**A. `quote-creator.service.ts`** -- after successful quote creation:
```typescript
await this.jobEventCreator.create({
  jobId: createdQuote.jobId,
  businessId: createdQuote.businessId,
  type: JobEventType.QUOTE_CREATED,
  metadata: { quoteId: createdQuote.id },
  occurredAt: DateTime.now(),
} as IJobEventDto);
```

**B. `quote-transition.service.ts`** -- this service handles quote status changes (sent, accepted, rejected). After each transition:
- When transitioning to "sent": use `JobEventType.QUOTE_SENT` with metadata `{ quoteId }`
- When transitioning to "accepted": use `JobEventType.QUOTE_ACCEPTED` with metadata `{ quoteId }`
- When transitioning to "rejected": use `JobEventType.QUOTE_REJECTED` with metadata `{ quoteId }`
- Read the service to find the transition method(s). Add event write after each successful status change.

**C. `schedule-creator.service.ts`** -- after successful schedule creation:
```typescript
await this.jobEventCreator.create({
  jobId: createdSchedule.jobId,
  businessId: createdSchedule.businessId,
  type: JobEventType.SCHEDULE_CREATED,
  metadata: { scheduleId: createdSchedule.id },
  occurredAt: DateTime.now(),
} as IJobEventDto);
```

**D. `schedule-updater.service.ts`** -- after status change (check if this service handles status transitions or if there's a separate schedule-transition.service.ts):
- If updating status: use `JobEventType.SCHEDULE_STATUS_CHANGED` with metadata `{ scheduleId, oldStatus, newStatus }`
- If this service only handles general updates, add event write only when status field changes

**E. `job-updater.service.ts`** -- after job status change:
```typescript
await this.jobEventCreator.create({
  jobId: updatedJob.id,
  businessId: updatedJob.businessId,
  type: JobEventType.JOB_STATUS_CHANGED,
  metadata: { oldStatus: existingJob.status, newStatus: updatedJob.status },
  occurredAt: DateTime.now(),
} as IJobEventDto);
```
- Only write event when status actually changes (compare old vs new). Read the service to find how updates work.

**F. Module updates** -- for each module whose services now inject `JobEventCreator`:
- Import `JobEventModule` in the module's `imports` array
- Use `forwardRef(() => JobEventModule)` if circular dependency arises (unlikely since JobEventModule doesn't import these modules)

Modules to update:
- `quote.module.ts`: add `JobEventModule` to imports
- `schedule.module.ts`: add `JobEventModule` to imports
- `job.module.ts`: add `JobEventModule` to imports

**G. Update existing tests:**

For each modified service's test file, add `JobEventCreator` to the mock providers:
```typescript
{ provide: JobEventCreator, useValue: { create: jest.fn().mockResolvedValue({}) } }
```
This ensures existing tests don't break due to the new dependency. The mock just resolves successfully.

At minimum, update these test files:
- `quote/test/services/quote-creator.service.spec.ts`
- `schedule/test/services/schedule-creator.service.spec.ts`
- Check for and update any other test files that create instances of the modified services.
  </action>
  <verify>
    <automated>cd trade-flow-api && npm run ci 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-api/src/quote/services/quote-creator.service.ts contains `this.jobEventCreator.create` and `JobEventType.QUOTE_CREATED`
    - trade-flow-api/src/quote/services/quote-transition.service.ts contains `this.jobEventCreator.create` and at least one of `QUOTE_SENT`, `QUOTE_ACCEPTED`, `QUOTE_REJECTED`
    - trade-flow-api/src/schedule/services/schedule-creator.service.ts contains `this.jobEventCreator.create` and `JobEventType.SCHEDULE_CREATED`
    - trade-flow-api/src/job/services/job-updater.service.ts contains `this.jobEventCreator.create` and `JobEventType.JOB_STATUS_CHANGED`
    - trade-flow-api/src/quote/quote.module.ts contains `JobEventModule`
    - trade-flow-api/src/schedule/schedule.module.ts contains `JobEventModule`
    - trade-flow-api/src/job/job.module.ts contains `JobEventModule`
    - `npm run ci` passes in trade-flow-api (all tests pass, lint clean, typecheck clean)
  </acceptance_criteria>
  <done>Quote, schedule, and job services write inline job events after their primary operations. Module imports updated. Existing tests updated with mock JobEventCreator. `npm run ci` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add inline event writes to all four estimate services</name>
  <files>
    trade-flow-api/src/estimate/services/estimate-creator.service.ts,
    trade-flow-api/src/estimate/services/estimate-transition.service.ts,
    trade-flow-api/src/estimate/services/estimate-to-quote-converter.service.ts,
    trade-flow-api/src/estimate/services/estimate-lost-marker.service.ts,
    trade-flow-api/src/estimate/estimate.module.ts
  </files>
  <read_first>
    trade-flow-api/src/estimate/services/estimate-creator.service.ts,
    trade-flow-api/src/estimate/services/estimate-transition.service.ts,
    trade-flow-api/src/estimate/services/estimate-to-quote-converter.service.ts,
    trade-flow-api/src/estimate/services/estimate-lost-marker.service.ts,
    trade-flow-api/src/estimate/estimate.module.ts,
    trade-flow-api/src/job-event/services/job-event-creator.service.ts,
    trade-flow-api/src/job-event/enums/job-event-type.enum.ts,
    trade-flow-api/src/job-event/data-transfer-objects/job-event.dto.ts
  </read_first>
  <action>
Apply the same inline event write pattern to all four estimate services (per D-06 -- estimate events: created/sent/responded/converted/lost):

**A. `estimate-creator.service.ts`** (class: `EstimateCreator`) -- after successful estimate creation:
- Use `JobEventType.ESTIMATE_CREATED` with metadata `{ estimateId: createdEstimate.id }`
- The estimate DTO should have `jobId` and `businessId` fields -- use those for the event

**B. `estimate-transition.service.ts`** (class: `EstimateTransitionService`) -- handles status transitions via `ALLOWED_TRANSITIONS` map:
- This service handles multiple transitions. Add event writes for the transitions that map to D-06 event types:
  - When transitioning to "sent" status: use `JobEventType.ESTIMATE_SENT` with metadata `{ estimateId }`
  - When transitioning to "responded" status: use `JobEventType.ESTIMATE_RESPONDED` with metadata `{ estimateId }`
- Read the service to understand the transition method signature. There may be a single `transition()` method that takes oldStatus/newStatus -- if so, add a conditional event write based on the new status.
- Note: This service may also handle other transitions (viewed, site_visit_requested). Only write events for the types defined in D-06 (sent, responded). Other transitions do not need events.

**C. `estimate-to-quote-converter.service.ts`** (class: `EstimateToQuoteConverter`) -- after successful conversion:
- Use `JobEventType.ESTIMATE_CONVERTED` with metadata `{ estimateId, quoteId }` (the quoteId of the newly created quote)
- Read the service to find where the conversion completes and the quoteId is available

**D. `estimate-lost-marker.service.ts`** (class: `EstimateLostMarker`) -- after marking as lost:
- Use `JobEventType.ESTIMATE_LOST` with metadata `{ estimateId, reason }` (include the lost reason if available in the service)
- Read the service to find where the lost marking completes

**E. Module update:**
- Add `JobEventModule` to `estimate.module.ts` imports array
- This module already has `forwardRef(() => QuoteModule)` so it's familiar with forward references; `JobEventModule` should not need `forwardRef` since it doesn't import `EstimateModule`

**F. Update existing estimate tests:**
- Check `trade-flow-api/src/estimate/test/services/` for test files that test the four modified services
- Add `{ provide: JobEventCreator, useValue: { create: jest.fn().mockResolvedValue({}) } }` to each test's mock providers
  </action>
  <verify>
    <automated>cd trade-flow-api && npm run ci 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-api/src/estimate/services/estimate-creator.service.ts contains `this.jobEventCreator.create` and `JobEventType.ESTIMATE_CREATED`
    - trade-flow-api/src/estimate/services/estimate-transition.service.ts contains `this.jobEventCreator.create` and at least `ESTIMATE_SENT` or `ESTIMATE_RESPONDED`
    - trade-flow-api/src/estimate/services/estimate-to-quote-converter.service.ts contains `this.jobEventCreator.create` and `JobEventType.ESTIMATE_CONVERTED`
    - trade-flow-api/src/estimate/services/estimate-lost-marker.service.ts contains `this.jobEventCreator.create` and `JobEventType.ESTIMATE_LOST`
    - trade-flow-api/src/estimate/estimate.module.ts contains `JobEventModule`
    - `npm run ci` passes in trade-flow-api (all tests pass, lint clean, typecheck clean)
  </acceptance_criteria>
  <done>All four estimate services write inline job events covering all D-06 estimate event types (created, sent, responded, converted, lost). Estimate module imports JobEventModule. Existing estimate tests updated with mock JobEventCreator. `npm run ci` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| existing services -> JobEventCreator | Internal service-to-service calls within the same process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | Inline event writes | mitigate | Events written only by server-side services after primary operation succeeds; no client-controlled event content; metadata is derived from server state (createdQuote.id, etc.) |
| T-01-09 | Repudiation | Event audit trail | accept | Events record what happened but don't include who triggered it (user ID not in event metadata); acceptable for v1 activity feed which is display-only |
</threat_model>

<verification>
1. `cd trade-flow-api && npm run ci` -- all API tests pass, lint clean, typecheck clean
2. `grep -r "jobEventCreator.create" trade-flow-api/src/ --include="*.ts" | grep -v test | grep -v spec` shows 9 service files with event writes (quote-creator, quote-transition, schedule-creator, schedule-updater, job-updater, estimate-creator, estimate-transition, estimate-to-quote-converter, estimate-lost-marker)
3. `grep -r "JobEventModule" trade-flow-api/src/ --include="*.module.ts"` shows 4 module imports (quote, schedule, job, estimate)
</verification>

<success_criteria>
- 9 existing services write inline job events covering all 12 event types from D-06
- All 4 estimate event types unconditionally covered: ESTIMATE_CREATED (creator), ESTIMATE_SENT + ESTIMATE_RESPONDED (transition), ESTIMATE_CONVERTED (converter), ESTIMATE_LOST (lost marker)
- 4 module imports updated (quote, schedule, job, estimate)
- All existing tests updated with mock JobEventCreator
- `npm run ci` passes in trade-flow-api
</success_criteria>

<output>
After completion, create `.planning/phases/01-job-detail-cleanup-remove-mocked-data-and-wire-real-data/01-03-SUMMARY.md`
</output>
