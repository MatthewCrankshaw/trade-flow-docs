---
phase: 01-job-detail-cleanup-remove-mocked-data-and-wire-real-data
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-ui/src/types/api.types.ts
  - trade-flow-ui/src/pages/JobDetailPage.tsx
  - trade-flow-ui/src/features/jobs/components/JobOverviewSection.tsx
  - trade-flow-ui/src/features/jobs/components/JobDetailTabs.tsx
  - trade-flow-ui/src/features/jobs/components/JobActionStrip.tsx
  - trade-flow-ui/src/features/jobs/hooks/generate-mock-timeline.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Job detail page shows real customer name fetched from API, not hardcoded 'John Smith'"
    - "Job detail page shows real job type name fetched from API, not hardcoded 'Window Installation'"
    - "Job detail page shows real job address from API response"
    - "Invoices tab is completely removed from the tab bar"
    - "Notes tab is completely removed from the tab bar"
    - "Access notes section (parking/pets/keys/gate code) is completely removed"
    - "Create Invoice button is completely removed from the action strip"
    - "Commercial section shows total quoted amount aggregated from real quote data"
    - "No MOCK_ constants or generateMockTimeline references remain in any modified file"
  artifacts:
    - path: "trade-flow-ui/src/types/api.types.ts"
      provides: "Job interface with jobTypeId and address fields"
      contains: "jobTypeId: string"
    - path: "trade-flow-ui/src/pages/JobDetailPage.tsx"
      provides: "Job detail page with real API data wiring"
    - path: "trade-flow-ui/src/features/jobs/components/JobOverviewSection.tsx"
      provides: "Overview with real commercial summary and no access notes"
    - path: "trade-flow-ui/src/features/jobs/components/JobDetailTabs.tsx"
      provides: "Tab component with only Schedules and Quotes tabs"
  key_links:
    - from: "JobDetailPage.tsx"
      to: "customerApi.ts"
      via: "useGetCustomerQuery hook"
      pattern: "useGetCustomerQuery"
    - from: "JobDetailPage.tsx"
      to: "jobTypeApi.ts"
      via: "useGetJobTypeQuery hook"
      pattern: "useGetJobTypeQuery"
    - from: "JobOverviewSection.tsx"
      to: "quote data"
      via: "totalQuotedAmount prop"
      pattern: "totalQuotedAmount"
---

<objective>
Remove all mocked data from the job detail page and wire real API data using existing RTK Query hooks. Remove UI sections for unbuilt features (Invoices tab, Notes tab, access notes, Create Invoice button). Update the frontend Job type to include `jobTypeId` and `address` fields that the API already returns. Replace the mock commercial summary with real quote-aggregated data (per D-01, D-02, D-03, D-09 through D-15).

Purpose: Makes the job detail page "honest" -- showing only real data from existing APIs with no placeholder stubs. This is the core cleanup deliverable of Phase 1.
Output: A job detail page that renders real customer, job type, address, schedule, and quote data. Mocked data completely eliminated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-job-detail-cleanup-remove-mocked-data-and-wire-real-data/01-CONTEXT.md
@.planning/phases/01-job-detail-cleanup-remove-mocked-data-and-wire-real-data/01-RESEARCH.md
@.planning/phases/01-job-detail-cleanup-remove-mocked-data-and-wire-real-data/01-PATTERNS.md

<interfaces>
<!-- Existing RTK Query hooks the executor will wire up. -->

From trade-flow-ui/src/features/customers/api/customerApi.ts:
```typescript
export const { useGetCustomerQuery, useGetCustomersQuery } = customerApi;
// useGetCustomerQuery(customerId: string, options?) => { data: Customer }
```

From trade-flow-ui/src/features/job-types/api/jobTypeApi.ts:
```typescript
export const { useGetJobTypesQuery, useGetJobTypeQuery } = jobTypeApi;
// useGetJobTypeQuery(jobTypeId: string, options?) => { data: JobType }
```

From trade-flow-ui/src/features/quotes/api/quoteApi.ts:
```typescript
export const { useGetQuotesQuery } = quoteApi;
// useGetQuotesQuery(businessId: string, options?) => { data: Quote[] }
```

From trade-flow-ui/src/types/api.types.ts (CURRENT Job interface):
```typescript
export interface Job {
  id: string;
  customerId: string;
  customerName: string;
  title: string;
  description: string | null;
  status: JobStatus;
}
```

From trade-flow-api/src/job/responses/job.response.ts (API already returns these fields):
```typescript
export interface IJobResponse {
  id: string;
  customerId: string;
  customerName: string;
  title: string;
  description: string | null;
  status: JobStatus;
  address: {
    addressLines: string[];
    countryCode: string;
    locality: string;
    postalCode: string;
    administrativeArea: string;
  } | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Job type and remove mock data from JobDetailPage</name>
  <files>
    trade-flow-ui/src/types/api.types.ts,
    trade-flow-ui/src/pages/JobDetailPage.tsx,
    trade-flow-ui/src/features/jobs/hooks/generate-mock-timeline.ts
  </files>
  <read_first>
    trade-flow-ui/src/types/api.types.ts,
    trade-flow-ui/src/pages/JobDetailPage.tsx,
    trade-flow-ui/src/features/jobs/hooks/generate-mock-timeline.ts,
    trade-flow-ui/src/features/customers/api/customerApi.ts,
    trade-flow-ui/src/features/job-types/api/jobTypeApi.ts,
    trade-flow-ui/src/features/quotes/api/quoteApi.ts,
    trade-flow-ui/src/features/jobs/api/jobApi.ts
  </read_first>
  <action>
**1. Update `api.types.ts` -- add missing fields to Job interface (per D-03):**

Add these fields to the existing `Job` interface:
```typescript
jobTypeId: string;
address: {
  addressLines: string[];
  countryCode: string;
  locality: string;
  postalCode: string;
  administrativeArea: string;
} | null;
```

The API already returns these fields but the UI type was missing them. Check the `jobApi.ts` `transformResponse` to ensure it passes through `jobTypeId` and `address` from the response. If the transformResponse strips them, update it to include them.

**2. Remove all mock data from `JobDetailPage.tsx` (per D-02):**

Delete these constants entirely (currently around lines 34-54):
- `MOCK_CUSTOMER` -- replaced by `useGetCustomerQuery`
- `MOCK_JOB_TYPE` -- replaced by `useGetJobTypeQuery`
- `MOCK_ACCESS_NOTES` -- removed entirely (per D-11)
- `MOCK_COMMERCIAL` -- replaced by quote aggregation (per D-14)

Delete the `generateMockTimeline` import and any usage of it (the timeline will be wired to real events in Plan 03).

**3. Wire real API data in `JobDetailPage.tsx`:**

Add these RTK Query hook calls after the existing `useGetJobQuery` call:
```typescript
const { data: customer } = useGetCustomerQuery(job?.customerId ?? "", { skip: !job?.customerId });
const { data: jobType } = useGetJobTypeQuery(job?.jobTypeId ?? "", { skip: !job?.jobTypeId });
```

The `skip` option prevents API calls when the job hasn't loaded yet (per Pitfall 1 in RESEARCH.md).

Quotes are already fetched (check existing `useGetQuotesQuery` call). Compute the total quoted amount:
```typescript
const totalQuotedAmount = (allQuotes ?? [])
  .filter((q) => q.jobId === jobId && q.status !== "deleted")
  .reduce((sum, q) => sum + q.totalAmount, 0);
```

Note: If quotes are fetched by businessId (not jobId), filter them to only this job's quotes.

**4. Update props passed to child components:**

- Pass `customer?.name` (or appropriate field) instead of `MOCK_CUSTOMER.name` to `JobOverviewSection`
- Pass `jobType?.name` instead of `MOCK_JOB_TYPE` to the header/overview
- Pass `job?.address` for address display
- Pass `totalQuotedAmount` instead of `MOCK_COMMERCIAL` to the commercial section
- Remove `accessNotes` prop (was `MOCK_ACCESS_NOTES`)
- Remove `handleCreateInvoice` handler and its passage to `JobActionStrip` (per D-12)
- For the timeline section: temporarily pass an empty array or conditionally render nothing. Plan 03 will wire the real timeline.

**5. Delete `generate-mock-timeline.ts` file entirely** (per D-02).
  </action>
  <verify>
    <automated>cd trade-flow-ui && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-ui/src/types/api.types.ts `Job` interface contains `jobTypeId: string` and `address:` with the address object type
    - trade-flow-ui/src/pages/JobDetailPage.tsx does NOT contain `MOCK_CUSTOMER`, `MOCK_JOB_TYPE`, `MOCK_ACCESS_NOTES`, `MOCK_COMMERCIAL`, or `generateMockTimeline`
    - trade-flow-ui/src/pages/JobDetailPage.tsx contains `useGetCustomerQuery` and `useGetJobTypeQuery`
    - trade-flow-ui/src/pages/JobDetailPage.tsx contains `skip: !job?.customerId` and `skip: !job?.jobTypeId`
    - trade-flow-ui/src/features/jobs/hooks/generate-mock-timeline.ts is deleted (file does not exist)
    - TypeScript compiles: `npx tsc --noEmit` exits 0 in trade-flow-ui
  </acceptance_criteria>
  <done>Job type updated with jobTypeId and address. All 4 mock constants removed from JobDetailPage. Real customer and job type data wired via RTK Query hooks with skip conditions. Mock timeline generator file deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Remove unbuilt feature UI sections and update commercial summary</name>
  <files>
    trade-flow-ui/src/features/jobs/components/JobOverviewSection.tsx,
    trade-flow-ui/src/features/jobs/components/JobDetailTabs.tsx,
    trade-flow-ui/src/features/jobs/components/JobActionStrip.tsx
  </files>
  <read_first>
    trade-flow-ui/src/features/jobs/components/JobOverviewSection.tsx,
    trade-flow-ui/src/features/jobs/components/JobDetailTabs.tsx,
    trade-flow-ui/src/features/jobs/components/JobActionStrip.tsx,
    trade-flow-ui/src/hooks/useCurrency.ts
  </read_first>
  <action>
**1. `JobOverviewSection.tsx` -- remove mock interfaces and access notes, update commercial (per D-11, D-14, D-15):**

- Delete `MockCommercialData` interface entirely
- Delete `MockAccessNotes` interface entirely
- Delete the `AccessNotesCard` component entirely (the whole component that renders parking/pets/keys/gate code)
- Remove `accessNotes` from the component's props interface
- Remove `commercial` prop that expects the old mock shape

- Add a new prop: `totalQuotedAmount: number` (minor units integer)
- Replace the commercial section with a simplified display:
  - Title: "Quoted" or "Total Quoted"
  - Value: format `totalQuotedAmount` using the `useCurrency` hook (or dinero.js formatting from the project's existing currency utilities). Check how other components format money -- likely `formatAmount(totalQuotedAmount)` or similar.
  - Remove the invoiced, paid, and outstanding rows entirely (per D-15)

- Add props for real data that Task 1 now passes: `customerName: string | undefined`, `jobTypeName: string | undefined`, `address` (the address object or null). Wire these into the appropriate display sections replacing any mock references.

**2. `JobDetailTabs.tsx` -- remove Invoices and Notes tabs (per D-09, D-10, D-13):**

- Delete `MOCK_INVOICES` constant entirely
- Delete `MOCK_NOTES` constant entirely
- Remove the Invoices `TabsTrigger` and `TabsContent` blocks (the trigger with Receipt icon and the content with InvoicesTable)
- Remove the Notes `TabsTrigger` and `TabsContent` blocks (the trigger with MessageSquare icon and the content with NotesList)
- Remove `InvoicesTable` and `NotesList` sub-components if they are defined inline in this file
- Remove unused icon imports (`Receipt`, `MessageSquare`) from lucide-react if no longer referenced
- Keep: Schedules tab, Quotes tab, and any other real tabs (Photos, Files if they exist)

**3. `JobActionStrip.tsx` -- remove Create Invoice button (per D-12):**

- Remove the `onCreateInvoice` prop from the component's props interface
- Remove the "Create Invoice" button JSX block (the Button with Receipt icon and onClick={onCreateInvoice})
- Remove the `Receipt` icon import from lucide-react if no longer used
- Keep all other action buttons (status changes, etc.)
  </action>
  <verify>
    <automated>cd trade-flow-ui && npm run ci 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-ui/src/features/jobs/components/JobOverviewSection.tsx does NOT contain `MockCommercialData`, `MockAccessNotes`, or `AccessNotesCard`
    - trade-flow-ui/src/features/jobs/components/JobOverviewSection.tsx contains `totalQuotedAmount` in props
    - trade-flow-ui/src/features/jobs/components/JobOverviewSection.tsx does NOT contain `invoiced`, `paid`, or `outstanding` display fields
    - trade-flow-ui/src/features/jobs/components/JobDetailTabs.tsx does NOT contain `MOCK_INVOICES`, `MOCK_NOTES`, `InvoicesTable`, or `NotesList`
    - trade-flow-ui/src/features/jobs/components/JobDetailTabs.tsx does NOT contain TabsTrigger with value="invoices" or value="notes"
    - trade-flow-ui/src/features/jobs/components/JobActionStrip.tsx does NOT contain `onCreateInvoice` or `Create Invoice`
    - `npm run ci` passes in trade-flow-ui (tests, lint, format, typecheck)
  </acceptance_criteria>
  <done>Invoices tab removed. Notes tab removed. Access notes section removed. Create Invoice button removed. Commercial section shows only total quoted amount from real data. All mock interfaces deleted. `npm run ci` passes in trade-flow-ui.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> RTK Query hooks | Client-side data fetching from authenticated API endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information Disclosure | Customer/JobType data display | accept | Data already fetched via authenticated RTK Query hooks with JwtAuthGuard on API side; no new exposure |
| T-01-07 | Tampering | Commercial summary calculation | accept | Client-side aggregation of server-provided quote data; display-only, no write path; server is source of truth |
</threat_model>

<verification>
1. `cd trade-flow-ui && npm run ci` -- full CI gate passes
2. Grep for removed items: `grep -r "MOCK_CUSTOMER\|MOCK_JOB_TYPE\|MOCK_ACCESS_NOTES\|MOCK_COMMERCIAL\|MOCK_INVOICES\|MOCK_NOTES\|generateMockTimeline\|Create Invoice\|onCreateInvoice" trade-flow-ui/src/` returns no matches
3. TypeScript compiles cleanly: `cd trade-flow-ui && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- Zero mock data constants remain in any job detail component
- Job detail page fetches and displays real customer name, job type name, and address
- Invoices tab, Notes tab, access notes section, and Create Invoice button are completely removed
- Commercial section shows only total quoted amount from real quotes
- generate-mock-timeline.ts file deleted
- `npm run ci` passes in trade-flow-ui
</success_criteria>

<output>
After completion, create `.planning/phases/01-job-detail-cleanup-remove-mocked-data-and-wire-real-data/01-02-SUMMARY.md`
</output>
