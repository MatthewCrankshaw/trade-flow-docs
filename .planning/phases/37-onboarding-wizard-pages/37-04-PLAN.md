---
phase: 37-onboarding-wizard-pages
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-api/src/quote/services/default-quote-settings-creator.service.ts
  - trade-flow-api/src/quote/quote.module.ts
  - trade-flow-api/src/business/services/business-creator.service.ts
  - trade-flow-api/src/business/business.module.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ONBD-01, ONBD-02, ONBD-03, ONBD-04, ONBD-05, ONBD-06, ONBD-07, TRIAL-02, TRIAL-03]
gap_closure: true

must_haves:
  truths:
    - "When a new business is created via POST /v1/business, a default QuoteSettings record is automatically created for that business"
    - "REQUIREMENTS.md accurately reflects which requirements are complete"
  artifacts:
    - path: "trade-flow-api/src/quote/services/default-quote-settings-creator.service.ts"
      provides: "DefaultQuoteSettingsCreatorService that writes a default QuoteSettings for a new business"
      exports: ["DefaultQuoteSettingsCreatorService"]
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement tracking"
      contains: "[x] **ONBD-02**"
  key_links:
    - from: "trade-flow-api/src/business/services/business-creator.service.ts"
      to: "trade-flow-api/src/quote/services/default-quote-settings-creator.service.ts"
      via: "constructor injection and call in create() method"
      pattern: "defaultQuoteSettingsCreator"
---

<objective>
Close two verification gaps from Phase 37: (1) auto-create default quote email template / QuoteSettings when a business is created, and (2) update REQUIREMENTS.md tracking for completed requirements.

Purpose: ONBD-04 requires all five default resource types (tax rates, job types, visit types, items, AND quote email template) to be auto-created at business setup. Currently only four are created. The documentation gap causes tracking inconsistency.
Output: DefaultQuoteSettingsCreatorService wired into BusinessCreator, accurate REQUIREMENTS.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-onboarding-wizard-pages/37-VERIFICATION.md
@.planning/phases/37-onboarding-wizard-pages/37-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DefaultQuoteSettingsCreatorService and wire into BusinessCreator</name>
  <files>
    trade-flow-api/src/quote/services/default-quote-settings-creator.service.ts
    trade-flow-api/src/quote/quote.module.ts
    trade-flow-api/src/business/services/business-creator.service.ts
    trade-flow-api/src/business/business.module.ts
  </files>
  <read_first>
    trade-flow-api/src/business/services/business-creator.service.ts
    trade-flow-api/src/business/business.module.ts
    trade-flow-api/src/quote/quote.module.ts
    trade-flow-api/src/quote/services/
    trade-flow-api/src/quote/repositories/
    trade-flow-api/src/quote/data-transfer-objects/
    trade-flow-api/src/business/services/default-tax-rates-creator.service.ts
    trade-flow-api/src/business/services/default-business-items-creator.service.ts
  </read_first>
  <action>
Create a DefaultQuoteSettingsCreatorService in the quote module that auto-creates a default QuoteSettings record for a newly created business. Follow the exact same pattern used by the existing default creator services (DefaultTaxRatesCreatorService, DefaultBusinessItemsCreatorService, DefaultJobTypesCreatorService, DefaultVisitTypesCreatorService).

Steps:

1. **Read the existing default creator pattern** from `default-tax-rates-creator.service.ts` or `default-business-items-creator.service.ts` to understand the service structure: Injectable class with a `create(businessId: string)` method that calls a repository to insert default records.

2. **Read the QuoteSettings repository and DTO** to understand the data shape for quote settings (likely includes fields like emailSubject, emailBody, replyTo, etc.). Also read any existing quote settings service to understand how quote settings are currently created (the verification noted it uses on-demand upsert).

3. **Create `trade-flow-api/src/quote/services/default-quote-settings-creator.service.ts`:**
   - Injectable NestJS service class named `DefaultQuoteSettingsCreatorService`
   - Constructor injects the QuoteSettings repository (match the existing injection pattern from other default creators)
   - Single public method: `async create(businessId: string): Promise<void>` (or matching return type of sibling default creators)
   - Creates a default QuoteSettings record with sensible defaults for the email template (e.g., a generic subject line like "Your quote from {businessName}" and a basic body template). Match whatever default content the on-demand upsert would create, or use values from the existing quote settings seeding/factory if one exists.
   - Log creation using the AppLogger pattern: `private readonly logger = new AppLogger(DefaultQuoteSettingsCreatorService.name)`

4. **Register in `trade-flow-api/src/quote/quote.module.ts`:**
   - Add `DefaultQuoteSettingsCreatorService` to the `providers` array
   - Add `DefaultQuoteSettingsCreatorService` to the `exports` array (so BusinessModule can import it)

5. **Wire into `trade-flow-api/src/business/services/business-creator.service.ts`:**
   - Add `private readonly defaultQuoteSettingsCreator: DefaultQuoteSettingsCreatorService` to the constructor parameters
   - Call `await this.defaultQuoteSettingsCreator.create(businessId)` in the `create()` method, after the existing four default creator calls (after DefaultVisitTypesCreatorService or whichever is last)
   - Wrap in try/catch consistent with how the other default creator calls handle errors (if they do)

6. **Update `trade-flow-api/src/business/business.module.ts`:**
   - If QuoteModule is not already imported, add it to the `imports` array so the DefaultQuoteSettingsCreatorService is available for injection
   - If QuoteModule is already imported, verify DefaultQuoteSettingsCreatorService is exported from it (step 4 handles this)
  </action>
  <verify>
    <automated>cd trade-flow-api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-api/src/quote/services/default-quote-settings-creator.service.ts exists and contains `export class DefaultQuoteSettingsCreatorService`
    - trade-flow-api/src/quote/services/default-quote-settings-creator.service.ts contains `@Injectable()` decorator
    - trade-flow-api/src/quote/services/default-quote-settings-creator.service.ts contains `async create(` method
    - trade-flow-api/src/quote/quote.module.ts providers array contains `DefaultQuoteSettingsCreatorService`
    - trade-flow-api/src/quote/quote.module.ts exports array contains `DefaultQuoteSettingsCreatorService`
    - trade-flow-api/src/business/services/business-creator.service.ts contains `defaultQuoteSettingsCreator`
    - trade-flow-api/src/business/services/business-creator.service.ts calls `this.defaultQuoteSettingsCreator.create(`
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>BusinessCreator now auto-creates all 5 default resource types including quote email template. ONBD-04 fully satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md tracking for completed requirements</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
Update REQUIREMENTS.md to reflect the actual completion status of Phase 37 requirements:

1. **ONBD-02** (line ~22): Change `- [ ] **ONBD-02**:` to `- [x] **ONBD-02**:` — BusinessStep form with trade selection is fully implemented in 37-02.

2. **ONBD-03** (line ~23): Change `- [ ] **ONBD-03**:` to `- [x] **ONBD-03**:` — GB/GBP is hardcoded in SetupLoadingScreen.

3. **ONBD-04** (line ~24): Change `- [ ] **ONBD-04**:` to `- [x] **ONBD-04**:` — After Task 1 of this plan, all 5 default resource types are auto-created.

4. **Traceability table** (lines ~86-110): Update the Status column:
   - ONBD-02: Change `Pending` to `Complete`
   - ONBD-03: Change `Pending` to `Complete`
   - ONBD-04: Change `Pending` to `Complete`

5. Update the `Last updated` line at the bottom to today's date: `*Last updated: 2026-04-02 after Phase 37 gap closure*`
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*ONBD-0[234]\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md contains `[x] **ONBD-02**`
    - .planning/REQUIREMENTS.md contains `[x] **ONBD-03**`
    - .planning/REQUIREMENTS.md contains `[x] **ONBD-04**`
    - .planning/REQUIREMENTS.md traceability table shows `Complete` for ONBD-02, ONBD-03, ONBD-04
    - Last updated line contains `2026-04-02`
  </acceptance_criteria>
  <done>REQUIREMENTS.md accurately reflects all Phase 37 requirement completion states. ONBD-02, ONBD-03, ONBD-04 marked complete.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes in trade-flow-api: `cd trade-flow-api && npx tsc --noEmit`
2. DefaultQuoteSettingsCreatorService is registered, exported, and called from BusinessCreator
3. REQUIREMENTS.md shows ONBD-02, ONBD-03, ONBD-04 as checked and Complete
4. All 9 phase requirement IDs (ONBD-01 through ONBD-07, TRIAL-02, TRIAL-03) show as Complete in REQUIREMENTS.md
</verification>

<success_criteria>
- BusinessCreator.create() calls all 5 default creator services including DefaultQuoteSettingsCreatorService
- REQUIREMENTS.md tracking matches actual implementation state
- TypeScript compilation passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/37-onboarding-wizard-pages/37-04-SUMMARY.md`
</output>
