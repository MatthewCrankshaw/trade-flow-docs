---
phase: 47-convert-to-quote-mark-as-lost
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-api/src/estimate/entities/estimate.entity.ts
  - trade-flow-api/src/estimate/data-transfer-objects/estimate.dto.ts
  - trade-flow-api/src/estimate/responses/estimate.response.ts
  - trade-flow-api/src/estimate/repositories/estimate.repository.ts
  - trade-flow-api/src/quote/entities/quote.entity.ts
  - trade-flow-api/src/quote/data-transfer-objects/quote.dto.ts
  - trade-flow-api/src/quote/responses/quote.response.ts
  - trade-flow-api/src/quote/repositories/quote.repository.ts
  - trade-flow-api/src/estimate/requests/mark-estimate-lost.request.ts
  - trade-flow-api/src/estimate/requests/convert-estimate.request.ts
  - trade-flow-api/src/estimate/enums/lost-reason.enum.ts
  - trade-flow-api/src/estimate/services/estimate-to-quote-converter.service.ts
  - trade-flow-api/src/estimate/services/estimate-lost-marker.service.ts
  - trade-flow-api/src/estimate/test/services/estimate-to-quote-converter.service.spec.ts
  - trade-flow-api/src/estimate/test/services/estimate-lost-marker.service.spec.ts
  - trade-flow-api/src/estimate/test/mocks/estimate-mock-generator.ts
autonomous: true
requirements:
  - CONV-01
  - CONV-02
  - CONV-04
  - CONV-05
  - LOST-01
  - LOST-02
user_setup: []

must_haves:
  truths:
    - "EstimateToQuoteConverter creates a draft quote from estimate line items with literal tax rates and no contingency"
    - "EstimateToQuoteConverter returns existing quoteId on idempotent re-call when convertedToQuoteId is already set"
    - "EstimateLostMarker transitions estimate to Lost with optional structured reason and freeform notes"
    - "Both services cancel follow-ups via IEstimateFollowupCanceller"
    - "Quote entity/DTO/response carry sourceEstimateId and sourceEstimateNumber for back-link"
  artifacts:
    - path: "trade-flow-api/src/estimate/services/estimate-to-quote-converter.service.ts"
      provides: "Convert estimate to quote service"
      exports: ["EstimateToQuoteConverter"]
    - path: "trade-flow-api/src/estimate/services/estimate-lost-marker.service.ts"
      provides: "Mark estimate as lost service"
      exports: ["EstimateLostMarker"]
    - path: "trade-flow-api/src/estimate/requests/mark-estimate-lost.request.ts"
      provides: "Request DTO with optional reason and notes"
      exports: ["MarkEstimateLostRequest"]
    - path: "trade-flow-api/src/estimate/enums/lost-reason.enum.ts"
      provides: "Shared lost/decline reason enum"
      exports: ["LostReason"]
    - path: "trade-flow-api/src/estimate/test/services/estimate-to-quote-converter.service.spec.ts"
      provides: "Converter unit tests"
      min_lines: 80
    - path: "trade-flow-api/src/estimate/test/services/estimate-lost-marker.service.spec.ts"
      provides: "Lost marker unit tests"
      min_lines: 40
  key_links:
    - from: "estimate-to-quote-converter.service.ts"
      to: "QuoteCreator"
      via: "QuoteCreator.create() for quote+line-item creation"
      pattern: "quoteCreator\\.create"
    - from: "estimate-to-quote-converter.service.ts"
      to: "EstimateRepository"
      via: "atomic convertedToQuoteId write"
      pattern: "estimateRepository"
    - from: "estimate-lost-marker.service.ts"
      to: "EstimateTransitionService"
      via: "transition to LOST"
      pattern: "estimateTransitionService\\.transition"
---

<objective>
Backend services and schema changes for Convert to Quote and Mark as Lost flows.

Purpose: Create the two terminal-transition services (EstimateToQuoteConverter, EstimateLostMarker) with their supporting entity/DTO schema extensions on both estimate and quote modules, request DTOs, and comprehensive unit tests.
Output: Two new service classes, entity/DTO/response schema changes on both modules, request classes, enum, and unit tests all passing CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-convert-to-quote-mark-as-lost/47-CONTEXT.md
@.planning/phases/47-convert-to-quote-mark-as-lost/47-RESEARCH.md
@.planning/phases/45-public-customer-page-response-handling/45-03-SUMMARY.md
@.planning/phases/41-estimate-module-crud-backend/41-07-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From estimate entity (Phase 41): IEstimateEntity has these reserved nullable fields:
- convertedAt?: Date (set by transition service)
- convertedToQuoteId?: ObjectId (Phase 47 writes this)
- lostAt?: Date (set by transition service)
Fields NOT yet on entity that must be added: convertIdempotencyKey?: string, lostReason?: string, lostNotes?: string

From estimate DTO: IEstimateDto mirrors entity fields. Same additions needed.

From estimate response: IEstimateResponse mirrors DTO fields exposed to API consumers.

From quote entity: IQuoteEntity has NO sourceEstimateId or sourceEstimateNumber. Must be added as nullable.

From quote DTO: IQuoteDto -- same, needs new fields.

From quote response: IQuoteResponse -- same, needs new fields.

From EstimateTransitionService:
```typescript
transition(estimate: IEstimateDto, targetStatus: EstimateStatus): IEstimateDto
```

From IEstimateFollowupCanceller:
```typescript
interface IEstimateFollowupCanceller {
  cancelAllFollowups(estimateId: string, revisionNumber: number): Promise<void>;
}
```
DI token: ESTIMATE_FOLLOWUP_CANCELLER

From EstimateRetriever:
```typescript
findByIdOrFail(authUser: IUserDto, estimateId: string): Promise<IEstimateDto>
```

From EstimateLineItemRetriever:
```typescript
findByEstimateId(estimateId: string): Promise<IEstimateLineItemDto[]>
```

From QuoteCreator:
```typescript
create(authUser: IUserDto, dto: ICreateQuoteRequest): Promise<IQuoteDto>
```

From decline reason taxonomy (Phase 45 D-CTA-04):
"too_expensive", "going_with_someone_else", "decided_not_to_do_work", "just_getting_idea", "timing_not_right"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Entity/DTO/Response schema extensions and request classes</name>
  <files>
    trade-flow-api/src/estimate/entities/estimate.entity.ts,
    trade-flow-api/src/estimate/data-transfer-objects/estimate.dto.ts,
    trade-flow-api/src/estimate/responses/estimate.response.ts,
    trade-flow-api/src/estimate/repositories/estimate.repository.ts,
    trade-flow-api/src/quote/entities/quote.entity.ts,
    trade-flow-api/src/quote/data-transfer-objects/quote.dto.ts,
    trade-flow-api/src/quote/responses/quote.response.ts,
    trade-flow-api/src/quote/repositories/quote.repository.ts,
    trade-flow-api/src/estimate/requests/mark-estimate-lost.request.ts,
    trade-flow-api/src/estimate/requests/convert-estimate.request.ts,
    trade-flow-api/src/estimate/enums/lost-reason.enum.ts
  </files>
  <read_first>
    trade-flow-api/src/estimate/entities/estimate.entity.ts,
    trade-flow-api/src/estimate/data-transfer-objects/estimate.dto.ts,
    trade-flow-api/src/estimate/responses/estimate.response.ts,
    trade-flow-api/src/estimate/repositories/estimate.repository.ts,
    trade-flow-api/src/quote/entities/quote.entity.ts,
    trade-flow-api/src/quote/data-transfer-objects/quote.dto.ts,
    trade-flow-api/src/quote/responses/quote.response.ts,
    trade-flow-api/src/quote/repositories/quote.repository.ts,
    trade-flow-api/src/quote/controllers/quote.controller.ts,
    trade-flow-api/src/estimate/requests/send-estimate.request.ts
  </read_first>
  <behavior>
    - Test: IEstimateEntity has convertIdempotencyKey, lostReason, lostNotes as optional fields
    - Test: IQuoteEntity has sourceEstimateId, sourceEstimateNumber as optional fields
    - Test: MarkEstimateLostRequest validates optional reason from LostReason enum and optional notes max 500 chars
    - Test: Estimate repository toDto maps new fields correctly
    - Test: Quote repository toDto maps new fields correctly
  </behavior>
  <action>
1. Create `trade-flow-api/src/estimate/enums/lost-reason.enum.ts`:
   ```typescript
   export enum LostReason {
     TOO_EXPENSIVE = "too_expensive",
     GOING_WITH_SOMEONE_ELSE = "going_with_someone_else",
     DECIDED_NOT_TO_DO_WORK = "decided_not_to_do_work",
     JUST_GETTING_IDEA = "just_getting_idea",
     TIMING_NOT_RIGHT = "timing_not_right",
   }
   ```
   These are the same values used in Phase 45's decline reason taxonomy (D-LOST-04).

2. Add to `IEstimateEntity` interface: `convertIdempotencyKey?: string`, `lostReason?: string`, `lostNotes?: string`. The fields `convertedAt`, `convertedToQuoteId`, `lostAt` should already exist from Phase 41 -- verify and add only what is missing.

3. Add to `IEstimateDto` interface: `convertIdempotencyKey?: string`, `lostReason?: string`, `lostNotes?: string`. Mirror the entity additions.

4. Add to `IEstimateResponse` interface: `convertedToQuoteId?: string`, `lostReason?: string`, `lostNotes?: string`, `lostAt?: string`, `convertedAt?: string`. Expose these to the API consumer. Do NOT expose `convertIdempotencyKey` in the response (internal field).

5. Update `EstimateRepository` `toDto()` method to map: `convertIdempotencyKey: entity.convertIdempotencyKey`, `lostReason: entity.lostReason`, `lostNotes: entity.lostNotes`. Verify existing mappings for `convertedAt`, `convertedToQuoteId`, `lostAt` and add if missing.

6. Update estimate `mapToResponse()` (in controller or response mapper) to include: `convertedToQuoteId`, `lostReason`, `lostNotes`, `lostAt`, `convertedAt`. Convert ObjectId to string for `convertedToQuoteId`.

7. Add to `IQuoteEntity` interface: `sourceEstimateId?: ObjectId`, `sourceEstimateNumber?: string`.

8. Add to `IQuoteDto` interface: `sourceEstimateId?: string`, `sourceEstimateNumber?: string`.

9. Add to `IQuoteResponse` interface: `sourceEstimateId?: string`, `sourceEstimateNumber?: string`.

10. Update `QuoteRepository` `toDto()` method to map: `sourceEstimateId: entity.sourceEstimateId?.toString()`, `sourceEstimateNumber: entity.sourceEstimateNumber`.

11. Update quote `mapToResponse()` (in controller) to include: `sourceEstimateId`, `sourceEstimateNumber`.

12. Create `trade-flow-api/src/estimate/requests/mark-estimate-lost.request.ts`:
    ```typescript
    import { IsOptional, IsString, MaxLength, IsEnum } from "class-validator";
    import { LostReason } from "@estimate/enums/lost-reason.enum";

    export class MarkEstimateLostRequest {
      @IsOptional()
      @IsEnum(LostReason)
      reason?: string;

      @IsOptional()
      @IsString()
      @MaxLength(500)
      notes?: string;
    }
    ```

13. Create `trade-flow-api/src/estimate/requests/convert-estimate.request.ts` as an empty class (the endpoint reads idempotency key from headers, not body):
    ```typescript
    export class ConvertEstimateRequest {}
    ```
  </action>
  <verify>
    <automated>cd /Users/mattc/Documents/projects/agent/trade-flow-api && npx tsc --noEmit --project tsconfig-check.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-api/src/estimate/entities/estimate.entity.ts contains `convertIdempotencyKey?: string`
    - trade-flow-api/src/estimate/entities/estimate.entity.ts contains `lostReason?: string`
    - trade-flow-api/src/estimate/entities/estimate.entity.ts contains `lostNotes?: string`
    - trade-flow-api/src/quote/entities/quote.entity.ts contains `sourceEstimateId`
    - trade-flow-api/src/quote/entities/quote.entity.ts contains `sourceEstimateNumber`
    - trade-flow-api/src/estimate/enums/lost-reason.enum.ts contains `TOO_EXPENSIVE = "too_expensive"`
    - trade-flow-api/src/estimate/enums/lost-reason.enum.ts contains `TIMING_NOT_RIGHT = "timing_not_right"`
    - trade-flow-api/src/estimate/requests/mark-estimate-lost.request.ts contains `@IsEnum(LostReason)`
    - trade-flow-api/src/estimate/requests/mark-estimate-lost.request.ts contains `@MaxLength(500)`
    - trade-flow-api/src/estimate/responses/estimate.response.ts contains `convertedToQuoteId`
    - trade-flow-api/src/estimate/responses/estimate.response.ts contains `lostReason`
    - trade-flow-api/src/quote/responses/quote.response.ts contains `sourceEstimateId`
    - trade-flow-api/src/quote/responses/quote.response.ts contains `sourceEstimateNumber`
    - TypeScript compilation passes with zero errors
  </acceptance_criteria>
  <done>All entity/DTO/response interfaces extended with new fields, repository toDto mappings updated, request DTOs created, LostReason enum created, TypeScript compiles cleanly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EstimateToQuoteConverter and EstimateLostMarker services with unit tests</name>
  <files>
    trade-flow-api/src/estimate/services/estimate-to-quote-converter.service.ts,
    trade-flow-api/src/estimate/services/estimate-lost-marker.service.ts,
    trade-flow-api/src/estimate/test/services/estimate-to-quote-converter.service.spec.ts,
    trade-flow-api/src/estimate/test/services/estimate-lost-marker.service.spec.ts,
    trade-flow-api/src/estimate/test/mocks/estimate-mock-generator.ts
  </files>
  <read_first>
    trade-flow-api/src/estimate/services/estimate-creator.service.ts,
    trade-flow-api/src/estimate/services/estimate-retriever.service.ts,
    trade-flow-api/src/estimate/services/estimate-transition.service.ts,
    trade-flow-api/src/quote/services/quote-creator.service.ts,
    trade-flow-api/src/estimate/repositories/estimate.repository.ts,
    trade-flow-api/src/estimate/services/estimate-line-item-retriever.service.ts,
    trade-flow-api/src/estimate/test/mocks/estimate-mock-generator.ts,
    trade-flow-api/src/estimate/data-transfer-objects/estimate.dto.ts,
    trade-flow-api/src/estimate/data-transfer-objects/estimate-line-item.dto.ts,
    trade-flow-api/src/quote/data-transfer-objects/quote.dto.ts,
    trade-flow-api/src/estimate/interfaces/estimate-followup-canceller.interface.ts,
    trade-flow-api/src/estimate/enums/estimate-status.enum.ts,
    trade-flow-api/src/estimate/enums/estimate-transitions.ts
  </read_first>
  <behavior>
    - Test: convert() creates a quote via QuoteCreator from SENT estimate with line items copied
    - Test: convert() creates a quote via QuoteCreator from RESPONDED estimate
    - Test: convert() returns existing quoteId when convertedToQuoteId is already set (idempotency)
    - Test: convert() transitions estimate to CONVERTED status
    - Test: convert() writes convertedToQuoteId and convertIdempotencyKey on estimate
    - Test: convert() calls cancelAllFollowups on the followup canceller
    - Test: convert() rejects estimates not in SENT or RESPONDED status
    - Test: convert() copies line items with literal tax rates (no contingency applied)
    - Test: markAsLost() transitions estimate to LOST status
    - Test: markAsLost() writes lostReason and lostNotes on estimate
    - Test: markAsLost() calls cancelAllFollowups on the followup canceller
    - Test: markAsLost() accepts null reason and null notes (both optional)
    - Test: markAsLost() rejects estimates not in SENT, VIEWED, or RESPONDED status
  </behavior>
  <action>
1. Create `trade-flow-api/src/estimate/services/estimate-to-quote-converter.service.ts`:

   @Injectable() class EstimateToQuoteConverter with constructor dependencies:
   - estimateRetriever: EstimateRetriever
   - estimateRepository: EstimateRepository
   - estimateLineItemRetriever: EstimateLineItemRetriever
   - estimateTransitionService: EstimateTransitionService
   - quoteCreator: QuoteCreator
   - quoteRepository: QuoteRepository (for back-link update)
   - @Inject(ESTIMATE_FOLLOWUP_CANCELLER) followupCanceller: IEstimateFollowupCanceller

   Method `async convert(authUser: IUserDto, estimateId: string, idempotencyKey: string): Promise<string>`:

   Step 1: `const estimate = await this.estimateRetriever.findByIdOrFail(authUser, estimateId)`
   
   Step 2: Idempotency check -- if `estimate.convertedToQuoteId` is already set, return `estimate.convertedToQuoteId` immediately.
   
   Step 3: Validate status is SENT or RESPONDED. If not, throw InvalidRequestError with ErrorCodes.ESTIMATE_INVALID_TRANSITION and details explaining only SENT/RESPONDED can be converted (per D-CONV-01, even though transition map allows VIEWED).
   
   Step 4: Load estimate line items via `this.estimateLineItemRetriever.findByEstimateId(estimateId)`.
   
   Step 5: Build quote creation request from estimate data. Map fields:
   - customerId from estimate.customerId
   - jobId from estimate.jobId
   - lineItems: map each IEstimateLineItemDto to quote line item shape (itemId, quantity, unitPrice, taxRate -- literal snapshot). Drop any contingency-related fields.
   - Notes/scope from estimate if applicable
   
   Step 6: Create quote via `this.quoteCreator.create(authUser, quoteRequest)`. This generates Q-YYYY-NNN number, validates customer/job, enforces policy.
   
   Step 7: Update quote with back-link fields via `this.quoteRepository.update(createdQuote.id, { sourceEstimateId: estimate.id, sourceEstimateNumber: estimate.number })`.
   
   Step 8: Transition estimate to CONVERTED via `this.estimateTransitionService.transition(estimate, EstimateStatus.CONVERTED)`.
   
   Step 9: Write `convertedToQuoteId` and `convertIdempotencyKey` on estimate via `this.estimateRepository.update(estimateId, { convertedToQuoteId: createdQuote.id, convertIdempotencyKey: idempotencyKey })`.
   
   Step 10: Cancel follow-ups via `this.followupCanceller.cancelAllFollowups(estimate.id, estimate.revisionNumber)`.
   
   Step 11: Return `createdQuote.id`.

2. Create `trade-flow-api/src/estimate/services/estimate-lost-marker.service.ts`:

   @Injectable() class EstimateLostMarker with constructor dependencies:
   - estimateRetriever: EstimateRetriever
   - estimateRepository: EstimateRepository
   - estimateTransitionService: EstimateTransitionService
   - @Inject(ESTIMATE_FOLLOWUP_CANCELLER) followupCanceller: IEstimateFollowupCanceller

   Method `async markAsLost(authUser: IUserDto, estimateId: string, reason?: string, notes?: string): Promise<IEstimateDto>`:

   Step 1: `const estimate = await this.estimateRetriever.findByIdOrFail(authUser, estimateId)`
   
   Step 2: Validate status is SENT, VIEWED, or RESPONDED. If not, throw InvalidRequestError with ErrorCodes.ESTIMATE_INVALID_TRANSITION.
   
   Step 3: Write `lostReason` and `lostNotes` on estimate via `this.estimateRepository.update(estimateId, { lostReason: reason ?? null, lostNotes: notes ?? null })`.
   
   Step 4: Transition to LOST via `this.estimateTransitionService.transition(estimate, EstimateStatus.LOST)`.
   
   Step 5: Cancel follow-ups via `this.followupCanceller.cancelAllFollowups(estimate.id, estimate.revisionNumber)`.
   
   Step 6: Re-fetch and return the updated estimate via `this.estimateRetriever.findByIdOrFail(authUser, estimateId)`.

3. Create unit test files following existing test patterns (see estimate-creator.service.spec.ts for mock setup):

   `estimate-to-quote-converter.service.spec.ts`:
   - Mock all dependencies (estimateRetriever, estimateRepository, estimateLineItemRetriever, estimateTransitionService, quoteCreator, quoteRepository, followupCanceller)
   - Use estimate mock generator for test data
   - Tests per the behavior list above (8+ specs)
   
   `estimate-lost-marker.service.spec.ts`:
   - Mock all dependencies
   - Tests per the behavior list above (5+ specs)

4. Update `trade-flow-api/src/estimate/test/mocks/estimate-mock-generator.ts` if needed to add helper methods for generating SENT/RESPONDED estimates with line items for converter test scenarios.
  </action>
  <verify>
    <automated>cd /Users/mattc/Documents/projects/agent/trade-flow-api && npm test -- --testPathPattern="estimate-to-quote-converter|estimate-lost-marker" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-api/src/estimate/services/estimate-to-quote-converter.service.ts contains `@Injectable()` and `class EstimateToQuoteConverter`
    - trade-flow-api/src/estimate/services/estimate-to-quote-converter.service.ts contains `convert(authUser: IUserDto`
    - trade-flow-api/src/estimate/services/estimate-to-quote-converter.service.ts contains `estimate.convertedToQuoteId` (idempotency check)
    - trade-flow-api/src/estimate/services/estimate-to-quote-converter.service.ts contains `quoteCreator.create`
    - trade-flow-api/src/estimate/services/estimate-to-quote-converter.service.ts contains `cancelAllFollowups`
    - trade-flow-api/src/estimate/services/estimate-lost-marker.service.ts contains `@Injectable()` and `class EstimateLostMarker`
    - trade-flow-api/src/estimate/services/estimate-lost-marker.service.ts contains `markAsLost(authUser: IUserDto`
    - trade-flow-api/src/estimate/services/estimate-lost-marker.service.ts contains `lostReason`
    - trade-flow-api/src/estimate/services/estimate-lost-marker.service.ts contains `cancelAllFollowups`
    - All converter and lost marker tests pass with zero failures
  </acceptance_criteria>
  <done>EstimateToQuoteConverter creates quotes from SENT/RESPONDED estimates with idempotency, line-item copy, transition, and follow-up cancellation. EstimateLostMarker transitions to Lost with optional reason/notes and follow-up cancellation. All unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Untrusted input: idempotency key header, lost reason/notes body |
| estimate module -> quote module | Cross-module write: converter creates quotes in another module's collection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Spoofing | Convert endpoint | mitigate | JwtAuthGuard + EstimatePolicy ownership check via EstimateRetriever.findByIdOrFail |
| T-47-02 | Tampering | Status transition | mitigate | EstimateTransitionService enforces ALLOWED_TRANSITIONS map; service-level status check restricts to SENT/RESPONDED (convert) and SENT/VIEWED/RESPONDED (lost) |
| T-47-03 | Tampering | Lost reason input | mitigate | class-validator @IsEnum(LostReason) on reason, @MaxLength(500) on notes |
| T-47-04 | Repudiation | Idempotency key | accept | Key stored on entity for audit; replay returns same result (safe by design) |
| T-47-05 | Elevation | Cross-module quote creation | mitigate | QuoteCreator.create() enforces QuotePolicy and customer/job validation |
</threat_model>

<verification>
- `cd /Users/mattc/Documents/projects/agent/trade-flow-api && npx tsc --noEmit --project tsconfig-check.json` -- zero errors
- `cd /Users/mattc/Documents/projects/agent/trade-flow-api && npm test -- --testPathPattern="estimate-to-quote-converter|estimate-lost-marker" --no-coverage` -- all pass
- `cd /Users/mattc/Documents/projects/agent/trade-flow-api && npm run ci` -- full CI gate passes
</verification>

<success_criteria>
- EstimateToQuoteConverter creates quotes with line-item copy, idempotency, transition, back-link, and follow-up cancellation
- EstimateLostMarker transitions to Lost with optional reason/notes and follow-up cancellation
- Estimate entity/DTO/response extended with convertIdempotencyKey, lostReason, lostNotes
- Quote entity/DTO/response extended with sourceEstimateId, sourceEstimateNumber
- All unit tests pass, full CI gate passes
</success_criteria>

<output>
After completion, create `.planning/phases/47-convert-to-quote-mark-as-lost/47-01-SUMMARY.md`
</output>
