---
phase: 02-inline-customer-and-job-creation-during-quote-estimate-creat
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - trade-flow-ui/src/features/estimates/components/CreateEstimateForm.tsx
  - trade-flow-ui/src/features/quotes/components/CreateQuoteForm.tsx
autonomous: false
requirements: [D-04, D-05, D-06, D-12]

must_haves:
  truths:
    - "Estimate creation dialog has job-first pattern identical to quote creation"
    - "Both quote and estimate dialogs have '+ New job' action that opens CreateJobDialog"
    - "After creating a job inline from quote/estimate dialog, the job auto-selects and customer auto-resolves"
    - "The full 3-level creation chain works: quote/estimate -> new job -> new customer -> all auto-select back"
  artifacts:
    - path: "trade-flow-ui/src/features/estimates/components/CreateEstimateForm.tsx"
      provides: "Job-first estimate creation with inline job creation"
      contains: "CreateJobDialog"
    - path: "trade-flow-ui/src/features/quotes/components/CreateQuoteForm.tsx"
      provides: "Job-first quote creation with inline job creation (already exists)"
      contains: "CreateJobDialog"
  key_links:
    - from: "trade-flow-ui/src/features/estimates/components/CreateEstimateForm.tsx"
      to: "trade-flow-ui/src/features/jobs/components/CreateJobDialog.tsx"
      via: "stacked dialog with onJobCreated callback"
      pattern: "CreateJobDialog"
    - from: "trade-flow-ui/src/features/quotes/components/CreateQuoteForm.tsx"
      to: "trade-flow-ui/src/features/jobs/components/CreateJobDialog.tsx"
      via: "stacked dialog with onJobCreated callback"
      pattern: "CreateJobDialog"
---

<objective>
Verify and finalize the full inline creation chain for both quote and estimate dialogs. The PATTERNS.md discovery revealed that CreateEstimateForm already has the job-first pattern with inline job creation, so this plan focuses on verifying parity, ensuring the job title field is optional (auto-generated by backend from Plan 01), and performing a visual checkpoint of the complete 3-level creation chain.

Purpose: Per D-04/D-05, estimate creation must have full parity with quotes. Per D-06/D-12, both dialogs need the "+ New job" action with auto-select after creation. Since Plan 02 enhanced CreateJobDialog with InlineCustomerForm, the full chain (quote/estimate -> job -> customer) should now work end-to-end.

Output: Verified estimate/quote parity, optional job title for inline creation, and visual confirmation of the full creation chain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-inline-customer-and-job-creation-during-quote-estimate-creat/02-CONTEXT.md
@.planning/phases/02-inline-customer-and-job-creation-during-quote-estimate-creat/02-PATTERNS.md
@.planning/phases/02-inline-customer-and-job-creation-during-quote-estimate-creat/02-UI-SPEC.md
@.planning/phases/02-inline-customer-and-job-creation-during-quote-estimate-creat/02-01-SUMMARY.md
@.planning/phases/02-inline-customer-and-job-creation-during-quote-estimate-creat/02-02-SUMMARY.md

<interfaces>
<!-- From 02-PATTERNS.md: CreateEstimateForm already has job-first pattern -->

From trade-flow-ui/src/features/estimates/components/CreateEstimateForm.tsx:
```typescript
// Already has job-first pattern with inline job creation (lines 57, 85-88, 297-303)
// Mirrors CreateQuoteForm exactly
const [createJobOpen, setCreateJobOpen] = useState(false);

const handleJobCreated = (job: { id: string; title: string }) => {
  setSelectedJobId(job.id);
  setSelectedJobTitle(job.title);
};

<CreateJobDialog
  open={createJobOpen}
  onOpenChange={setCreateJobOpen}
  businessId={businessId}
  onJobCreated={handleJobCreated}
/>
```

From trade-flow-ui/src/features/quotes/components/CreateQuoteForm.tsx:
```typescript
// Already has job-first pattern with inline job creation
const [createJobOpen, setCreateJobOpen] = useState(false);

const handleJobCreated = (job: { id: string; title: string }) => {
  setSelectedJobId(job.id);
  setSelectedJobTitle(job.title);
};

// "+ New job" action in Command dropdown:
<CommandSeparator />
<CommandGroup>
  <CommandItem
    onSelect={() => {
      setJobPopoverOpen(false);
      setCreateJobOpen(true);
    }}
  >
    <Plus className="mr-1 h-4 w-4" />
    Add new job
  </CommandItem>
</CommandGroup>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and align estimate dialog parity with quote dialog</name>
  <files>
    trade-flow-ui/src/features/estimates/components/CreateEstimateForm.tsx,
    trade-flow-ui/src/features/quotes/components/CreateQuoteForm.tsx
  </files>
  <read_first>
    trade-flow-ui/src/features/estimates/components/CreateEstimateForm.tsx,
    trade-flow-ui/src/features/quotes/components/CreateQuoteForm.tsx,
    trade-flow-ui/src/features/jobs/components/CreateJobDialog.tsx
  </read_first>
  <action>
    Read both CreateEstimateForm.tsx and CreateQuoteForm.tsx and verify the following parity points exist in BOTH files:

    **Parity checklist:**

    1. **Job selector with "+ New job" action**: Both files must have a `<CommandItem>` with `<Plus />` icon and text "Add new job" (or "+ New job") that sets `createJobOpen` to `true`. If CreateEstimateForm is missing this action, add it matching the exact pattern from CreateQuoteForm.

    2. **CreateJobDialog stacked dialog**: Both files must render `<CreateJobDialog open={createJobOpen} onOpenChange={setCreateJobOpen} businessId={businessId} onJobCreated={handleJobCreated} />` as a sibling JSX element (not nested inside Dialog.Content).

    3. **handleJobCreated callback**: Both files must have a handler that calls `setSelectedJobId(job.id)` and `setSelectedJobTitle(job.title)` (or equivalent state setters).

    4. **Customer auto-resolve from job**: Both files must display the customer name auto-resolved from `job.customerId` / `job.customerName` fields. Verify this is read-only display, not a separate selector.

    **If CreateEstimateForm already has all 4 parity points:** No code changes needed. This is expected based on the PATTERNS.md discovery.

    **If any parity point is missing:** Copy the pattern from CreateQuoteForm and adapt for estimates.

    **Additional check -- "+ New job" copywriting per UI-SPEC:**
    The UI-SPEC specifies the action text as "+ New job". If either file uses different text (e.g., "Add new job"), update to match the UI-SPEC copywriting contract: `+ New job`.

    **Note on job title:** The job title field in CreateJobDialog is handled in Plan 02. No changes needed here -- both forms call CreateJobDialog which internally handles optional title + backend auto-generation.
  </action>
  <verify>
    <automated>cd /Users/mattc/Documents/projects/agent/trade-flow-ui && npm run ci 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `CreateEstimateForm.tsx` contains `createJobOpen` state variable
    - `CreateEstimateForm.tsx` contains `<CreateJobDialog` JSX element
    - `CreateEstimateForm.tsx` contains `handleJobCreated` (or equivalent) function
    - `CreateEstimateForm.tsx` contains CommandItem with "+ New job" text
    - `CreateQuoteForm.tsx` contains CommandItem with "+ New job" text
    - Both files have parity: same job selector pattern, same stacked dialog pattern, same auto-select pattern
    - `npm run ci` in trade-flow-ui exits with code 0
  </acceptance_criteria>
  <done>
    - Estimate creation dialog has full parity with quote creation dialog per D-04/D-05
    - Both dialogs have "+ New job" action per D-06
    - Job auto-selects after inline creation per D-12
    - Customer auto-resolves from job data in both dialogs
    - CI gate passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of full 3-level creation chain</name>
  <what-built>
    The complete inline creation chain is now wired:
    - Quote/Estimate dialog -> "+ New job" -> CreateJobDialog (stacked)
    - CreateJobDialog -> "+ New customer" -> InlineCustomerForm (stacked again)
    - Creating a customer inline -> auto-selects in job form -> creating job -> auto-selects in quote/estimate form
    - Job title auto-generated by backend (no title field needed)
  </what-built>
  <how-to-verify>
    **Test the full chain from Quote creation:**
    1. Navigate to the Quotes page
    2. Click "New Quote" to open the quote creation dialog
    3. In the Job selector, click "+ New job" -- verify CreateJobDialog opens as a stacked dialog on top
    4. In CreateJobDialog, in the customer selector, click "+ New customer" -- verify InlineCustomerForm opens as a third stacked dialog
    5. Type a customer name (e.g., "Test Customer") and click "Create Customer"
    6. Verify: InlineCustomerForm closes, customer auto-selects in CreateJobDialog customer field
    7. Select a job type from the dropdown
    8. Click "Create Job"
    9. Verify: CreateJobDialog closes, job auto-selects in the quote dialog job field, customer name displays
    10. Verify: The auto-generated job title appears (e.g., "Plumbing - Test Customer #1")

    **Test the same chain from Estimate creation:**
    11. Navigate to the Estimates page
    12. Click "New Estimate" and repeat steps 3-10

    **Test ESC behavior:**
    13. Open the full 3-level chain
    14. Press ESC -- verify only the topmost dialog (InlineCustomerForm) closes
    15. Press ESC again -- verify only CreateJobDialog closes
    16. Verify quote/estimate dialog is still open with any previously entered data preserved

    **Test cancel behavior:**
    17. Open the chain, enter data in quote dialog, open CreateJobDialog, then click Cancel
    18. Verify quote dialog data is preserved

    **Test mobile (resize browser to < 768px):**
    19. Open the creation chain on mobile viewport -- verify dialogs are fullscreen per Quick task 260417-bwd pattern
  </how-to-verify>
  <resume-signal>Type "approved" if all verifications pass, or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | No new trust boundaries -- all mutations use existing authenticated endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Spoofing | CreateEstimateForm.tsx | accept | Uses same authenticated RTK Query endpoints as CreateQuoteForm. No new auth surface. Firebase JWT enforced on all API calls via prepareHeaders. |
</threat_model>

<verification>
1. `cd trade-flow-ui && npm run ci` -- full CI gate passes
2. `cd trade-flow-api && npm run ci` -- full API CI gate passes
3. Visual verification: full 3-level creation chain works for both quotes and estimates
4. ESC behavior: closes only topmost dialog
5. Mobile: fullscreen dialogs on small viewports
</verification>

<success_criteria>
- Both quote and estimate dialogs have identical job-first creation UX
- The full creation chain works: quote/estimate -> new job -> new customer -> all auto-select back
- Job titles are auto-generated by the backend
- Dialog stacking, ESC, and cancel behavior work correctly
- Mobile fullscreen dialogs work
- CI gates pass in both repos
</success_criteria>

<output>
After completion, create `.planning/phases/02-inline-customer-and-job-creation-during-quote-estimate-creat/02-03-SUMMARY.md`
</output>
