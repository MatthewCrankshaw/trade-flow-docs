---
phase: 39-welcome-dashboard-and-final-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-ui/src/components/onboarding/OnboardingDialogs.tsx
  - trade-flow-ui/src/components/onboarding/PrerequisiteAlert.tsx
  - trade-flow-ui/src/store/slices/onboardingSlice.ts
  - trade-flow-ui/src/store/middleware/onboardingMiddleware.ts
  - trade-flow-ui/src/contexts/OnboardingProvider.tsx
  - trade-flow-ui/src/contexts/OnboardingContext.ts
  - trade-flow-ui/src/contexts/OnboardingContext.types.ts
  - trade-flow-ui/src/hooks/useOnboarding.ts
  - trade-flow-ui/src/store/utils/onboarding-storage.ts
  - trade-flow-ui/src/App.tsx
  - trade-flow-ui/src/store/index.ts
autonomous: true
requirements:
  - DASH-01

must_haves:
  truths:
    - "Old onboarding wizard components are deleted from the codebase"
    - "Old onboarding Redux slice and middleware are removed from the store"
    - "Old onboarding context providers and hooks are deleted"
    - "No file in the codebase imports any old onboarding module"
    - "TypeScript compilation passes after removal"
  artifacts:
    - path: "trade-flow-ui/src/store/index.ts"
      provides: "Redux store config without onboardingSlice or onboardingMiddleware"
    - path: "trade-flow-ui/src/App.tsx"
      provides: "App root without OnboardingDialogs or OnboardingProvider"
  key_links:
    - from: "trade-flow-ui/src/store/index.ts"
      to: "Redux store shape"
      via: "no onboarding key in root reducer"
      pattern: "onboarding"
---

<objective>
Remove all remnants of the old pre-Phase 37 onboarding system from the trade-flow-ui codebase. This includes the old wizard dialog components, Redux slice, middleware, context providers, hooks, and localStorage utility. All references in consuming files (App.tsx, store config, feature components) must be cleaned.

Purpose: Complete the "Final Cleanup" portion of Phase 39 (per D-09, D-10). The old onboarding system was replaced by the Phase 37 mandatory wizard; these files are dead code.
Output: Clean codebase with no old onboarding imports, TypeScript compiles cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-welcome-dashboard-and-final-cleanup/39-CONTEXT.md
@.planning/phases/39-welcome-dashboard-and-final-cleanup/39-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover and remove all old onboarding files</name>
  <files>
    trade-flow-ui/src/components/onboarding/ (directory deletion)
    trade-flow-ui/src/store/slices/onboardingSlice.ts
    trade-flow-ui/src/store/middleware/onboardingMiddleware.ts
    trade-flow-ui/src/contexts/OnboardingProvider.tsx
    trade-flow-ui/src/contexts/OnboardingContext.ts
    trade-flow-ui/src/contexts/OnboardingContext.types.ts
    trade-flow-ui/src/hooks/useOnboarding.ts
    trade-flow-ui/src/store/utils/onboarding-storage.ts
  </files>
  <read_first>
    trade-flow-ui/src/store/index.ts
    trade-flow-ui/src/App.tsx
    trade-flow-ui/src/store/slices/onboardingSlice.ts
    trade-flow-ui/src/store/middleware/onboardingMiddleware.ts
    trade-flow-ui/src/hooks/useOnboarding.ts
    trade-flow-ui/src/contexts/OnboardingProvider.tsx
  </read_first>
  <action>
Per D-09 and D-10, remove the old pre-Phase 37 onboarding system. The research identified these files to delete.

**Step 1: Full discovery** -- Before deleting anything, run these greps to find ALL consumers of old onboarding:

```bash
cd trade-flow-ui
grep -rn "onboardingSlice\|onboardingReducer\|onboarding:" src/ --include="*.ts" --include="*.tsx"
grep -rn "onboardingMiddleware" src/ --include="*.ts" --include="*.tsx"
grep -rn "OnboardingDialogs\|OnboardingProvider\|OnboardingContext" src/ --include="*.ts" --include="*.tsx"
grep -rn "useOnboarding" src/ --include="*.ts" --include="*.tsx"
grep -rn "openOnboarding\|closeOnboarding" src/ --include="*.ts" --include="*.tsx"
grep -rn "onboarding-storage" src/ --include="*.ts" --include="*.tsx"
grep -rn "PrerequisiteAlert" src/ --include="*.ts" --include="*.tsx"
```

Record every file that imports or references old onboarding. The research lists these known files to delete:

1. `src/components/onboarding/OnboardingDialogs.tsx`
2. `src/components/onboarding/PrerequisiteAlert.tsx`
3. `src/components/onboarding/` -- entire directory (may have additional files discovered by grep)
4. `src/store/slices/onboardingSlice.ts`
5. `src/store/middleware/onboardingMiddleware.ts`
6. `src/contexts/OnboardingProvider.tsx`
7. `src/contexts/OnboardingContext.ts`
8. `src/contexts/OnboardingContext.types.ts`
9. `src/hooks/useOnboarding.ts`
10. `src/store/utils/onboarding-storage.ts`

**Step 2: Delete all identified files** using `rm` or `git rm`.

**Step 3: Remove from barrel exports** -- Check and clean any `index.ts` barrel files that re-export deleted modules:
- `src/store/slices/index.ts` (if it exists, remove onboardingSlice export)
- `src/hooks/index.ts` (if it exists, remove useOnboarding export)
- `src/contexts/index.ts` (if it exists, remove OnboardingProvider/Context exports)

**IMPORTANT:** Do NOT delete anything from `src/features/onboarding/` -- that is the NEW Phase 37 onboarding wizard. Only delete the OLD system files listed above.
  </action>
  <verify>
    <automated>cd trade-flow-ui && ls src/components/onboarding/ 2>&1 | grep -c "No such file" && ls src/store/slices/onboardingSlice.ts 2>&1 | grep -c "No such file" && ls src/hooks/useOnboarding.ts 2>&1 | grep -c "No such file"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/onboarding/` directory does not exist
    - `src/store/slices/onboardingSlice.ts` does not exist
    - `src/store/middleware/onboardingMiddleware.ts` does not exist
    - `src/contexts/OnboardingProvider.tsx` does not exist
    - `src/contexts/OnboardingContext.ts` does not exist
    - `src/contexts/OnboardingContext.types.ts` does not exist
    - `src/hooks/useOnboarding.ts` does not exist
    - `src/store/utils/onboarding-storage.ts` does not exist
    - `src/features/onboarding/` directory STILL exists (new Phase 37 wizard -- must NOT be deleted)
  </acceptance_criteria>
  <done>All old onboarding system files are deleted; new Phase 37 onboarding wizard remains intact</done>
</task>

<task type="auto">
  <name>Task 2: Clean all old onboarding references from consuming files</name>
  <files>
    trade-flow-ui/src/App.tsx
    trade-flow-ui/src/store/index.ts
  </files>
  <read_first>
    trade-flow-ui/src/App.tsx
    trade-flow-ui/src/store/index.ts
  </read_first>
  <action>
After file deletion in Task 1, remove all dangling imports and references from consuming files. The grep results from Task 1 will reveal the full list, but the known consumers are:

**`src/store/index.ts`:**
- Remove the `onboardingSlice` import line (e.g., `import onboardingReducer from "./slices/onboardingSlice"` or similar)
- Remove `onboarding: onboardingReducer` from the `reducer` config object in `configureStore`
- Remove `onboardingMiddleware` import and remove it from the `middleware` array in `configureStore`
- The Redux store shape should no longer have an `onboarding` key

**`src/App.tsx`:**
- Remove `<OnboardingDialogs />` component render (if present)
- Remove `<OnboardingProvider>` wrapper (if present -- may wrap children)
- Remove import lines for `OnboardingDialogs`, `OnboardingProvider`
- Remove any `OnboardingContext` import

**Any feature component importing `openOnboarding()` or `closeOnboarding()`:**
- The grep from Task 1 will identify these files
- Remove the dispatch calls: `dispatch(openOnboarding())`, `dispatch(closeOnboarding())`
- Remove the import of the action creator from `onboardingSlice`
- If the dispatch was the only reason for the import, remove the `useAppDispatch` import too (only if no other dispatches remain in the file)

**Verify no references remain:**
```bash
cd trade-flow-ui
grep -rn "onboardingSlice\|onboardingMiddleware\|OnboardingDialogs\|OnboardingProvider\|OnboardingContext\|useOnboarding\|openOnboarding\|closeOnboarding\|onboarding-storage\|PrerequisiteAlert" src/ --include="*.ts" --include="*.tsx"
```

The only results should be from `src/features/onboarding/` (the NEW wizard -- that is expected and correct).
  </action>
  <verify>
    <automated>cd trade-flow-ui && npx tsc --noEmit 2>&1 | head -30 && echo "---GREP CHECK---" && grep -rn "onboardingSlice\|onboardingMiddleware\|OnboardingDialogs\|OnboardingProvider\|useOnboarding\|openOnboarding\|closeOnboarding\|onboarding-storage" src/ --include="*.ts" --include="*.tsx" | grep -v "src/features/onboarding/" | head -20 || echo "No old onboarding references found"</automated>
  </verify>
  <acceptance_criteria>
    - `src/store/index.ts` does NOT contain `onboardingSlice` or `onboardingReducer` or `onboardingMiddleware`
    - `src/store/index.ts` reducer config does NOT have an `onboarding` key
    - `src/App.tsx` does NOT contain `OnboardingDialogs` or `OnboardingProvider`
    - `grep -rn "onboardingSlice\|onboardingMiddleware\|OnboardingDialogs\|OnboardingProvider\|useOnboarding\|openOnboarding\|closeOnboarding\|onboarding-storage" src/ --include="*.ts" --include="*.tsx" | grep -v "src/features/onboarding/"` returns empty
    - `npx tsc --noEmit` passes with zero errors
    - ESLint passes: `npx eslint src/App.tsx src/store/index.ts --no-error-on-unmatched-pattern` returns no errors
  </acceptance_criteria>
  <done>All old onboarding references removed from consuming files; TypeScript compiles cleanly; no dangling imports or references to old system remain outside src/features/onboarding/</done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `cd trade-flow-ui && npx tsc --noEmit` passes with zero errors
2. No old onboarding imports: `grep -rn "onboardingSlice\|onboardingMiddleware\|OnboardingDialogs\|OnboardingProvider\|useOnboarding\|openOnboarding\|closeOnboarding\|onboarding-storage" trade-flow-ui/src/ --include="*.ts" --include="*.tsx" | grep -v "features/onboarding/"` returns empty
3. New wizard intact: `ls trade-flow-ui/src/features/onboarding/components/OnboardingWizard.tsx` exists
4. Redux store clean: `grep "onboarding" trade-flow-ui/src/store/index.ts` returns nothing
</verification>

<success_criteria>
- All 10 old onboarding files deleted from codebase
- All imports and references to old onboarding removed from App.tsx, store config, and any feature components
- New Phase 37 onboarding wizard (src/features/onboarding/) remains intact and functional
- TypeScript compiles with zero errors
- No runtime references to deleted modules
</success_criteria>

<output>
After completion, create `.planning/phases/39-welcome-dashboard-and-final-cleanup/39-02-SUMMARY.md`
</output>
