---
phase: 56-impersonation-backend-audit
plan: 03
type: execute
wave: 1
depends_on: ["56-02"]
files_modified:
  - src/support/support.module.ts
  - nodemon-worker.json
  - tsconfig.json
  - src/auth/auth.guard.ts
autonomous: true
gap_closure: true
requirements: [IMP-01, IMP-06]

must_haves:
  truths:
    - "Server boots without errors after cold start with ImpersonationModule registered"
    - "trade-flow-worker boots without TS2339 error on request.impersonator"
    - "Health check returns live response after boot"
  artifacts:
    - path: "src/support/support.module.ts"
      provides: "SupportModule with ImpersonationModule import"
      contains: "ImpersonationModule"
    - path: "src/auth/auth.guard.ts"
      provides: "JwtAuthGuard with resolvable ImpersonationAuditRepository in all consumer modules"
  key_links:
    - from: "src/support/support.module.ts"
      to: "src/impersonation/impersonation.module.ts"
      via: "NestJS module import"
      pattern: "ImpersonationModule"
    - from: "src/auth/auth.guard.ts"
      to: "src/impersonation/repositories/impersonation-audit.repository.ts"
      via: "constructor injection"
      pattern: "ImpersonationAuditRepository"
---

<objective>
Fix two boot failures from Phase 56 Plan 02: (A) SupportModule missing ImpersonationModule import causing UnknownDependenciesException, and (B) Express type augmentation not picked up by worker ts-node config causing TS2339.

Purpose: The server cannot boot at all -- every UAT test is blocked by these wiring issues.
Output: Both trade-flow-api and trade-flow-worker boot cleanly with ImpersonationModule fully wired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-impersonation-backend-audit/56-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix NestJS dependency resolution -- add ImpersonationModule to SupportModule imports</name>
  <files>src/support/support.module.ts</files>
  <action>
    The JwtAuthGuard constructor now injects ImpersonationAuditRepository (added in Plan 02). Every module that uses JwtAuthGuard must have ImpersonationModule in its import chain so NestJS can resolve the dependency.

    Gap reason: SupportModule imports UserModule but not ImpersonationModule. When SupportModule's controller uses @UseGuards(JwtAuthGuard), NestJS cannot resolve ImpersonationAuditRepository at index [3].

    Fix: Add ImpersonationModule to SupportModule's imports array. Use forwardRef if there is a circular dependency between SupportModule and ImpersonationModule; otherwise, a direct import is sufficient.

    Also check whether ANY OTHER module that uses JwtAuthGuard is missing the ImpersonationModule import. Scan all *.module.ts files for modules that reference JwtAuthGuard (directly or via controllers that use it) and verify each one imports ImpersonationModule either directly or transitively through AppModule. Since ImpersonationModule is registered in AppModule, if the guard's dependency is provided globally that may suffice -- but the UAT error proves it does not for SupportModule, so add explicit imports where needed.
  </action>
  <verify>
    <automated>cd ../trade-flow-api && npx nest build 2>&1 | tail -20</automated>
  </verify>
  <done>SupportModule (and any other affected modules) import ImpersonationModule. NestJS compiles and resolves all dependencies without UnknownDependenciesException.</done>
</task>

<task type="auto">
  <name>Task 2: Fix worker TypeScript compilation -- ensure express.d.ts type augmentation is included</name>
  <files>nodemon-worker.json, tsconfig.json, src/auth/auth.guard.ts</files>
  <action>
    Gap reason: trade-flow-worker crashes with TS2339 "Property 'impersonator' does not exist on type 'Request'" because the Express type augmentation in src/auth/types/express.d.ts is not picked up by the worker's ts-node config.

    Investigate the worker's tsconfig and nodemon-worker.json setup:
    1. Check if nodemon-worker.json points to a different tsconfig than the main app
    2. Check if the worker tsconfig includes src/auth/types/ in its file scope
    3. Check if the express.d.ts file uses proper module augmentation syntax (declare global vs declare module)

    Possible fixes (apply whichever resolves the issue):
    - If worker uses a separate tsconfig, add src/auth/types/express.d.ts to its include paths
    - If the .d.ts file uses incorrect augmentation syntax, fix the declaration to use proper Express namespace augmentation that works with both ts-node register and standard tsc
    - If nodemon-worker.json uses --files=false or similar flag that skips .d.ts, add --files flag or adjust the ts-node configuration

    The fix must make `request.impersonator` compile cleanly in both the API and worker entry points.

    After fixing, run the full CI gate to confirm nothing is broken.
  </action>
  <verify>
    <automated>cd ../trade-flow-api && npm run ci 2>&1 | tail -30</automated>
  </verify>
  <done>Both trade-flow-api and trade-flow-worker compile without TS2339 errors. npm run ci passes with all tests green, lint clean, format clean, typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced -- this plan only fixes module wiring and TypeScript configuration.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-10 | Tampering | support.module.ts | accept | Module import is additive only -- no authorization logic changed |
</threat_model>

<verification>
1. `cd ../trade-flow-api && npm run ci` passes (tests, lint, format, typecheck)
2. `cd ../trade-flow-api && npm run start:dev` boots without errors (manual spot check)
3. Worker process compiles without TS2339 on request.impersonator
</verification>

<success_criteria>
- Server boots cleanly with ImpersonationModule fully wired
- Worker compiles without type errors on request.impersonator
- All 943+ tests pass
- CI gate (tests, lint, format, typecheck) passes
</success_criteria>

<output>
After completion, create `.planning/phases/56-impersonation-backend-audit/56-03-SUMMARY.md`
</output>
