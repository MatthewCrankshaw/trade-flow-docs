---
phase: 18-quote-email-sending
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-api/src/email/services/email-sender.service.ts
  - trade-flow-api/src/quote/services/quote-email-sender.service.ts
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [DLVR-01]

must_haves:
  truths:
    - "SendGrid errors return a meaningful error message to the client instead of a generic 500 CORE_0"
    - "Placeholder SendGrid API key is detected and a clear error is returned at send time"
    - "REQUIREMENTS.md DLVR-01 accurately reflects Phase 18 scope (email with link, no PDF)"
  artifacts:
    - path: "trade-flow-api/src/email/services/email-sender.service.ts"
      provides: "Try/catch around sgMail.send() with meaningful error propagation"
      contains: "catch"
    - path: "trade-flow-api/src/quote/services/quote-email-sender.service.ts"
      provides: "Wrapped email send with quote-specific error context"
      contains: "HttpException"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected DLVR-01 text without PDF attachment clause"
      contains: "DLVR-01a"
  key_links:
    - from: "trade-flow-api/src/email/services/email-sender.service.ts"
      to: "@sendgrid/mail"
      via: "try/catch around sgMail.send()"
      pattern: "try.*sgMail\\.send"
---

<objective>
Fix email sending error handling and correct DLVR-01 requirements accuracy.

Purpose: The send endpoint currently returns a generic 500 CORE_0 error when SendGrid fails (e.g., placeholder API key). Users need meaningful error feedback. REQUIREMENTS.md also needs correction to reflect the deliberate PDF deferral to Phase 20.
Output: Hardened email sending with proper error messages, corrected requirements document.
</objective>

<execution_context>
@/Users/mcrankshaw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mcrankshaw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-quote-email-sending/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error handling to EmailSenderService and QuoteEmailSender</name>
  <files>
    trade-flow-api/src/email/services/email-sender.service.ts,
    trade-flow-api/src/quote/services/quote-email-sender.service.ts
  </files>
  <read_first>
    trade-flow-api/src/email/services/email-sender.service.ts,
    trade-flow-api/src/quote/services/quote-email-sender.service.ts,
    trade-flow-api/src/email/interfaces/send-email.dto.ts,
    trade-flow-api/src/core/errors/error-codes.enum.ts
  </read_first>
  <action>
    In `email-sender.service.ts`:
    1. In the constructor, after `sgMail.setApiKey(apiKey)`, add a warning log if apiKey is falsy or matches known placeholder patterns (contains "your-" or "placeholder" or equals "your-sendgrid-api-key-here"). Store a boolean `private apiKeyConfigured: boolean` on the class.
    2. In `sendEmail()`, add a guard at the top: if `!this.apiKeyConfigured`, throw `new HttpException('Email sending is not configured. Set a valid SENDGRID_API_KEY environment variable.', HttpStatus.SERVICE_UNAVAILABLE)`. Import `HttpException` and `HttpStatus` from `@nestjs/common`.
    3. Wrap `await sgMail.send(msg)` in a try/catch. In the catch block:
       - Type the error as `unknown`
       - Check if error has a `response` property with `body` (SendGrid error shape): `if (error && typeof error === 'object' && 'response' in error)`
       - Extract the message: `const sgError = error as { response?: { body?: { errors?: { message: string }[] } } }; const detail = sgError.response?.body?.errors?.[0]?.message ?? 'Unknown SendGrid error';`
       - Throw `new HttpException(\`Email delivery failed: \${detail}\`, HttpStatus.BAD_GATEWAY)`
       - For non-SendGrid errors, throw `new HttpException('Email delivery failed unexpectedly', HttpStatus.INTERNAL_SERVER_ERROR)`
    4. Import `AppLogger` from `@core/services/app-logger.service` and add `private readonly logger = new AppLogger(EmailSenderService.name);`. Log the original error in the catch block with `this.logger.error('SendGrid send failed', { to: email.to, error: String(error) })`.

    In `quote-email-sender.service.ts`:
    5. Wrap the `await this.emailSenderService.sendEmail(...)` call (line 52) and the subsequent `quoteTransitionService.transition()` call in a try/catch block. In the catch:
       - If the error is already an `HttpException`, re-throw it (the EmailSenderService errors are already well-formed)
       - Otherwise throw `new HttpException('Failed to send quote email', HttpStatus.INTERNAL_SERVER_ERROR)`
       - Import `HttpException` and `HttpStatus` from `@nestjs/common`
  </action>
  <verify>
    <automated>cd /Users/mcrankshaw/PersonalProjects/trade-flow-api && npx jest --testPathPatterns="quote-email-sender" --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "try" trade-flow-api/src/email/services/email-sender.service.ts returns at least 1
    - grep -c "HttpException" trade-flow-api/src/email/services/email-sender.service.ts returns at least 2
    - grep -c "apiKeyConfigured" trade-flow-api/src/email/services/email-sender.service.ts returns at least 2
    - grep "SERVICE_UNAVAILABLE" trade-flow-api/src/email/services/email-sender.service.ts matches
    - grep "BAD_GATEWAY" trade-flow-api/src/email/services/email-sender.service.ts matches
    - grep -c "HttpException" trade-flow-api/src/quote/services/quote-email-sender.service.ts returns at least 1
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>SendGrid errors return meaningful HTTP error responses instead of generic 500 CORE_0. Placeholder API key detected and returns 503 at send time.</done>
</task>

<task type="auto">
  <name>Task 2: Correct DLVR-01 in REQUIREMENTS.md to reflect Phase 18 vs Phase 20 scope</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md,
    .planning/phases/18-quote-email-sending/18-CONTEXT.md
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`:
    1. Replace the single DLVR-01 line:
       `- [x] **DLVR-01**: User can send a quote to a customer via email with a PDF attachment and a link to view the quote online`
       With two lines:
       `- [x] **DLVR-01a**: User can send a quote to a customer via email with a link to view the quote online`
       `- [ ] **DLVR-01b**: Sent quote emails include the PDF as an attachment`
    2. In the Traceability table, replace the DLVR-01 row with two rows:
       `| DLVR-01a | Phase 18 | Complete |`
       `| DLVR-01b | Phase 20 | Pending |`
    3. Update the coverage count: increment total requirements by 1 (since we split one into two). Update "Mapped to phases" accordingly.
    4. In Phase 20's PDF-01 requirement, it already covers PDF attachment in emails. No change needed there, but verify DLVR-01b does not duplicate PDF-01. If they overlap, add a note: "See also PDF-01" after DLVR-01b.
  </action>
  <verify>
    <automated>grep -c "DLVR-01a" /Users/mcrankshaw/PersonalProjects/.planning/REQUIREMENTS.md && grep -c "DLVR-01b" /Users/mcrankshaw/PersonalProjects/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "DLVR-01a" .planning/REQUIREMENTS.md matches "User can send a quote to a customer via email with a link"
    - grep "DLVR-01b" .planning/REQUIREMENTS.md matches "PDF as an attachment"
    - grep "DLVR-01a.*Phase 18.*Complete" .planning/REQUIREMENTS.md matches in Traceability table
    - grep "DLVR-01b.*Phase 20.*Pending" .planning/REQUIREMENTS.md matches in Traceability table
    - The string "DLVR-01:" (without a or b suffix) no longer appears
  </acceptance_criteria>
  <done>DLVR-01 split into DLVR-01a (Phase 18, email with link, complete) and DLVR-01b (Phase 20, PDF attachment, pending). Requirements accurately reflect implementation scope.</done>
</task>

</tasks>

<verification>
- `cd trade-flow-api && npm run typecheck` passes
- `cd trade-flow-api && npx jest --testPathPatterns="quote-email" --no-coverage` passes
- grep confirms HttpException in email-sender.service.ts
- grep confirms DLVR-01a and DLVR-01b in REQUIREMENTS.md
</verification>

<success_criteria>
- Email sending errors return structured HTTP errors (503 for missing key, 502 for SendGrid failure) instead of 500 CORE_0
- REQUIREMENTS.md accurately reflects Phase 18 scope without PDF attachment clause in DLVR-01a
</success_criteria>

<output>
After completion, create `.planning/phases/18-quote-email-sending/18-04-SUMMARY.md`
</output>
