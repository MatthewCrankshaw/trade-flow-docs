---
phase: 46-follow-up-queue-automation
plan: 06
type: execute
wave: 1
depends_on: [46-04]
files_modified:
  - trade-flow-api/src/worker/processors/estimate-followup.processor.ts
  - trade-flow-api/src/worker/test/processors/estimate-followup.processor.spec.ts
autonomous: true
gap_closure: true
requirements: [FUP-04]

must_haves:
  truths:
    - "EstimateFollowupProcessor renders follow-up emails using EstimateFollowupEmailRenderer with the estimate-followup.html Maizzle template"
    - "Each follow-up email includes followupStep (3d/10d/21d) passed to the renderer for step-specific copy variations"
    - "Follow-up emails contain Go Ahead, Message [FirstName], Not right for me CTAs via the purpose-built template"
  artifacts:
    - path: "trade-flow-api/src/worker/processors/estimate-followup.processor.ts"
      provides: "Correct renderer wiring for follow-up emails"
      contains: "EstimateFollowupEmailRenderer"
    - path: "trade-flow-api/src/worker/test/processors/estimate-followup.processor.spec.ts"
      provides: "Tests asserting correct renderer is called with followupStep"
      contains: "estimateFollowupEmailRenderer"
  key_links:
    - from: "trade-flow-api/src/worker/processors/estimate-followup.processor.ts"
      to: "trade-flow-api/src/email/services/estimate-followup-email-renderer.service.ts"
      via: "constructor injection"
      pattern: "EstimateFollowupEmailRenderer"
---

<objective>
Fix critical bug CR-01: replace the wrong email renderer in EstimateFollowupProcessor so follow-up emails use the purpose-built estimate-followup.html template with step-specific copy and 3 CTAs instead of the generic estimate email renderer.

Purpose: Without this fix, customers receive a generic estimate email with hardcoded copy instead of the step-specific follow-up template with Go Ahead / Message [FirstName] / Not right for me CTAs (blocks FUP-04 and SC#2).
Output: Corrected processor using EstimateFollowupEmailRenderer, updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-follow-up-queue-automation/46-CONTEXT.md
@.planning/phases/46-follow-up-queue-automation/46-VERIFICATION.md
@.planning/phases/46-follow-up-queue-automation/46-04-SUMMARY.md

<interfaces>
<!-- The correct renderer that should be used (currently created but not wired to processor) -->

From trade-flow-api/src/email/services/estimate-followup-email-renderer.service.ts:
- Class: EstimateFollowupEmailRenderer
- Method: render(data: EstimateFollowupEmailData): Promise<string>
- Type: FollowupStep = "3d" | "10d" | "21d"
- Interface EstimateFollowupEmailData: { businessName, followupStep, estimateNumber, viewUrl, traderFirstName, priceDisplay }

From trade-flow-api/src/worker/processors/estimate-followup.processor.ts (current wrong state):
- Imports EstimateEmailRenderer (WRONG -- should be EstimateFollowupEmailRenderer)
- STEP_TO_LABEL map: { 1: "3d", 2: "10d", 3: "21d" }
- followupStep computed at ~line 104 but only used in log message, never passed to renderer

From trade-flow-api/src/email/email.module.ts:
- EstimateFollowupEmailRenderer is already registered as a provider and exported
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace EstimateEmailRenderer with EstimateFollowupEmailRenderer in processor and update tests</name>
  <files>
    trade-flow-api/src/worker/processors/estimate-followup.processor.ts,
    trade-flow-api/src/worker/test/processors/estimate-followup.processor.spec.ts
  </files>
  <read_first>
    trade-flow-api/src/worker/processors/estimate-followup.processor.ts,
    trade-flow-api/src/worker/test/processors/estimate-followup.processor.spec.ts,
    trade-flow-api/src/email/services/estimate-followup-email-renderer.service.ts,
    trade-flow-api/src/email/email.module.ts,
    trade-flow-api/src/worker/worker.module.ts
  </read_first>
  <behavior>
    - Test: handleFollowup calls estimateFollowupEmailRenderer.render (not estimateEmailRenderer.render)
    - Test: render is called with { businessName, followupStep, estimateNumber, viewUrl, traderFirstName, priceDisplay }
    - Test: followupStep value is "3d" for step 1, "10d" for step 2, "21d" for step 3
    - Test: rendered HTML from EstimateFollowupEmailRenderer is passed to emailSenderService.sendEmail
  </behavior>
  <action>
    In estimate-followup.processor.ts:

    1. Replace the import of EstimateEmailRenderer with:
       `import { EstimateFollowupEmailRenderer } from "@email/services/estimate-followup-email-renderer.service";`

    2. In the constructor, replace `private readonly estimateEmailRenderer: EstimateEmailRenderer` with:
       `private readonly estimateFollowupEmailRenderer: EstimateFollowupEmailRenderer`

    3. In handleFollowup(), replace the render call (currently ~line 107 calling `this.estimateEmailRenderer.render(...)` with hardcoded message) with:
       ```
       const followupStep = STEP_TO_LABEL[step] as "3d" | "10d" | "21d";
       const html = await this.estimateFollowupEmailRenderer.render({
         businessName: business.name,
         followupStep,
         estimateNumber: estimate.number,
         viewUrl,
         traderFirstName: business.name,
         priceDisplay,
       });
       ```
       Note: The `followupStep` variable may already be computed earlier in the method for logging. Reuse it, but ensure it is cast to the FollowupStep type and passed to the renderer.

    4. Remove any hardcoded message string like "Just following up on your estimate..." that was being passed to the old renderer.

    5. Remove the now-unused EstimateEmailRenderer import if no other code in this file uses it.

    In estimate-followup.processor.spec.ts:

    6. Replace the mock for EstimateEmailRenderer with a mock for EstimateFollowupEmailRenderer:
       - Mock method: `render` returning a resolved Promise with `"<html>followup</html>"`
       - Provide it in the testing module under the class token EstimateFollowupEmailRenderer

    7. Update all test assertions that check `estimateEmailRenderer.render` to check `estimateFollowupEmailRenderer.render` instead.

    8. Add/update assertion that render is called with an object containing `followupStep` matching the expected step label ("3d", "10d", or "21d").

    9. Ensure existing tests for expiry handling (handleExpiry) are not broken -- they should not reference any email renderer.

    Gap reason: CR-01 in 46-REVIEW.md -- processor imports EstimateEmailRenderer instead of EstimateFollowupEmailRenderer. The purpose-built template with 3 CTAs is never used.
  </action>
  <verify>
    <automated>cd /Users/mattc/Documents/projects/agent/trade-flow-api && npx jest --testPathPattern="estimate-followup.processor" --no-coverage --verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - estimate-followup.processor.ts contains `EstimateFollowupEmailRenderer` import (grep confirms)
    - estimate-followup.processor.ts does NOT contain `EstimateEmailRenderer` import (grep confirms zero matches)
    - estimate-followup.processor.ts contains `estimateFollowupEmailRenderer.render(` call
    - estimate-followup.processor.ts contains `followupStep` passed to the render call (not just in a log message)
    - estimate-followup.processor.spec.ts contains mock for `EstimateFollowupEmailRenderer`
    - All processor tests pass (both follow-up and expiry)
    - `npm run ci` passes in trade-flow-api
  </acceptance_criteria>
  <done>EstimateFollowupProcessor uses EstimateFollowupEmailRenderer with followupStep, businessName, estimateNumber, viewUrl, traderFirstName, priceDisplay -- follow-up emails now render the purpose-built template with 3 CTAs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BullMQ job payload -> processor | Job data from Redis queue consumed by worker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-06 | T (Tampering) | estimate-followup.processor.ts | mitigate | Already mitigated: processor re-reads estimate/business/customer from MongoDB at execution time, never trusts job payload for PII (per 46-04 SUMMARY). This fix does not change that pattern. |
</threat_model>

<verification>
1. `grep "EstimateFollowupEmailRenderer" trade-flow-api/src/worker/processors/estimate-followup.processor.ts` returns matches
2. `grep "EstimateEmailRenderer" trade-flow-api/src/worker/processors/estimate-followup.processor.ts` returns zero matches
3. `grep "followupStep" trade-flow-api/src/worker/processors/estimate-followup.processor.ts` shows it passed to render call
4. All processor tests pass
5. `npm run ci` passes
</verification>

<success_criteria>
Follow-up emails are rendered using the purpose-built estimate-followup.html Maizzle template with step-specific copy (3d/10d/21d) and three CTAs (Go Ahead, Message [FirstName], Not right for me), closing verification gap 1 and satisfying FUP-04.
</success_criteria>

<output>
After completion, create `.planning/phases/46-follow-up-queue-automation/46-06-SUMMARY.md`
</output>
