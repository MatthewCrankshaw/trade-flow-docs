---
phase: 36-public-landing-page-and-route-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-ui/src/pages/landing/LandingHeader.tsx
  - trade-flow-ui/src/pages/landing/HeroSection.tsx
  - trade-flow-ui/src/pages/landing/FeaturesSection.tsx
  - trade-flow-ui/src/pages/landing/PricingSection.tsx
  - trade-flow-ui/src/pages/landing/LandingFooter.tsx
  - trade-flow-ui/src/pages/LandingPage.tsx
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04
  - LAND-05
  - LAND-06

must_haves:
  truths:
    - "Unauthenticated visitor sees a landing page with hero, features, pricing, and navigation"
    - "Hero section displays trade-specific value proposition headline and Start Free Trial CTA"
    - "Three feature cards highlight Jobs, Quotes, and Scheduling capabilities"
    - "Pricing section shows GBP 6/month with 30-day free trial messaging"
    - "Start Free Trial CTA navigates to /login?mode=signup"
    - "Log in button navigates to /login"
    - "Authenticated user is redirected to /dashboard without seeing landing page"
  artifacts:
    - path: "trade-flow-ui/src/pages/LandingPage.tsx"
      provides: "Lazy-loaded landing page with Firebase auth redirect"
      contains: "export default function LandingPage"
    - path: "trade-flow-ui/src/pages/landing/LandingHeader.tsx"
      provides: "Sticky header with logo and nav buttons"
      contains: "export function LandingHeader"
    - path: "trade-flow-ui/src/pages/landing/HeroSection.tsx"
      provides: "Hero section with headline and CTA"
      contains: "export function HeroSection"
    - path: "trade-flow-ui/src/pages/landing/FeaturesSection.tsx"
      provides: "Three feature highlight cards"
      contains: "export function FeaturesSection"
    - path: "trade-flow-ui/src/pages/landing/PricingSection.tsx"
      provides: "Pricing card with GBP 6/month"
      contains: "export function PricingSection"
    - path: "trade-flow-ui/src/pages/landing/LandingFooter.tsx"
      provides: "Footer with copyright"
      contains: "export function LandingFooter"
  key_links:
    - from: "trade-flow-ui/src/pages/LandingPage.tsx"
      to: "firebase/auth"
      via: "onAuthStateChanged for auth redirect"
      pattern: "onAuthStateChanged.*auth.*user.*navigate.*dashboard"
    - from: "trade-flow-ui/src/pages/landing/HeroSection.tsx"
      to: "/login?mode=signup"
      via: "navigate on CTA click"
      pattern: "navigate.*login.*mode=signup"
    - from: "trade-flow-ui/src/pages/landing/LandingHeader.tsx"
      to: "/login"
      via: "navigate on button clicks"
      pattern: "navigate.*login"
---

<objective>
Create the public landing page and all its sub-components for Trade Flow. The landing page is the first thing potential customers see -- it communicates the value proposition for tradespeople and drives sign-up conversions.

Purpose: Deliver a complete, bundle-isolated marketing page with hero section, feature highlights, pricing card, and navigation CTAs. Authenticated users are redirected to the dashboard.
Output: 6 new files in `src/pages/` and `src/pages/landing/` that compose the full landing page experience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-public-landing-page-and-route-restructure/36-CONTEXT.md
@.planning/phases/36-public-landing-page-and-route-restructure/36-RESEARCH.md
@.planning/phases/36-public-landing-page-and-route-restructure/36-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create landing page sub-components (header, hero, features, pricing, footer)</name>
  <files>
    trade-flow-ui/src/pages/landing/LandingHeader.tsx
    trade-flow-ui/src/pages/landing/HeroSection.tsx
    trade-flow-ui/src/pages/landing/FeaturesSection.tsx
    trade-flow-ui/src/pages/landing/PricingSection.tsx
    trade-flow-ui/src/pages/landing/LandingFooter.tsx
  </files>
  <read_first>
    trade-flow-ui/src/components/ui/button.tsx
    trade-flow-ui/src/components/ui/card.tsx
    trade-flow-ui/src/config/firebase.ts
    .planning/phases/36-public-landing-page-and-route-restructure/36-UI-SPEC.md
    .planning/phases/36-public-landing-page-and-route-restructure/36-CONTEXT.md
  </read_first>
  <action>
Create the `src/pages/landing/` directory and five sub-components. All components follow the UI-SPEC layout specifications exactly, with shadcn rule adjustments (use `flex flex-col gap-*` instead of `space-y-*`, use `size-*` instead of `w-* h-*`, icons inside Button use `data-icon` attribute with no sizing classes).

**IMPORT RESTRICTIONS (per D-10):** These files may ONLY import from:
- Their own `src/pages/landing/` directory (sibling imports)
- `@/components/ui/*` (Button, Card, CardHeader, CardTitle, CardContent, CardFooter)
- `lucide-react` (Briefcase, FileText, CalendarDays, Check, ArrowRight, Loader2)
- `react-router-dom` (useNavigate)
- React itself

NO imports from `@/store`, `@/features/*`, `@/hooks/*`, `@/providers/*`, `@/config/*`.

**LandingHeader.tsx** (per D-13, UI-SPEC):
- Sticky header: `sticky top-0 z-50 bg-background/80 backdrop-blur border-b`
- Container: `max-w-5xl mx-auto px-4`
- Layout: `flex items-center justify-between h-14`
- Left: `<span className="text-lg font-semibold">Trade Flow</span>`
- Right: `<div className="flex gap-2">`
  - `<Button variant="outline" size="sm" onClick={() => navigate("/login")}>Log in</Button>`
  - `<Button size="sm" onClick={() => navigate("/login?mode=signup")}>Start Free Trial</Button>`

**HeroSection.tsx** (per D-01, D-12, UI-SPEC):
- Section wrapper: `<section className="py-16 md:py-24">`
- Container: `<div className="mx-auto max-w-3xl px-4 text-center">`
- Headline `<h1>`: `"Run your trade business in one place"` with classes `text-4xl font-semibold tracking-tight md:text-5xl`
- Subheadline `<p>`: `"Jobs, quotes, schedules -- sorted. Built for plumbers, electricians, builders, and every trade in between."` with classes `mx-auto mt-4 max-w-2xl text-lg text-muted-foreground`
- Use em dash character (--) not the literal two dashes in the rendered text
- CTA Button: `<Button size="lg" className="mt-8" onClick={() => navigate("/login?mode=signup")}>Start Free Trial <ArrowRight data-icon="inline-end" /></Button>`

**FeaturesSection.tsx** (per D-02, D-04, UI-SPEC):
- Section wrapper: `<section className="py-12 bg-muted/50">`
- Container: `<div className="mx-auto max-w-5xl px-4">`
- Grid: `<div className="grid grid-cols-1 md:grid-cols-3 gap-4">`
- Three feature cards using `Card`, `CardHeader`, `CardTitle`, `CardContent`:
  1. Icon: `<Briefcase className="size-8 text-primary mb-2" />`, Title: `"Jobs from start to finish"`, Description: `"Track every job from first call to final payment. Customers, materials, labour -- all in one place."`
  2. Icon: `<FileText className="size-8 text-primary mb-2" />`, Title: `"Professional quotes in minutes"`, Description: `"Build quotes, send them by email, and let customers accept online. No chasing, no paperwork."`
  3. Icon: `<CalendarDays className="size-8 text-primary mb-2" />`, Title: `"Your diary, your way"`, Description: `"Book visits, set reminders, and see what's coming up. Never double-book again."`
- CardTitle uses: `text-lg font-semibold`
- CardContent uses: `text-sm text-muted-foreground`
- Use em dash character in descriptions, not literal dashes

**PricingSection.tsx** (per D-03, UI-SPEC):
- Section wrapper: `<section className="py-12">`
- Container: `<div className="mx-auto max-w-md px-4 text-center">`
- Single Card with CardHeader, CardContent, CardFooter:
  - CardHeader: CardTitle `"Trade Flow Pro"` (`text-lg font-semibold`), Price display: `<div className="mt-2"><span className="text-lg font-semibold">£6</span><span className="text-xs text-muted-foreground"> / month</span></div>`
  - CardContent: Feature list in `<ul className="flex flex-col gap-3">` (NOT `space-y-3` per shadcn rules). Each item: `<li className="flex items-center gap-2"><Check className="size-4 text-primary" /><span className="text-sm">{text}</span></li>`
  - Features: "30-day free trial", "No card required", "Manage jobs, quotes, and schedules", "Cancel any time"
  - CardFooter: `<Button className="w-full" onClick={() => navigate("/login?mode=signup")}>Start Free Trial</Button>`
- PricingSection needs `useNavigate` from react-router-dom for the CTA

**LandingFooter.tsx** (per UI-SPEC):
- Footer wrapper: `<footer className="py-8 border-t">`
- Container: `<div className="mx-auto max-w-5xl px-4 text-center">`
- Copyright: `<p className="text-xs text-muted-foreground">Trade Flow {new Date().getFullYear()}</p>`
  </action>
  <verify>
    <automated>cd trade-flow-ui && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-ui/src/pages/landing/LandingHeader.tsx contains `export function LandingHeader`
    - trade-flow-ui/src/pages/landing/LandingHeader.tsx contains `navigate("/login?mode=signup")`
    - trade-flow-ui/src/pages/landing/LandingHeader.tsx contains `navigate("/login")`
    - trade-flow-ui/src/pages/landing/LandingHeader.tsx contains `sticky top-0`
    - trade-flow-ui/src/pages/landing/HeroSection.tsx contains `export function HeroSection`
    - trade-flow-ui/src/pages/landing/HeroSection.tsx contains `Run your trade business in one place`
    - trade-flow-ui/src/pages/landing/HeroSection.tsx contains `data-icon="inline-end"`
    - trade-flow-ui/src/pages/landing/FeaturesSection.tsx contains `export function FeaturesSection`
    - trade-flow-ui/src/pages/landing/FeaturesSection.tsx contains `Briefcase`
    - trade-flow-ui/src/pages/landing/FeaturesSection.tsx contains `FileText`
    - trade-flow-ui/src/pages/landing/FeaturesSection.tsx contains `CalendarDays`
    - trade-flow-ui/src/pages/landing/FeaturesSection.tsx contains `md:grid-cols-3`
    - trade-flow-ui/src/pages/landing/PricingSection.tsx contains `export function PricingSection`
    - trade-flow-ui/src/pages/landing/PricingSection.tsx contains `Trade Flow Pro`
    - trade-flow-ui/src/pages/landing/PricingSection.tsx contains `flex flex-col gap-3`
    - trade-flow-ui/src/pages/landing/PricingSection.tsx does NOT contain `space-y-`
    - trade-flow-ui/src/pages/landing/LandingFooter.tsx contains `export function LandingFooter`
    - trade-flow-ui/src/pages/landing/LandingFooter.tsx contains `getFullYear()`
    - No file in src/pages/landing/ imports from `@/store` or `@/features/` or `@/hooks/` or `@/providers/`
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>All five landing page sub-components exist with correct content, styling per UI-SPEC, shadcn rule compliance (gap not space-y, size-* not w-*/h-*, data-icon on icons in buttons), and no forbidden imports.</done>
</task>

<task type="auto">
  <name>Task 2: Create LandingPage orchestrator with Firebase auth redirect</name>
  <files>
    trade-flow-ui/src/pages/LandingPage.tsx
  </files>
  <read_first>
    trade-flow-ui/src/config/firebase.ts
    trade-flow-ui/src/pages/landing/LandingHeader.tsx
    trade-flow-ui/src/pages/landing/HeroSection.tsx
    trade-flow-ui/src/pages/landing/FeaturesSection.tsx
    trade-flow-ui/src/pages/landing/PricingSection.tsx
    trade-flow-ui/src/pages/landing/LandingFooter.tsx
    .planning/phases/36-public-landing-page-and-route-restructure/36-RESEARCH.md
  </read_first>
  <action>
Create `src/pages/LandingPage.tsx` as the lazy-loadable orchestrator component (per D-09, D-14).

**CRITICAL: This is a default export** so `React.lazy(() => import('./pages/LandingPage'))` works in App.tsx.

**Auth redirect logic (per D-14, Pitfall 1):**
1. Import `onAuthStateChanged` from `"firebase/auth"` and `auth` from `"@/config/firebase"` -- direct Firebase import, NOT the useAuth hook
2. State: `const [checking, setChecking] = useState(true)` -- starts true to prevent flash
3. In `useEffect`, subscribe to `onAuthStateChanged(auth, (user) => { ... })`:
   - If `user` is truthy: `navigate("/dashboard", { replace: true })`
   - If `user` is null: `setChecking(false)`
4. Return cleanup: `return unsubscribe` from the useEffect
5. Dependency array: `[navigate]`

**Loading state (while `checking` is true):**
```tsx
<div className="flex items-center justify-center min-h-screen bg-background">
  <Loader2 className="size-8 animate-spin text-muted-foreground" aria-label="Loading" />
</div>
```

**Rendered content (when `checking` is false):**
```tsx
<div className="min-h-screen bg-background">
  <LandingHeader />
  <main>
    <HeroSection />
    <FeaturesSection />
    <PricingSection />
  </main>
  <LandingFooter />
</div>
```

**Imports allowed (per D-10):**
- `{ useEffect, useState }` from `"react"`
- `{ onAuthStateChanged }` from `"firebase/auth"`
- `{ auth }` from `"@/config/firebase"`
- `{ useNavigate }` from `"react-router-dom"`
- `{ Loader2 }` from `"lucide-react"`
- All five sub-components from `"./landing/LandingHeader"` etc.
  </action>
  <verify>
    <automated>cd trade-flow-ui && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-ui/src/pages/LandingPage.tsx contains `export default function LandingPage`
    - trade-flow-ui/src/pages/LandingPage.tsx contains `onAuthStateChanged`
    - trade-flow-ui/src/pages/LandingPage.tsx contains `import { auth } from "@/config/firebase"`
    - trade-flow-ui/src/pages/LandingPage.tsx contains `navigate("/dashboard", { replace: true })`
    - trade-flow-ui/src/pages/LandingPage.tsx contains `setChecking(false)`
    - trade-flow-ui/src/pages/LandingPage.tsx contains `aria-label="Loading"`
    - trade-flow-ui/src/pages/LandingPage.tsx contains `<LandingHeader />`
    - trade-flow-ui/src/pages/LandingPage.tsx contains `<HeroSection />`
    - trade-flow-ui/src/pages/LandingPage.tsx contains `<FeaturesSection />`
    - trade-flow-ui/src/pages/LandingPage.tsx contains `<PricingSection />`
    - trade-flow-ui/src/pages/LandingPage.tsx contains `<LandingFooter />`
    - trade-flow-ui/src/pages/LandingPage.tsx does NOT contain `useAuth`
    - trade-flow-ui/src/pages/LandingPage.tsx does NOT contain `@/store`
    - trade-flow-ui/src/pages/LandingPage.tsx does NOT contain `@/features/`
    - trade-flow-ui/src/pages/LandingPage.tsx does NOT contain `@/hooks/`
    - trade-flow-ui/src/pages/LandingPage.tsx does NOT contain `@/providers/`
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>LandingPage.tsx exists with default export, Firebase auth redirect via direct onAuthStateChanged (not useAuth), loading spinner during auth check, and composition of all five sub-components. No forbidden imports present.</done>
</task>

</tasks>

<verification>
1. `cd trade-flow-ui && npx tsc --noEmit` -- zero TypeScript errors
2. Grep all files in `src/pages/landing/` and `src/pages/LandingPage.tsx` for forbidden imports (`@/store`, `@/features/`, `@/hooks/`, `@/providers/`) -- zero matches
3. Grep for `space-y-` in landing page files -- zero matches (shadcn rule compliance)
4. All 6 files exist with correct named/default exports
</verification>

<success_criteria>
- Six new files created: LandingPage.tsx + 5 sub-components in landing/ directory
- Landing page content matches UI-SPEC copywriting contract exactly
- Bundle isolation maintained: no imports from store, features, hooks, or providers
- Auth redirect implemented via direct Firebase onAuthStateChanged
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/36-public-landing-page-and-route-restructure/36-01-SUMMARY.md`
</output>
