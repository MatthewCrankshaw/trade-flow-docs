---
phase: 54-user-management
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-ui/src/features/auth/components/OnboardingGuard.tsx
  - trade-flow-ui/src/pages/LoginPage.tsx
  - trade-flow-ui/src/components/layouts/DashboardLayout.tsx
  - trade-flow-ui/src/features/support/components/RoleBadge.tsx
autonomous: true
gap_closure: true
requirements:
  - SACC-01
  - UMGT-03

must_haves:
  truths:
    - "Support user login navigates to /support, not /onboarding"
    - "Support nav menu highlights only the active item"
    - "RoleBadge renders without crashing when roleName is null or undefined"
  artifacts:
    - path: "trade-flow-ui/src/features/auth/components/OnboardingGuard.tsx"
      provides: "Support user bypass matching PaywallGuard pattern"
    - path: "trade-flow-ui/src/pages/LoginPage.tsx"
      provides: "savedLocation ignored for support users"
    - path: "trade-flow-ui/src/components/layouts/DashboardLayout.tsx"
      provides: "NavLink with end prop for exact matching"
    - path: "trade-flow-ui/src/features/support/components/RoleBadge.tsx"
      provides: "Null-safe roleName handling"
  key_links:
    - from: "OnboardingGuard.tsx"
      to: "support role check"
      via: "hasSupportRoles bypass before onboarding checks"
    - from: "LoginPage.tsx"
      to: "/support redirect"
      via: "Support role check takes precedence over savedLocation"
---

<objective>
Fix UI issues found in UAT: support user login redirect, nav menu highlighting, and RoleBadge crash on null roleName.

Purpose: UAT Tests 1, 2b, and 10 (RoleBadge crash) fail due to missing guard bypasses, NavLink prefix matching, and absent null guards.

Output: Support users land on /support after login, nav highlights correctly, RoleBadge handles null gracefully.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-support-access-routing/53-02-SUMMARY.md
@.planning/phases/54-user-management/54-04-SUMMARY.md

Key context from Phase 53:
- App.tsx route tree: support routes are under SupportGuard, outside OnboardingGuard/PaywallGuard
- LoginPage uses AlreadyAuthenticatedRedirect component with RTK Query for role-aware redirect
- DashboardLayout uses getSupportNavigationItems() for support users, getBusinessNavigationItems() for business users
- PaywallGuard already has a support user bypass (confirmed in UAT diagnosis)
- OnboardingGuard is MISSING the equivalent bypass
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix support user login redirect and OnboardingGuard bypass</name>
  <files>
    trade-flow-ui/src/features/auth/components/OnboardingGuard.tsx
    trade-flow-ui/src/pages/LoginPage.tsx
  </files>
  <action>
    1. **OnboardingGuard.tsx:**
       Add a support user bypass at the top of the guard logic, before the hasDisplayName/hasBusiness checks.
       Follow the same pattern used by PaywallGuard:
       - Check if the user has support roles (e.g., `user.supportRoles?.length > 0` or however PaywallGuard checks it).
       - If the user has support roles, render `<Outlet />` immediately without checking onboarding state.
       - This prevents support users (who may not have a business) from being redirected to /onboarding.

       Read PaywallGuard first to match the exact pattern for checking support roles.

    2. **LoginPage.tsx:**
       In the login success redirect logic (inside AlreadyAuthenticatedRedirect or the post-login handler):
       - When the user has support roles, always redirect to `/support` regardless of savedLocation/from state.
       - The current code checks `savedLocation` (from ProtectedRoute location state) before checking roles, which can send support users to business pages.
       - Reorder: check support roles FIRST, redirect to `/support`. Only fall through to savedLocation/dashboard for non-support users.

    Gap reasons:
    - Test 1: Support users hit OnboardingGuard and get redirected to /onboarding because they have no business
    - Test 1: savedLocation overrides role-based redirect for support users
  </action>
  <verify>
    <automated>cd trade-flow-ui && npm run ci 2>&1 | tail -20</automated>
  </verify>
  <done>
    Support users bypass OnboardingGuard and are redirected to /support after login, never seeing onboarding. savedLocation does not override support user redirect.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix nav highlight and RoleBadge null guard</name>
  <files>
    trade-flow-ui/src/components/layouts/DashboardLayout.tsx
    trade-flow-ui/src/features/support/components/RoleBadge.tsx
  </files>
  <action>
    1. **DashboardLayout.tsx -- NavLink highlighting:**
       The Dashboard nav item uses href `/support` which is a prefix of `/support/users`, `/support/businesses`, etc. NavLink's default behavior uses prefix matching, so `/support` matches all support sub-routes.
       Fix: Add the `end` prop to the NavLink for the Dashboard item. This makes it use exact matching, so `/support` only highlights when the path is exactly `/support`.
       Check how NavLink components are rendered in DashboardLayout. If they use a custom wrapper or map over navigation items, add `end: true` to the Dashboard navigation config item (in navigation.ts if that's where items are defined) or apply the `end` prop directly in the NavLink JSX.
       The `end` prop should be applied to the root/dashboard item of each navigation group (both support `/support` and business `/dashboard` if applicable).

    2. **RoleBadge.tsx (line 5 area):**
       Add a null/undefined guard before calling `.replace()` on roleName.
       If roleName is null or undefined, render a fallback (e.g., return `<Badge variant="secondary">Unknown</Badge>` or return null).
       Example: `if (!roleName) return null;` at the top of the component, before the `.replace()` call.
       This is defense-in-depth -- the API fix in Plan 06 will resolve the root cause (wrong field projection), but the UI should never crash on null data.

    Gap reasons:
    - Test 2b: Dashboard stays highlighted when navigating to /support/users
    - Test 10: RoleBadge crashes with TypeError when roleName is null/undefined
  </action>
  <verify>
    <automated>cd trade-flow-ui && npm run ci 2>&1 | tail -20</automated>
  </verify>
  <done>
    Nav menu highlights only the active item. RoleBadge renders gracefully when roleName is null. Full UI CI passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries -- all changes are UI-side fixes to existing components.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-gc-03 | E (Elevation of Privilege) | OnboardingGuard.tsx | mitigate | Support bypass checks user.supportRoles from authenticated API data, not client-side editable state; backend SupportGuard + PermissionGuard enforce server-side |
| T-54-gc-04 | S (Spoofing) | LoginPage.tsx | accept | Redirect target is hardcoded /support, not user-controlled; actual access controlled by SupportGuard |
</threat_model>

<verification>
1. `cd trade-flow-ui && npm run ci` passes (tests, lint, format, typecheck)
2. Support user login redirects to /support (not /onboarding)
3. Clicking "Users" in support nav highlights only Users, not Dashboard
4. RoleBadge renders without error when roleName is null
</verification>

<success_criteria>
UAT Tests 1, 2b, and 10 (RoleBadge crash) pass on re-test. Support login flow works end-to-end without hitting onboarding.
</success_criteria>

<output>
After completion, create `.planning/phases/54-user-management/54-07-SUMMARY.md`
</output>
