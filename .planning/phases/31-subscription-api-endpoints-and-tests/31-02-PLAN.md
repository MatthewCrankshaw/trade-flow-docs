---
phase: 31-subscription-api-endpoints-and-tests
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - trade-flow-api/src/subscription/test/mocks/subscription-mock-generator.ts
  - trade-flow-api/src/subscription/test/services/subscription-creator.service.spec.ts
  - trade-flow-api/src/subscription/test/services/subscription-retriever.service.spec.ts
  - trade-flow-api/src/subscription/test/services/subscription-updater.service.spec.ts
  - trade-flow-api/src/subscription/test/repositories/subscription.repository.spec.ts
  - trade-flow-api/src/subscription/test/controllers/subscription.controller.spec.ts
  - trade-flow-api/src/subscription/test/guards/subscription.guard.spec.ts
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]

must_haves:
  truths:
    - "SubscriptionCreatorService unit tests cover checkout session creation and portal session creation with Stripe SDK mocked"
    - "SubscriptionUpdaterService unit tests cover all 5 webhook event types and cancelAtPeriodEnd method"
    - "SubscriptionRetrieverService unit tests cover found subscription and not-found (404) scenarios"
    - "SubscriptionRepository unit tests cover upsert, findByUserId, and findByStripeSubscriptionId operations"
    - "SubscriptionController spec covers webhook signature valid->200 and invalid->400 scenarios"
    - "SubscriptionGuard spec covers public routes, skip decorator, read methods, support users, active/trialing pass, and blocked statuses"
  artifacts:
    - path: "trade-flow-api/src/subscription/test/mocks/subscription-mock-generator.ts"
      provides: "SubscriptionMockGenerator with createSubscriptionDto and createUserDto factory methods"
      exports: ["SubscriptionMockGenerator"]
    - path: "trade-flow-api/src/subscription/test/services/subscription-creator.service.spec.ts"
      provides: "TEST-01 unit tests for SubscriptionCreator"
    - path: "trade-flow-api/src/subscription/test/services/subscription-updater.service.spec.ts"
      provides: "TEST-02 unit tests for SubscriptionUpdater"
    - path: "trade-flow-api/src/subscription/test/services/subscription-retriever.service.spec.ts"
      provides: "TEST-03 unit tests for SubscriptionRetriever"
    - path: "trade-flow-api/src/subscription/test/repositories/subscription.repository.spec.ts"
      provides: "TEST-04 unit tests for SubscriptionRepository"
    - path: "trade-flow-api/src/subscription/test/controllers/subscription.controller.spec.ts"
      provides: "Webhook signature verification tests (D-06)"
    - path: "trade-flow-api/src/subscription/test/guards/subscription.guard.spec.ts"
      provides: "Guard behavior tests (D-09 through D-13)"
  key_links:
    - from: "trade-flow-api/src/subscription/test/mocks/subscription-mock-generator.ts"
      to: "trade-flow-api/src/subscription/data-transfer-objects/subscription.dto.ts"
      via: "createSubscriptionDto returns ISubscriptionDto"
      pattern: "ISubscriptionDto"
    - from: "trade-flow-api/src/subscription/test/services/*.spec.ts"
      to: "trade-flow-api/src/subscription/test/mocks/subscription-mock-generator.ts"
      via: "import SubscriptionMockGenerator for test data"
      pattern: "SubscriptionMockGenerator"
---

<objective>
Create comprehensive unit tests for all subscription services, repository, controller, and guard.

Purpose: Requirement TEST-01 through TEST-04 mandate unit test coverage for the subscription service layer and repository. Additionally, D-06 requires controller-level webhook signature tests and D-09-D-13 require guard behavior tests. All tests follow the project's AAA pattern with jest.Mocked<T> and Test.createTestingModule.

Output: SubscriptionMockGenerator, 4 service/repository test files, 1 controller spec, 1 guard spec -- all passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-subscription-api-endpoints-and-tests/31-CONTEXT.md
@.planning/phases/31-subscription-api-endpoints-and-tests/31-RESEARCH.md
@.planning/phases/31-subscription-api-endpoints-and-tests/31-01-SUMMARY.md
@.planning/codebase/TESTING.md

<interfaces>
<!-- Key types and patterns from Plan 01 output that tests will exercise. -->

From trade-flow-api/src/subscription/services/subscription-retriever.service.ts:
```typescript
export class SubscriptionRetriever {
  findByUser(authUser: IUserDto): Promise<ISubscriptionDto>;
}
```

From trade-flow-api/src/subscription/services/subscription-updater.service.ts:
```typescript
export class SubscriptionUpdater {
  // Phase 30 webhook methods (already exist):
  // handleCheckoutSessionCompleted(event): Promise<void>
  // handleSubscriptionUpdated(event): Promise<void>
  // handleSubscriptionDeleted(event): Promise<void>
  // handleInvoicePaymentSucceeded(event): Promise<void>
  // handleInvoicePaymentFailed(event): Promise<void>
  // Phase 31 addition:
  cancelAtPeriodEnd(authUser: IUserDto): Promise<ISubscriptionDto>;
}
```

From trade-flow-api/src/subscription/services/subscription-creator.service.ts:
```typescript
export class SubscriptionCreator {
  createCheckoutSession(authUser: IUserDto): Promise<ICheckoutResponse>;
  createPortalSession(authUser: IUserDto): Promise<{ url: string }>;
}
```

From trade-flow-api/src/subscription/guards/subscription.guard.ts:
```typescript
export class SubscriptionGuard implements CanActivate {
  canActivate(context: ExecutionContext): Promise<boolean>;
}
```

From trade-flow-api/src/subscription/decorators/skip-subscription-check.decorator.ts:
```typescript
export const SkipSubscriptionCheck = () => SetMetadata("skipSubscriptionCheck", true);
```

Mock Stripe SDK shape (from RESEARCH.md):
```typescript
const mockStripe = {
  subscriptions: { update: jest.fn() },
  billingPortal: { sessions: { create: jest.fn() } },
  customers: { create: jest.fn() },
  checkout: { sessions: { create: jest.fn(), retrieve: jest.fn() } },
  webhooks: { constructEvent: jest.fn() },
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SubscriptionMockGenerator and service unit tests (TEST-01, TEST-02, TEST-03)</name>
  <files>
    trade-flow-api/src/subscription/test/mocks/subscription-mock-generator.ts,
    trade-flow-api/src/subscription/test/services/subscription-creator.service.spec.ts,
    trade-flow-api/src/subscription/test/services/subscription-retriever.service.spec.ts,
    trade-flow-api/src/subscription/test/services/subscription-updater.service.spec.ts
  </files>
  <read_first>
    trade-flow-api/src/subscription/services/subscription-creator.service.ts,
    trade-flow-api/src/subscription/services/subscription-retriever.service.ts,
    trade-flow-api/src/subscription/services/subscription-updater.service.ts,
    trade-flow-api/src/subscription/data-transfer-objects/subscription.dto.ts,
    trade-flow-api/src/subscription/enums/subscription-status.enum.ts,
    trade-flow-api/src/subscription/repositories/subscription.repository.ts,
    trade-flow-api/src/subscription/providers/stripe.provider.ts,
    trade-flow-api/src/business/test/mocks/business-mock-generator.ts,
    trade-flow-api/src/business/test/services/business-creator.service.spec.ts,
    trade-flow-api/src/user/data-transfer-objects/user.dto.ts,
    trade-flow-api/src/core/errors/resource-not-found.error.ts,
    trade-flow-api/src/core/errors/invalid-request.error.ts
  </read_first>
  <behavior>
    SubscriptionMockGenerator:
    - createSubscriptionDto(overrides?) returns ISubscriptionDto with sensible defaults (status: ACTIVE, cancelAtPeriodEnd: false, dates set)
    - createUserDto(overrides?) returns IUserDto with externalAuthUserId, supportRoles: DtoCollection.empty()

    TEST-01 (SubscriptionCreatorService):
    - createCheckoutSession: creates Stripe customer, creates local subscription record, creates checkout session, returns { url }
    - createCheckoutSession: propagates Stripe SDK error
    - createPortalSession: finds subscription by userId, creates billing portal session with correct return_url, returns { url }
    - createPortalSession: throws ResourceNotFoundError when no subscription exists

    TEST-03 (SubscriptionRetrieverService):
    - findByUser: returns subscription DTO when found
    - findByUser: throws ResourceNotFoundError when not found

    TEST-02 (SubscriptionUpdaterService):
    - cancelAtPeriodEnd: calls stripe.subscriptions.update with cancel_at_period_end: true, updates local record, returns updated DTO
    - cancelAtPeriodEnd: throws InvalidRequestError when no subscription exists
    - cancelAtPeriodEnd: throws InvalidRequestError when subscription is canceled
    - cancelAtPeriodEnd: throws InvalidRequestError when stripeSubscriptionId is missing
    - handleCheckoutSessionCompleted: upserts subscription with status trialing (read actual method signature from file)
    - handleSubscriptionUpdated: upserts subscription with synced fields
    - handleSubscriptionDeleted: sets status to canceled with canceledAt
    - handleInvoicePaymentSucceeded: sets status to active
    - handleInvoicePaymentFailed: sets status to past_due
  </behavior>
  <action>
    **1. Create SubscriptionMockGenerator** (`test/mocks/subscription-mock-generator.ts`):
    - Follow `BusinessMockGenerator` pattern exactly -- static class with factory methods
    - `static createSubscriptionDto(overrides?: Partial<ISubscriptionDto>): ISubscriptionDto` with defaults:
      - `id: new ObjectId().toString()`
      - `userId: "firebase-uid-123"`
      - `stripeCustomerId: "cus_test123"`
      - `stripeSubscriptionId: "sub_test123"`
      - `stripeLatestCheckoutSessionId: "cs_test123"`
      - `status: SubscriptionStatus.ACTIVE`
      - `currentPeriodEnd: new Date("2026-05-01T00:00:00Z")`
      - `trialEnd: new Date("2026-04-29T00:00:00Z")`
      - `cancelAtPeriodEnd: false`
      - `canceledAt: undefined`
      - `createdAt: new Date("2026-03-29T00:00:00Z")`
      - `updatedAt: new Date("2026-03-29T00:00:00Z")`
    - `static createUserDto(overrides?: Partial<IUserDto>): IUserDto` -- read actual IUserDto fields from user.dto.ts, include all required fields with sensible defaults. `supportRoles: DtoCollection.empty()`, `businessRoles: DtoCollection.empty()`, `externalAuthUserId: "firebase-uid-123"`.

    **2. Create subscription-creator.service.spec.ts** (TEST-01 + BILL-03):
    - `describe("SubscriptionCreator")` with `beforeEach` using `Test.createTestingModule`
    - Providers: `SubscriptionCreator`, mock `SubscriptionRepository` (all methods as `jest.fn()`), mock `STRIPE_CLIENT` using mockStripe object, mock `ConfigService` with `get: jest.fn().mockReturnValue("https://app.tradeflow.test")`
    - `afterEach(() => jest.clearAllMocks())`
    - Tests for `createCheckoutSession`:
      - "should create Stripe customer, local record, and checkout session" -- mock `customers.create` returns `{ id: "cus_new" }`, `checkout.sessions.create` returns `{ url: "https://checkout.stripe.com/test" }`, repository.create returns mock DTO. Assert `customers.create` called with email and metadata, `checkout.sessions.create` called with correct params (trial_period_days: 30, etc.), returns `{ url }`
      - "should propagate Stripe error" -- mock `customers.create` to reject, assert rejects with same error
    - Tests for `createPortalSession`:
      - "should create billing portal session and return URL" -- mock `repository.findByUserId` returns subscription DTO, `billingPortal.sessions.create` returns `{ url: "https://billing.stripe.com/test" }`. Assert called with `{ customer: "cus_test123", return_url: "https://app.tradeflow.test/settings?tab=billing" }`, returns `{ url }`
      - "should throw ResourceNotFoundError when no subscription" -- mock `repository.findByUserId` returns null, assert rejects with `ResourceNotFoundError`

    **3. Create subscription-retriever.service.spec.ts** (TEST-03):
    - `describe("SubscriptionRetriever")` with `Test.createTestingModule`
    - Providers: `SubscriptionRetriever`, mock `SubscriptionRepository`
    - Tests for `findByUser`:
      - "should return subscription when found" -- mock `findByUserId` returns DTO, assert result matches
      - "should throw ResourceNotFoundError when not found" -- mock `findByUserId` returns null, assert rejects with `ResourceNotFoundError`

    **4. Create subscription-updater.service.spec.ts** (TEST-02):
    - `describe("SubscriptionUpdater")` with `Test.createTestingModule`
    - Providers: `SubscriptionUpdater`, mock `SubscriptionRepository`, mock `STRIPE_CLIENT`, mock `AppLogger` or `Logger` as needed
    - Read the actual SubscriptionUpdater service to determine exact method signatures for the 5 webhook event handlers
    - Tests for `cancelAtPeriodEnd`:
      - "should call Stripe update with cancel_at_period_end and sync locally" -- mock `findByUserId` returns active DTO, mock `stripe.subscriptions.update` returns `{ current_period_end: 1746057600 }`, mock `upsertByUserId` returns updated DTO. Assert `stripe.subscriptions.update` called with `(subscription.stripeSubscriptionId, { cancel_at_period_end: true })`. Assert `upsertByUserId` called with `cancelAtPeriodEnd: true`.
      - "should throw InvalidRequestError when no subscription" -- mock `findByUserId` returns null
      - "should throw InvalidRequestError when subscription is canceled" -- mock `findByUserId` returns DTO with `status: SubscriptionStatus.CANCELED`
      - "should throw InvalidRequestError when stripeSubscriptionId is missing" -- mock `findByUserId` returns DTO with `stripeSubscriptionId: undefined`
    - Tests for each of the 5 webhook event handlers (read actual method signatures first):
      - checkout.session.completed: assert upsert called with status trialing
      - customer.subscription.updated: assert upsert called with synced status/dates
      - customer.subscription.deleted: assert upsert called with status canceled + canceledAt
      - invoice.payment_succeeded: assert upsert called with status active
      - invoice.payment_failed: assert upsert called with status past_due
  </action>
  <verify>
    <automated>cd trade-flow-api && npm test -- --testPathPattern="subscription-(creator|retriever|updater).service" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `subscription-mock-generator.ts` exists with `export class SubscriptionMockGenerator`
    - `subscription-mock-generator.ts` contains `static createSubscriptionDto` and `static createUserDto`
    - `subscription-creator.service.spec.ts` contains `describe("SubscriptionCreator")`
    - `subscription-creator.service.spec.ts` contains test for `createCheckoutSession` and `createPortalSession`
    - `subscription-creator.service.spec.ts` contains `ResourceNotFoundError` assertion for portal no-subscription case
    - `subscription-retriever.service.spec.ts` contains `describe("SubscriptionRetriever")`
    - `subscription-retriever.service.spec.ts` contains `ResourceNotFoundError` assertion for not-found case
    - `subscription-updater.service.spec.ts` contains `describe("SubscriptionUpdater")`
    - `subscription-updater.service.spec.ts` contains `cancel_at_period_end` in test assertions
    - `subscription-updater.service.spec.ts` contains `InvalidRequestError` assertion
    - `subscription-updater.service.spec.ts` contains tests for all 5 event types (checkout.session.completed, customer.subscription.updated, customer.subscription.deleted, invoice.payment_succeeded, invoice.payment_failed)
    - All 3 spec files pass: `npm test -- --testPathPattern="subscription-(creator|retriever|updater).service"` exits 0
  </acceptance_criteria>
  <done>SubscriptionMockGenerator created. TEST-01: SubscriptionCreator tests pass (checkout + portal). TEST-02: SubscriptionUpdater tests pass (5 event types + cancelAtPeriodEnd + error cases). TEST-03: SubscriptionRetriever tests pass (found + 404). All service tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create repository, controller, and guard unit tests (TEST-04, D-06, D-09-D-13)</name>
  <files>
    trade-flow-api/src/subscription/test/repositories/subscription.repository.spec.ts,
    trade-flow-api/src/subscription/test/controllers/subscription.controller.spec.ts,
    trade-flow-api/src/subscription/test/guards/subscription.guard.spec.ts
  </files>
  <read_first>
    trade-flow-api/src/subscription/repositories/subscription.repository.ts,
    trade-flow-api/src/subscription/controllers/subscription.controller.ts,
    trade-flow-api/src/subscription/guards/subscription.guard.ts,
    trade-flow-api/src/subscription/decorators/skip-subscription-check.decorator.ts,
    trade-flow-api/src/subscription/test/mocks/subscription-mock-generator.ts,
    trade-flow-api/src/business/test/repositories/business.repository.spec.ts,
    trade-flow-api/src/core/services/mongo-connection.service.ts,
    trade-flow-api/src/subscription/enums/subscription-status.enum.ts,
    trade-flow-api/src/subscription/providers/stripe.provider.ts
  </read_first>
  <behavior>
    TEST-04 (SubscriptionRepository):
    - upsertByUserId: calls collection.findOneAndUpdate with userId filter and $set/$setOnInsert, returns mapped DTO
    - findByUserId: calls collection.findOne with userId filter, returns DTO when found, null when not found
    - findByStripeSubscriptionId: calls collection.findOne with stripeSubscriptionId filter, returns DTO when found, null when not found

    Controller spec (D-06):
    - WebhookController with valid Stripe signature: constructEvent succeeds, event enqueued, returns 200
    - WebhookController with invalid Stripe signature: constructEvent throws, returns 400 BadRequestException

    Guard spec (D-09 through D-13):
    - Public route (@Public metadata): returns true without DB lookup
    - @SkipSubscriptionCheck route: returns true without DB lookup
    - GET request: returns true without DB lookup
    - HEAD request: returns true without DB lookup
    - Support user (non-empty supportRoles): returns true without DB lookup
    - POST request with active subscription: returns true
    - POST request with trialing subscription: returns true
    - POST request with past_due subscription: throws ForbiddenError
    - POST request with canceled subscription: throws ForbiddenError
    - POST request with no subscription record: throws ForbiddenError
  </behavior>
  <action>
    **1. Create subscription.repository.spec.ts** (TEST-04):
    - Follow `business.repository.spec.ts` pattern exactly
    - `describe("SubscriptionRepository")` with `Test.createTestingModule`
    - Providers: `SubscriptionRepository`, mock `MongoConnectionService` with `getCollection: jest.fn()`
    - Create a `mockCollection` object with mocked MongoDB methods: `findOne: jest.fn()`, `findOneAndUpdate: jest.fn()`, `insertOne: jest.fn()`, `createIndex: jest.fn()`
    - `beforeEach`: set `mongoConnection.getCollection.mockReturnValue(mockCollection)`
    - Tests for `upsertByUserId`:
      - "should upsert subscription by userId" -- mock `findOneAndUpdate` returns `{ value: entityDoc }`. Assert called with `{ userId: "firebase-uid-123" }` filter and `$set` containing update fields. Assert returns mapped ISubscriptionDto.
    - Tests for `findByUserId`:
      - "should return subscription when found" -- mock `findOne` returns entity doc, assert returns DTO
      - "should return null when not found" -- mock `findOne` returns null, assert returns null
    - Tests for `findByStripeSubscriptionId`:
      - "should return subscription when found" -- mock `findOne` returns entity doc with matching stripeSubscriptionId
      - "should return null when not found" -- mock `findOne` returns null

    **2. Create subscription.controller.spec.ts** (D-06 -- webhook signature test):
    - `describe("WebhookController")` with `Test.createTestingModule`
    - Providers: the controller class (read actual class name from file), mock `STRIPE_CLIENT` with `webhooks: { constructEvent: jest.fn() }`, mock `QueueProducer` with `enqueueStripeWebhook: jest.fn()`, mock `ConfigService`
    - Create mock request object: `{ rawBody: Buffer.from('{}'), headers: { "stripe-signature": "sig_test" } }`
    - Test "should return 200 when signature is valid":
      - Mock `constructEvent` returns `{ id: "evt_123", type: "checkout.session.completed", data: { object: {} } }`
      - Mock `enqueueStripeWebhook` resolves
      - Call the webhook handler method
      - Assert response status 200 (or handler returns successfully)
    - Test "should return 400 when signature is invalid":
      - Mock `constructEvent` throws `new Error("Invalid signature")`
      - Call the webhook handler method
      - Assert throws `BadRequestException` (HTTP 400)

    **3. Create subscription.guard.spec.ts** (D-09 through D-13):
    - `describe("SubscriptionGuard")` with `Test.createTestingModule`
    - Providers: `SubscriptionGuard`, mock `Reflector`, mock `SubscriptionRepository`
    - Create helper to build mock `ExecutionContext`:
      ```typescript
      const createMockContext = (method: string, user?: IUserDto) => ({
        getHandler: () => ({}),
        getClass: () => ({}),
        switchToHttp: () => ({
          getRequest: () => ({ method, user }),
        }),
      } as unknown as ExecutionContext);
      ```
    - Tests:
      - "should allow public routes" -- mock `reflector.getAllAndOverride` returns true for `isPublic`. Assert `canActivate` returns true. Assert `subscriptionRepository.findByUserId` NOT called.
      - "should allow @SkipSubscriptionCheck routes" -- mock reflector returns false for isPublic, true for skipSubscriptionCheck. Assert returns true.
      - "should allow GET requests without DB lookup" -- mock reflector returns false for both. Create context with method "GET" and a user. Assert returns true. Assert `findByUserId` NOT called.
      - "should allow HEAD requests without DB lookup" -- same as GET but method "HEAD".
      - "should allow support users without DB lookup" -- create user with non-empty `supportRoles` (read actual shape from IUserDto). Assert returns true. Assert `findByUserId` NOT called.
      - "should allow POST with active subscription" -- mock `findByUserId` returns DTO with `status: SubscriptionStatus.ACTIVE`. Assert returns true.
      - "should allow POST with trialing subscription" -- mock `findByUserId` returns DTO with `status: SubscriptionStatus.TRIALING`. Assert returns true.
      - "should throw ForbiddenError for POST with past_due subscription" -- mock `findByUserId` returns DTO with `status: SubscriptionStatus.PAST_DUE`. Assert rejects with `ForbiddenError`.
      - "should throw ForbiddenError for POST with canceled subscription" -- mock `findByUserId` returns DTO with `status: SubscriptionStatus.CANCELED`. Assert rejects with `ForbiddenError`.
      - "should throw ForbiddenError for POST with no subscription record" -- mock `findByUserId` returns null. Assert rejects with `ForbiddenError`.
  </action>
  <verify>
    <automated>cd trade-flow-api && npm test -- --testPathPattern="subscription\.(repository|controller|guard)" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `subscription.repository.spec.ts` contains `describe("SubscriptionRepository")`
    - `subscription.repository.spec.ts` contains tests for `upsertByUserId`, `findByUserId`, `findByStripeSubscriptionId`
    - `subscription.controller.spec.ts` contains `describe("WebhookController")` or contains webhook handler test
    - `subscription.controller.spec.ts` contains `BadRequestException` assertion for invalid signature
    - `subscription.controller.spec.ts` contains `constructEvent` mock
    - `subscription.guard.spec.ts` contains `describe("SubscriptionGuard")`
    - `subscription.guard.spec.ts` contains tests for: isPublic, skipSubscriptionCheck, GET, HEAD, supportRoles, ACTIVE, TRIALING, PAST_DUE, CANCELED, null subscription
    - `subscription.guard.spec.ts` contains `ForbiddenError` assertions
    - `subscription.guard.spec.ts` asserts `findByUserId` NOT called for public/skip/GET/HEAD/support scenarios
    - All 3 spec files pass: `npm test -- --testPathPattern="subscription\.(repository|controller|guard)"` exits 0
    - Full test suite passes: `npm test` exits 0
  </acceptance_criteria>
  <done>TEST-04: SubscriptionRepository tests pass (upsert, findByUserId, findByStripeSubscriptionId). Controller spec passes (valid signature -> 200, invalid -> 400). Guard spec passes (all 10 scenarios covering D-09 through D-13). Full test suite green.</done>
</task>

</tasks>

<verification>
1. `cd trade-flow-api && npm test -- --testPathPattern=subscription` -- all subscription tests pass
2. `cd trade-flow-api && npm test` -- full suite green (existing + new tests)
3. Count test files: 7 new files in `src/subscription/test/` (1 mock generator, 3 service specs, 1 repo spec, 1 controller spec, 1 guard spec)
</verification>

<success_criteria>
- SubscriptionMockGenerator provides factory methods for test data
- TEST-01: SubscriptionCreatorService tests cover checkout + portal creation with Stripe mocked
- TEST-02: SubscriptionUpdaterService tests cover 5 webhook event types + cancelAtPeriodEnd + error paths
- TEST-03: SubscriptionRetrieverService tests cover found and not-found scenarios
- TEST-04: SubscriptionRepository tests cover upsert, findByUserId, findByStripeSubscriptionId
- Controller spec verifies webhook signature valid/invalid paths (D-06)
- Guard spec verifies all bypass paths and blocking paths (D-09 through D-13)
- Full test suite passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/31-subscription-api-endpoints-and-tests/31-02-SUMMARY.md`
</output>
