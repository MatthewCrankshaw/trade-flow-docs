---
phase: 45-public-customer-page-response-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-api/src/estimate/enums/estimate-status.enum.ts
  - trade-flow-api/src/estimate/enums/estimate-response-type.enum.ts
  - trade-flow-api/src/estimate/enums/estimate-decline-reason.enum.ts
  - trade-flow-api/src/estimate/enums/estimate-transitions.ts
  - trade-flow-api/src/estimate/test/enums/estimate-transitions.spec.ts
  - trade-flow-api/src/estimate/entities/estimate.entity.ts
  - trade-flow-api/src/estimate/data-transfer-objects/estimate.dto.ts
  - trade-flow-api/src/estimate/data-transfer-objects/estimate-response-summary.dto.ts
  - trade-flow-api/src/estimate/repositories/estimate.repository.ts
  - trade-flow-api/src/estimate/test/mocks/estimate-mock.generator.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/milestones/v1.8-ROADMAP.md
autonomous: true
requirements:
  - RESP-01
  - RESP-02
  - RESP-03
  - RESP-04

must_haves:
  truths:
    - "EstimateStatus enum has no SITE_VISIT_REQUESTED value"
    - "EstimateResponseType enum has exactly three values: PROCEED, MESSAGE, DECLINE"
    - "EstimateDeclineReason enum has exactly five structured reasons"
    - "ALLOWED_TRANSITIONS map includes VIEWED -> RESPONDED and VIEWED -> DECLINED"
    - "Estimate entity has a responses array field"
    - "REQUIREMENTS.md RESP-01..04 reflect the 3-action conversational model"
  artifacts:
    - path: "trade-flow-api/src/estimate/enums/estimate-decline-reason.enum.ts"
      provides: "Structured decline reason enum"
      exports: ["EstimateDeclineReason"]
    - path: "trade-flow-api/src/estimate/enums/estimate-response-type.enum.ts"
      provides: "3-action response type enum"
      exports: ["EstimateResponseType"]
  key_links:
    - from: "trade-flow-api/src/estimate/enums/estimate-transitions.ts"
      to: "trade-flow-api/src/estimate/enums/estimate-status.enum.ts"
      via: "ALLOWED_TRANSITIONS map keys and values"
      pattern: "ALLOWED_TRANSITIONS.*VIEWED.*RESPONDED"
---

<objective>
Update Phase 41's estimate enums, entity, and transition map to reflect the Phase 45 3-action conversational CTA model (proceed/message/decline), removing the original 4-button model artifacts (SITE_VISIT_REQUESTED status, SITE_VISIT_REQUEST/SEND_ME_QUOTE/QUESTION response types). Also creates a new EstimateDeclineReason enum, adds the responses[] array to the entity, and updates REQUIREMENTS.md and ROADMAP.md to reflect the revised CTA definitions.

Purpose: All subsequent plans depend on the corrected enum values and entity shape. Landing these changes first prevents cascading compile errors.
Output: Updated enums, entity, DTO, repository, transition map, tests, and planning docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-public-customer-page-response-handling/45-CONTEXT.md
@.planning/phases/45-public-customer-page-response-handling/45-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update estimate enums and transition map for 3-action CTA model</name>
  <files>
    trade-flow-api/src/estimate/enums/estimate-status.enum.ts,
    trade-flow-api/src/estimate/enums/estimate-response-type.enum.ts,
    trade-flow-api/src/estimate/enums/estimate-decline-reason.enum.ts,
    trade-flow-api/src/estimate/enums/estimate-transitions.ts,
    trade-flow-api/src/estimate/test/enums/estimate-transitions.spec.ts,
    trade-flow-api/src/estimate/test/mocks/estimate-mock.generator.ts
  </files>
  <read_first>
    trade-flow-api/src/estimate/enums/estimate-status.enum.ts,
    trade-flow-api/src/estimate/enums/estimate-response-type.enum.ts,
    trade-flow-api/src/estimate/enums/estimate-transitions.ts,
    trade-flow-api/src/estimate/test/enums/estimate-transitions.spec.ts,
    trade-flow-api/src/estimate/test/mocks/estimate-mock.generator.ts,
    trade-flow-api/src/quote/enums/quote-response-type.enum.ts
  </read_first>
  <action>
1. **EstimateStatus enum** (`estimate-status.enum.ts`): Remove `SITE_VISIT_REQUESTED = "site_visit_requested"` if present. The final enum values must be exactly:
   ```typescript
   export enum EstimateStatus {
     DRAFT = "draft",
     SENT = "sent",
     VIEWED = "viewed",
     RESPONDED = "responded",
     CONVERTED = "converted",
     DECLINED = "declined",
     EXPIRED = "expired",
     LOST = "lost",
     DELETED = "deleted",
   }
   ```

2. **EstimateResponseType enum** (`estimate-response-type.enum.ts`): Replace the existing 4-value enum with the 3-action model (per D-CTA-01, D-API-05):
   ```typescript
   export enum EstimateResponseType {
     PROCEED = "proceed",
     MESSAGE = "message",
     DECLINE = "decline",
   }
   ```

3. **EstimateDeclineReason enum** (NEW file `estimate-decline-reason.enum.ts`): Create with exactly these values (per D-CTA-04):
   ```typescript
   export enum EstimateDeclineReason {
     TOO_EXPENSIVE = "too_expensive",
     GOING_WITH_SOMEONE_ELSE = "going_with_someone_else",
     DECIDED_NOT_TO_DO_WORK = "decided_not_to_do_work",
     JUST_GETTING_IDEA = "just_getting_idea",
     TIMING_NOT_RIGHT = "timing_not_right",
   }
   ```

4. **ALLOWED_TRANSITIONS** (`estimate-transitions.ts`): Remove any entry with `SITE_VISIT_REQUESTED` as a source or target. Ensure these transitions exist:
   - `SENT -> [VIEWED, RESPONDED, DECLINED, EXPIRED, LOST, DELETED]`
   - `VIEWED -> [RESPONDED, DECLINED, EXPIRED, LOST, DELETED]`
   - `RESPONDED -> [CONVERTED, LOST, DELETED]`
   - `DECLINED -> [LOST, DELETED]`
   Keep all other existing transitions (DRAFT -> SENT, etc.) unchanged.

5. **Transition spec** (`estimate-transitions.spec.ts`): Remove any test cases referencing `SITE_VISIT_REQUESTED`. Verify the spec covers VIEWED -> RESPONDED, VIEWED -> DECLINED as valid, and RESPONDED -> VIEWED as rejected.

6. **Mock generator** (`estimate-mock.generator.ts`): If the mock generator creates response-type or status values, update to use the new enum values. Remove any `siteVisitAvailability` mock data. Add a `generateEstimateResponse()` helper returning `{ type: EstimateResponseType.PROCEED, message: null, reason: null, respondedAt: new Date() }`.
  </action>
  <verify>
    <automated>cd trade-flow-api && npx jest --testPathPattern=estimate-transitions --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `estimate-status.enum.ts` does NOT contain `SITE_VISIT_REQUESTED`
    - `estimate-response-type.enum.ts` contains exactly `PROCEED = "proceed"`, `MESSAGE = "message"`, `DECLINE = "decline"` and no other values
    - `estimate-decline-reason.enum.ts` exists and exports `EstimateDeclineReason` with 5 values
    - `estimate-transitions.ts` ALLOWED_TRANSITIONS does NOT contain key or value `SITE_VISIT_REQUESTED`
    - `estimate-transitions.ts` contains `EstimateStatus.VIEWED]: [` with `EstimateStatus.RESPONDED` and `EstimateStatus.DECLINED` in the array
    - `estimate-transitions.spec.ts` does NOT reference `SITE_VISIT_REQUESTED`
    - `npx jest --testPathPattern=estimate-transitions --no-coverage` exits 0
  </acceptance_criteria>
  <done>EstimateStatus has no SITE_VISIT_REQUESTED, EstimateResponseType has proceed/message/decline, EstimateDeclineReason exists with 5 reasons, transition map and tests updated, all estimate tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add responses array to estimate entity, DTO, and repository</name>
  <files>
    trade-flow-api/src/estimate/entities/estimate.entity.ts,
    trade-flow-api/src/estimate/data-transfer-objects/estimate.dto.ts,
    trade-flow-api/src/estimate/data-transfer-objects/estimate-response-summary.dto.ts,
    trade-flow-api/src/estimate/repositories/estimate.repository.ts
  </files>
  <read_first>
    trade-flow-api/src/estimate/entities/estimate.entity.ts,
    trade-flow-api/src/estimate/data-transfer-objects/estimate.dto.ts,
    trade-flow-api/src/estimate/data-transfer-objects/estimate-response-summary.dto.ts,
    trade-flow-api/src/estimate/repositories/estimate.repository.ts,
    trade-flow-api/src/quote/entities/quote.entity.ts
  </read_first>
  <action>
1. **IEstimateResponseEntity interface** (add to `estimate.entity.ts`):
   ```typescript
   export interface IEstimateResponseEntity {
     type: string;
     message?: string;
     reason?: string;
     respondedAt: Date;
   }
   ```
   Add `responses?: IEstimateResponseEntity[];` to `IEstimateEntity`. Remove `siteVisitAvailability?: string;` if present.

2. **IEstimateResponseDto** (update `estimate.dto.ts` or create new `estimate-response.dto.ts`):
   ```typescript
   export interface IEstimateResponseDto {
     type: string;
     message: string | null;
     reason: string | null;
     respondedAt: string;
   }
   ```
   Add `responses?: IEstimateResponseDto[];` to `IEstimateDto`. Remove `siteVisitAvailability?: string;` if present.

3. **IEstimateResponseSummaryDto** (`estimate-response-summary.dto.ts`): Remove `siteVisitAvailability` field. Update to derive summary from the responses array:
   ```typescript
   export interface IEstimateResponseSummaryDto {
     lastResponseType: string | null;
     lastResponseAt: string | null;
     lastResponseMessage: string | null;
     declineReason: string | null;
   }
   ```

4. **EstimateRepository** (`estimate.repository.ts`):
   - In `toDto()`: Map `entity.responses` to `IEstimateResponseDto[]` (convert `respondedAt` Date to ISO string). If `responses` is undefined/null, set to `[]`.
   - Add a `pushResponse(estimateId: string, response: IEstimateResponseEntity): Promise<IEstimateDto>` method that uses `$push` to append to the responses array:
     ```typescript
     const result = await this.mongoDbWriter.findOneAndUpdate(
       COLLECTION,
       { _id: new ObjectId(estimateId) },
       { $push: { responses: response }, $set: { updatedAt: new Date() } },
       { returnDocument: "after" },
     );
     ```
   - Remove any `siteVisitAvailability` from the entity-to-DTO mapping.

5. Ensure the `toEntity()` method (if it exists) maps `dto.responses` back to entity shape.
  </action>
  <verify>
    <automated>cd trade-flow-api && npx jest --testPathPattern=estimate --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `estimate.entity.ts` contains `export interface IEstimateResponseEntity`
    - `estimate.entity.ts` contains `responses?: IEstimateResponseEntity[]`
    - `estimate.entity.ts` does NOT contain `siteVisitAvailability`
    - `estimate.dto.ts` contains `responses?: IEstimateResponseDto[]`
    - `estimate-response-summary.dto.ts` does NOT contain `siteVisitAvailability`
    - `estimate.repository.ts` contains `pushResponse` method
    - `estimate.repository.ts` contains `$push.*responses`
    - All existing estimate tests pass: `npx jest --testPathPattern=estimate --no-coverage` exits 0
  </acceptance_criteria>
  <done>Estimate entity has responses array, DTO maps responses, repository has pushResponse method, siteVisitAvailability removed everywhere, all existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md and ROADMAP.md for 3-action CTA model</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/milestones/v1.8-ROADMAP.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/milestones/v1.8-ROADMAP.md
  </read_first>
  <action>
1. **REQUIREMENTS.md** -- Rewrite RESP-01 through RESP-04 to reflect the 3-action model per CONTEXT.md D-CTA-01..04:

   Replace RESP-01 with:
   `- [ ] **RESP-01**: Customer can tap "Happy to Proceed" to signal intent to proceed with the work; status transitions to RESPONDED and trader is notified`

   Replace RESP-02 with:
   `- [ ] **RESP-02**: Customer can tap "Message [Tradesperson First Name]" to send a freeform message (max 2000 characters) inline; status transitions to RESPONDED and trader is notified with the message`

   Replace RESP-03 with:
   `- [ ] **RESP-03**: Customer can tap "Not right for me" to decline with a structured reason (Too expensive / Going with someone else / Decided not to do the work / Just getting an idea of costs / Timing isn't right) plus optional freeform text (max 500 characters); status transitions to DECLINED and trader is notified with reason and message`

   Replace RESP-04 with:
   `- [ ] **RESP-04**: After any response submission, the action area is replaced with an inline success confirmation ("Thanks! [Name] has been notified and will be in touch.") and future visits show a read-only terminal view`

2. **ROADMAP.md** Phase 45 -- Rewrite success criterion #3:
   Replace: "The four response buttons work end-to-end: ..."
   With: "The three response actions work end-to-end: 'Happy to Proceed' records proceed intent and transitions to RESPONDED; 'Message [First Name]' opens an inline textarea capturing a freeform message and transitions to RESPONDED; 'Not right for me' captures a structured decline reason (5 presets plus optional freeform) and transitions to DECLINED. Each response persists to the estimate's responses array, sends a trader notification email, and replaces the action area with an inline confirmation."

   Rewrite success criterion #4 response types: Change "SiteVisitRequested" to just "Responded / Declined" in the status transition description. Remove any mention of `SiteVisitRequested` status.

3. **v1.8-ROADMAP.md** -- Apply the same SC #3 and #4 rewrites from step 2.
  </action>
  <verify>
    <automated>grep -c "SITE_VISIT_REQUESTED\|site_visit_requested\|Site visit\|Book a site visit\|Send me a quote\|I have a question" .planning/REQUIREMENTS.md .planning/ROADMAP.md || true</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md RESP-01 contains "Happy to Proceed"
    - REQUIREMENTS.md RESP-02 contains "Message [Tradesperson First Name]"
    - REQUIREMENTS.md RESP-03 contains "Not right for me"
    - REQUIREMENTS.md RESP-04 contains "inline success confirmation"
    - REQUIREMENTS.md does NOT contain "Book a site visit" or "Send me a quote" or "I have a question" in RESP-01..04
    - ROADMAP.md Phase 45 SC #3 contains "three response actions"
    - ROADMAP.md Phase 45 does NOT contain "SiteVisitRequested"
  </acceptance_criteria>
  <done>REQUIREMENTS.md RESP-01..04 rewritten for 3-action model, ROADMAP.md and v1.8-ROADMAP.md success criteria updated, no references to the original 4-button model remain in Phase 45 sections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan modifies only internal enums, entity types, and planning docs -- no external inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-00 | Tampering | estimate-transitions.ts | accept | Enum/transition changes are compile-time validated; existing tests catch regressions |
</threat_model>

<verification>
1. `cd trade-flow-api && npx jest --testPathPattern=estimate --no-coverage` -- all estimate tests pass
2. `cd trade-flow-api && npx tsc --noEmit` -- no compile errors from enum/entity changes
3. `grep "SITE_VISIT_REQUESTED" trade-flow-api/src/estimate/ -r` -- zero matches
4. `grep "siteVisitAvailability" trade-flow-api/src/estimate/ -r` -- zero matches
</verification>

<success_criteria>
- EstimateStatus enum has exactly 9 values (no SITE_VISIT_REQUESTED)
- EstimateResponseType enum has exactly 3 values (PROCEED, MESSAGE, DECLINE)
- EstimateDeclineReason enum exists with 5 values
- Estimate entity has responses[] array, no siteVisitAvailability
- ALLOWED_TRANSITIONS has no SITE_VISIT_REQUESTED references
- Repository has pushResponse method
- All estimate tests pass
- REQUIREMENTS.md and ROADMAP.md reflect 3-action model
</success_criteria>

<output>
After completion, create `.planning/phases/45-public-customer-page-response-handling/45-01-SUMMARY.md`
</output>
