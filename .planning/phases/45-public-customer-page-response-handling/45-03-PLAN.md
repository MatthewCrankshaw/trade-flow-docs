---
phase: 45-public-customer-page-response-handling
plan: 03
type: execute
wave: 3
depends_on: ["45-02"]
files_modified:
  - trade-flow-api/src/estimate/services/estimate-response-handler.service.ts
  - trade-flow-api/src/estimate/controllers/public-estimate.controller.ts
  - trade-flow-api/src/email/services/estimate-notification-email-renderer.service.ts
  - trade-flow-api/src/email/templates/estimate-response.html
  - trade-flow-api/src/estimate/estimate.module.ts
  - trade-flow-api/src/estimate/test/services/estimate-response-handler.service.spec.ts
  - trade-flow-api/src/document-token/guards/document-session-auth.guard.ts
autonomous: true
requirements:
  - RESP-01
  - RESP-02
  - RESP-03
  - RESP-04
  - RESP-05
  - RESP-06

must_haves:
  truths:
    - "POST /v1/public/estimate/:token/respond accepts proceed, message, and decline types"
    - "Response is persisted to estimate's responses array before email is attempted"
    - "Status transitions to RESPONDED for proceed/message and DECLINED for decline"
    - "Trader receives notification email with response type, customer name, and message"
    - "Email delivery failure does not block response persistence or status transition"
    - "Respondable state validation rejects responses on terminal estimates"
  artifacts:
    - path: "trade-flow-api/src/estimate/services/estimate-response-handler.service.ts"
      provides: "Response processing with status transition and notification"
      exports: ["EstimateResponseHandler"]
    - path: "trade-flow-api/src/email/services/estimate-notification-email-renderer.service.ts"
      provides: "Maizzle template rendering for trader notification"
      exports: ["EstimateNotificationEmailRenderer"]
    - path: "trade-flow-api/src/email/templates/estimate-response.html"
      provides: "Maizzle HTML template for response notification"
  key_links:
    - from: "trade-flow-api/src/estimate/controllers/public-estimate.controller.ts"
      to: "trade-flow-api/src/estimate/services/estimate-response-handler.service.ts"
      via: "constructor injection, respondToEstimate method"
      pattern: "estimateResponseHandler.handleResponse"
    - from: "trade-flow-api/src/estimate/services/estimate-response-handler.service.ts"
      to: "trade-flow-api/src/estimate/repositories/estimate.repository.ts"
      via: "pushResponse to persist response"
      pattern: "estimateRepository.pushResponse"
    - from: "trade-flow-api/src/estimate/services/estimate-response-handler.service.ts"
      to: "trade-flow-api/src/email/services/estimate-notification-email-renderer.service.ts"
      via: "render notification email"
      pattern: "estimateNotificationEmailRenderer.render"
---

<objective>
Build the POST /v1/public/estimate/:token/respond endpoint with the EstimateResponseHandler service (respondable state validation, response persistence, status transitions, failure-tolerant trader notification email). Also creates the Maizzle notification email template, updates the EstimateModule wiring, and fixes the DocumentSessionAuthGuard to handle estimate tokens in the expired/revoked path.

Purpose: Enables RESP-01 (proceed action), RESP-02 (message action), RESP-03 (decline action), RESP-04 (success confirmation), RESP-05 (trader notification email), and RESP-06 (status transitions on customer response). Completes the entire backend public estimate API.
Output: Working POST respond endpoint, EstimateResponseHandler, notification email, module wiring, guard fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-public-customer-page-response-handling/45-CONTEXT.md
@.planning/phases/45-public-customer-page-response-handling/45-RESEARCH.md
@.planning/phases/45-public-customer-page-response-handling/45-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 outputs -->

From trade-flow-api/src/estimate/controllers/public-estimate.controller.ts:
- Already has GET handler; POST handler to be added
- Constructor already has PublicEstimateRetriever injected

From trade-flow-api/src/estimate/requests/respond-to-estimate.request.ts:
- RespondToEstimateRequest { type: EstimateResponseType, message?: string, reason?: EstimateDeclineReason, declineMessage?: string }

From trade-flow-api/src/estimate/responses/public-estimate.response.ts:
- IPublicEstimateResponse interface (customer-safe fields)

From trade-flow-api/src/estimate/repositories/estimate.repository.ts:
- pushResponse(estimateId: string, response: IEstimateResponseEntity): Promise<IEstimateDto>

From trade-flow-api/src/estimate/services/estimate-transition.service.ts:
- publicTransition(estimateId: string, targetStatus: EstimateStatus): Promise<IEstimateDto>

<!-- From existing codebase patterns -->

From trade-flow-api/src/document-token/services/quote-response-handler.service.ts:
- Pattern: validate respondable state, persist response, transition status, send notification (try/catch)

From trade-flow-api/src/email/services/notification-email-renderer.service.ts:
- Pattern: Maizzle render(templateName, data) -> HTML string

From trade-flow-api/src/document-token/guards/document-session-auth.guard.ts:
- Pitfall: expired/revoked path uses quoteRepository.findByIdOrFail for business name lookup; needs estimate branch
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EstimateResponseHandler and EstimateNotificationEmailRenderer services</name>
  <files>
    trade-flow-api/src/estimate/services/estimate-response-handler.service.ts,
    trade-flow-api/src/email/services/estimate-notification-email-renderer.service.ts,
    trade-flow-api/src/email/templates/estimate-response.html,
    trade-flow-api/src/estimate/test/services/estimate-response-handler.service.spec.ts
  </files>
  <read_first>
    trade-flow-api/src/document-token/services/quote-response-handler.service.ts,
    trade-flow-api/src/email/services/notification-email-renderer.service.ts,
    trade-flow-api/src/email/templates/,
    trade-flow-api/src/estimate/repositories/estimate.repository.ts,
    trade-flow-api/src/estimate/services/estimate-transition.service.ts,
    trade-flow-api/src/estimate/enums/estimate-status.enum.ts,
    trade-flow-api/src/estimate/enums/estimate-response-type.enum.ts,
    trade-flow-api/src/estimate/entities/estimate.entity.ts,
    trade-flow-api/src/estimate/requests/respond-to-estimate.request.ts,
    trade-flow-api/src/business/repositories/business.repository.ts,
    trade-flow-api/src/user/repositories/user.repository.ts
  </read_first>
  <action>
1. **EstimateResponseHandler** (NEW `estimate-response-handler.service.ts`): Injectable service. Constructor dependencies: `EstimateRepository`, `EstimateTransitionService`, `BusinessRepository`, `EstimateNotificationEmailRenderer`, `EmailSenderService` (or whatever the existing email-sending service is called -- read from QuoteResponseHandler), `AppLogger`.

   Method `handleResponse(token: IDocumentTokenDto, request: RespondToEstimateRequest): Promise<IEstimateDto>`:

   Step 1 -- **Resolve estimate**: Use the same latest-revision resolution as PublicEstimateRetriever: query `{ rootEstimateId: token.documentId, isCurrent: true }` with fallback to direct ID lookup.

   Step 2 -- **Validate respondable state** (per Pitfall 6): Check estimate status is `VIEWED` or `SENT`. If not, throw `InvalidRequestError(ErrorCodes.ESTIMATE_NOT_RESPONDABLE, "This estimate can no longer receive responses")`. Add `ESTIMATE_NOT_RESPONDABLE` to ErrorCodes enum and ERRORS_MAP if not present.

   Step 3 -- **Build response entity**:
   ```typescript
   const responseEntity: IEstimateResponseEntity = {
     type: request.type,
     message: request.type === EstimateResponseType.MESSAGE ? request.message : (request.type === EstimateResponseType.DECLINE ? request.declineMessage : null) ?? null,
     reason: request.type === EstimateResponseType.DECLINE ? request.reason ?? null : null,
     respondedAt: new Date(),
   };
   ```

   Step 4 -- **Persist response**: Call `estimateRepository.pushResponse(estimate.id, responseEntity)`.

   Step 5 -- **Transition status**: Determine target status:
   - `request.type === EstimateResponseType.PROCEED` -> `EstimateStatus.RESPONDED`
   - `request.type === EstimateResponseType.MESSAGE` -> `EstimateStatus.RESPONDED`
   - `request.type === EstimateResponseType.DECLINE` -> `EstimateStatus.DECLINED`
   Call `estimateTransitionService.publicTransition(estimate.id, targetStatus)`.

   Step 6 -- **Send notification email** (failure-tolerant per D-NOTIFY-03):
   ```typescript
   try {
     const business = await this.businessRepository.findByIdOrFail(estimate.businessId);
     const html = await this.estimateNotificationEmailRenderer.render({
       customerName: estimate.customerName,
       estimateNumber: estimate.estimateNumber,
       responseType: request.type,
       message: responseEntity.message,
       reason: responseEntity.reason,
       estimateId: estimate.id,
       businessName: business.name,
     });
     const subject = this.buildSubject(estimate.customerName, estimate.estimateNumber, request.type);
     await this.emailSenderService.send({
       to: business.email,
       subject,
       html,
     });
   } catch (emailError) {
     this.logger.error("Failed to send estimate response notification email", {
       estimateId: estimate.id,
       error: String(emailError),
     });
   }
   ```

   Step 7 -- **Return updated estimate** from the pushResponse result.

   Private method `buildSubject(customerName, estimateNumber, type)`: Returns subject lines per D-NOTIFY-02:
   - PROCEED: `"{customerName} responded to Estimate {estimateNumber} -- Happy to Proceed"`
   - MESSAGE: `"{customerName} responded to Estimate {estimateNumber} -- Message"`
   - DECLINE: `"{customerName} responded to Estimate {estimateNumber} -- Declined"`

2. **EstimateNotificationEmailRenderer** (NEW `estimate-notification-email-renderer.service.ts`): Mirror the existing `NotificationEmailRenderer` pattern. Renders the `estimate-response` Maizzle template with data. Read the existing renderer to match the exact dynamic import / render pattern used.

3. **Maizzle template** (`estimate-response.html`): Single template with conditional sections per D-NOTIFY-01. Include:
   - Heading: "{customerName} responded to your estimate"
   - Estimate reference: "{estimateNumber}"
   - Response type summary (conditional):
     - PROCEED: "Great news! {customerName} is happy to proceed with the work."
     - MESSAGE: "{customerName} sent you a message:" followed by quoted message text
     - DECLINE: "{customerName} declined this estimate." with reason if provided, and decline message if provided
   - CTA button: "View Estimate" linking to the authenticated estimate detail page URL (construct from base URL + `/estimates/{estimateId}`)
   - **HTML escaping for user content** (T-45-06): Read the existing email templates and the NotificationEmailRenderer to determine how user content is escaped. If Maizzle's template expressions auto-escape HTML entities (check the existing templates for `{{ }}` vs `{{{ }}}` syntax), use the auto-escaping syntax for all user-provided fields (message, reason). If Maizzle does NOT auto-escape, import or create an `escapeHtml` utility (check `src/core/utilities/` or `src/email/utilities/` for an existing one) and apply it to `message` and `reason` data before passing to the template renderer. The key requirement: user-provided content must never render as raw HTML in the email.

4. **Test file** (`estimate-response-handler.service.spec.ts`): Unit tests covering:
   - Proceed type: persists response, transitions to RESPONDED, sends notification
   - Message type: persists response with message text, transitions to RESPONDED
   - Decline type: persists response with reason and declineMessage, transitions to DECLINED
   - Rejects response on terminal estimate (CONVERTED) with ESTIMATE_NOT_RESPONDABLE
   - Rejects response on DECLINED estimate
   - Email failure does not throw -- response is still persisted
   - Email subject matches expected pattern for each type
   Mock all dependencies.
  </action>
  <verify>
    <automated>cd trade-flow-api && npx jest --testPathPattern=estimate-response-handler --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `estimate-response-handler.service.ts` contains `@Injectable()` and `handleResponse` method
    - `estimate-response-handler.service.ts` contains respondable state check: `VIEWED` or `SENT`
    - `estimate-response-handler.service.ts` contains `pushResponse` call before `publicTransition`
    - `estimate-response-handler.service.ts` contains try/catch around email send with `this.logger.error` in catch
    - `estimate-notification-email-renderer.service.ts` contains `@Injectable()` and `render` method
    - `estimate-response.html` exists in templates directory
    - `estimate-response.html` contains conditional sections for proceed, message, and decline
    - `estimate-response.html` user-provided content uses auto-escaping syntax or pre-escaped data (per T-45-06)
    - `estimate-response-handler.service.spec.ts` exits 0 with all tests passing
    - Spec file covers proceed, message, decline, terminal-state rejection, and email-failure tolerance
  </acceptance_criteria>
  <done>EstimateResponseHandler validates respondable state, persists response, transitions status, sends failure-tolerant notification. Template renders all three types with HTML-escaped user content. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add POST respond endpoint to controller, fix guard, wire EstimateModule</name>
  <files>
    trade-flow-api/src/estimate/controllers/public-estimate.controller.ts,
    trade-flow-api/src/document-token/guards/document-session-auth.guard.ts,
    trade-flow-api/src/estimate/estimate.module.ts,
    trade-flow-api/src/estimate/test/controllers/public-estimate.controller.spec.ts
  </files>
  <read_first>
    trade-flow-api/src/estimate/controllers/public-estimate.controller.ts,
    trade-flow-api/src/document-token/guards/document-session-auth.guard.ts,
    trade-flow-api/src/document-token/controllers/public-quote.controller.ts,
    trade-flow-api/src/estimate/estimate.module.ts,
    trade-flow-api/src/estimate/services/estimate-response-handler.service.ts,
    trade-flow-api/src/estimate/requests/respond-to-estimate.request.ts,
    trade-flow-api/src/estimate/test/controllers/public-estimate.controller.spec.ts,
    trade-flow-api/src/core/errors/error-codes.enum.ts,
    trade-flow-api/src/core/errors/errors-map.constant.ts
  </read_first>
  <action>
1. **PublicEstimateController** -- Add POST /respond handler to the existing controller (per D-API-03):
   - Add `EstimateResponseHandler` to the constructor injection
   - Add the respond endpoint:
   ```typescript
   @Throttle({ default: { limit: 10, ttl: 60000 } })
   @UseGuards(DocumentSessionAuthGuard)
   @Post("estimate/:token/respond")
   public async respondToEstimate(
     @Req() request: { documentToken: IDocumentTokenDto },
     @Body() body: RespondToEstimateRequest,
   ): Promise<IResponse<IPublicEstimateResponse>> {
     if (request.documentToken.documentType !== "estimate") {
       throw createHttpError(
         new ResourceNotFoundError(ErrorCodes.DOCUMENT_TOKEN_TYPE_MISMATCH, "Estimate not found"),
       );
     }
     try {
       const updatedEstimate = await this.estimateResponseHandler.handleResponse(
         request.documentToken,
         body,
       );
       // Re-fetch the public view to return consistent response shape
       const response = await this.publicEstimateRetriever.getPublicEstimate(
         request.documentToken,
       );
       return createResponse([response]);
     } catch (error) {
       throw createHttpError(error);
     }
   }
   ```
   Note the rate limit is 10/min (stricter than GET's 60/min) to prevent response spam.

2. **DocumentSessionAuthGuard fix** (per Research Pitfall 1): The guard's expired/revoked token path looks up business name using `quoteRepository.findByIdOrFail(tokenDto.documentId)`. Add a branch for estimate tokens:
   - Read the guard to find the exact line where `quoteRepository` is used for business name lookup
   - Add a conditional: if `tokenDto.documentType === "estimate"`, use `estimateRepository.findByIdOrFail(tokenDto.documentId)` instead
   - Inject `EstimateRepository` into the guard's constructor. If this creates a circular dependency (EstimateModule imports DocumentTokenModule which imports EstimateRepository), use `@Inject(forwardRef(() => EstimateRepository))` pattern
   - If forwardRef is too complex, extract the business-name lookup into a minimal helper that takes the repository directly, or simply catch the ResourceNotFoundError from quoteRepository and try estimateRepository as a fallback

3. **EstimateModule wiring** (`estimate.module.ts`):
   - Add `PublicEstimateController` to the `controllers` array
   - Add `PublicEstimateRetriever`, `EstimateResponseHandler` to the `providers` array
   - Ensure `EstimateNotificationEmailRenderer` is provided (either in EstimateModule or imported from EmailModule)
   - Import `DocumentTokenModule` if not already imported (needed for DocumentTokenRepository access)
   - Import `BusinessModule` if not already imported (needed for BusinessRepository)

4. **ErrorCodes**: If `ESTIMATE_NOT_RESPONDABLE` was not already added in Task 1, add it now:
   - Add `ESTIMATE_NOT_RESPONDABLE = "estimate_not_respondable"` to `ErrorCodes` enum
   - Add mapping in `ERRORS_MAP`: `[ErrorCodes.ESTIMATE_NOT_RESPONDABLE]: { statusCode: 422, message: "This estimate can no longer receive responses" }`

5. **Controller spec update**: Add test cases for the POST endpoint:
   - Delegates to EstimateResponseHandler.handleResponse
   - Returns updated public estimate response
   - Throws for non-estimate token type
   - Wraps errors in createHttpError
  </action>
  <verify>
    <automated>cd trade-flow-api && npx jest --testPathPattern=public-estimate --no-coverage && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `public-estimate.controller.ts` has `@Post("estimate/:token/respond")` handler
    - `public-estimate.controller.ts` has `@Throttle({ default: { limit: 10, ttl: 60000 } })` on POST
    - `public-estimate.controller.ts` has `EstimateResponseHandler` in constructor
    - `document-session-auth.guard.ts` handles `documentType === "estimate"` for expired/revoked business name lookup
    - `estimate.module.ts` `controllers` array contains `PublicEstimateController`
    - `estimate.module.ts` `providers` array contains `PublicEstimateRetriever` and `EstimateResponseHandler`
    - `npx jest --testPathPattern=public-estimate --no-coverage` exits 0
    - `npx tsc --noEmit` exits 0 (no compile errors)
  </acceptance_criteria>
  <done>POST respond endpoint wired to controller with 10/min rate limit, guard handles estimate tokens, EstimateModule registers all new services and controllers, compile and tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Full CI gate for backend</name>
  <files>
    trade-flow-api/openapi.yaml
  </files>
  <read_first>
    trade-flow-api/openapi.yaml
  </read_first>
  <action>
1. **OpenAPI update** (`openapi.yaml`): Add the two new public endpoints:
   - `GET /v1/public/estimate/{token}`: Returns public estimate view. Response schema references IPublicEstimateResponse fields. Tags: ["Public Estimate"].
   - `POST /v1/public/estimate/{token}/respond`: Accepts RespondToEstimateRequest body. Response returns updated public estimate. Tags: ["Public Estimate"].
   Include request/response schemas with accurate field descriptions.

2. **Run full CI gate**: Execute `npm run ci` in trade-flow-api. This runs tests, lint, format check, and type check. Fix any issues that arise from the enum, entity, and service changes across Plans 01-03.
  </action>
  <verify>
    <automated>cd trade-flow-api && npm run ci</automated>
  </verify>
  <acceptance_criteria>
    - `openapi.yaml` contains `/v1/public/estimate/{token}` GET path
    - `openapi.yaml` contains `/v1/public/estimate/{token}/respond` POST path
    - `npm run ci` exits 0 (all tests, lint, format, typecheck pass)
  </acceptance_criteria>
  <done>OpenAPI docs updated with both public estimate endpoints. Full CI gate passes in trade-flow-api.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Customer browser -> POST /respond | Untrusted user input (message, reason) crosses trust boundary |
| EstimateResponseHandler -> EmailSender | Response persistence must complete before (unreliable) email send |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04 | Tampering | POST /respond body | mitigate | class-validator on RespondToEstimateRequest: @IsEnum for type/reason, @IsNotEmpty() + @MaxLength(2000) for message, @MaxLength(500) for declineMessage |
| T-45-05 | Tampering | Response replay | mitigate | Respondable state check (VIEWED/SENT only) rejects duplicate responses on terminal estimates |
| T-45-06 | Tampering | XSS in notification email | mitigate | User-provided content (message, reason) HTML-escaped before Maizzle template rendering -- use Maizzle auto-escaping syntax or pre-escape via utility (read existing templates for pattern) |
| T-45-07 | Denial of Service | POST /respond | mitigate | ThrottlerGuard 10 req/min (stricter than GET) prevents response spam |
| T-45-08 | Spoofing | DocumentSessionAuthGuard | mitigate | Guard fix adds estimate token handling for expired/revoked path; prevents 500 errors on expired estimate tokens |
</threat_model>

<verification>
1. `cd trade-flow-api && npm run ci` -- full CI gate green
2. POST /respond with proceed type persists response and transitions to RESPONDED
3. POST /respond with decline type transitions to DECLINED
4. POST /respond on terminal estimate returns 422
5. Email failure does not block response
6. Guard correctly handles expired estimate tokens
</verification>

<success_criteria>
- EstimateResponseHandler processes all three response types correctly
- Status transitions: proceed/message -> RESPONDED, decline -> DECLINED
- Email notification sent failure-tolerantly with HTML-escaped user content
- Respondable state validation rejects terminal estimates
- Guard handles estimate tokens in expired/revoked path
- Module wiring complete
- OpenAPI docs updated
- Full CI gate passes
</success_criteria>

<output>
After completion, create `.planning/phases/45-public-customer-page-response-handling/45-03-SUMMARY.md`
</output>
