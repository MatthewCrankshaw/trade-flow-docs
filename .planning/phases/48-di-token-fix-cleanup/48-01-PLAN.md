---
phase: 48-di-token-fix-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-api/src/estimate/estimate.module.ts
  - trade-flow-api/src/estimate/requests/convert-estimate.request.ts
  - trade-flow-api/src/estimate/test/services/estimate-reviser.service.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "EstimateModule providers array does not contain NoopEstimateFollowupCanceller or ESTIMATE_FOLLOWUP_CANCELLER token"
    - "EstimateModule exports array does not contain ESTIMATE_FOLLOWUP_CANCELLER"
    - "EstimateModule imports array still contains EstimateFollowupsModule"
    - "ConvertEstimateRequest file does not exist"
    - "npm run ci passes in trade-flow-api"
  artifacts:
    - path: "trade-flow-api/src/estimate/estimate.module.ts"
      provides: "Clean module with no local followup canceller override"
      contains: "EstimateFollowupsModule"
  key_links:
    - from: "trade-flow-api/src/estimate/estimate.module.ts"
      to: "trade-flow-api/src/estimate-followups/estimate-followups.module.ts"
      via: "NestJS module import"
      pattern: "EstimateFollowupsModule"
---

<objective>
Fix the critical DI token resolution bug where EstimateModule's local NoopEstimateFollowupCanceller provider overrides the real BullMQEstimateFollowupCanceller, remove the duplicate Noop class registration, delete the empty ConvertEstimateRequest class, and update the affected test.

Purpose: In production, all follow-up cancellation calls (on revision, conversion, mark-as-lost, customer response) currently resolve to a no-op because NestJS local providers take precedence over imported module exports. This fix ensures the real BullMQ canceller is used.
Output: Clean EstimateModule with correct DI resolution, no dead code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-di-token-fix-cleanup/48-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DI token override and remove duplicate Noop registration from EstimateModule</name>
  <files>trade-flow-api/src/estimate/estimate.module.ts</files>
  <read_first>
    trade-flow-api/src/estimate/estimate.module.ts
    trade-flow-api/src/estimate-followups/estimate-followups.module.ts
    trade-flow-api/src/estimate/services/noop-estimate-followup-canceller.service.ts
    trade-flow-api/src/estimate/services/estimate-followup-canceller.interface.ts
  </read_first>
  <action>
Edit `trade-flow-api/src/estimate/estimate.module.ts`:

1. **Remove from providers array** — delete these two entries:
   - `NoopEstimateFollowupCanceller` (the standalone class provider)
   - `{ provide: ESTIMATE_FOLLOWUP_CANCELLER, useClass: NoopEstimateFollowupCanceller }` (the token provider)

2. **Remove from exports array** — delete:
   - `ESTIMATE_FOLLOWUP_CANCELLER`

3. **Remove now-unused imports** at top of file:
   - `import { ESTIMATE_FOLLOWUP_CANCELLER } from "@estimate/services/estimate-followup-canceller.interface";`
   - `import { NoopEstimateFollowupCanceller } from "@estimate/services/noop-estimate-followup-canceller.service";`

4. **Keep untouched:**
   - `EstimateFollowupsModule` in the `imports` array (this is the correct source of the ESTIMATE_FOLLOWUP_CANCELLER token via BullMQEstimateFollowupCanceller)
   - The `NoopEstimateFollowupCanceller` class FILE itself (used in test specs)

Do NOT remove any other providers, imports, or exports. Do NOT modify the NoopEstimateFollowupCanceller service file.
  </action>
  <verify>
    <automated>cd trade-flow-api && grep -c "NoopEstimateFollowupCanceller" src/estimate/estimate.module.ts | grep -q "^0$" && grep -c "ESTIMATE_FOLLOWUP_CANCELLER" src/estimate/estimate.module.ts | grep -q "^0$" && grep -q "EstimateFollowupsModule" src/estimate/estimate.module.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "NoopEstimateFollowupCanceller" trade-flow-api/src/estimate/estimate.module.ts` returns zero matches
    - `grep "ESTIMATE_FOLLOWUP_CANCELLER" trade-flow-api/src/estimate/estimate.module.ts` returns zero matches
    - `grep "EstimateFollowupsModule" trade-flow-api/src/estimate/estimate.module.ts` returns at least one match (import still present)
    - `trade-flow-api/src/estimate/services/noop-estimate-followup-canceller.service.ts` still exists (NOT deleted)
  </acceptance_criteria>
  <done>EstimateModule no longer has a local ESTIMATE_FOLLOWUP_CANCELLER provider or NoopEstimateFollowupCanceller class provider. The import of EstimateFollowupsModule provides the real BullMQ canceller.</done>
</task>

<task type="auto">
  <name>Task 2: Delete ConvertEstimateRequest and update reviser test</name>
  <files>trade-flow-api/src/estimate/requests/convert-estimate.request.ts, trade-flow-api/src/estimate/test/services/estimate-reviser.service.spec.ts</files>
  <read_first>
    trade-flow-api/src/estimate/requests/convert-estimate.request.ts
    trade-flow-api/src/estimate/test/services/estimate-reviser.service.spec.ts
  </read_first>
  <action>
**Part A: Delete empty ConvertEstimateRequest**

Delete file `trade-flow-api/src/estimate/requests/convert-estimate.request.ts`. This file contains only `export class ConvertEstimateRequest {}` and has zero import references anywhere in the codebase (verified by grep in research).

Before deleting, run `grep -r "ConvertEstimateRequest" trade-flow-api/src/` to confirm zero imports. If any import is found, update that file to remove the import before deleting.

**Part B: Remove stale DI resolution test from estimate-reviser.service.spec.ts**

In `trade-flow-api/src/estimate/test/services/estimate-reviser.service.spec.ts`, find and remove the test block that asserts "resolves ESTIMATE_FOLLOWUP_CANCELLER to NoopEstimateFollowupCanceller" (approximately lines 260-264). This test validated the old (now-incorrect) behavior where the Noop was the intended DI resolution. Since we fixed the module in Task 1, this test would assert wrong production behavior.

The test module setup in the spec file that uses `{ provide: ESTIMATE_FOLLOWUP_CANCELLER, useClass: NoopEstimateFollowupCanceller }` for unit test isolation should remain unchanged — that is correct for isolating the unit under test from BullMQ.

**Part C: Run full CI gate**

Run `cd trade-flow-api && npm run ci` to confirm all tests pass, lint is clean, types check, and formatting is correct.
  </action>
  <verify>
    <automated>cd trade-flow-api && test ! -f src/estimate/requests/convert-estimate.request.ts && npm run ci</automated>
  </verify>
  <acceptance_criteria>
    - `trade-flow-api/src/estimate/requests/convert-estimate.request.ts` does not exist
    - `grep -r "ConvertEstimateRequest" trade-flow-api/src/` returns zero matches
    - `grep "resolves ESTIMATE_FOLLOWUP_CANCELLER to Noop" trade-flow-api/src/estimate/test/services/estimate-reviser.service.spec.ts` returns zero matches
    - `grep "useClass: NoopEstimateFollowupCanceller" trade-flow-api/src/estimate/test/services/estimate-reviser.service.spec.ts` returns at least one match (test module setup preserved)
    - `npm run ci` in trade-flow-api exits 0
  </acceptance_criteria>
  <done>ConvertEstimateRequest deleted with zero remaining references. Stale DI resolution test removed. Full CI gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Module DI resolution | Internal NestJS container — no external trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Denial of Service | EstimateModule DI | accept | Removing a local provider cannot introduce new attack vectors; it restores intended behavior. If Redis/BullMQ is down, QueueModule throws at startup (hard gate, not a new risk). |
| T-48-02 | Tampering | ConvertEstimateRequest removal | accept | File has zero references; deletion cannot alter any request validation path. |
</threat_model>

<verification>
1. `grep -c "NoopEstimateFollowupCanceller" trade-flow-api/src/estimate/estimate.module.ts` returns `0`
2. `grep -c "ESTIMATE_FOLLOWUP_CANCELLER" trade-flow-api/src/estimate/estimate.module.ts` returns `0`
3. `test ! -f trade-flow-api/src/estimate/requests/convert-estimate.request.ts` succeeds
4. `cd trade-flow-api && npm run ci` exits 0
</verification>

<success_criteria>
- EstimateModule has zero local providers for ESTIMATE_FOLLOWUP_CANCELLER token
- NoopEstimateFollowupCanceller class file still exists for test use
- ConvertEstimateRequest file deleted, zero dangling references
- Stale DI resolution test removed, unit test isolation setup preserved
- trade-flow-api CI gate fully green
</success_criteria>

<output>
After completion, create `.planning/phases/48-di-token-fix-cleanup/48-01-SUMMARY.md`
</output>
