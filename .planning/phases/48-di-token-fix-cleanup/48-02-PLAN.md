---
phase: 48-di-token-fix-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-ui/src/types/estimate.ts
  - trade-flow-ui/src/pages/EstimateDetailPage.tsx
  - trade-flow-ui/src/pages/EstimatesPage.tsx
  - trade-flow-ui/src/features/estimates/components/EstimatesCardList.tsx
  - trade-flow-ui/src/features/estimates/components/EstimatesTable.tsx
  - trade-flow-ui/src/features/estimates/components/EstimateActionStrip.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "EstimateStatus type union does not contain site_visit_requested"
    - "No frontend file references site_visit_requested"
    - "npm run ci passes in trade-flow-ui"
  artifacts:
    - path: "trade-flow-ui/src/types/estimate.ts"
      provides: "Clean EstimateStatus union without dead status"
  key_links:
    - from: "trade-flow-ui/src/types/estimate.ts"
      to: "trade-flow-ui/src/pages/EstimateDetailPage.tsx"
      via: "TypeScript type import"
      pattern: "EstimateStatus"
---

<objective>
Remove the dead `site_visit_requested` status from all 6 frontend files where it persists after its backend removal in Phase 45-01.

Purpose: The backend enum no longer contains `site_visit_requested`, making these frontend references dead code — unreachable branches in badge maps, filter arrays, and type unions.
Output: Clean frontend code with no dead status branches. TypeScript compilation enforces completeness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-di-token-fix-cleanup/48-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove site_visit_requested from EstimateStatus type and all component references</name>
  <files>trade-flow-ui/src/types/estimate.ts, trade-flow-ui/src/pages/EstimateDetailPage.tsx, trade-flow-ui/src/pages/EstimatesPage.tsx, trade-flow-ui/src/features/estimates/components/EstimatesCardList.tsx, trade-flow-ui/src/features/estimates/components/EstimatesTable.tsx, trade-flow-ui/src/features/estimates/components/EstimateActionStrip.tsx</files>
  <read_first>
    trade-flow-ui/src/types/estimate.ts
    trade-flow-ui/src/pages/EstimateDetailPage.tsx
    trade-flow-ui/src/pages/EstimatesPage.tsx
    trade-flow-ui/src/features/estimates/components/EstimatesCardList.tsx
    trade-flow-ui/src/features/estimates/components/EstimatesTable.tsx
    trade-flow-ui/src/features/estimates/components/EstimateActionStrip.tsx
  </read_first>
  <action>
Edit each of these 6 files to remove `site_visit_requested`. The order matters — start with the type file so TypeScript immediately flags any remaining references.

**File 1: `trade-flow-ui/src/types/estimate.ts`**
Remove `"site_visit_requested"` from the `EstimateStatus` union type. The union should contain only: `"draft" | "sent" | "viewed" | "responded" | "converted" | "declined" | "expired" | "lost" | "deleted"` (the exact values present in the backend enum).

**File 2: `trade-flow-ui/src/pages/EstimateDetailPage.tsx`**
Remove the `site_visit_requested: "..."` entry from the status-to-badge-variant mapping object (approximately line 48). This is a key-value pair in a `Record<EstimateStatus, ...>` — removing the key aligns with the narrowed type.

**File 3: `trade-flow-ui/src/pages/EstimatesPage.tsx`**
Remove `"site_visit_requested"` from TWO arrays in the `TAB_STATUS_GROUPS` object:
- The `all` array (approximately line 30)
- The `responded` array (approximately line 33)

**File 4: `trade-flow-ui/src/features/estimates/components/EstimatesCardList.tsx`**
Remove the `site_visit_requested: "..."` entry from the status badge variant mapping object (approximately line 24).

**File 5: `trade-flow-ui/src/features/estimates/components/EstimatesTable.tsx`**
Remove the `site_visit_requested: "..."` entry from the status badge variant mapping object (approximately line 24).

**File 6: `trade-flow-ui/src/features/estimates/components/EstimateActionStrip.tsx`**
Remove `"site_visit_requested"` from the `canResend` status array (approximately line 50).

After all edits, run `cd trade-flow-ui && grep -r "site_visit_requested" src/` to confirm zero remaining references. Then run `npm run ci` to confirm TypeScript compilation, lint, formatting, and tests all pass.
  </action>
  <verify>
    <automated>cd trade-flow-ui && grep -r "site_visit_requested" src/ | grep -c . | grep -q "^0$" && npm run ci</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "site_visit_requested" trade-flow-ui/src/` returns zero matches
    - `trade-flow-ui/src/types/estimate.ts` does NOT contain the string `site_visit_requested`
    - `trade-flow-ui/src/pages/EstimateDetailPage.tsx` does NOT contain the string `site_visit_requested`
    - `trade-flow-ui/src/pages/EstimatesPage.tsx` does NOT contain the string `site_visit_requested`
    - `trade-flow-ui/src/features/estimates/components/EstimatesCardList.tsx` does NOT contain the string `site_visit_requested`
    - `trade-flow-ui/src/features/estimates/components/EstimatesTable.tsx` does NOT contain the string `site_visit_requested`
    - `trade-flow-ui/src/features/estimates/components/EstimateActionStrip.tsx` does NOT contain the string `site_visit_requested`
    - `npm run ci` in trade-flow-ui exits 0
  </acceptance_criteria>
  <done>All 6 frontend files are clean of site_visit_requested. TypeScript compilation confirms no dangling references. Full CI gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | No trust boundaries affected — removing dead frontend string literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-03 | Information Disclosure | EstimateStatus type | accept | Removing a dead status value from a client-side type union has no security impact. The backend already rejects this value. |
</threat_model>

<verification>
1. `grep -r "site_visit_requested" trade-flow-ui/src/` returns zero matches
2. `cd trade-flow-ui && npm run ci` exits 0
</verification>

<success_criteria>
- Zero occurrences of `site_visit_requested` in any file under `trade-flow-ui/src/`
- TypeScript compilation passes (narrowed type union catches any missed references)
- All existing tests pass
- Lint and formatting clean
</success_criteria>

<output>
After completion, create `.planning/phases/48-di-token-fix-cleanup/48-02-SUMMARY.md`
</output>
