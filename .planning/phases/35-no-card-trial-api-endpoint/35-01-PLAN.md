---
phase: 35-no-card-trial-api-endpoint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-api/src/subscription/services/subscription-trial-creator.service.ts
  - trade-flow-api/src/subscription/repositories/subscription.repository.ts
  - trade-flow-api/src/subscription/controllers/subscription.controller.ts
  - trade-flow-api/src/subscription/subscription.module.ts
  - trade-flow-api/src/subscription/test/services/subscription-trial-creator.service.spec.ts
autonomous: true
requirements: [TRIAL-01, TRIAL-04]

must_haves:
  truths:
    - "POST /v1/subscription/trial creates a Stripe subscription with 30-day trial and no payment method required"
    - "The local MongoDB subscription record is written synchronously in the endpoint response"
    - "Trial endpoint rejects with HTTP 422 if any subscription record already exists for the user"
    - "Stripe subscription is created with trial_settings.end_behavior.missing_payment_method set to cancel"
  artifacts:
    - path: "trade-flow-api/src/subscription/services/subscription-trial-creator.service.ts"
      provides: "Trial creation business logic"
      exports: ["SubscriptionTrialCreator"]
    - path: "trade-flow-api/src/subscription/test/services/subscription-trial-creator.service.spec.ts"
      provides: "Unit tests for trial creator"
      contains: "SubscriptionTrialCreator"
  key_links:
    - from: "trade-flow-api/src/subscription/controllers/subscription.controller.ts"
      to: "SubscriptionTrialCreator"
      via: "DI injection, @Post('trial') route"
      pattern: "createTrial.*subscriptionTrialCreator"
    - from: "trade-flow-api/src/subscription/services/subscription-trial-creator.service.ts"
      to: "SubscriptionRepository"
      via: "DI injection, findByUserId + upsertByStripeCustomerId"
      pattern: "subscriptionRepository\\.findByUserId"
    - from: "trade-flow-api/src/subscription/services/subscription-trial-creator.service.ts"
      to: "Stripe SDK"
      via: "STRIPE_CLIENT injection, customers.create + subscriptions.create"
      pattern: "stripe\\.subscriptions\\.create"
---

<objective>
Create the POST /v1/subscription/trial endpoint that starts a 30-day no-card Stripe trial for new users, with synchronous local record creation and duplicate guard.

Purpose: Enable Phase 37's onboarding wizard to activate a free trial after business setup, without requiring payment details.
Output: Working trial endpoint with unit tests, ready for webhook compatibility (Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-no-card-trial-api-endpoint/35-CONTEXT.md
@.planning/phases/35-no-card-trial-api-endpoint/35-RESEARCH.md
@.planning/milestones/v1.6-phases/29-subscription-module-foundation/29-CONTEXT.md
@.planning/milestones/v1.6-phases/30-stripe-checkout-and-webhooks/30-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. From Phase 29/30 summaries. -->

From subscription entity (Phase 29 D-08):
- userId (string), stripeCustomerId (string), stripeSubscriptionId (string, optional)
- status (SubscriptionStatus enum), currentPeriodEnd (Date, optional), trialEnd (Date, optional)
- cancelAtPeriodEnd (boolean), canceledAt (Date, optional), createdAt/updatedAt (Date)

From SubscriptionStatus enum (Phase 29 D-09):
- TRIALING = "trialing", ACTIVE = "active", PAST_DUE = "past_due", CANCELED = "canceled", INCOMPLETE = "incomplete"

From SubscriptionRepository (Phase 30-01):
- upsertByStripeCustomerId(dto: ISubscriptionDto): Promise<ISubscriptionDto>
- upsertByStripeSubscriptionId(dto: ISubscriptionDto): Promise<ISubscriptionDto>
- findByStripeCustomerId(id: string): Promise<ISubscriptionDto | null>
- findByCheckoutSessionId(id: string): Promise<ISubscriptionDto | null>

From SubscriptionCreator (Phase 29-02):
- Uses @Inject(STRIPE_CLIENT) private readonly stripe: Stripe
- Uses authUser.externalAuthUserId for Stripe metadata userId (NOT authUser.userId or authUser.id)
- SubscriptionController uses @UseGuards(JwtAuthGuard), WebhookController does not

From IUserDto:
- externalAuthUserId: string (Firebase UID -- the user identity key)
- email: string

From STRIPE_CLIENT injection token:
- Provides typed Stripe instance
- Created as factory provider in SubscriptionModule using ConfigService.get('STRIPE_SECRET_KEY')
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SubscriptionTrialCreator service with findByUserId repo method</name>
  <files>
    trade-flow-api/src/subscription/services/subscription-trial-creator.service.ts,
    trade-flow-api/src/subscription/repositories/subscription.repository.ts,
    trade-flow-api/src/subscription/test/services/subscription-trial-creator.service.spec.ts
  </files>
  <read_first>
    trade-flow-api/src/subscription/services/subscription-creator.service.ts,
    trade-flow-api/src/subscription/repositories/subscription.repository.ts,
    trade-flow-api/src/subscription/data-transfer-objects/subscription.dto.ts,
    trade-flow-api/src/subscription/entities/subscription.entity.ts,
    trade-flow-api/src/subscription/enums/subscription-status.enum.ts,
    trade-flow-api/src/core/errors/invalid-request.error.ts,
    trade-flow-api/src/core/enums/error-codes.enum.ts
  </read_first>
  <behavior>
    - Test 1: create() calls stripe.customers.create with authUser.email and metadata.userId = authUser.externalAuthUserId, then calls stripe.subscriptions.create with the customer ID
    - Test 2: create() calls stripe.subscriptions.create with trial_period_days: 30 and trial_settings.end_behavior.missing_payment_method: "cancel" (TRIAL-04)
    - Test 3: create() calls subscriptionRepository.upsertByStripeCustomerId with status TRIALING, correct stripeSubscriptionId, trialEnd converted from Stripe unix seconds to Date, currentPeriodEnd converted from Stripe unix seconds to Date
    - Test 4: create() returns the upserted subscription DTO
    - Test 5: create() throws InvalidRequestError when subscriptionRepository.findByUserId returns an existing record (per D-09)
    - Test 6: create() reuses existing Stripe Customer when findByUserId returns null but a stripeCustomerId exists on a prior record -- WAIT, D-09 says reject if ANY record exists. So if findByUserId returns non-null, always reject. If null, always create new customer. This simplifies things.
    - Test 7: create() passes configService.get("STRIPE_PRICE_ID") as the price in items[0].price (per D-03)
  </behavior>
  <action>
    1. Add findByUserId method to SubscriptionRepository:
       ```typescript
       async findByUserId(userId: string): Promise<ISubscriptionDto | null> {
         const entity = await this.model.findOne({ userId }).exec();
         return entity ? this.mapToDto(entity) : null;
       }
       ```

    2. Create SubscriptionTrialCreator service at `src/subscription/services/subscription-trial-creator.service.ts`:
       - @Injectable() class with AppLogger
       - Constructor injects: @Inject(STRIPE_CLIENT) stripe: Stripe, subscriptionRepository: SubscriptionRepository, configService: ConfigService
       - Single public method: async create(authUser: IUserDto): Promise<ISubscriptionDto>
       - Flow per D-01 through D-05, D-09:
         a. Call this.subscriptionRepository.findByUserId(authUser.externalAuthUserId)
         b. If found, throw new InvalidRequestError(ErrorCodes.DUPLICATE_RESOURCE, "Trial already used. Manage your subscription in Billing.") -- per D-09, D-11
         c. Create Stripe Customer: this.stripe.customers.create({ email: authUser.email, metadata: { userId: authUser.externalAuthUserId } })
         d. Get priceId from this.configService.get<string>("STRIPE_PRICE_ID")
         e. Create Stripe Subscription: this.stripe.subscriptions.create({
              customer: customer.id,
              items: [{ price: priceId }],
              trial_period_days: 30,
              payment_settings: { save_default_payment_method: "on_subscription" },
              trial_settings: { end_behavior: { missing_payment_method: "cancel" } },
            })
         f. Build DTO: { userId: authUser.externalAuthUserId, stripeCustomerId: customer.id, stripeSubscriptionId: subscription.id, status: SubscriptionStatus.TRIALING, currentPeriodEnd: new Date(subscription.current_period_end * 1000), trialEnd: new Date(subscription.trial_end! * 1000), cancelAtPeriodEnd: false }
         g. Return this.subscriptionRepository.upsertByStripeCustomerId(dto)
       - Log at key points: "Creating trial subscription for user {userId}", "Trial subscription created: {subscriptionId}"

    3. Create unit test at `src/subscription/test/services/subscription-trial-creator.service.spec.ts`:
       - Mock STRIPE_CLIENT with customers.create and subscriptions.create stubs
       - Mock SubscriptionRepository with findByUserId and upsertByStripeCustomerId stubs
       - Mock ConfigService with get returning "price_test_123" for STRIPE_PRICE_ID
       - Test all 7 behaviors listed above
       - Follow existing test patterns from the codebase (e.g., subscription-creator.service.spec.ts if it exists, or other service specs)
  </action>
  <verify>
    <automated>cd trade-flow-api && npx jest src/subscription/test/services/subscription-trial-creator.service.spec.ts --no-coverage -x</automated>
  </verify>
  <acceptance_criteria>
    - File `trade-flow-api/src/subscription/services/subscription-trial-creator.service.ts` exists and contains `export class SubscriptionTrialCreator`
    - File contains `trial_period_days: 30`
    - File contains `missing_payment_method: "cancel"` (TRIAL-04 per D-05)
    - File contains `findByUserId(authUser.externalAuthUserId)` (D-09 guard)
    - File contains `new InvalidRequestError` with message containing "Trial already used" (D-11)
    - File contains `this.stripe.customers.create` and `this.stripe.subscriptions.create`
    - File contains `upsertByStripeCustomerId` (D-02 sync write)
    - File contains `new Date(subscription.current_period_end * 1000)` and `new Date(subscription.trial_end` (unix seconds to ms conversion)
    - Repository file contains `async findByUserId(userId: string)`
    - Test file exists and `npx jest subscription-trial-creator --no-coverage` exits 0
  </acceptance_criteria>
  <done>SubscriptionTrialCreator service creates Stripe customer + subscription with 30-day trial and missing_payment_method cancel, writes local record via upsert, rejects duplicates with 422. All unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire trial endpoint into controller and module</name>
  <files>
    trade-flow-api/src/subscription/controllers/subscription.controller.ts,
    trade-flow-api/src/subscription/subscription.module.ts
  </files>
  <read_first>
    trade-flow-api/src/subscription/controllers/subscription.controller.ts,
    trade-flow-api/src/subscription/subscription.module.ts,
    trade-flow-api/src/subscription/services/subscription-trial-creator.service.ts,
    trade-flow-api/src/subscription/responses/subscription.response.ts
  </read_first>
  <action>
    1. Add SubscriptionTrialCreator to SubscriptionModule providers array in subscription.module.ts (alongside existing SubscriptionCreator and other providers).

    2. Add trial route to SubscriptionController in subscription.controller.ts:
       - Inject SubscriptionTrialCreator in constructor
       - Add route handler:
         ```typescript
         @Post("trial")
         @HttpCode(HttpStatus.CREATED)
         async createTrial(@AuthUser() authUser: IUserDto): Promise<IResponse<ISubscriptionResponse>> {
           const subscription = await this.subscriptionTrialCreator.create(authUser);
           return { data: [this.mapToResponse(subscription)] };
         }
         ```
       - Use the existing mapToResponse method (or equivalent response mapping pattern already in SubscriptionController) -- per D-04, return same shape as GET /v1/subscription
       - The @AuthUser() decorator and IUserDto import should already exist from the checkout route
       - The IResponse wrapper and ISubscriptionResponse should already exist from existing subscription endpoints

    3. Verify the full test suite still passes after wiring.
  </action>
  <verify>
    <automated>cd trade-flow-api && npx jest --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - File `trade-flow-api/src/subscription/controllers/subscription.controller.ts` contains `@Post("trial")`
    - File contains `createTrial` method
    - File contains `subscriptionTrialCreator.create(authUser)`
    - File contains `HttpStatus.CREATED` or `HttpCode(201)`
    - File `trade-flow-api/src/subscription/subscription.module.ts` contains `SubscriptionTrialCreator`
    - `cd trade-flow-api && npx jest --no-coverage` exits 0 with no test failures
  </acceptance_criteria>
  <done>POST /v1/subscription/trial route is accessible via JwtAuthGuard-protected SubscriptionController, delegates to SubscriptionTrialCreator, returns subscription response in standard format. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `cd trade-flow-api && npx jest --no-coverage` -- full suite passes with no regressions
2. `cd trade-flow-api && npx tsc --noEmit` -- TypeScript compilation succeeds
3. Trial creator test file has tests for: success path, duplicate rejection, Stripe params (trial_period_days, missing_payment_method), sync write
</verification>

<success_criteria>
- POST /v1/subscription/trial endpoint exists, protected by JwtAuthGuard
- SubscriptionTrialCreator creates Stripe Customer + Subscription with trial_period_days: 30 and missing_payment_method: "cancel"
- Local subscription record written synchronously via upsertByStripeCustomerId with status TRIALING
- Duplicate guard rejects with HTTP 422 if any subscription exists for the userId
- All unit tests pass, full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/35-no-card-trial-api-endpoint/35-01-SUMMARY.md`
</output>
