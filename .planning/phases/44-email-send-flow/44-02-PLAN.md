---
phase: 44-email-send-flow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-api/src/email/templates/estimate-sent.html
  - trade-flow-api/src/email/services/estimate-email-renderer.service.ts
  - trade-flow-api/src/email/email.module.ts
  - trade-flow-api/src/email/test/services/estimate-email-renderer.service.spec.ts
  - trade-flow-api/src/estimate/enums/estimate-email-send-type.enum.ts
  - trade-flow-api/src/estimate/entities/estimate-email-send.entity.ts
  - trade-flow-api/src/estimate/data-transfer-objects/estimate-email-send.dto.ts
  - trade-flow-api/src/estimate/repositories/estimate-email-send.repository.ts
  - trade-flow-api/src/estimate/services/estimate-email-send-creator.service.ts
  - trade-flow-api/src/estimate/test/services/estimate-email-send-creator.service.spec.ts
  - trade-flow-api/src/estimate/test/mocks/estimate-email.mock-generator.ts
  - trade-flow-api/src/estimate/utilities/format-range.utility.ts
  - trade-flow-api/src/estimate/utilities/format-range.utility.spec.ts
  - trade-flow-api/src/document-token/repositories/document-token.repository.ts
  - trade-flow-api/src/document-token/test/repositories/document-token.repository.spec.ts
  - trade-flow-api/src/core/errors/error-codes.enum.ts
autonomous: true
requirements:
  - SND-04
  - SND-05

must_haves:
  truths:
    - "Rendered estimate email always contains the non-binding legal disclaimer regardless of message content"
    - "estimate_email_sends collection stores immutable audit rows with full HTML"
    - "DocumentTokenRepository can find active tokens by documentId+documentType and extend their expiry"
    - "formatRange utility produces '£X.XX - £Y.YY' for range mode and 'From £X.XX' for from mode"
  artifacts:
    - path: "trade-flow-api/src/email/templates/estimate-sent.html"
      provides: "Maizzle email template with static legal disclaimer"
      contains: "This is an estimate, not a fixed price commitment"
    - path: "trade-flow-api/src/email/services/estimate-email-renderer.service.ts"
      provides: "HTML email rendering via Maizzle"
      exports: ["EstimateEmailRenderer"]
    - path: "trade-flow-api/src/estimate/repositories/estimate-email-send.repository.ts"
      provides: "Write-only audit repository"
      contains: "estimate_email_sends"
    - path: "trade-flow-api/src/estimate/utilities/format-range.utility.ts"
      provides: "Server-side price range formatting"
      exports: ["formatRange"]
    - path: "trade-flow-api/src/document-token/repositories/document-token.repository.ts"
      provides: "Extended token lookup and expiry methods"
      contains: "findActiveByDocumentId"
  key_links:
    - from: "trade-flow-api/src/email/services/estimate-email-renderer.service.ts"
      to: "trade-flow-api/src/email/templates/estimate-sent.html"
      via: "fs.readFileSync template path"
      pattern: "estimate-sent.html"
    - from: "trade-flow-api/src/estimate/repositories/estimate-email-send.repository.ts"
      to: "estimate_email_sends collection"
      via: "MongoDbWriter"
      pattern: "estimate_email_sends"
---

<objective>
Create the email rendering infrastructure (Maizzle template with mandatory legal disclaimer, renderer service), the audit persistence layer (estimate_email_sends collection), the format-range utility, and extend DocumentTokenRepository with token-reuse methods.

Purpose: SND-04 requires rendered HTML audit persistence; SND-05 requires non-removable legal disclaimer in the email template. These are the building blocks that Plan 03 (EstimateEmailSender) orchestrates.

Output: estimate-sent.html template, EstimateEmailRenderer, estimate_email_sends collection with repository/creator, formatRange utility, DocumentTokenRepository extensions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-email-send-flow/44-CONTEXT.md
@.planning/phases/44-email-send-flow/44-RESEARCH.md
@.planning/phases/44-email-send-flow/44-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Maizzle email template + EstimateEmailRenderer + structural disclaimer test</name>
  <files>
    trade-flow-api/src/email/templates/estimate-sent.html,
    trade-flow-api/src/email/services/estimate-email-renderer.service.ts,
    trade-flow-api/src/email/email.module.ts,
    trade-flow-api/src/email/test/services/estimate-email-renderer.service.spec.ts,
    trade-flow-api/src/estimate/utilities/format-range.utility.ts,
    trade-flow-api/src/estimate/utilities/format-range.utility.spec.ts
  </files>
  <read_first>
    trade-flow-api/src/email/templates/quote-email.html,
    trade-flow-api/src/email/services/quote-email-renderer.service.ts,
    trade-flow-api/src/email/email.module.ts,
    trade-flow-api/src/email/test/services/quote-email-renderer.service.spec.ts
  </read_first>
  <action>
**estimate-sent.html** (new Maizzle template at `src/email/templates/estimate-sent.html`):
Mirror `quote-email.html` structure (centered card, grey background, max-width 600px) with this layout top-to-bottom per D-TPL-03 and UI-SPEC:

1. **Business name header**: `{{ businessName }}` -- 20px bold, dark grey #1a1a1a.
2. **Legal disclaimer card** (static HTML, per D-LGL-01/02/03 -- this block is NEVER template-variable-driven):
```html
<table role="presentation" width="100%" cellpadding="0" cellspacing="0" border="0"
       style="background-color: #fffbeb; border: 1px solid #fcd34d; border-radius: 6px; margin-bottom: 24px;">
  <tr>
    <td style="padding: 16px;">
      <p style="margin: 0; font-size: 14px; line-height: 1.5; color: #78350f; font-weight: 600;">
        This is an estimate, not a fixed price commitment. A firm quote will be provided after a site visit.
      </p>
    </td>
  </tr>
</table>
```
3. **Trader's personal message**: `{{{ message }}}` (triple-brace for raw HTML) -- 16px, #374151, line-height 1.6.
4. **CTA button**: "View Estimate Online" linking to `{{ viewUrl }}` -- #2563eb background, #ffffff text, 16px semibold, padding 12px 32px, border-radius 6px. Include MSO VML conditional comment for Outlook (copy pattern from quote-email.html).
5. **Summary card**:
   - `Estimate: {{ estimateNumber }}` -- 14px grey #6b7280
   - `Date: {{ estimateDate }}` -- 14px grey #6b7280
   - `Price: {{ priceDisplay }}` -- 14px semibold #1a1a1a (bold)
   - Conditional: if `{{ contingencyNote }}` is not empty, render "Includes a {{ contingencyPercent }}% uncertainty buffer" in 12px grey #6b7280. Use Maizzle `<if>` conditional or inline ternary.
   - Conditional: if `{{ uncertaintyNotes }}` is not empty, render "Notes: {{ uncertaintyNotes }}" in 12px grey #6b7280.
6. **Footer**: "Sent via Trade Flow" -- 12px #9ca3af centered.

All template variables use `{{ }}` double-brace (escaped) EXCEPT `{{{ message }}}` which uses triple-brace (raw HTML, already sanitised by the sender service).

**EstimateEmailRenderer** (`src/email/services/estimate-email-renderer.service.ts`):
Mirror `QuoteEmailRenderer` exactly. Key points:
- `EstimateEmailData` interface: `{ businessName: string, message: string, viewUrl: string, estimateNumber: string, estimateDate: string, priceDisplay: string, contingencyPercent: number, contingencyNote?: string, uncertaintyNotes?: string }`
- `render(data: EstimateEmailData): Promise<string>` method
- Read template from `path.join(__dirname, "../templates/estimate-sent.html")`
- Use the EXACT same `Function('return import("@maizzle/framework")')()` dynamic import pattern from `QuoteEmailRenderer` lines 28-42. Do NOT simplify this -- it handles CommonJS/ESM interop.
- `escapeHtml(str)` helper function copied from QuoteEmailRenderer (escapes `&`, `<`, `>`, `"`, `'`)
- Replace `{{ var }}` placeholders with escaped values; replace `{{{ message }}}` with the raw sanitised HTML
- If Maizzle render fails, return the populated template without CSS inlining (fallback, same as quote pattern)

**EmailModule** (`src/email/email.module.ts`): Add `EstimateEmailRenderer` to the `providers` array and `exports` array.

**formatRange utility** (`src/estimate/utilities/format-range.utility.ts`):
Create a pure function `formatRange(lowMinorUnits: number, highMinorUnits: number, displayMode: "range" | "from", currencyCode: string = "GBP"): string`:
- `range` mode: returns `"£X.XX - £Y.YY"` (convert minor units to major by dividing by 100, format with 2 decimal places, use `£` for GBP)
- `from` mode: returns `"From £X.XX"` (uses `lowMinorUnits` only)
- This must produce byte-identical output to the Phase 43 frontend `formatRange` helper per D-TPL-02.
- Export the function as a named export.

**formatRange spec** (`src/estimate/utilities/format-range.utility.spec.ts`):
Test cases:
- `formatRange(12000, 13200, "range", "GBP")` returns `"£120.00 - £132.00"`
- `formatRange(12000, 13200, "from", "GBP")` returns `"From £120.00"`
- `formatRange(0, 0, "range", "GBP")` returns `"£0.00 - £0.00"`
- `formatRange(999, 1001, "range", "GBP")` returns `"£9.99 - £10.01"`

**EstimateEmailRenderer spec** (`src/email/test/services/estimate-email-renderer.service.spec.ts`):
Mirror `quote-email-renderer.service.spec.ts` structure. Add D-GATE-03 structural tests:
1. Render with default empty message -> output contains "This is an estimate, not a fixed price commitment"
2. Render with message "Hello" -> output contains the disclaimer
3. Render with a large paragraph message -> output contains the disclaimer
4. Render with message containing HTML tags attempting to comment out the disclaimer (e.g. `<!-- `) -> output still contains the disclaimer
All four assertions check for the exact string: `This is an estimate, not a fixed price commitment. A firm quote will be provided after a site visit.`
  </action>
  <verify>
    <automated>cd trade-flow-api && npx jest --testPathPattern="estimate-email-renderer|format-range" --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-api/src/email/templates/estimate-sent.html contains the verbatim string "This is an estimate, not a fixed price commitment. A firm quote will be provided after a site visit."
    - trade-flow-api/src/email/templates/estimate-sent.html contains `{{{ message }}}` (triple-brace)
    - trade-flow-api/src/email/templates/estimate-sent.html contains `{{ viewUrl }}`
    - trade-flow-api/src/email/templates/estimate-sent.html contains "View Estimate Online"
    - trade-flow-api/src/email/services/estimate-email-renderer.service.ts exports `EstimateEmailRenderer` and `EstimateEmailData`
    - trade-flow-api/src/email/services/estimate-email-renderer.service.ts contains `Function('return import` (ESM interop pattern)
    - trade-flow-api/src/email/email.module.ts contains `EstimateEmailRenderer`
    - trade-flow-api/src/estimate/utilities/format-range.utility.ts exports `formatRange`
    - All four disclaimer structural tests pass
    - All formatRange tests pass
  </acceptance_criteria>
  <done>Maizzle template renders estimate emails with mandatory legal disclaimer. EstimateEmailRenderer produces complete HTML. formatRange produces byte-identical output to frontend helper. Four structural tests verify disclaimer cannot be removed.</done>
</task>

<task type="auto">
  <name>Task 2: Audit collection + DocumentTokenRepository extensions + error codes</name>
  <files>
    trade-flow-api/src/estimate/enums/estimate-email-send-type.enum.ts,
    trade-flow-api/src/estimate/entities/estimate-email-send.entity.ts,
    trade-flow-api/src/estimate/data-transfer-objects/estimate-email-send.dto.ts,
    trade-flow-api/src/estimate/repositories/estimate-email-send.repository.ts,
    trade-flow-api/src/estimate/services/estimate-email-send-creator.service.ts,
    trade-flow-api/src/estimate/test/services/estimate-email-send-creator.service.spec.ts,
    trade-flow-api/src/estimate/test/mocks/estimate-email.mock-generator.ts,
    trade-flow-api/src/document-token/repositories/document-token.repository.ts,
    trade-flow-api/src/document-token/test/repositories/document-token.repository.spec.ts,
    trade-flow-api/src/core/errors/error-codes.enum.ts
  </files>
  <read_first>
    trade-flow-api/src/document-token/repositories/document-token.repository.ts,
    trade-flow-api/src/document-token/entities/document-token.entity.ts,
    trade-flow-api/src/document-token/data-transfer-objects/document-token.dto.ts,
    trade-flow-api/src/core/errors/error-codes.enum.ts
  </read_first>
  <action>
**EstimateEmailSendType enum** (`src/estimate/enums/estimate-email-send-type.enum.ts`):
```typescript
export enum EstimateEmailSendType {
  INITIAL = "initial",
  RESEND = "resend",
  FOLLOWUP_3D = "followup_3d",
  FOLLOWUP_10D = "followup_10d",
  FOLLOWUP_21D = "followup_21d",
}
```
All five values defined now per D-AUDIT-02 so Phase 46 only consumes them.

**IEstimateEmailSendEntity** (`src/estimate/entities/estimate-email-send.entity.ts`):
```typescript
export interface IEstimateEmailSendEntity {
  _id: ObjectId;
  estimateId: ObjectId;
  rootEstimateId: ObjectId;
  revisionNumber: number;
  businessId: ObjectId;
  type: EstimateEmailSendType;
  to: string;
  subject: string;
  html: string;
  sentAt: Date;
  createdAt: Date;
  updatedAt: Date;
}
```

**IEstimateEmailSendDto** (`src/estimate/data-transfer-objects/estimate-email-send.dto.ts`):
```typescript
export interface IEstimateEmailSendDto {
  id: string;
  estimateId: string;
  rootEstimateId: string;
  revisionNumber: number;
  businessId: string;
  type: EstimateEmailSendType;
  to: string;
  subject: string;
  html: string;
  sentAt: DateTime;
  createdAt: DateTime;
  updatedAt: DateTime;
}
```

**EstimateEmailSendRepository** (`src/estimate/repositories/estimate-email-send.repository.ts`):
- `static readonly COLLECTION = "estimate_email_sends"`
- `create(dto: Omit<IEstimateEmailSendDto, "id" | "createdAt" | "updatedAt">): Promise<IEstimateEmailSendDto>` -- write-only, immutable
- `findAllByEstimateId(estimateId: string): Promise<IEstimateEmailSendDto[]>` -- sorted by `sentAt: -1`
- `findAllByRootEstimateId(rootEstimateId: string): Promise<IEstimateEmailSendDto[]>` -- sorted by `sentAt: -1`
- NO `update()` or `delete()` methods (immutable per D-AUDIT-01)
- `ensureIndexes()` method creating three indexes per D-AUDIT-03:
  - `{ estimateId: 1, sentAt: -1 }`
  - `{ rootEstimateId: 1, sentAt: -1 }`
  - `{ businessId: 1, sentAt: -1 }`
- Standard `toDto` / `toEntity` mapping methods

**EstimateEmailSendCreator** (`src/estimate/services/estimate-email-send-creator.service.ts`):
- `create(data: { estimateId, rootEstimateId, revisionNumber, businessId, type, to, subject, html, sentAt }): Promise<IEstimateEmailSendDto>`
- Delegates to `EstimateEmailSendRepository.create()`
- No policy check needed (only called internally by EstimateEmailSender, never exposed via HTTP)

**Mock generator** (`src/estimate/test/mocks/estimate-email.mock-generator.ts`): Generate `createMockEstimateEmailSend()` returning a valid `IEstimateEmailSendDto` fixture.

**EstimateEmailSendCreator spec**: Test that `create()` calls repository with correct fields and returns the created DTO.

**DocumentTokenRepository extensions** (`src/document-token/repositories/document-token.repository.ts`):
Add two new methods per D-TKN-02 and D-TKN-03:

1. `findActiveByDocumentId(documentId: string, documentType: "quote" | "estimate"): Promise<IDocumentTokenDto | null>`:
   - Query: `{ documentId: new ObjectId(documentId), documentType, revokedAt: { $exists: false }, expiresAt: { $gt: new Date() } }` sorted by `createdAt: -1`, limit 1
   - Returns null if no active token found
   - Uses existing `this.fetcher.findOne` pattern

2. `extendExpiry(tokenId: string, newExpiresAt: DateTime): Promise<IDocumentTokenDto>`:
   - Uses `this.writer.findOneAndUpdate` with `{ _id: new ObjectId(tokenId) }` and `{ $set: { expiresAt: newExpiresAt.toJSDate(), updatedAt: new Date() } }`
   - Throws `ResourceNotFoundError(ErrorCodes.DOCUMENT_TOKEN_NOT_FOUND, "Document token not found")` if null result
   - Returns updated DTO via `toDto()`

**DocumentTokenRepository spec**: Add tests for both new methods. Test `findActiveByDocumentId` returns null when no active token, returns the token when one exists. Test `extendExpiry` updates the expiry date and throws when token not found.

**Error codes** (`src/core/errors/error-codes.enum.ts`): Add `ESTIMATE_NOT_SENDABLE = "estimate_not_sendable"` and `DOCUMENT_TOKEN_NOT_FOUND = "document_token_not_found"` (if not already present from Phase 41).
  </action>
  <verify>
    <automated>cd trade-flow-api && npx jest --testPathPattern="estimate-email-send-creator|document-token.repository" --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-api/src/estimate/enums/estimate-email-send-type.enum.ts contains all five enum values: INITIAL, RESEND, FOLLOWUP_3D, FOLLOWUP_10D, FOLLOWUP_21D
    - trade-flow-api/src/estimate/repositories/estimate-email-send.repository.ts contains `COLLECTION = "estimate_email_sends"`
    - trade-flow-api/src/estimate/repositories/estimate-email-send.repository.ts does NOT contain any `update` or `delete` method
    - trade-flow-api/src/estimate/repositories/estimate-email-send.repository.ts contains `estimateId: 1, sentAt: -1` index
    - trade-flow-api/src/document-token/repositories/document-token.repository.ts contains `findActiveByDocumentId`
    - trade-flow-api/src/document-token/repositories/document-token.repository.ts contains `extendExpiry`
    - trade-flow-api/src/core/errors/error-codes.enum.ts contains `ESTIMATE_NOT_SENDABLE`
    - All audit creator tests pass
    - All document-token repository tests pass
  </acceptance_criteria>
  <done>Audit collection infrastructure ready for EstimateEmailSender to persist send records. DocumentTokenRepository can find and extend active tokens. All five enum values defined for Phase 46 consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EstimateEmailSender -> estimate_email_sends | Internal write path, no external input |
| DocumentTokenRepository -> document_tokens | Token lookup and expiry extension |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-05 | Repudiation | estimate_email_sends | mitigate | Write-once collection: no update/delete methods exposed; rows are immutable after creation per D-AUDIT-01 |
| T-44-06 | Tampering | estimate-sent.html | mitigate | Legal disclaimer is static HTML in template, not stored in DB or user-editable; structural test D-GATE-03 verifies presence in every render |
| T-44-07 | Information Disclosure | DocumentTokenRepository.findActiveByDocumentId | mitigate | Method is internal (not exposed via HTTP); called only by EstimateEmailSender which is policy-gated |
| T-44-08 | Tampering | estimate_email_sends HTML field | accept | HTML is stored exactly as sent to Resend per D-AUDIT-05; no re-sanitisation needed since it was already sanitised before rendering |
</threat_model>

<verification>
cd trade-flow-api && npx jest --testPathPattern="estimate-email-renderer|format-range|estimate-email-send-creator|document-token.repository" --no-coverage
cd trade-flow-api && npm run ci
</verification>

<success_criteria>
- estimate-sent.html renders with mandatory legal disclaimer in all cases (4 structural tests)
- estimate_email_sends collection has write-only repository with 3 indexes
- DocumentTokenRepository has findActiveByDocumentId and extendExpiry methods
- formatRange produces correct output for range and from modes
- All tests pass, CI green
</success_criteria>

<output>
After completion, create `.planning/phases/44-email-send-flow/44-02-SUMMARY.md`
</output>
