---
phase: 19
slug: customer-response
status: draft
shadcn_initialized: true
preset: new-york
created: 2026-03-21
---

# Phase 19 -- UI Design Contract

> Visual and interaction contract for the Customer Response phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | new-york, baseColor neutral, cssVariables true |
| Component library | radix (via shadcn) |
| Icon library | lucide-react |
| Font | Inter (font-sans), IBM Plex Mono (font-mono) |

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Tailwind | Usage |
|-------|-------|----------|-------|
| 2xs | 4px | `1` | Tight inline gaps, micro alignment nudges |
| sm | 8px | `2` | Field-level spacing |
| md | 12px | `3` | Dense list/card stacking |
| base | 16px | `4` | Component-level rhythm |
| lg | 24px | `6` | Section-level rhythm |
| xl | 48px | `12` | Empty state vertical padding |

Exceptions: 44px touch target minimum for Accept/Decline buttons on mobile (use `min-h-11` which is 44px).

### Phase-Specific Spacing

| Element | Spacing | Rationale |
|---------|---------|-----------|
| Totals to response buttons | `space-y-6` (24px) | Visual break before decision point |
| Between Accept and Decline buttons | `gap-3` (12px) | Enough separation to prevent mis-taps on mobile |
| Status banner to quote content | Card primitive default `gap-6` | Consistent with existing card spacing |
| Decline reason textarea to submit | `space-y-4` (16px) | Standard form field rhythm |

---

## Typography

From project design system (`trade-flow-ui/.github/copilot-instructions/design-system/typography.md`):

| Role | Size | Weight | Line Height | Tailwind |
|------|------|--------|-------------|----------|
| Body | 14px | 400 (normal) | normal | `text-sm` |
| Label | 14px | 500 (medium) | none | `text-sm font-medium leading-none` |
| Card section title | 18px | 500 (medium) | none | `text-lg font-medium` |
| Status banner text | 14px | 400 (normal) | normal | `text-sm` |

Declared weights: 400 (normal), 500 (medium). Two weights only.

### Phase-Specific Typography

| Element | Pattern | Classes |
|---------|---------|---------|
| Accept button label | Sentence case, medium weight | `text-sm font-medium` (Button default) |
| Decline button label | Sentence case, medium weight | `text-sm font-medium` (Button default) |
| Status banner message | Body text inside coloured banner | `text-sm` |
| Decline reason label | Standard form label | `text-sm font-medium leading-none` |
| Expired notice heading | Empty state heading pattern | `text-lg font-medium` |
| Expired notice body | Muted secondary text | `text-sm text-muted-foreground` |

---

## Color

From project design system (`trade-flow-ui/src/index.css` and `colours.md`):

| Role | Token | Value (light) | Usage |
|------|-------|---------------|-------|
| Dominant (60%) | `background` | oklch(0.99 0 0) | Page canvas behind quote card |
| Secondary (30%) | `card` | oklch(1 0 0) | Quote card surface |
| Accent (10%) | `primary` | oklch(0.31 0.09 245) Petrol Blue | Accept button only |
| Destructive | `destructive` | oklch(0.64 0.21 25) Danger Red | Not used for Decline -- see below |
| Success | `success` | oklch(0.69 0.18 165) | Accepted status banner background (10% opacity) + left border |
| Warning | `warning` | oklch(0.75 0.15 85) | Declined status banner background (10% opacity) + left border |

### Phase-Specific Color Assignments

| Element | Token/Class | Rationale |
|---------|-------------|-----------|
| Accept button | `variant="default"` (primary) | Primary action -- closing a deal. Petrol Blue bg, white text. |
| Decline button | `variant="outline"` | Secondary action -- not destructive, just a choice. Neutral border, foreground text. |
| Accepted banner | `border-success bg-success/10` | Matches existing accepted banner in PublicQuoteCard (line 30) |
| Declined banner | `border-warning bg-warning/10` | Matches existing rejected banner in PublicQuoteCard (line 40) |
| Expired banner | `border-muted bg-muted` | Neutral/informational -- not an error, just expired |
| Loading spinner on Accept | `text-primary-foreground` | Spinner visible on primary bg |
| Loading spinner on Decline | `text-foreground` | Spinner visible on outline bg |

Accent reserved for: Accept button (the only primary CTA on this page).

---

## Component Inventory

### New Components (Phase 19)

| Component | Location | shadcn Primitives Used |
|-----------|----------|----------------------|
| `PublicQuoteResponseButtons` | `features/public-quote/components/` | Button |
| `DeclineReasonForm` | `features/public-quote/components/` | Textarea, Button |
| `QuoteExpiredNotice` | `features/public-quote/components/` | (inline markup) |

### Modified Components (Phase 19)

| Component | Change |
|-----------|--------|
| `PublicQuoteCard` | Add response buttons below totals when status is "sent"; show status banners for accepted/rejected (already exists); show expired notice when validUntil has passed |

### Existing shadcn Components Required

| Component | Already Installed | Usage |
|-----------|-------------------|-------|
| Button | Yes | Accept and Decline buttons |
| Textarea | Yes | Decline reason input |
| Card | Yes | Existing quote card container |
| Separator | Yes | Existing visual dividers |

No new shadcn components need to be installed.

---

## Interaction States

### Accept Flow (D-01)

| State | Visual | Behavior |
|-------|--------|----------|
| Default | Two buttons side-by-side: "Accept Quote" (primary) + "Decline Quote" (outline) | Both enabled |
| Accept loading | Accept button shows spinner, text changes to "Accepting...", both buttons disabled | POST request in flight |
| Accept success | Buttons replaced with accepted status banner. Banner: green left border, green/10 bg, text: "You accepted this quote on {date}" | Page content remains visible below |
| Accept error | Toast notification (top-center): "Something went wrong. Please try again." Buttons re-enabled. | Network/server error |

### Decline Flow (D-02, D-03)

| State | Visual | Behavior |
|-------|--------|----------|
| Decline clicked | Accept button hides. Decline button area expands to show: Textarea (optional reason) + "Decline Quote" button (outline) + "Go Back" text button | Inline expansion, no modal |
| Decline reason input | Textarea with placeholder, character counter below | 500 character limit |
| Decline loading | Decline Quote button shows spinner, text changes to "Declining...", both Decline Quote and Go Back disabled | POST request in flight |
| Decline success | Form replaced with declined status banner. Banner: amber left border, warning/10 bg, text: "You declined this quote on {date}" | Page content remains visible below |
| Decline error | Toast notification: "Something went wrong. Please try again." Buttons re-enabled. | Network/server error |
| Go Back clicked | Returns to two-button default state | No network request |

### Expired Quote (D-08)

| State | Visual |
|-------|--------|
| validUntil date has passed | No buttons shown. Instead: muted banner with "This quote has expired" heading + "Please contact {businessName} directly for an updated quote." body text |

### Already Responded (D-09)

| State | Visual |
|-------|--------|
| status === "accepted" | Accepted banner (already exists in PublicQuoteCard). No buttons. |
| status === "rejected" | Declined banner (already exists in PublicQuoteCard). No buttons. |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Accept CTA | "Accept Quote" |
| Decline CTA | "Decline Quote" |
| Accept loading label | "Accepting..." |
| Decline submit CTA | "Decline Quote" |
| Decline loading label | "Declining..." |
| Decline back CTA | "Go Back" |
| Decline reason label | "Reason (optional)" |
| Decline reason placeholder | "Let them know why, if you'd like" |
| Decline character limit | 500 characters, counter shows "{count}/500" only when > 0 characters entered |
| Accepted banner | "You accepted this quote on {date}" |
| Declined banner | "You declined this quote on {date}" |
| Expired heading | "This quote has expired" |
| Expired body | "Please contact {businessName} directly for an updated quote." |
| Error toast | "Something went wrong. Please try again." |
| Already responded (revisit) | Same accepted/declined banner as above -- no additional copy needed |

### Notification Email Copy (Backend)

| Element | Copy |
|---------|------|
| Accept email subject | "{customerName} accepted your quote {quoteNumber}" |
| Accept email body | "{customerName} has accepted your quote {quoteNumber} for {jobTitle}." |
| Decline email subject | "{customerName} declined your quote {quoteNumber}" |
| Decline email body (no reason) | "{customerName} has declined your quote {quoteNumber} for {jobTitle}." |
| Decline email body (with reason) | "{customerName} has declined your quote {quoteNumber} for {jobTitle}.\n\nReason: {declineReason}" |
| Email CTA button | "View Quote" |

---

## Layout Contract

### Button Placement (D-04)

Buttons appear inside `CardFooter`, below the totals section and PDF download button. Reading flow: business header -> quote details -> line items -> totals -> PDF download -> response buttons.

```
+-----------------------------------+
| [Status banner if responded]      |
|-----------------------------------|
| Business Name                     |
| Quote Number / Date / Valid Until |
| Customer Name                     |
|-----------------------------------|
| Line Items                        |
|-----------------------------------|
| Totals                            |
|-----------------------------------|
| [Download PDF]  (existing)        |
|                                   |
| [Accept Quote]  [Decline Quote]   |
+-----------------------------------+
```

### Button Layout

- Full width on mobile: both buttons stack vertically, `flex flex-col gap-3`
- Side-by-side on sm+: `flex flex-col gap-3 sm:flex-row sm:justify-end`
- Accept button appears first (left on desktop, top on mobile)
- Both buttons use default size (`h-9`)
- On mobile, both buttons use `w-full`; on sm+, auto width with `sm:w-auto`

### Decline Reason Form Layout

- Replaces the two-button layout inline (no modal/dialog)
- Textarea: full width, 3 rows default, max 500 characters
- Character counter: right-aligned below textarea, `text-xs text-muted-foreground`
- Decline Quote + Go Back buttons below: `flex justify-end gap-2`
- Decline Quote button: `variant="outline"`, Go Back button: `variant="ghost"`

---

## Mobile Considerations

- Public quote page is already mobile-first (Phase 17 success criteria: "displays cleanly on mobile")
- Accept/Decline buttons must meet 44px minimum touch target (`min-h-11`)
- Textarea uses `text-base md:text-sm` (prevents iOS zoom on focus)
- Toast notifications at top-center work well on mobile (existing pattern)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Button, Textarea, Card, Separator (all pre-installed) | not required |

No third-party registries declared. No new shadcn components to install.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
