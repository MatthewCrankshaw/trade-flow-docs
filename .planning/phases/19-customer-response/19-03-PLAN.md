---
phase: 19-customer-response
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - trade-flow-api/src/quote-token/test/services/quote-response-handler.service.spec.ts
  - trade-flow-api/src/email/test/services/notification-email-renderer.service.spec.ts
  - trade-flow-api/src/quote/test/services/quote-transition.service.spec.ts
autonomous: true
requirements:
  - RESP-02
  - RESP-03
  - RESP-04
  - NOTF-01
  - NOTF-02

must_haves:
  truths:
    - "QuoteResponseHandler.accept transitions quote to ACCEPTED and calls notification email"
    - "QuoteResponseHandler.decline transitions quote to REJECTED, stores reason, and calls notification email"
    - "QuoteResponseHandler returns success when quote is already in target status (idempotency)"
    - "QuoteResponseHandler throws when quote is expired (validUntil passed)"
    - "Notification email failure does not cause accept/decline to fail"
    - "NotificationEmailRenderer renders template with businessName, message, and viewUrl"
    - "QuoteTransitionService.publicTransition validates transitions and sets timestamps"
  artifacts:
    - path: "trade-flow-api/src/quote-token/test/services/quote-response-handler.service.spec.ts"
      provides: "Unit tests for QuoteResponseHandler (accept, decline, idempotency, expiry, email failure)"
      min_lines: 80
    - path: "trade-flow-api/src/email/test/services/notification-email-renderer.service.spec.ts"
      provides: "Unit tests for NotificationEmailRenderer (template rendering)"
      min_lines: 30
    - path: "trade-flow-api/src/quote/test/services/quote-transition.service.spec.ts"
      provides: "Updated tests for publicTransition method on QuoteTransitionService"
      min_lines: 40
  key_links:
    - from: "trade-flow-api/src/quote-token/test/services/quote-response-handler.service.spec.ts"
      to: "trade-flow-api/src/quote-token/services/quote-response-handler.service.ts"
      via: "imports and instantiates with mocked dependencies"
      pattern: "QuoteResponseHandler"
---

<objective>
Write unit tests for the backend services created in Plan 01: QuoteResponseHandler (orchestration), NotificationEmailRenderer (template), and publicTransition on QuoteTransitionService.

Purpose: Validates RESP-02, RESP-03, RESP-04, NOTF-01, NOTF-02 requirements with automated test coverage. Catches regressions in the response flow.
Output: Three test spec files covering the critical backend behaviors.
</objective>

<execution_context>
@/Users/mcrankshaw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mcrankshaw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-customer-response/19-CONTEXT.md
@.planning/phases/19-customer-response/19-01-SUMMARY.md

<interfaces>
<!-- Services created in Plan 01 that these tests target -->

QuoteResponseHandler (from Plan 01):
```typescript
@Injectable()
export class QuoteResponseHandler {
  constructor(
    quoteTransitionService, quoteRepository, publicQuoteRetriever,
    notificationEmailRenderer, emailSenderService, businessUserRepository,
    userRepository, businessRepository, customerRepository, jobRepository, configService
  ) {}
  public async accept(tokenDto: IQuoteTokenDto): Promise<IPublicQuoteResponse> { ... }
  public async decline(tokenDto: IQuoteTokenDto, reason?: string): Promise<IPublicQuoteResponse> { ... }
  private async validateQuoteNotExpired(quoteId: string): Promise<void> { ... }
  private async sendNotificationEmail(quoteId: string, action: "accepted" | "declined", declineReason?: string): Promise<void> { ... }
}
```

QuoteTransitionService.publicTransition (from Plan 01):
```typescript
public async publicTransition(quoteId: string, targetStatus: QuoteStatus, declineReason?: string): Promise<IQuoteDto> { ... }
```

NotificationEmailRenderer (from Plan 01):
```typescript
export interface NotificationEmailData { businessName: string; message: string; viewUrl: string; }
export class NotificationEmailRenderer {
  public async render(data: NotificationEmailData): Promise<string> { ... }
}
```

Existing test patterns (from Phase 17):
```typescript
// Guard spec uses helper for mock ExecutionContext
// Service spec uses Money.fromMajorUnits/DtoCollection.create for realistic mocks
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QuoteResponseHandler unit tests</name>
  <files>
    trade-flow-api/src/quote-token/test/services/quote-response-handler.service.spec.ts
  </files>
  <read_first>
    trade-flow-api/src/quote-token/services/quote-response-handler.service.ts,
    trade-flow-api/src/quote-token/test/services/public-quote-retriever.service.spec.ts,
    trade-flow-api/src/quote/test/services/quote-transition.service.spec.ts,
    trade-flow-api/CLAUDE.md
  </read_first>
  <behavior>
    - Test 1: accept() calls publicTransition with ACCEPTED and returns fresh public quote response
    - Test 2: decline() calls publicTransition with REJECTED and passes reason
    - Test 3: decline() without reason calls publicTransition with undefined reason
    - Test 4: accept() on expired quote (validUntil in the past) throws InvalidRequestError
    - Test 5: decline() on expired quote throws InvalidRequestError
    - Test 6: accept() sends notification email with correct subject containing customer name and quote number
    - Test 7: decline() sends notification email with decline reason in body when reason provided
    - Test 8: decline() sends notification email without reason line when no reason provided
    - Test 9: accept() succeeds even when email sending throws (email failure isolated)
    - Test 10: accept() returns success when quote is already ACCEPTED (idempotency via publicTransition)
  </behavior>
  <action>
    Create `trade-flow-api/src/quote-token/test/services/quote-response-handler.service.spec.ts`:

    - Use NestJS `Test.createTestingModule` pattern with all dependencies mocked via `jest.fn()`
    - Mock dependencies: `QuoteTransitionService`, `QuoteRepository`, `PublicQuoteRetriever`, `NotificationEmailRenderer`, `EmailSenderService`, `BusinessUserRepository`, `UserRepository`, `BusinessRepository`, `CustomerRepository`, `JobRepository`, `ConfigService`

    - Create helper factories for mock data:
      - `createMockTokenDto(overrides?)`: returns `IQuoteTokenDto` with defaults `{ id: "token-1", token: "abc123", quoteId: "quote-1", expiresAt: DateTime.now().plus({ days: 7 }), sentAt: DateTime.now(), recipientEmail: "customer@example.com" }`
      - `createMockQuoteDto(overrides?)`: returns `IQuoteDto` with defaults `{ id: "quote-1", businessId: "biz-1", customerId: "cust-1", jobId: "job-1", number: "Q-001", title: "Test Quote", quoteDate: DateTime.now(), status: QuoteStatus.SENT, validUntil: DateTime.now().plus({ days: 30 }), lineItems: DtoCollection.empty(), totals: { subTotal: Money.zero("GBP"), taxTotal: Money.zero("GBP"), total: Money.zero("GBP") } }`
      - `createMockPublicQuoteResponse(overrides?)`: returns `IPublicQuoteResponse` with defaults

    - Default mock setups (in `beforeEach`):
      - `quoteRepository.findByIdOrFail` returns mock quote DTO
      - `quoteTransitionService.publicTransition` returns mock quote DTO
      - `publicQuoteRetriever.getPublicQuote` returns mock public response
      - `businessUserRepository.findByBusinessId` returns `{ id: "bu-1", businessId: "biz-1", userId: "user-1" }`
      - `userRepository.findByIdOrFail` returns `{ id: "user-1", email: "tradesperson@example.com", name: "Mike" }`
      - `businessRepository.findByIdOrFail` returns `{ id: "biz-1", name: "Mike's Plumbing" }`
      - `customerRepository.findByIdOrFail` returns `{ id: "cust-1", name: "John Smith" }`
      - `jobRepository.findByIdOrFail` returns `{ id: "job-1", title: "Bathroom Renovation" }`
      - `notificationEmailRenderer.render` returns `"<html>notification</html>"`
      - `emailSenderService.sendEmail` resolves void
      - `configService.get` returns appropriate values for "CORS_ORIGIN" and "RESEND_FROM_EMAIL"

    - `afterEach`: `jest.clearAllMocks()`

    - **Test structure** (nested describes):
      - `describe("QuoteResponseHandler")`:
        - `describe("accept")`:
          - `it("should transition quote to ACCEPTED and return public response")` -- verify publicTransition called with (quoteId, QuoteStatus.ACCEPTED), verify getPublicQuote called
          - `it("should send notification email to tradesperson")` -- verify emailSenderService.sendEmail called with to: "tradesperson@example.com", subject containing "John Smith accepted your quote Q-001"
          - `it("should throw when quote is expired")` -- set mock quote validUntil to past date, expect InvalidRequestError
          - `it("should succeed even when notification email fails")` -- make emailSenderService.sendEmail throw, verify no error propagated, verify publicTransition still called
          - `it("should handle already-accepted quote via idempotent transition")` -- make publicTransition return quote with ACCEPTED status, verify no error
        - `describe("decline")`:
          - `it("should transition quote to REJECTED with reason and return public response")` -- verify publicTransition called with (quoteId, QuoteStatus.REJECTED, "Too expensive")
          - `it("should transition quote to REJECTED without reason when none provided")` -- verify publicTransition called with (quoteId, QuoteStatus.REJECTED, undefined)
          - `it("should send notification email with decline reason in body")` -- verify render called with message containing "Reason: Too expensive"
          - `it("should send notification email without reason line when no reason")` -- verify render called with message NOT containing "Reason:"
          - `it("should throw when quote is expired")` -- same as accept test
  </action>
  <verify>
    <automated>cd /Users/mcrankshaw/PersonalProjects/trade-flow-api && npm run test -- --testPathPattern="quote-response-handler" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-api/src/quote-token/test/services/quote-response-handler.service.spec.ts exists with at least 10 test cases
    - File contains `describe("QuoteResponseHandler")`
    - File contains test for `should transition quote to ACCEPTED`
    - File contains test for `should transition quote to REJECTED with reason`
    - File contains test for `should throw when quote is expired`
    - File contains test for `should succeed even when notification email fails`
    - File contains test for `should send notification email`
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>QuoteResponseHandler has 10 unit tests covering: accept flow, decline flow with/without reason, expiry validation, notification email sending with correct content, email failure isolation, and idempotency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NotificationEmailRenderer and publicTransition unit tests</name>
  <files>
    trade-flow-api/src/email/test/services/notification-email-renderer.service.spec.ts,
    trade-flow-api/src/quote/test/services/quote-transition.service.spec.ts
  </files>
  <read_first>
    trade-flow-api/src/email/services/notification-email-renderer.service.ts,
    trade-flow-api/src/email/templates/notification-email.html,
    trade-flow-api/src/quote/services/quote-transition.service.ts,
    trade-flow-api/src/quote/test/services/quote-transition.service.spec.ts,
    trade-flow-api/CLAUDE.md
  </read_first>
  <behavior>
    NotificationEmailRenderer:
    - Test 1: render() replaces {{ businessName }} in template output
    - Test 2: render() replaces {{{ message }}} with raw HTML (not escaped)
    - Test 3: render() replaces {{ viewUrl }} in template output
    - Test 4: render() escapes businessName HTML characters (prevents XSS)

    QuoteTransitionService.publicTransition:
    - Test 5: publicTransition transitions SENT -> ACCEPTED and sets acceptedAt
    - Test 6: publicTransition transitions SENT -> REJECTED and sets rejectedAt + declineReason
    - Test 7: publicTransition returns existing quote when already in target status (idempotency)
    - Test 8: publicTransition throws InvalidRequestError for invalid transition (e.g. DRAFT -> ACCEPTED)
    - Test 9: publicTransition does NOT call quotePolicy or accessControllerFactory (no auth check)
  </behavior>
  <action>
    1. **Create NotificationEmailRenderer spec** (new file `trade-flow-api/src/email/test/services/notification-email-renderer.service.spec.ts`):
       - Create `email/test/services/` directory if needed
       - Instantiate `NotificationEmailRenderer` directly (no NestJS module needed -- it has no injected deps)
       - Tests:
         - `it("should replace businessName in rendered output")` -- call render with `{ businessName: "Mike's Plumbing", message: "Hello", viewUrl: "https://example.com" }`, expect output to contain "Mike&#39;s Plumbing" (escaped) or "Mike's Plumbing" depending on position (escaped in `{{ }}` slots)
         - `it("should include raw HTML message in output")` -- call render with message `"<strong>Accepted</strong>"`, expect output to contain `<strong>Accepted</strong>` (NOT escaped)
         - `it("should include viewUrl in output")` -- expect output to contain `https://example.com/quotes/123`
         - `it("should escape HTML in businessName")` -- call render with businessName `"<script>alert(1)</script>"`, expect output NOT to contain `<script>` (should contain `&lt;script&gt;`)

    2. **Update QuoteTransitionService spec** (existing file, or create if not exists):
       - Read existing spec file first. If it exists, ADD new describe block for `publicTransition`.
       - If it does not exist, create `trade-flow-api/src/quote/test/services/quote-transition.service.spec.ts` with full setup.

       - Mock setup for publicTransition tests:
         - `quoteRepository.findByIdOrFail` returns mock quote DTO with `status: QuoteStatus.SENT`
         - `quoteRepository.update` returns the updated DTO

       - Tests in `describe("publicTransition")`:
         - `it("should transition SENT to ACCEPTED and set acceptedAt")` -- call publicTransition("quote-1", QuoteStatus.ACCEPTED), verify update called with status ACCEPTED and acceptedAt set to a DateTime
         - `it("should transition SENT to REJECTED and set rejectedAt and declineReason")` -- call publicTransition("quote-1", QuoteStatus.REJECTED, "Too expensive"), verify update called with rejectedAt set and declineReason "Too expensive"
         - `it("should return existing quote when already in target status")` -- set mock quote status to ACCEPTED, call publicTransition with ACCEPTED, verify update NOT called, verify returns existing quote
         - `it("should throw for invalid transition")` -- set mock quote status to DRAFT, call publicTransition with ACCEPTED, expect InvalidRequestError
         - `it("should not check authorization policies")` -- verify quotePolicy and accessControllerFactory are NOT called (assert `.not.toHaveBeenCalled()`)
  </action>
  <verify>
    <automated>cd /Users/mcrankshaw/PersonalProjects/trade-flow-api && npm run test -- --testPathPattern="(notification-email-renderer|quote-transition)" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-api/src/email/test/services/notification-email-renderer.service.spec.ts exists with at least 4 test cases
    - File contains `describe("NotificationEmailRenderer")`
    - File contains test for `should escape HTML in businessName`
    - trade-flow-api/src/quote/test/services/quote-transition.service.spec.ts contains `describe("publicTransition")`
    - File contains test for `should transition SENT to ACCEPTED`
    - File contains test for `should return existing quote when already in target status`
    - File contains test for `should throw for invalid transition`
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>NotificationEmailRenderer has 4 tests covering template variable replacement and XSS escaping. QuoteTransitionService.publicTransition has 5 tests covering valid transitions, idempotency, invalid transitions, and absence of auth checks.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/mcrankshaw/PersonalProjects/trade-flow-api && npm run test -- --testPathPattern="(quote-response-handler|notification-email-renderer|quote-transition)" --no-coverage
```
</verification>

<success_criteria>
- All 19 unit tests pass (10 response handler + 4 renderer + 5 transition)
- QuoteResponseHandler tests cover: accept, decline with/without reason, expiry validation, email failure isolation, notification content
- NotificationEmailRenderer tests cover: variable replacement, raw HTML passthrough, XSS escaping
- publicTransition tests cover: valid transitions, timestamps, idempotency, invalid transitions, no auth check
</success_criteria>

<output>
After completion, create `.planning/phases/19-customer-response/19-03-SUMMARY.md`
</output>
