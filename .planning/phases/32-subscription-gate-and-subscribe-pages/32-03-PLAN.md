---
phase: 32-subscription-gate-and-subscribe-pages
plan: 03
type: execute
wave: 3
depends_on: ["32-02"]
files_modified:
  - trade-flow-ui/src/pages/CustomersPage.tsx
  - trade-flow-ui/src/pages/JobsPage.tsx
  - trade-flow-ui/src/pages/JobDetailPage.tsx
  - trade-flow-ui/src/pages/QuotesPage.tsx
  - trade-flow-ui/src/pages/QuoteDetailPage.tsx
  - trade-flow-ui/src/pages/ItemsPage.tsx
  - trade-flow-ui/src/pages/BusinessPage.tsx
  - trade-flow-ui/src/pages/DashboardPage.tsx
autonomous: true
requirements: [GATE-01, GATE-04]

must_haves:
  truths:
    - "Clicking Add Customer without subscription opens PaywallModal with 'Add a Customer' title"
    - "Clicking Create Job without subscription opens PaywallModal with 'Create Jobs' title"
    - "Clicking Create Quote without subscription opens PaywallModal with 'Create Quotes' title"
    - "Clicking Add Item without subscription opens PaywallModal with 'Add an Item' title"
    - "Clicking Edit Business without subscription opens PaywallModal with 'Edit Business Details' title"
    - "Support users can perform all write actions without paywall"
    - "Users with trialing or active subscription can perform all write actions without paywall"
    - "DashboardBanner appears on dashboard for inactive users"
  artifacts:
    - path: "trade-flow-ui/src/pages/CustomersPage.tsx"
      provides: "Paywall-gated customer write actions"
      contains: "openPaywall"
    - path: "trade-flow-ui/src/pages/JobsPage.tsx"
      provides: "Paywall-gated job write actions"
      contains: "openPaywall"
    - path: "trade-flow-ui/src/pages/QuotesPage.tsx"
      provides: "Paywall-gated quote write actions"
      contains: "openPaywall"
    - path: "trade-flow-ui/src/pages/ItemsPage.tsx"
      provides: "Paywall-gated item write actions"
      contains: "openPaywall"
    - path: "trade-flow-ui/src/pages/BusinessPage.tsx"
      provides: "Paywall-gated business write actions"
      contains: "openPaywall"
    - path: "trade-flow-ui/src/pages/DashboardPage.tsx"
      provides: "Dashboard with DashboardBanner for inactive users"
      contains: "DashboardBanner"
  key_links:
    - from: "trade-flow-ui/src/pages/CustomersPage.tsx"
      to: "trade-flow-ui/src/features/subscription/hooks/useSubscription.ts"
      via: "useSubscription() for isActive + openPaywall"
      pattern: "useSubscription"
    - from: "trade-flow-ui/src/pages/DashboardPage.tsx"
      to: "trade-flow-ui/src/features/subscription/components/DashboardBanner.tsx"
      via: "DashboardBanner rendered on dashboard"
      pattern: "DashboardBanner"
---

<objective>
Wire paywall triggers into all existing page write-action handlers and add the DashboardBanner to the dashboard page.

Purpose: Complete the soft-gate enforcement (GATE-01) by intercepting every write action across all business pages. When a user without an active subscription clicks create/edit/delete, the PaywallModal opens with a feature-aware title instead of the action proceeding. This is the primary conversion mechanism (D-15).
Output: All business pages check subscription status before write actions. Dashboard shows read-only mode banner for inactive users.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-subscription-gate-and-subscribe-pages/32-CONTEXT.md
@.planning/phases/32-subscription-gate-and-subscribe-pages/32-UI-SPEC.md
@.planning/phases/32-subscription-gate-and-subscribe-pages/32-01-SUMMARY.md
@.planning/phases/32-subscription-gate-and-subscribe-pages/32-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

useSubscription hook:
```typescript
const { isActive, isLoading, openPaywall } = useSubscription();
// isActive: true for trialing/active/support users
// openPaywall: (featureLabel?: string) => void
```

Import path:
```typescript
import { useSubscription } from "@/features/subscription";
import { DashboardBanner } from "@/features/subscription";
```

<!-- Paywall trigger pattern (apply to ALL write actions) -->

```typescript
// Pattern to apply in every page's write-action handlers:
const { isActive, openPaywall } = useSubscription();

const handleCreateSomething = () => {
  if (!isActive) {
    openPaywall("Feature Label Here");
    return;
  }
  // ...existing logic (open form dialog, etc.)
};
```

<!-- Feature label mapping from UI-SPEC -->

| Page | Action | Feature Label |
|------|--------|---------------|
| CustomersPage | Add Customer | "Add a Customer" |
| CustomersPage | Edit Customer | "Edit Customer Details" |
| CustomersPage | Toggle Status | "Update Customer Status" |
| JobsPage | Create Job | "Create Jobs" |
| JobsPage | Edit Job | "Edit Job Details" |
| JobDetailPage | Edit Job Details | "Edit Job Details" |
| JobDetailPage | Add Schedule | "Add a Schedule" |
| QuotesPage | Create Quote | "Create Quotes" |
| QuoteDetailPage | Send Quote | "Send This Quote" |
| QuoteDetailPage | Transition Quote | "Update Quote Status" |
| QuoteDetailPage | Add Line Item | "Add Quote Items" |
| ItemsPage | Add Item | "Add an Item" |
| ItemsPage | Edit Item | "Edit Item Details" |
| BusinessPage | Edit Business | "Edit Business Details" |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire paywall triggers into all business page write-action handlers and add DashboardBanner</name>
  <files>
    trade-flow-ui/src/pages/CustomersPage.tsx,
    trade-flow-ui/src/pages/JobsPage.tsx,
    trade-flow-ui/src/pages/JobDetailPage.tsx,
    trade-flow-ui/src/pages/QuotesPage.tsx,
    trade-flow-ui/src/pages/QuoteDetailPage.tsx,
    trade-flow-ui/src/pages/ItemsPage.tsx,
    trade-flow-ui/src/pages/BusinessPage.tsx,
    trade-flow-ui/src/pages/DashboardPage.tsx
  </files>
  <read_first>
    trade-flow-ui/src/pages/CustomersPage.tsx,
    trade-flow-ui/src/pages/JobsPage.tsx,
    trade-flow-ui/src/pages/JobDetailPage.tsx,
    trade-flow-ui/src/pages/QuotesPage.tsx,
    trade-flow-ui/src/pages/QuoteDetailPage.tsx,
    trade-flow-ui/src/pages/ItemsPage.tsx,
    trade-flow-ui/src/pages/BusinessPage.tsx,
    trade-flow-ui/src/pages/DashboardPage.tsx,
    trade-flow-ui/src/features/subscription/hooks/useSubscription.ts
  </read_first>
  <action>
    For EACH page listed below, apply this modification pattern:

    a. Add import: `import { useSubscription } from "@/features/subscription";`
    b. Inside the component function, add: `const { isActive, openPaywall } = useSubscription();`
    c. Find each write-action handler (create, edit, delete, toggle, send, transition) and add the guard at the top of the handler body:
       ```typescript
       if (!isActive) {
         openPaywall("FEATURE_LABEL");
         return;
       }
       ```
    d. The FEATURE_LABEL for each handler is listed below. Use the exact string.

    **CustomersPage.tsx:**
    - Find the handler that opens the create customer dialog (likely sets form mode to "create" or opens a dialog). Guard with: `openPaywall("Add a Customer")`
    - Find the handler that opens the edit customer dialog. Guard with: `openPaywall("Edit Customer Details")`
    - Find the handler that toggles customer status. Guard with: `openPaywall("Update Customer Status")`

    **JobsPage.tsx:**
    - Find the handler for creating a job. Guard with: `openPaywall("Create Jobs")`
    - Find the handler for editing a job. Guard with: `openPaywall("Edit Job Details")`

    **JobDetailPage.tsx:**
    - Find the handler for editing job details. Guard with: `openPaywall("Edit Job Details")`
    - Find the handler for adding a schedule entry. Guard with: `openPaywall("Add a Schedule")`

    **QuotesPage.tsx:**
    - Find the handler for creating a quote. Guard with: `openPaywall("Create Quotes")`

    **QuoteDetailPage.tsx:**
    - Find the handler for sending a quote. Guard with: `openPaywall("Send This Quote")`
    - Find the handler for transitioning quote status. Guard with: `openPaywall("Update Quote Status")`
    - Find the handler for adding a line item. Guard with: `openPaywall("Add Quote Items")`

    **ItemsPage.tsx:**
    - Find the handler for adding an item. Guard with: `openPaywall("Add an Item")`
    - Find the handler for editing an item. Guard with: `openPaywall("Edit Item Details")`

    **BusinessPage.tsx:**
    - Find the handler for editing business details. Guard with: `openPaywall("Edit Business Details")`

    **DashboardPage.tsx:**
    - Import `DashboardBanner` from `@/features/subscription`
    - Render `<DashboardBanner />` near the top of the page content, below any page heading and above the main dashboard content

    IMPORTANT NOTES:
    - Read each file first to find the exact handler function names. They may be named differently than expected (e.g., `onAddCustomer`, `handleCreateCustomer`, `setIsCreateDialogOpen`, etc.)
    - The guard goes at the VERY TOP of each handler function body, before any existing logic
    - If a write action is triggered by a callback passed as a prop (e.g., `onEdit={(customer) => ...}`), add the guard inside that callback
    - Do NOT gate read-only actions (viewing, navigating to detail pages, filtering, sorting)
    - Do NOT gate settings page actions (settings is exempt per GATE-02)
    - Support users bypass automatically because `isActive` is `true` for them (D-03, D-09)
  </action>
  <verify>
    <automated>cd trade-flow-ui && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-ui/src/pages/CustomersPage.tsx contains `useSubscription`
    - trade-flow-ui/src/pages/CustomersPage.tsx contains `openPaywall("Add a Customer")`
    - trade-flow-ui/src/pages/CustomersPage.tsx contains `openPaywall("Edit Customer Details")`
    - trade-flow-ui/src/pages/JobsPage.tsx contains `useSubscription`
    - trade-flow-ui/src/pages/JobsPage.tsx contains `openPaywall("Create Jobs")`
    - trade-flow-ui/src/pages/JobDetailPage.tsx contains `useSubscription`
    - trade-flow-ui/src/pages/JobDetailPage.tsx contains `openPaywall("Edit Job Details")`
    - trade-flow-ui/src/pages/JobDetailPage.tsx contains `openPaywall("Add a Schedule")`
    - trade-flow-ui/src/pages/QuotesPage.tsx contains `useSubscription`
    - trade-flow-ui/src/pages/QuotesPage.tsx contains `openPaywall("Create Quotes")`
    - trade-flow-ui/src/pages/QuoteDetailPage.tsx contains `openPaywall("Send This Quote")`
    - trade-flow-ui/src/pages/QuoteDetailPage.tsx contains `openPaywall("Add Quote Items")`
    - trade-flow-ui/src/pages/ItemsPage.tsx contains `useSubscription`
    - trade-flow-ui/src/pages/ItemsPage.tsx contains `openPaywall("Add an Item")`
    - trade-flow-ui/src/pages/BusinessPage.tsx contains `openPaywall("Edit Business Details")`
    - trade-flow-ui/src/pages/DashboardPage.tsx contains `DashboardBanner`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All business page write-action handlers gated with subscription check. Inactive users see PaywallModal with feature-aware title on any write action. Active/trialing/support users pass through to existing logic. Dashboard shows DashboardBanner for inactive users. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `cd trade-flow-ui && npx tsc --noEmit` passes
- `cd trade-flow-ui && npm run lint` passes
- Every page with write actions has `useSubscription` import and `openPaywall` calls
- DashboardPage renders DashboardBanner
- Feature labels match the UI-SPEC table exactly
</verification>

<success_criteria>
- Every write action across all 7 business pages is gated by `isActive` check
- PaywallModal appears with correct feature-aware title for each action type
- Active/trialing users and support users are NOT blocked (isActive is true for them)
- DashboardBanner visible on dashboard for inactive users
- No read-only actions (viewing, navigating, filtering) are gated
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/32-subscription-gate-and-subscribe-pages/32-03-SUMMARY.md`
</output>
