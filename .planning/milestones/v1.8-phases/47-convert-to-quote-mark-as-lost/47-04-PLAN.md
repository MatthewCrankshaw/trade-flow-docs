---
phase: 47-convert-to-quote-mark-as-lost
plan: 04
type: execute
wave: 4
depends_on: ["47-03"]
files_modified:
  - trade-flow-ui/src/features/quotes/components/QuoteSourceEstimateLink.tsx
  - trade-flow-ui/src/pages/QuoteDetailPage.tsx
  - trade-flow-ui/src/features/quotes/components/index.ts
autonomous: false
requirements:
  - CONV-06
  - CONV-03
user_setup: []

must_haves:
  truths:
    - "Quote detail page shows 'Converted from E-YYYY-NNN' back-link when sourceEstimateId is present"
    - "Back-link navigates to /estimates/{sourceEstimateId}"
    - "Full convert flow works end-to-end: click Convert -> POST -> navigate to quote edit page"
    - "Full mark-as-lost flow works end-to-end: click Mark as Lost -> dialog -> POST -> locked state displayed"
  artifacts:
    - path: "trade-flow-ui/src/features/quotes/components/QuoteSourceEstimateLink.tsx"
      provides: "Back-link from converted quote to source estimate"
      min_lines: 15
  key_links:
    - from: "QuoteSourceEstimateLink.tsx"
      to: "/estimates/{sourceEstimateId}"
      via: "Link or navigate"
      pattern: "estimates.*sourceEstimateId"
---

<objective>
Quote back-link component and end-to-end verification of both flows.

Purpose: Complete CONV-06 (back-link on quote detail page) and verify both terminal flows work end-to-end across API and UI.
Output: QuoteSourceEstimateLink component, quote detail page integration, and verified flows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/47-convert-to-quote-mark-as-lost/47-CONTEXT.md
@.planning/phases/47-convert-to-quote-mark-as-lost/47-UI-SPEC.md
@.planning/phases/47-convert-to-quote-mark-as-lost/47-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: QuoteSourceEstimateLink and quote detail page integration</name>
  <files>
    trade-flow-ui/src/features/quotes/components/QuoteSourceEstimateLink.tsx,
    trade-flow-ui/src/pages/QuoteDetailPage.tsx,
    trade-flow-ui/src/features/quotes/components/index.ts
  </files>
  <read_first>
    trade-flow-ui/src/pages/QuoteDetailPage.tsx,
    trade-flow-ui/src/features/quotes/components/index.ts,
    trade-flow-ui/src/types/quote.ts,
    trade-flow-ui/src/features/estimates/components/EstimateConvertedLink.tsx
  </read_first>
  <action>
1. Create `trade-flow-ui/src/features/quotes/components/QuoteSourceEstimateLink.tsx`:

   Props: `{ sourceEstimateId: string; sourceEstimateNumber: string }`

   Per UI-SPEC QuoteSourceEstimateLink Contract:
   - Container: `flex items-center gap-2`
   - Icon: `ArrowLeft` from lucide-react, `h-3.5 w-3.5 text-muted-foreground`
   - Text: `text-sm text-muted-foreground` "Converted from "
   - Link: `text-sm text-primary hover:underline cursor-pointer` wrapping `sourceEstimateNumber` (e.g. "E-2026-001")
   - Click navigates to `/estimates/${sourceEstimateId}` via `useNavigate()` or `<Link>`

2. Update `trade-flow-ui/src/features/quotes/components/index.ts` barrel to export `QuoteSourceEstimateLink`.

3. Update `trade-flow-ui/src/pages/QuoteDetailPage.tsx`:
   - Import `QuoteSourceEstimateLink` from `@/features/quotes`
   - Conditionally render `QuoteSourceEstimateLink` when `quote.sourceEstimateId` is present and `quote.sourceEstimateNumber` is present
   - Position: below the quote title/number area, before the main content section
   - Props: `sourceEstimateId={quote.sourceEstimateId}`, `sourceEstimateNumber={quote.sourceEstimateNumber}`

4. Run `npm run ci` in trade-flow-ui.
  </action>
  <verify>
    <automated>cd /Users/mattc/Documents/projects/agent/trade-flow-ui && npm run ci 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-ui/src/features/quotes/components/QuoteSourceEstimateLink.tsx contains `ArrowLeft`
    - trade-flow-ui/src/features/quotes/components/QuoteSourceEstimateLink.tsx contains `Converted from`
    - trade-flow-ui/src/features/quotes/components/QuoteSourceEstimateLink.tsx contains `sourceEstimateId`
    - trade-flow-ui/src/features/quotes/components/QuoteSourceEstimateLink.tsx contains `sourceEstimateNumber`
    - trade-flow-ui/src/pages/QuoteDetailPage.tsx contains `QuoteSourceEstimateLink`
    - trade-flow-ui/src/pages/QuoteDetailPage.tsx contains `sourceEstimateId`
    - npm run ci exits with code 0
  </acceptance_criteria>
  <done>QuoteSourceEstimateLink renders "Converted from E-YYYY-NNN" with navigable link on quote detail page, CI passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: E2E verification of Convert to Quote and Mark as Lost flows</name>
  <files>n/a</files>
  <action>Human verifies both terminal flows end-to-end with both repos running locally. No code changes in this task.</action>
  <what-built>
    Complete Convert to Quote and Mark as Lost functionality:
    - Backend: Two POST endpoints (convert, mark-lost) with services, idempotency, status transitions, follow-up cancellation
    - Frontend: Convert to Quote button, Mark as Lost dialog with reason picker, locked state displays, quote back-link
  </what-built>
  <how-to-verify>
    **Prerequisites:** Both trade-flow-api and trade-flow-ui running locally.

    **Test 1: Convert to Quote flow**
    1. Navigate to Estimates list, find or create a SENT estimate (or send a draft)
    2. Open the estimate detail page
    3. Verify "Convert to Quote" button is visible (blue primary button with ArrowRightLeft icon)
    4. Click "Convert to Quote"
    5. Verify button shows "Converting..." with spinner
    6. Verify navigation to /quotes/{quoteId}/edit (the new quote's edit page)
    7. Verify the quote has the same line items as the estimate (without contingency)
    8. Save the quote and navigate to its detail page
    9. Verify "Converted from E-YYYY-NNN" back-link appears below the quote title
    10. Click the back-link -- verify it navigates to the source estimate
    11. Verify the source estimate shows "Converted to Quote" chip/link and no action buttons

    **Test 2: Mark as Lost flow**
    1. Navigate to Estimates list, find or create a SENT estimate
    2. Open the estimate detail page
    3. Verify "Mark as Lost" button is visible (red outline button with XCircle icon)
    4. Click "Mark as Lost"
    5. Verify dialog opens with title "Mark estimate as lost?"
    6. Select "Timing isn't right" reason chip (verify it highlights)
    7. Type a note in the textarea
    8. Click "Mark as Lost" button in dialog
    9. Verify success toast "Estimate marked as lost"
    10. Verify estimate detail page shows Lost status badge
    11. Verify amber reason card appears with "Reason: Timing isn't right", the note in quotes, and timestamp
    12. Verify no action buttons (no Edit, no Revise, no Send, no Convert, no Mark as Lost)

    **Test 3: Status-based button visibility**
    1. DRAFT estimate: only Send and Delete visible (no Convert, no Mark as Lost)
    2. SENT estimate: Re-send, Convert to Quote, Mark as Lost all visible
    3. VIEWED estimate: Re-send and Mark as Lost visible (no Convert)
    4. RESPONDED estimate: Re-send, Convert to Quote, Mark as Lost all visible
    5. CONVERTED estimate: only "Converted to Quote" link visible
    6. LOST estimate: no buttons, reason card in content area
  </how-to-verify>
  <verify>
    <automated>echo "Human verification -- no automated command"</automated>
  </verify>
  <done>All three test scenarios pass visual verification</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI navigation | Back-link uses estimate ID from API response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-12 | Information Disclosure | QuoteSourceEstimateLink | accept | Only shows to authenticated trader viewing their own quote; estimate ID not sensitive |
</threat_model>

<verification>
- `cd /Users/mattc/Documents/projects/agent/trade-flow-ui && npm run ci` -- full CI gate passes
- Human verification of both flows end-to-end
</verification>

<success_criteria>
- QuoteSourceEstimateLink renders on quote detail page for converted quotes
- Back-link navigates to source estimate
- Both terminal flows verified end-to-end by human
- All CI gates pass in both repos
</success_criteria>

<output>
After completion, create `.planning/phases/47-convert-to-quote-mark-as-lost/47-04-SUMMARY.md`
</output>
