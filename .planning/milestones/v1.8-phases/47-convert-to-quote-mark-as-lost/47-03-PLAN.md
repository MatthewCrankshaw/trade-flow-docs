---
phase: 47-convert-to-quote-mark-as-lost
plan: 03
type: execute
wave: 3
depends_on: ["47-02"]
files_modified:
  - trade-flow-ui/src/types/estimate.ts
  - trade-flow-ui/src/types/quote.ts
  - trade-flow-ui/src/features/estimates/api/estimateApi.ts
  - trade-flow-ui/src/features/estimates/components/EstimateActionStrip.tsx
  - trade-flow-ui/src/features/estimates/components/MarkAsLostDialog.tsx
  - trade-flow-ui/src/features/estimates/components/EstimateLostReasonCard.tsx
  - trade-flow-ui/src/features/estimates/components/EstimateConvertedLink.tsx
  - trade-flow-ui/src/features/estimates/components/index.ts
  - trade-flow-ui/src/pages/EstimateDetailPage.tsx
autonomous: true
requirements:
  - CONV-01
  - CONV-02
  - CONV-03
  - CONV-05
  - LOST-01
user_setup: []

must_haves:
  truths:
    - "Trader sees Convert to Quote button only on SENT or RESPONDED estimates"
    - "Trader sees Mark as Lost button only on SENT, VIEWED, or RESPONDED estimates"
    - "Convert to Quote click generates idempotency key, POSTs, and navigates to /quotes/{quoteId}/edit"
    - "Mark as Lost dialog shows 5 reason chips and optional freeform textarea, submits POST"
    - "Converted estimates show EstimateConvertedLink instead of action buttons"
    - "Lost estimates show EstimateLostReasonCard with reason/notes/timestamp and no action buttons"
  artifacts:
    - path: "trade-flow-ui/src/features/estimates/components/MarkAsLostDialog.tsx"
      provides: "Confirmation dialog with reason picker"
      min_lines: 50
    - path: "trade-flow-ui/src/features/estimates/components/EstimateLostReasonCard.tsx"
      provides: "Read-only card for lost reason display"
      min_lines: 20
    - path: "trade-flow-ui/src/features/estimates/components/EstimateConvertedLink.tsx"
      provides: "Chip/link to converted quote"
      min_lines: 15
  key_links:
    - from: "EstimateActionStrip.tsx"
      to: "estimateApi.ts"
      via: "useConvertEstimateMutation and useMarkEstimateLostMutation hooks"
      pattern: "useConvertEstimateMutation|useMarkEstimateLostMutation"
    - from: "EstimateActionStrip.tsx"
      to: "/quotes/{quoteId}/edit"
      via: "useNavigate after successful convert"
      pattern: "navigate.*quotes"
---

<objective>
Frontend UI for Convert to Quote and Mark as Lost flows on the estimate detail page.

Purpose: Add the Convert to Quote button, Mark as Lost button with dialog, locked state displays (EstimateConvertedLink, EstimateLostReasonCard), and RTK Query mutations to the estimate frontend.
Output: Fully interactive convert and mark-as-lost flows with proper locked state rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/47-convert-to-quote-mark-as-lost/47-CONTEXT.md
@.planning/phases/47-convert-to-quote-mark-as-lost/47-RESEARCH.md
@.planning/phases/47-convert-to-quote-mark-as-lost/47-UI-SPEC.md
@.planning/phases/44-email-send-flow/44-04-SUMMARY.md
@.planning/phases/47-convert-to-quote-mark-as-lost/47-02-SUMMARY.md

<interfaces>
<!-- From Plan 01/02 backend outputs -->

POST /v1/estimates/:id/convert
- Headers: Idempotency-Key (string, UUID v4)
- Response: { data: [{ quoteId: string }] }

POST /v1/estimates/:id/mark-lost
- Body: { reason?: string, notes?: string }
- Response: standard estimate response (IEstimateResponse with lostReason, lostNotes, lostAt)

Estimate type additions needed:
- convertedAt?: string
- convertedToQuoteId?: string
- lostAt?: string
- lostReason?: string
- lostNotes?: string

Quote type additions needed:
- sourceEstimateId?: string
- sourceEstimateNumber?: string

From EstimateActionStrip (Phase 44-04):
- Props: estimate, onDelete, businessName?, userName?, defaultSubject?, defaultMessage?
- Already has: Send Estimate / Re-send / Delete buttons with status-based visibility
- SendEstimateDialog rendered inside

Decline reason labels (Phase 45 D-CTA-04, same as lost reasons):
"Too expensive", "Going with someone else", "Decided not to do the work",
"Just getting an idea of costs", "Timing isn't right"

API reason values:
"too_expensive", "going_with_someone_else", "decided_not_to_do_work",
"just_getting_idea", "timing_not_right"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend types, RTK Query mutations, and static display components</name>
  <files>
    trade-flow-ui/src/types/estimate.ts,
    trade-flow-ui/src/types/quote.ts,
    trade-flow-ui/src/features/estimates/api/estimateApi.ts,
    trade-flow-ui/src/features/estimates/components/EstimateLostReasonCard.tsx,
    trade-flow-ui/src/features/estimates/components/EstimateConvertedLink.tsx,
    trade-flow-ui/src/features/estimates/components/MarkAsLostDialog.tsx,
    trade-flow-ui/src/features/estimates/components/index.ts
  </files>
  <read_first>
    trade-flow-ui/src/types/estimate.ts,
    trade-flow-ui/src/types/quote.ts,
    trade-flow-ui/src/features/estimates/api/estimateApi.ts,
    trade-flow-ui/src/features/estimates/components/index.ts,
    trade-flow-ui/src/features/estimates/components/EstimateActionStrip.tsx,
    trade-flow-ui/src/components/ui/dialog.tsx,
    trade-flow-ui/src/components/ui/badge.tsx,
    trade-flow-ui/src/components/ui/textarea.tsx,
    trade-flow-ui/src/components/ui/button.tsx
  </read_first>
  <action>
1. Update `trade-flow-ui/src/types/estimate.ts` -- add to the `Estimate` interface:
   ```typescript
   convertedAt?: string;
   convertedToQuoteId?: string;
   lostAt?: string;
   lostReason?: string;
   lostNotes?: string;
   ```

2. Update `trade-flow-ui/src/types/quote.ts` -- add to the `Quote` interface:
   ```typescript
   sourceEstimateId?: string;
   sourceEstimateNumber?: string;
   ```

3. Add two new mutations to `trade-flow-ui/src/features/estimates/api/estimateApi.ts` inside the existing `injectEndpoints` call:

   ```typescript
   convertEstimate: builder.mutation<{ quoteId: string }, { estimateId: string; idempotencyKey: string }>({
     query: ({ estimateId, idempotencyKey }) => ({
       url: `/v1/estimates/${estimateId}/convert`,
       method: "POST",
       headers: { "Idempotency-Key": idempotencyKey },
     }),
     transformResponse: (response: StandardResponse<{ quoteId: string }>) => response.data[0],
     invalidatesTags: (_result, _error, { estimateId }) => [
       { type: "Estimate", id: estimateId },
       { type: "Estimate", id: "LIST" },
       { type: "Quote", id: "LIST" },
     ],
   }),

   markEstimateLost: builder.mutation<Estimate, { estimateId: string; reason?: string; notes?: string }>({
     query: ({ estimateId, ...body }) => ({
       url: `/v1/estimates/${estimateId}/mark-lost`,
       method: "POST",
       body,
     }),
     transformResponse: unwrapSingle,
     invalidatesTags: (_result, _error, { estimateId }) => [
       { type: "Estimate", id: estimateId },
       { type: "Estimate", id: "LIST" },
     ],
   }),
   ```

   Export hooks: `useConvertEstimateMutation`, `useMarkEstimateLostMutation`.

4. Create `trade-flow-ui/src/features/estimates/components/EstimateLostReasonCard.tsx`:

   Props: `{ reason?: string; notes?: string; lostAt?: string }`

   A card styled per UI-SPEC EstimateLostReasonCard Contract:
   - Container: `border border-warning-border bg-warning rounded-lg p-4`
   - Layout: `flex items-start gap-3`
   - Left: `AlertTriangle` icon from lucide-react, `h-4 w-4 text-warning-icon shrink-0`
   - Right content: `flex flex-col gap-1`
     - Label: `text-sm font-semibold text-warning-foreground` with text "Lost"
     - Reason line (if reason): `text-sm text-warning-foreground` with text "Reason: {humanReadableLabel}" -- map API values to display labels:
       - `too_expensive` -> "Too expensive"
       - `going_with_someone_else` -> "Going with someone else"
       - `decided_not_to_do_work` -> "Decided not to do the work"
       - `just_getting_idea` -> "Just getting an idea of costs"
       - `timing_not_right` -> "Timing isn't right"
     - Notes (if notes): `text-sm text-warning-foreground italic` wrapped in quotes
     - Timestamp: `text-xs text-muted-foreground` with "Marked as lost on {formatted date}" using the project date formatting convention

   Conditional rendering per UI-SPEC: only show reason line if reason is present, only show notes if notes is present.

5. Create `trade-flow-ui/src/features/estimates/components/EstimateConvertedLink.tsx`:

   Props: `{ convertedToQuoteId: string; quoteNumber?: string }`

   Per UI-SPEC EstimateActionStrip Converted state:
   - Container: Badge with `variant="outline"` wrapping a flex layout
   - `ArrowRightLeft` icon `h-4 w-4` from lucide-react
   - Text: "Converted to Quote" followed by quoteNumber (if available) as `text-primary hover:underline cursor-pointer`
   - Clicking navigates to `/quotes/${convertedToQuoteId}` via `useNavigate()`

6. Create `trade-flow-ui/src/features/estimates/components/MarkAsLostDialog.tsx`:

   Props: `{ open: boolean; onOpenChange: (open: boolean) => void; estimateId: string }`

   Per UI-SPEC MarkAsLostDialog Layout Contract:
   - Dialog with `sm:max-w-md`
   - DialogHeader with DialogTitle "Mark estimate as lost?" and DialogDescription "This will lock the estimate and cancel any pending follow-ups."
   - Reason picker: `flex flex-wrap gap-2` with 5 reason chips as Badge/Button toggles:
     - Labels: "Too expensive", "Going with someone else", "Decided not to do the work", "Just getting an idea of costs", "Timing isn't right"
     - Values: "too_expensive", "going_with_someone_else", "decided_not_to_do_work", "just_getting_idea", "timing_not_right"
     - Single-select with deselect (tap selected chip to clear). Unselected: `variant="outline" text-sm min-h-9`. Selected: `bg-primary text-primary-foreground text-sm min-h-9`.
   - Label "Reason (optional)" in `text-sm font-semibold`
   - Textarea with label "Anything else? (optional)" in `text-sm font-semibold`, placeholder "Add a note if you'd like", maxLength 500, rows 3
   - Character counter: `{count}/500` in `text-xs text-muted-foreground` right-aligned, visible only when text is entered
   - DialogFooter: "Keep Estimate" (`variant="outline"`) and "Mark as Lost" (`variant="destructive"`)
   - Uses `useMarkEstimateLostMutation` hook
   - On submit: call mutation with `{ estimateId, reason: selectedReason || undefined, notes: notesText || undefined }`
   - Loading state: confirm button shows "Marking..." with `Loader2 animate-spin`, disabled
   - On success: `toast.success("Estimate marked as lost")`, call `onOpenChange(false)`
   - On error: `toast.error("Failed to mark estimate as lost. Please try again.")`, dialog stays open
   - Reset state when dialog opens (clear reason selection and notes text)

7. Update `trade-flow-ui/src/features/estimates/components/index.ts` barrel export to include:
   - `MarkAsLostDialog`
   - `EstimateLostReasonCard`
   - `EstimateConvertedLink`
  </action>
  <verify>
    <automated>cd /Users/mattc/Documents/projects/agent/trade-flow-ui && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-ui/src/types/estimate.ts contains `convertedToQuoteId?: string`
    - trade-flow-ui/src/types/estimate.ts contains `lostReason?: string`
    - trade-flow-ui/src/types/estimate.ts contains `lostNotes?: string`
    - trade-flow-ui/src/types/quote.ts contains `sourceEstimateId?: string`
    - trade-flow-ui/src/types/quote.ts contains `sourceEstimateNumber?: string`
    - trade-flow-ui/src/features/estimates/api/estimateApi.ts contains `convertEstimate`
    - trade-flow-ui/src/features/estimates/api/estimateApi.ts contains `markEstimateLost`
    - trade-flow-ui/src/features/estimates/api/estimateApi.ts contains `Idempotency-Key`
    - trade-flow-ui/src/features/estimates/components/MarkAsLostDialog.tsx contains `Mark estimate as lost?`
    - trade-flow-ui/src/features/estimates/components/MarkAsLostDialog.tsx contains `too_expensive`
    - trade-flow-ui/src/features/estimates/components/MarkAsLostDialog.tsx contains `useMarkEstimateLostMutation`
    - trade-flow-ui/src/features/estimates/components/MarkAsLostDialog.tsx contains `500`
    - trade-flow-ui/src/features/estimates/components/EstimateLostReasonCard.tsx contains `AlertTriangle`
    - trade-flow-ui/src/features/estimates/components/EstimateLostReasonCard.tsx contains `bg-warning`
    - trade-flow-ui/src/features/estimates/components/EstimateLostReasonCard.tsx contains `Marked as lost on`
    - trade-flow-ui/src/features/estimates/components/EstimateConvertedLink.tsx contains `ArrowRightLeft`
    - trade-flow-ui/src/features/estimates/components/EstimateConvertedLink.tsx contains `convertedToQuoteId`
    - TypeScript compiles with zero errors
  </acceptance_criteria>
  <done>Frontend types extended, RTK Query mutations added, MarkAsLostDialog created with reason picker and freeform textarea, EstimateLostReasonCard and EstimateConvertedLink display components created</done>
</task>

<task type="auto">
  <name>Task 2: EstimateActionStrip integration and EstimateDetailPage wiring</name>
  <files>
    trade-flow-ui/src/features/estimates/components/EstimateActionStrip.tsx,
    trade-flow-ui/src/pages/EstimateDetailPage.tsx
  </files>
  <read_first>
    trade-flow-ui/src/features/estimates/components/EstimateActionStrip.tsx,
    trade-flow-ui/src/pages/EstimateDetailPage.tsx,
    trade-flow-ui/src/features/estimates/components/MarkAsLostDialog.tsx,
    trade-flow-ui/src/features/estimates/components/EstimateLostReasonCard.tsx,
    trade-flow-ui/src/features/estimates/components/EstimateConvertedLink.tsx,
    trade-flow-ui/src/features/estimates/api/estimateApi.ts
  </read_first>
  <action>
1. Update `EstimateActionStrip.tsx` to add Convert to Quote and Mark as Lost buttons with locked state handling:

   **New state variables:**
   - `const [lostDialogOpen, setLostDialogOpen] = useState(false)`
   - `const [convertEstimate, { isLoading: isConverting }] = useConvertEstimateMutation()`
   - `const navigate = useNavigate()`

   **Convert to Quote button handler:**
   ```typescript
   const handleConvert = async () => {
     const idempotencyKey = crypto.randomUUID();
     try {
       const result = await convertEstimate({
         estimateId: estimate.id,
         idempotencyKey,
       }).unwrap();
       toast.success("Estimate converted to quote");
       navigate(`/quotes/${result.quoteId}/edit`);
     } catch {
       toast.error("Failed to convert estimate. Please try again.");
     }
   };
   ```

   **Render logic (replaces/extends existing button rendering):**

   - If `estimate.status === "converted"`: render ONLY `<EstimateConvertedLink convertedToQuoteId={estimate.convertedToQuoteId!} />`. No other buttons.
   
   - If `estimate.status === "lost"`: render nothing in the action strip (no buttons). The `EstimateLostReasonCard` renders in the detail page content area, not the action strip.
   
   - If `estimate.status` is any other terminal status (`"declined"`, `"expired"`, `"deleted"`): render no buttons.
   
   - If `estimate.status === "draft"`: render existing Send Estimate + Delete buttons (unchanged).
   
   - If `estimate.status === "sent"` or `estimate.status === "responded"`: render existing Re-send button AND new "Convert to Quote" button AND new "Mark as Lost" button.
     - Convert to Quote button: `<Button variant="default" size="sm" onClick={handleConvert} disabled={isConverting}>` with `ArrowRightLeft` icon `h-4 w-4`. Label: `isConverting ? "Converting..." : "Convert to Quote"`. Icon: `isConverting ? <Loader2 className="h-4 w-4 animate-spin" /> : <ArrowRightLeft className="h-4 w-4" />`.
     - Mark as Lost button: `<Button variant="outline" size="sm" className="text-destructive border-destructive hover:bg-destructive/10" onClick={() => setLostDialogOpen(true)}>` with `XCircle` icon `h-4 w-4`.
   
   - If `estimate.status === "viewed"`: render existing Re-send button AND "Mark as Lost" button (no Convert per D-CONV-01).
   
   **Render MarkAsLostDialog at bottom of component:**
   ```tsx
   <MarkAsLostDialog
     open={lostDialogOpen}
     onOpenChange={setLostDialogOpen}
     estimateId={estimate.id}
   />
   ```

   **Import new icons:** Add `ArrowRightLeft`, `XCircle`, `Loader2` to lucide-react imports (some may already be imported).

2. Update `EstimateDetailPage.tsx` (or `EstimateDetailContent`/`EstimateEditor` -- read the file to determine exact component structure):

   Render `EstimateLostReasonCard` when `estimate.status === "lost"`:
   - Position: above the line items card, in the main content area
   - Props: `reason={estimate.lostReason}`, `notes={estimate.lostNotes}`, `lostAt={estimate.lostAt}`

   Import `EstimateLostReasonCard` from the features barrel.

3. Run `npm run ci` in trade-flow-ui to verify full CI gate passes.
  </action>
  <verify>
    <automated>cd /Users/mattc/Documents/projects/agent/trade-flow-ui && npm run ci 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-ui/src/features/estimates/components/EstimateActionStrip.tsx contains `useConvertEstimateMutation`
    - trade-flow-ui/src/features/estimates/components/EstimateActionStrip.tsx contains `Convert to Quote`
    - trade-flow-ui/src/features/estimates/components/EstimateActionStrip.tsx contains `Mark as Lost`
    - trade-flow-ui/src/features/estimates/components/EstimateActionStrip.tsx contains `crypto.randomUUID()`
    - trade-flow-ui/src/features/estimates/components/EstimateActionStrip.tsx contains `ArrowRightLeft`
    - trade-flow-ui/src/features/estimates/components/EstimateActionStrip.tsx contains `XCircle`
    - trade-flow-ui/src/features/estimates/components/EstimateActionStrip.tsx contains `MarkAsLostDialog`
    - trade-flow-ui/src/features/estimates/components/EstimateActionStrip.tsx contains `EstimateConvertedLink`
    - trade-flow-ui/src/features/estimates/components/EstimateActionStrip.tsx contains `Estimate converted to quote`
    - EstimateDetailPage (or EstimateEditor) contains `EstimateLostReasonCard`
    - npm run ci exits with code 0
  </acceptance_criteria>
  <done>EstimateActionStrip renders Convert to Quote and Mark as Lost buttons with correct status-based visibility, locked states show EstimateConvertedLink or no buttons, EstimateLostReasonCard rendered on detail page for Lost estimates, UI CI gate passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> API | Authenticated RTK Query mutations with JWT in prepareHeaders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-09 | Spoofing | RTK mutations | mitigate | JWT auth via prepareHeaders (existing pattern) |
| T-47-10 | Information Disclosure | Lost reason display | accept | Only displayed to authenticated trader on their own estimate |
| T-47-11 | Tampering | Idempotency key | mitigate | crypto.randomUUID() generates cryptographically random UUID; no user input |
</threat_model>

<verification>
- `cd /Users/mattc/Documents/projects/agent/trade-flow-ui && npm run ci` -- full CI gate passes
- Visual check: Convert to Quote button visible on SENT/RESPONDED estimates only
- Visual check: Mark as Lost button visible on SENT/VIEWED/RESPONDED estimates only
- Visual check: Converted estimate shows EstimateConvertedLink, no action buttons
- Visual check: Lost estimate shows EstimateLostReasonCard above line items, no action buttons
</verification>

<success_criteria>
- Convert to Quote button on SENT/RESPONDED estimates triggers POST with idempotency key and navigates to quote edit page
- Mark as Lost button opens dialog with 5 reason chips and freeform textarea, submits POST
- Converted estimates display EstimateConvertedLink with link to converted quote
- Lost estimates display EstimateLostReasonCard with reason, notes, and timestamp
- All terminal statuses hide action buttons
- Full UI CI gate passes
</success_criteria>

<output>
After completion, create `.planning/phases/47-convert-to-quote-mark-as-lost/47-03-SUMMARY.md`
</output>
