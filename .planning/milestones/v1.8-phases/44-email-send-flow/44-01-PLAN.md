---
phase: 44-email-send-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-api/src/estimate-settings/estimate-settings.module.ts
  - trade-flow-api/src/estimate-settings/controllers/estimate-settings.controller.ts
  - trade-flow-api/src/estimate-settings/services/default-estimate-settings-creator.service.ts
  - trade-flow-api/src/estimate-settings/services/estimate-settings-retriever.service.ts
  - trade-flow-api/src/estimate-settings/services/estimate-settings-updater.service.ts
  - trade-flow-api/src/estimate-settings/repositories/estimate-settings.repository.ts
  - trade-flow-api/src/estimate-settings/entities/estimate-settings.entity.ts
  - trade-flow-api/src/estimate-settings/data-transfer-objects/estimate-settings.dto.ts
  - trade-flow-api/src/estimate-settings/requests/update-estimate-settings.request.ts
  - trade-flow-api/src/estimate-settings/responses/estimate-settings.response.ts
  - trade-flow-api/src/estimate-settings/policies/estimate-settings.policy.ts
  - trade-flow-api/src/estimate-settings/test/services/default-estimate-settings-creator.service.spec.ts
  - trade-flow-api/src/estimate-settings/test/services/estimate-settings-retriever.service.spec.ts
  - trade-flow-api/src/estimate-settings/test/services/estimate-settings-updater.service.spec.ts
  - trade-flow-api/src/estimate-settings/test/mocks/estimate-settings.mock-generator.ts
  - trade-flow-api/src/business/services/business-creator.service.ts
  - trade-flow-api/src/business/business.module.ts
  - trade-flow-api/src/business/test/services/business-creator.service.spec.ts
  - trade-flow-api/src/app.module.ts
  - trade-flow-api/tsconfig.json
autonomous: true
requirements:
  - SND-02
  - SND-07

must_haves:
  truths:
    - "GET /v1/business/:businessId/estimate-settings returns estimate settings for a business"
    - "PATCH /v1/business/:businessId/estimate-settings updates estimateEmailSubject and estimateEmailBody"
    - "PATCH rejects a subject that does not contain the word 'Estimate' (case-insensitive)"
    - "Creating a new business also creates a default estimate-settings row"
    - "estimate-settings module is fully independent of quote-settings module"
  artifacts:
    - path: "trade-flow-api/src/estimate-settings/estimate-settings.module.ts"
      provides: "NestJS module registration"
      contains: "@Module"
    - path: "trade-flow-api/src/estimate-settings/controllers/estimate-settings.controller.ts"
      provides: "GET + PATCH endpoints"
      exports: ["EstimateSettingsController"]
    - path: "trade-flow-api/src/estimate-settings/requests/update-estimate-settings.request.ts"
      provides: "Subject validation with @Matches(/estimate/i)"
      contains: "@Matches"
    - path: "trade-flow-api/src/estimate-settings/repositories/estimate-settings.repository.ts"
      provides: "MongoDB estimate_settings collection access"
      contains: "estimate_settings"
  key_links:
    - from: "trade-flow-api/src/business/services/business-creator.service.ts"
      to: "DefaultEstimateSettingsCreatorService"
      via: "DI injection and .create() call"
      pattern: "DefaultEstimateSettingsCreatorService"
    - from: "trade-flow-api/src/app.module.ts"
      to: "EstimateSettingsModule"
      via: "imports array"
      pattern: "EstimateSettingsModule"
---

<objective>
Create the standalone `estimate-settings` backend module mirroring `quote-settings` 1:1, wire it into BusinessCreator for eager default creation on business signup, and register it in AppModule.

Purpose: SND-02 requires an independent estimate-settings API surface; SND-07 requires subject-line validation containing "Estimate". This plan delivers the full backend module so Plan 03 (EstimateEmailSender) can read template defaults and Plan 04 (frontend) can call the settings API.

Output: Complete `src/estimate-settings/` module with controller, services, repository, entity, DTO, request, response, policy, and tests. BusinessCreator extended to create default estimate settings alongside quote settings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-email-send-flow/44-CONTEXT.md
@.planning/phases/44-email-send-flow/44-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create estimate-settings module (entity, DTO, repository, services, controller, policy, request, response, tests)</name>
  <files>
    trade-flow-api/src/estimate-settings/estimate-settings.module.ts,
    trade-flow-api/src/estimate-settings/controllers/estimate-settings.controller.ts,
    trade-flow-api/src/estimate-settings/services/default-estimate-settings-creator.service.ts,
    trade-flow-api/src/estimate-settings/services/estimate-settings-retriever.service.ts,
    trade-flow-api/src/estimate-settings/services/estimate-settings-updater.service.ts,
    trade-flow-api/src/estimate-settings/repositories/estimate-settings.repository.ts,
    trade-flow-api/src/estimate-settings/entities/estimate-settings.entity.ts,
    trade-flow-api/src/estimate-settings/data-transfer-objects/estimate-settings.dto.ts,
    trade-flow-api/src/estimate-settings/requests/update-estimate-settings.request.ts,
    trade-flow-api/src/estimate-settings/responses/estimate-settings.response.ts,
    trade-flow-api/src/estimate-settings/policies/estimate-settings.policy.ts,
    trade-flow-api/src/estimate-settings/test/services/default-estimate-settings-creator.service.spec.ts,
    trade-flow-api/src/estimate-settings/test/services/estimate-settings-retriever.service.spec.ts,
    trade-flow-api/src/estimate-settings/test/services/estimate-settings-updater.service.spec.ts,
    trade-flow-api/src/estimate-settings/test/mocks/estimate-settings.mock-generator.ts
  </files>
  <read_first>
    trade-flow-api/src/quote-settings/quote-settings.module.ts,
    trade-flow-api/src/quote-settings/controllers/quote-settings.controller.ts,
    trade-flow-api/src/quote-settings/services/default-quote-settings-creator.service.ts,
    trade-flow-api/src/quote-settings/services/quote-settings-retriever.service.ts,
    trade-flow-api/src/quote-settings/services/quote-settings-updater.service.ts,
    trade-flow-api/src/quote-settings/repositories/quote-settings.repository.ts,
    trade-flow-api/src/quote-settings/entities/quote-settings.entity.ts,
    trade-flow-api/src/quote-settings/data-transfer-objects/quote-settings.dto.ts,
    trade-flow-api/src/quote-settings/requests/update-quote-settings.request.ts,
    trade-flow-api/src/quote-settings/responses/quote-settings.response.ts,
    trade-flow-api/src/quote-settings/policies/quote-settings.policy.ts,
    trade-flow-api/src/core/errors/error-codes.enum.ts
  </read_first>
  <action>
Mirror every file in `src/quote-settings/` into a new `src/estimate-settings/` module with these renames:

**Entity** (`estimate-settings.entity.ts`): Interface `IEstimateSettingsEntity` with fields `_id: ObjectId`, `businessId: ObjectId`, `estimateEmailSubject?: string`, `estimateEmailBody?: string`, `createdAt: Date`, `updatedAt: Date`. No `legalCopy` field (per D-LGL-01, D-LGL-04).

**DTO** (`estimate-settings.dto.ts`): Interface `IEstimateSettingsDto` with `id: string`, `businessId: string`, `estimateEmailSubject?: string`, `estimateEmailBody?: string`, `createdAt: DateTime`, `updatedAt: DateTime`.

**Repository** (`estimate-settings.repository.ts`): Class `EstimateSettingsRepository` with `static readonly COLLECTION = "estimate_settings"`. Methods: `create(dto)`, `findByBusinessId(businessId)`, `update(businessId, dto)` (upsert pattern matching quote-settings). Index: `{ businessId: 1 }` unique. Mirror `toDto`/`toEntity` mapping methods from `QuoteSettingsRepository`.

**Services:**
- `DefaultEstimateSettingsCreatorService`: method `create(businessId: string)` writes a row with both template fields set to `undefined`. Mirrors `DefaultQuoteSettingsCreatorService`.
- `EstimateSettingsRetriever`: method `findByBusinessId(businessId: string)` returns `IEstimateSettingsDto`. Mirrors `QuoteSettingsRetriever`.
- `EstimateSettingsUpdater`: method `update(authUser, businessId, request)` calls policy check then `repository.update()`. Mirrors `QuoteSettingsUpdater`.

**Request** (`update-estimate-settings.request.ts`): Class `UpdateEstimateSettingsRequest` with:
- `@IsOptional() @IsString() estimateEmailSubject?: string` with `@Matches(/estimate/i, { message: "Estimate email subject must contain the word 'Estimate'" })` -- per D-LGL-05, SND-07. The `@Matches` decorator MUST be present alongside `@IsOptional()` so it only fires when the field is provided.
- `@IsOptional() @IsString() estimateEmailBody?: string`

**Response** (`estimate-settings.response.ts`): Interface `IEstimateSettingsResponse` with `id: string`, `businessId: string`, `estimateEmailSubject?: string`, `estimateEmailBody?: string`.

**Policy** (`estimate-settings.policy.ts`): Class `EstimateSettingsPolicy` with `canRead(authUser, businessId)` and `canWrite(authUser, businessId)` methods checking business ownership via the same pattern as `QuoteSettingsPolicy`.

**Controller** (`estimate-settings.controller.ts`):
- `@UseGuards(JwtAuthGuard) @Get("v1/business/:businessId/estimate-settings")` -- returns `IEstimateSettingsResponse` wrapped in `createResponse([...])`. If no row exists, return stub `{ id: "", businessId, estimateEmailSubject: undefined, estimateEmailBody: undefined }` (null-safe per D-SET-04).
- `@UseGuards(JwtAuthGuard) @Patch("v1/business/:businessId/estimate-settings")` -- accepts `UpdateEstimateSettingsRequest`, calls updater, returns updated response. Uses `createHttpError(error)` in catch block.

**Module** (`estimate-settings.module.ts`): `@Module` importing `CoreModule`, providing `EstimateSettingsController`, `DefaultEstimateSettingsCreatorService`, `EstimateSettingsRetriever`, `EstimateSettingsUpdater`, `EstimateSettingsRepository`, `EstimateSettingsPolicy`. Exports `DefaultEstimateSettingsCreatorService` and `EstimateSettingsRetriever` (needed by BusinessModule and EstimateModule respectively).

**Tests**: Create mock generator (`estimate-settings.mock-generator.ts`) mirroring the quote-settings mock pattern. Create specs for all three services mirroring existing quote-settings test files.

**Error codes**: Add `ESTIMATE_SUBJECT_MISSING_KEYWORD = "estimate_subject_missing_keyword"` to `ErrorCodes` enum in `src/core/errors/error-codes.enum.ts`.
  </action>
  <verify>
    <automated>cd trade-flow-api && npx jest --testPathPattern="estimate-settings" --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-api/src/estimate-settings/estimate-settings.module.ts contains `@Module` and `EstimateSettingsModule`
    - trade-flow-api/src/estimate-settings/repositories/estimate-settings.repository.ts contains `COLLECTION = "estimate_settings"`
    - trade-flow-api/src/estimate-settings/requests/update-estimate-settings.request.ts contains `@Matches(/estimate/i`
    - trade-flow-api/src/estimate-settings/controllers/estimate-settings.controller.ts contains `v1/business/:businessId/estimate-settings`
    - trade-flow-api/src/estimate-settings/entities/estimate-settings.entity.ts contains `estimateEmailSubject` and `estimateEmailBody`
    - trade-flow-api/src/estimate-settings/entities/estimate-settings.entity.ts does NOT contain `legalCopy`
    - trade-flow-api/src/core/errors/error-codes.enum.ts contains `ESTIMATE_SUBJECT_MISSING_KEYWORD`
    - All estimate-settings test files pass with zero failures
  </acceptance_criteria>
  <done>Complete estimate-settings module with GET/PATCH endpoints, subject validation via @Matches(/estimate/i), and all service tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: Wire BusinessCreator extension + AppModule registration + tsconfig paths</name>
  <files>
    trade-flow-api/src/business/services/business-creator.service.ts,
    trade-flow-api/src/business/business.module.ts,
    trade-flow-api/src/business/test/services/business-creator.service.spec.ts,
    trade-flow-api/src/app.module.ts,
    trade-flow-api/tsconfig.json
  </files>
  <read_first>
    trade-flow-api/src/business/services/business-creator.service.ts,
    trade-flow-api/src/business/business.module.ts,
    trade-flow-api/src/business/test/services/business-creator.service.spec.ts,
    trade-flow-api/src/app.module.ts,
    trade-flow-api/tsconfig.json
  </read_first>
  <action>
**tsconfig.json**: Add path alias `"@estimate-settings/*": ["src/estimate-settings/*"]` to the `paths` object. Verify existing aliases for pattern consistency (e.g. `@quote-settings/*` maps to `src/quote-settings/*`). Also add the corresponding entry to `tsconfig.build.json` and `jest` moduleNameMapper in `package.json` (or `jest.config.ts`) if that's where the project configures Jest path aliases.

**AppModule** (`src/app.module.ts`): Add `EstimateSettingsModule` to the `imports` array. Import statement: `import { EstimateSettingsModule } from "@estimate-settings/estimate-settings.module";`.

**BusinessModule** (`src/business/business.module.ts`): Add `EstimateSettingsModule` to the `imports` array so `DefaultEstimateSettingsCreatorService` is available for injection in `BusinessCreator`.

**BusinessCreator** (`src/business/services/business-creator.service.ts`):
1. Add constructor injection: `private readonly defaultEstimateSettingsCreator: DefaultEstimateSettingsCreatorService`
2. In the `create()` method, find the line calling `this.defaultQuoteSettingsCreator.create(createdBusiness.id)` (or equivalent)
3. Add a parallel call: `this.defaultEstimateSettingsCreator.create(createdBusiness.id)` alongside it. Both calls should be awaited (match the existing pattern -- if the quote call is awaited sequentially, add the estimate call sequentially after it; if wrapped in Promise.all, add to the same Promise.all).
4. Per D-SET-06: failures in either creator propagate to the business creation response.

**BusinessCreator spec** (`src/business/test/services/business-creator.service.spec.ts`):
1. Add a mock provider for `DefaultEstimateSettingsCreatorService` mirroring the existing `DefaultQuoteSettingsCreatorService` mock
2. Add a test assertion that `defaultEstimateSettingsCreator.create` is called with the created business id
3. Pattern: find the existing assertion for `defaultQuoteSettingsCreator.create` (approximately line 226) and add a mirror assertion for the estimate variant
  </action>
  <verify>
    <automated>cd trade-flow-api && npx jest --testPathPattern="business-creator" --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-api/tsconfig.json contains `"@estimate-settings/*"`
    - trade-flow-api/src/app.module.ts contains `EstimateSettingsModule`
    - trade-flow-api/src/business/business.module.ts contains `EstimateSettingsModule`
    - trade-flow-api/src/business/services/business-creator.service.ts contains `defaultEstimateSettingsCreator`
    - trade-flow-api/src/business/services/business-creator.service.ts contains `.create(` call for estimate settings
    - trade-flow-api/src/business/test/services/business-creator.service.spec.ts contains `DefaultEstimateSettingsCreatorService`
    - business-creator tests pass with zero failures
  </acceptance_criteria>
  <done>BusinessCreator creates default estimate settings on business signup. EstimateSettingsModule registered in AppModule and BusinessModule. tsconfig path alias configured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> estimate-settings API | Untrusted input for subject/body template fields |
| estimate-settings -> MongoDB | Template content persisted in DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Spoofing | estimate-settings controller | mitigate | `JwtAuthGuard` on both GET and PATCH endpoints; auth required |
| T-44-02 | Tampering | UpdateEstimateSettingsRequest | mitigate | `class-validator` decorators with `ValidationPipe` whitelist:true, forbidNonWhitelisted:true; `@Matches(/estimate/i)` on subject |
| T-44-03 | Elevation of Privilege | estimate-settings PATCH | mitigate | `EstimateSettingsPolicy.canWrite()` verifies business ownership before allowing update |
| T-44-04 | Information Disclosure | estimate-settings GET | mitigate | `EstimateSettingsPolicy.canRead()` gates reads to business owner only |
</threat_model>

<verification>
cd trade-flow-api && npx jest --testPathPattern="estimate-settings|business-creator" --no-coverage
cd trade-flow-api && npm run ci
</verification>

<success_criteria>
- estimate-settings module is fully functional with GET/PATCH endpoints
- Subject validation rejects subjects missing "Estimate" (case-insensitive)
- BusinessCreator creates both quote and estimate settings on business creation
- All tests pass, CI green
</success_criteria>

<output>
After completion, create `.planning/phases/44-email-send-flow/44-01-SUMMARY.md`
</output>
