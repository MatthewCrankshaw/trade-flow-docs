---
phase: 46-follow-up-queue-automation
plan: 07
type: execute
wave: 1
depends_on: [46-05]
files_modified:
  - trade-flow-api/src/estimate/services/estimate-response-handler.service.ts
  - trade-flow-api/src/estimate/test/services/estimate-response-handler.service.spec.ts
autonomous: true
gap_closure: true
requirements: [FUP-05]

must_haves:
  truths:
    - "Customer response (PROCEED, MESSAGE, DECLINE) cancels all pending follow-up and expiry jobs for that estimate/revision"
    - "EstimateResponseHandler injects ESTIMATE_FOLLOWUP_CANCELLER and calls cancelAllFollowups after successful response transition"
    - "Tests assert canceller is called with correct estimateId and revisionNumber on each response type"
  artifacts:
    - path: "trade-flow-api/src/estimate/services/estimate-response-handler.service.ts"
      provides: "Follow-up cancellation on customer response"
      contains: "ESTIMATE_FOLLOWUP_CANCELLER"
    - path: "trade-flow-api/src/estimate/test/services/estimate-response-handler.service.spec.ts"
      provides: "Tests asserting canceller called on response"
      contains: "cancelAllFollowups"
  key_links:
    - from: "trade-flow-api/src/estimate/services/estimate-response-handler.service.ts"
      to: "trade-flow-api/src/estimate/interfaces/estimate-followup-canceller.interface.ts"
      via: "@Inject(ESTIMATE_FOLLOWUP_CANCELLER)"
      pattern: "cancelAllFollowups"
---

<objective>
Wire follow-up cancellation into EstimateResponseHandler so customer responses (PROCEED, MESSAGE, DECLINE) cancel all pending follow-up and expiry BullMQ jobs, closing verification gap 2.

Purpose: Without this fix, when a customer responds to an estimate, the 3/10/21-day follow-up jobs remain in Redis and fire anyway. RESPONDED is in FOLLOWUP_WORTHY_STATUSES, so PROCEED and MESSAGE responses still trigger stale follow-ups. This blocks FUP-05 and SC#3.
Output: EstimateResponseHandler with canceller injection, updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-follow-up-queue-automation/46-CONTEXT.md
@.planning/phases/46-follow-up-queue-automation/46-VERIFICATION.md
@.planning/phases/46-follow-up-queue-automation/46-02-SUMMARY.md

<interfaces>
<!-- IEstimateFollowupCanceller interface (from Phase 42, rebound in Phase 46) -->

From trade-flow-api/src/estimate/interfaces/estimate-followup-canceller.interface.ts:
- Interface: IEstimateFollowupCanceller
  - cancelAllFollowups(estimateId: string, revisionNumber: number): Promise<void>
  - cancelAllFollowupsForChain(rootEstimateId: string): Promise<void>
- DI Token: ESTIMATE_FOLLOWUP_CANCELLER (string constant exported from same file)

From trade-flow-api/src/estimate/services/estimate-response-handler.service.ts (current state):
- Class: EstimateResponseHandler
- Method: handleResponse(estimateId: string, dto: IEstimateResponseDto): Promise<IEstimateDto>
- Constructor injects: EstimateRepository, EstimateTransitionService, AppLogger
- Does NOT inject ESTIMATE_FOLLOWUP_CANCELLER (this is the gap)
- After transitioning status, does NOT call cancelAllFollowups

From trade-flow-api/src/estimate/estimate.module.ts:
- EstimateResponseHandler is registered as a provider
- ESTIMATE_FOLLOWUP_CANCELLER is already provided (NoopEstimateFollowupCanceller by default, rebound to BullMQEstimateFollowupCanceller when EstimateFollowupsModule is imported)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject ESTIMATE_FOLLOWUP_CANCELLER into EstimateResponseHandler and call cancelAllFollowups on response</name>
  <files>
    trade-flow-api/src/estimate/services/estimate-response-handler.service.ts,
    trade-flow-api/src/estimate/test/services/estimate-response-handler.service.spec.ts
  </files>
  <read_first>
    trade-flow-api/src/estimate/services/estimate-response-handler.service.ts,
    trade-flow-api/src/estimate/test/services/estimate-response-handler.service.spec.ts,
    trade-flow-api/src/estimate/interfaces/estimate-followup-canceller.interface.ts,
    trade-flow-api/src/estimate/estimate.module.ts
  </read_first>
  <behavior>
    - Test: handleResponse with PROCEED response calls cancelAllFollowups(estimateId, revisionNumber)
    - Test: handleResponse with MESSAGE response calls cancelAllFollowups(estimateId, revisionNumber)
    - Test: handleResponse with DECLINE response calls cancelAllFollowups(estimateId, revisionNumber)
    - Test: cancelAllFollowups is called AFTER the status transition succeeds (not before)
    - Test: if cancelAllFollowups throws, the response still succeeds (error is logged, not propagated)
  </behavior>
  <action>
    In estimate-response-handler.service.ts:

    1. Add import for the DI token and interface:
       ```
       import {
         ESTIMATE_FOLLOWUP_CANCELLER,
         IEstimateFollowupCanceller,
       } from "@estimate/interfaces/estimate-followup-canceller.interface";
       ```

    2. Add `@Inject(ESTIMATE_FOLLOWUP_CANCELLER)` constructor parameter:
       ```
       @Inject(ESTIMATE_FOLLOWUP_CANCELLER)
       private readonly estimateFollowupCanceller: IEstimateFollowupCanceller,
       ```
       Also add `import { Inject } from "@nestjs/common";` if not already imported.

    3. In the handleResponse() method, AFTER the successful status transition (after the call to estimateTransitionService that transitions to RESPONDED or DECLINED), add follow-up cancellation wrapped in try/catch:
       ```
       try {
         await this.estimateFollowupCanceller.cancelAllFollowups(
           estimate.id,
           estimate.revisionNumber,
         );
       } catch (error) {
         this.logger.error(
           `Failed to cancel follow-ups for estimate ${estimate.id}: ${error}`,
           "EstimateResponseHandler",
         );
       }
       ```
       The try/catch ensures that a Redis/queue error does not break the customer's response flow. The response is the primary operation; cancellation is best-effort cleanup.

    4. The cancellation should use `estimate.id` (the current revision's id) and `estimate.revisionNumber` from the estimate loaded at the start of handleResponse. These values identify the specific revision whose follow-ups should be cancelled.

    In estimate-response-handler.service.spec.ts:

    5. Add a mock for ESTIMATE_FOLLOWUP_CANCELLER in the testing module providers:
       ```
       {
         provide: ESTIMATE_FOLLOWUP_CANCELLER,
         useValue: {
           cancelAllFollowups: jest.fn().mockResolvedValue(undefined),
           cancelAllFollowupsForChain: jest.fn().mockResolvedValue(undefined),
         },
       }
       ```

    6. Add test cases:
       - "calls cancelAllFollowups after PROCEED response" -- assert cancelAllFollowups called with (estimateId, revisionNumber)
       - "calls cancelAllFollowups after MESSAGE response" -- same assertion
       - "calls cancelAllFollowups after DECLINE response" -- same assertion
       - "does not propagate cancelAllFollowups error" -- mock cancelAllFollowups to reject, assert handleResponse still resolves successfully

    7. Ensure existing tests still pass by providing the mock in the module setup.

    Gap reason: Verification gap 2 -- EstimateResponseHandler does not cancel follow-ups on customer response. RESPONDED is in FOLLOWUP_WORTHY_STATUSES, so follow-up jobs fire even after PROCEED/MESSAGE responses.
  </action>
  <verify>
    <automated>cd /Users/mattc/Documents/projects/agent/trade-flow-api && npx jest --testPathPattern="estimate-response-handler" --no-coverage --verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - estimate-response-handler.service.ts contains `ESTIMATE_FOLLOWUP_CANCELLER` import (grep confirms)
    - estimate-response-handler.service.ts contains `@Inject(ESTIMATE_FOLLOWUP_CANCELLER)` in constructor
    - estimate-response-handler.service.ts contains `cancelAllFollowups(` call
    - estimate-response-handler.service.ts contains try/catch around cancelAllFollowups (error isolation)
    - estimate-response-handler.service.spec.ts contains mock for ESTIMATE_FOLLOWUP_CANCELLER
    - estimate-response-handler.service.spec.ts contains at least 3 new test cases (PROCEED, MESSAGE, DECLINE cancellation)
    - All response handler tests pass
    - `npm run ci` passes in trade-flow-api
  </acceptance_criteria>
  <done>Customer responses (PROCEED, MESSAGE, DECLINE) cancel all pending follow-up and expiry jobs via ESTIMATE_FOLLOWUP_CANCELLER, with error isolation so response flow is never broken by queue failures</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public customer page -> response endpoint | Unauthenticated customer submitting response via DocumentSessionAuthGuard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-07 | D (Denial of Service) | estimate-response-handler.service.ts | mitigate | cancelAllFollowups failure is caught and logged, never propagated to customer response flow. Customer always receives success response regardless of queue state. |
</threat_model>

<verification>
1. `grep "ESTIMATE_FOLLOWUP_CANCELLER" trade-flow-api/src/estimate/services/estimate-response-handler.service.ts` returns matches
2. `grep "cancelAllFollowups" trade-flow-api/src/estimate/services/estimate-response-handler.service.ts` returns matches
3. `grep "cancelAllFollowups" trade-flow-api/src/estimate/test/services/estimate-response-handler.service.spec.ts` returns matches
4. All response handler tests pass
5. `npm run ci` passes
</verification>

<success_criteria>
Customer response exit transition is wired: PROCEED, MESSAGE, and DECLINE responses all cancel pending follow-up and expiry jobs, closing verification gap 2 and satisfying the customer-response leg of FUP-05.
</success_criteria>

<output>
After completion, create `.planning/phases/46-follow-up-queue-automation/46-07-SUMMARY.md`
</output>
