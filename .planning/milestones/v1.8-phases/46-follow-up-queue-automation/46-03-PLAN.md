---
phase: 46-follow-up-queue-automation
plan: 03
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - trade-flow-api/src/email/templates/estimate-followup.html
  - trade-flow-api/src/email/services/estimate-followup-email-renderer.service.ts
  - trade-flow-api/src/estimate-followups/test/services/estimate-followup-email-renderer.service.spec.ts
autonomous: true
requirements:
  - FUP-04

must_haves:
  truths:
    - "Maizzle template estimate-followup.html renders a follow-up email with estimate number, View Estimate link, and 3 CTAs"
    - "EstimateFollowupEmailRenderer renders the template with followupStep, estimateNumber, viewUrl, traderFirstName, priceDisplay, and businessName variables"
    - "All three CTAs appear: Go Ahead, Message [FirstName], Not right for me"
    - "Disclaimer text appears below primary CTA"
  artifacts:
    - path: "trade-flow-api/src/email/templates/estimate-followup.html"
      provides: "Maizzle HTML email template for follow-up reminders"
      contains: "Go Ahead"
    - path: "trade-flow-api/src/email/services/estimate-followup-email-renderer.service.ts"
      provides: "Service that renders estimate-followup.html with template variables"
      exports: ["EstimateFollowupEmailRenderer"]
  key_links:
    - from: "trade-flow-api/src/email/services/estimate-followup-email-renderer.service.ts"
      to: "trade-flow-api/src/email/templates/estimate-followup.html"
      via: "dynamic import and render call"
      pattern: "estimate-followup"
---

<objective>
Create the estimate-followup.html Maizzle template and the EstimateFollowupEmailRenderer service that renders it. One shared template with a `followupStep` variable for minor copy variations across the 3d/10d/21d emails.

Purpose: Every follow-up email sent by EstimateFollowupProcessor uses this template. The 3 CTAs (Go Ahead, Message [FirstName], Not right for me) match the initial send email and the public customer page per D-EMAIL-04.
Output: Maizzle template file, renderer service, unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-follow-up-queue-automation/46-CONTEXT.md
@.planning/phases/46-follow-up-queue-automation/46-RESEARCH.md

<interfaces>
<!-- Key types the executor needs -->

From Phase 46 RESEARCH.md — template variable shape:
```typescript
export interface EstimateFollowupEmailData {
  businessName: string;
  followupStep: "3d" | "10d" | "21d";
  estimateNumber: string;       // "E-2026-001"
  viewUrl: string;              // existing token URL
  traderFirstName: string;      // for "Message [First Name]" CTA
  priceDisplay: string;         // "£120 - £132" or "From £120"
}
```

From existing Maizzle pattern (estimate-sent.html or quote-sent.html in src/email/templates/):
- Templates use Maizzle Tailwind CSS syntax
- Dynamic import ESM pattern: `const { render } = await import("@maizzle/framework")`
- Templates receive variables via the Maizzle render config object
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Maizzle template and EstimateFollowupEmailRenderer service with unit test</name>
  <files>
    trade-flow-api/src/email/templates/estimate-followup.html,
    trade-flow-api/src/email/services/estimate-followup-email-renderer.service.ts,
    trade-flow-api/src/estimate-followups/test/services/estimate-followup-email-renderer.service.spec.ts
  </files>
  <read_first>
    trade-flow-api/src/email/templates/estimate-sent.html,
    trade-flow-api/src/email/templates/quote-sent.html,
    trade-flow-api/src/email/services/estimate-email-renderer.service.ts,
    trade-flow-api/src/email/services/quote-email-renderer.service.ts
  </read_first>
  <behavior>
    - Test 1: Renderer returns HTML containing the estimateNumber (e.g. "E-2026-001")
    - Test 2: Renderer returns HTML containing the viewUrl as an href
    - Test 3: Renderer returns HTML containing "Go Ahead" (primary CTA text)
    - Test 4: Renderer returns HTML containing "Message" and traderFirstName (secondary CTA)
    - Test 5: Renderer returns HTML containing "Not right for me" (tertiary CTA)
    - Test 6: Renderer returns HTML containing the disclaimer text "This is an estimate"
  </behavior>
  <action>
1. Create `src/email/templates/estimate-followup.html` as a Maizzle template. Mirror the structure of the existing `estimate-sent.html` template (same layout, header, footer) but with follow-up-specific content per D-EMAIL-01 through D-EMAIL-05:

   - **Subject reference:** Subject will be set by the processor, not in the template. Template body only.
   - **Header:** Business name (`{{ businessName }}`)
   - **Body copy:** Brief, warm follow-up message. Use `followupStep` for minor copy variation:
     - `3d`: "Just checking in — have you had a chance to look over your estimate?"
     - `10d`: "Following up on your estimate — we'd love to hear from you."
     - `21d`: "We wanted to reach out one more time about your estimate."
   - **Estimate reference:** "Estimate {{ estimateNumber }}" with price display (`{{ priceDisplay }}`)
   - **Primary CTA button:** "View Estimate" linking to `{{ viewUrl }}` (the main action is to view, per D-EMAIL-02)
   - **Three response CTAs (below the View button):**
     - Primary: "Go Ahead" button linking to `{{ viewUrl }}#respond` (Phase 45 handles the anchor)
     - Secondary: "Message {{ traderFirstName }}" button linking to `{{ viewUrl }}#message`
     - Tertiary: "Not right for me" as a text link to `{{ viewUrl }}#decline`
   - **Disclaimer:** "This is an estimate — the final cost may differ from this approximate figure." in smaller text below the CTAs.
   - **Footer:** Standard business footer

   Use Tailwind CSS classes consistent with existing templates. Keep the template brief per D-EMAIL-02.

2. Create `src/email/services/estimate-followup-email-renderer.service.ts`:
   - Define `EstimateFollowupEmailData` interface (businessName, followupStep, estimateNumber, viewUrl, traderFirstName, priceDisplay)
   - Export `EstimateFollowupEmailRenderer` class annotated with `@Injectable()`
   - `render(data: EstimateFollowupEmailData): Promise<string>` method that:
     a. Uses `const { render } = await import("@maizzle/framework")` (ESM dynamic import)
     b. Reads the template from `estimate-followup.html`
     c. Passes data variables to Maizzle render config
     d. Returns the rendered HTML string
   - Mirror the exact render pattern from the existing `EstimateEmailRenderer` or `QuoteEmailRenderer` — same dynamic import, same path resolution, same Maizzle config shape.

3. Create unit test `src/estimate-followups/test/services/estimate-followup-email-renderer.service.spec.ts`:
   - Call `renderer.render()` with test data: `{ businessName: "Smith Plumbing", followupStep: "3d", estimateNumber: "E-2026-001", viewUrl: "https://app.test/estimate/abc123", traderFirstName: "John", priceDisplay: "£120 - £132" }`
   - Assert the returned HTML string contains: "E-2026-001", "https://app.test/estimate/abc123", "Go Ahead", "John", "Not right for me", "This is an estimate"
   - If the existing email renderer tests mock the Maizzle import, follow the same mocking approach. If they call Maizzle directly (integration-style), do the same.

4. Run tests and verify all pass.
  </action>
  <verify>
    <automated>cd trade-flow-api && npm run test -- --testPathPattern=estimate-followup-email-renderer 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `trade-flow-api/src/email/templates/estimate-followup.html` exists and contains `{{ estimateNumber }}`, `{{ viewUrl }}`, `{{ businessName }}`, `{{ traderFirstName }}`, `{{ followupStep }}`
    - `trade-flow-api/src/email/templates/estimate-followup.html` contains "Go Ahead", "Not right for me", and "This is an estimate"
    - `estimate-followup-email-renderer.service.ts` contains `export class EstimateFollowupEmailRenderer` and `async render(data: EstimateFollowupEmailData)`
    - `estimate-followup-email-renderer.service.spec.ts` contains at least 4 `it(` blocks (not `it.todo`)
    - All tests pass: `npm run test -- --testPathPattern=estimate-followup-email-renderer` exits 0
  </acceptance_criteria>
  <done>Maizzle estimate-followup.html template renders a brief follow-up email with estimate number, View Estimate link, 3 response CTAs (Go Ahead, Message [FirstName], Not right for me), and the non-binding disclaimer. Renderer service and unit tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template variables | Data injected into Maizzle template from job payload + DB read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04 | Injection | Template variable injection (XSS in email) | mitigate | Maizzle HTML-escapes template variables by default (double-brace syntax `{{ var }}`). URL variables are embedded as href attributes only. No raw/unescaped output used. Business name and trader name are stored in MongoDB from authenticated input (already validated). |
| T-46-05 | Spoofing | Email sent to wrong recipient | accept | Template does not determine the recipient — the processor reads recipient from MongoDB at execution time (FUP-06, Plan 04). Template only renders display content. |
</threat_model>

<verification>
1. Template contains all required CTAs: `grep "Go Ahead" trade-flow-api/src/email/templates/estimate-followup.html`
2. Template contains disclaimer: `grep "This is an estimate" trade-flow-api/src/email/templates/estimate-followup.html`
3. Renderer tests pass: `cd trade-flow-api && npm run test -- --testPathPattern=estimate-followup-email-renderer` exits 0.
</verification>

<success_criteria>
- estimate-followup.html renders with all 3 CTAs and disclaimer text
- EstimateFollowupEmailRenderer.render() produces valid HTML with injected variables
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/46-follow-up-queue-automation/46-03-SUMMARY.md`
</output>
