---
phase: 45
slug: public-customer-page-response-handling
status: draft
shadcn_initialized: true
preset: new-york
created: 2026-04-12
---

# Phase 45 -- UI Design Contract

> Visual and interaction contract for the Public Customer Estimate Page and Response Handling. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | new-york, baseColor neutral, cssVariables true |
| Component library | radix (via shadcn/ui) |
| Icon library | lucide-react |
| Font | Inter (font-sans), IBM Plex Mono (font-mono) |

Source: Phase 17/19 UI-SPEC (established project standard).

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Tailwind | Usage in this phase |
|-------|-------|----------|---------------------|
| 2xs | 4px | `1` | Icon-text gap within buttons, inline padding |
| sm | 8px | `2` | Field-level spacing within decline reason chips |
| base | 16px | `4` | Card content rhythm, stacked section spacing |
| lg | 24px | `6` | Section-level spacing between card sections (disclaimer to content, content to actions) |
| xl | 48px | `12` | Error/empty state vertical padding, page-level top/bottom padding on mobile |

Exceptions:
- 44px touch target minimum for all response buttons on mobile (use `min-h-11` which is 44px). Source: Phase 19 UI-SPEC precedent.
- 12px (`gap-3`) used between response buttons. Justification: 8px (`gap-2`) is too tight for three stacked full-width buttons of different visual weight (primary, outline, text link); 16px (`gap-4`) creates excessive separation that weakens the visual grouping of the action area. 12px provides clear separation while maintaining cohesion. This is a phase-specific exception; the standard scale applies everywhere else.

### Phase-Specific Spacing

Per shadcn rules, all vertical stacking uses `flex flex-col gap-*` instead of `space-y-*`.

| Element | Spacing | Rationale |
|---------|---------|-----------|
| Business header to disclaimer card | `flex flex-col gap-4` (16px) | Tight coupling -- disclaimer is part of document header |
| Disclaimer card to estimate content | `flex flex-col gap-6` (24px) | Visual break before main content |
| Price range to "Why is this a range?" | `flex flex-col gap-2` (8px) | Closely related content |
| Estimate content to response actions | `flex flex-col gap-6` (24px) | Visual break before decision point (matches Phase 19 pattern) |
| Between response buttons | `gap-3` (12px) | Enough separation between primary, secondary, tertiary actions (see exception above) |
| Inline textarea expansion to submit button | `flex flex-col gap-4` (16px) | Standard form field rhythm |
| Decline reason chips | `gap-2` (8px) | Compact chip grid |

---

## Typography

From project design system (established in Phase 17/19):

| Role | Size | Weight | Line Height | Tailwind | Usage in this phase |
|------|------|--------|-------------|----------|---------------------|
| Body | 14px | 400 (normal) | normal | `text-sm` | Scope description, uncertainty notes, validity text, disclaimer body |
| Label | 14px | 600 (semibold) | none | `text-sm font-semibold leading-none` | "Why is this a range?" heading, decline prompt, section labels |
| Section heading | 18px | 600 (semibold) | none | `text-lg font-semibold` | Business name, price range display |
| Display | 24px | 600 (semibold) | default | `text-2xl font-semibold` | Estimate number heading |
| Metadata | 12px | 400 (normal) | default | `text-xs` | Estimate date, validity date, response timestamps |

Declared weights: 400 (normal), 600 (semibold). Two weights only. Note: shadcn Button components use `font-medium` (weight 500) internally as their default styling; this is an inherited component internal and not a project-declared weight. All project-authored text uses only 400 or 600.

### Phase-Specific Typography

| Element | Classes | Rationale |
|---------|---------|-----------|
| Price range value | `text-lg font-semibold` | Focal point of the estimate -- must stand out |
| "From X" value | `text-lg font-semibold` | Same treatment as range |
| Disclaimer body text | `text-sm` | Body copy weight; the border/background treatment provides emphasis |
| "Happy to Proceed" button | Button default (inherits component styling) | Standard shadcn button typography |
| "Message [Name]" button | Button default (inherits component styling) | Standard shadcn button typography |
| "Not right for me" link | `text-sm text-muted-foreground underline` | Visually subdued tertiary action |
| Decline reason chips | `text-sm` | Standard chip text |
| "Why is this a range?" heading | `text-sm font-semibold` | Label weight for section heading -- uses declared semibold weight |
| Decline prompt | `text-sm font-semibold` | Label weight for inline heading -- uses declared semibold weight |
| Uncertainty note items | `text-sm text-muted-foreground` | Secondary information, not primary content |
| Terminal state message | `text-sm` | Body copy for summary messages |
| CTA disclaimer text | `text-xs text-muted-foreground` | Fine print below primary button |

---

## Color

From project design system (established in Phase 17/19):

| Role | Token | Value (light) | Usage in this phase |
|------|-------|---------------|---------------------|
| Dominant (60%) | `background` | oklch(0.99 0 0) / #FCFCFC | Full page canvas behind the estimate card |
| Secondary (30%) | `card` | oklch(1 0 0) / #FFFFFF | Estimate card surface, inline form surfaces |
| Accent (10%) | `primary` | oklch(0.31 0.09 245) Petrol Blue | "Happy to Proceed" button only |
| Destructive | `destructive` | oklch(0.64 0.21 25) | Not used this phase (decline is not destructive) |
| Muted text | `muted-foreground` | -- | Secondary labels, metadata, tertiary link, uncertainty notes |
| Foreground | `foreground` | #1A1A1A | Business name, price range, scope text, primary copy |
| Warning surface | `--warning` | amber-50 equivalent | Non-binding disclaimer card background |
| Warning border | `--warning-border` | amber-200 equivalent | Non-binding disclaimer card border |
| Warning foreground | `--warning-foreground` | amber-900 equivalent | Non-binding disclaimer card body text |
| Warning icon | `--warning-icon` | amber-600 equivalent | Non-binding disclaimer card icon color |
| Success | `success` | oklch(0.69 0.18 165) | Success confirmation after response submission |

### Warning Semantic Tokens

The disclaimer card requires a warning color palette. Rather than using raw Tailwind amber utilities (`bg-amber-50`, `border-amber-200`, `text-amber-600`, `text-amber-900`), define semantic CSS variables in the global CSS file (`src/index.css`) under `@theme inline`:

```css
--warning: oklch(0.96 0.03 90);          /* amber-50 equivalent - surface */
--warning-border: oklch(0.88 0.07 85);   /* amber-200 equivalent - border */
--warning-foreground: oklch(0.28 0.06 60); /* amber-900 equivalent - body text */
--warning-icon: oklch(0.55 0.15 55);     /* amber-600 equivalent - icon */
```

Usage in components: `bg-warning`, `border-warning-border`, `text-warning-foreground`, `text-warning-icon`.

If defining new CSS variables is not feasible in this phase's scope, document the raw amber utilities as an accepted one-off exception with this comment in the component: `// TODO: Replace raw amber utilities with --warning semantic tokens when available`.

### Phase-Specific Color Assignments

| Element | Token/Class | Rationale |
|---------|-------------|-----------|
| "Happy to Proceed" button | `variant="default"` (primary) | Primary CTA. Petrol Blue bg, white text. |
| "Message [Name]" button | `variant="outline"` | Secondary action. Neutral border, foreground text. |
| "Not right for me" link | `text-muted-foreground hover:text-foreground` | Tertiary action. Muted, not attention-grabbing. |
| Disclaimer card | `bg-warning border border-warning-border rounded-lg` | Warm, attention-drawing without being alarming. Distinct from content cards. |
| Disclaimer icon | `text-warning-icon` | Matches disclaimer card warmth. Use `Info` icon from Lucide. |
| Decline reason chips (unselected) | `variant="outline"` | Neutral, selectable appearance |
| Decline reason chips (selected) | `bg-primary text-primary-foreground` | Clear selection state using accent |
| Response success state | `text-foreground` with `CheckCircle` icon `text-success` | Confirmation without being a full banner |
| Terminal state (customer-triggered) | `text-foreground` | Informational summary, neutral tone |
| Terminal state (trader-triggered) | `text-muted-foreground` with `bg-muted rounded-lg` | Subdued, the estimate is no longer relevant |
| Loading spinner | `text-primary-foreground` on primary button, `text-foreground` on outline button | Visible against respective backgrounds |

Accent reserved for: "Happy to Proceed" primary CTA button, selected decline reason chips.

---

## Component Inventory

### New Components (Phase 45)

| Component | Location | shadcn Primitives Used |
|-----------|----------|----------------------|
| `PublicEstimatePage` | `pages/` | -- (route-level composition) |
| `PublicEstimateCard` | `features/public-estimate/components/` | Card, CardHeader, CardContent, CardFooter, Separator |
| `PublicEstimateDisclaimer` | `features/public-estimate/components/` | -- (styled div with Info icon) |
| `PublicEstimatePriceRange` | `features/public-estimate/components/` | -- (uses formatRange from Phase 43) |
| `PublicEstimateUncertainty` | `features/public-estimate/components/` | -- (conditional "Why is this a range?" section) |
| `PublicEstimateResponseButtons` | `features/public-estimate/components/` | Button |
| `PublicEstimateMessageForm` | `features/public-estimate/components/` | Textarea, Button |
| `PublicEstimateDeclineForm` | `features/public-estimate/components/` | Button (chips), Textarea, Button |
| `PublicEstimateTerminalState` | `features/public-estimate/components/` | -- (text display) |
| `PublicEstimateSkeleton` | `features/public-estimate/components/` | Skeleton, Card |
| `PublicEstimateError` | `features/public-estimate/components/` | -- (error card with icon) |

### Existing shadcn Components Required

| Component | Already Installed | Usage |
|-----------|-------------------|-------|
| Card, CardHeader, CardContent, CardFooter | Yes | Main estimate card layout |
| Button | Yes | Response CTAs, submit buttons, decline chips |
| Textarea | Yes | Message form, decline freeform |
| Skeleton | Yes | Loading state |
| Separator | Yes | Section dividers within card |

No new shadcn components need to be installed.

---

## Page Layout Contract

### Public Estimate Page (`/estimate/:token`)

**Focal point:** The estimate card itself -- centered on neutral background with no competing navigation or chrome. Mirrors Phase 17 public quote page exactly.

**Route placement:** Outside `AuthenticatedLayout` in `App.tsx`. No sidebar, no header, no Trade Flow app chrome. Source: D-PAGE-01.

**Page structure:**

```
+---------------------------------------------+
|  bg-background full viewport                |
|                                             |
|  +-------------------------------------+   |
|  |  Estimate Card (max-w-3xl mx-auto)  |   |
|  |                                      |   |
|  |  +- Business Header ---------------+|   |
|  |  | Business Name                   ||   |
|  |  | Business phone / email          ||   |
|  |  +----------------------------------+|   |
|  |                                      |   |
|  |  +- Disclaimer Card ---------------+|   |
|  |  | (i) "This is an estimate, not   ||   |
|  |  |  a fixed price commitment..."   ||   |
|  |  +----------------------------------+|   |
|  |                                      |   |
|  |  +- Estimate Details ---------------+|   |
|  |  | Estimate #  |  Date  | Valid To  ||   |
|  |  +----------------------------------+|   |
|  |                                      |   |
|  |  +- Scope / Description ------------+|   |
|  |  | "Bathroom renovation including..." ||   |
|  |  +----------------------------------+|   |
|  |                                      |   |
|  |  +- Price Range --------------------+|   |
|  |  | "L1,200 - L1,320"               ||   |
|  |  |                                  ||   |
|  |  | Why is this a range?             ||   |
|  |  | "The final cost may vary         ||   |
|  |  |  depending on: condition of      ||   |
|  |  |  existing pipework * materials..." ||   |
|  |  +----------------------------------+|   |
|  |                                      |   |
|  |  +- Notes --------------------------+|   |
|  |  | (if present, tradesperson notes) ||   |
|  |  +----------------------------------+|   |
|  |                                      |   |
|  |  +- Validity -----------------------+|   |
|  |  | "Valid until 15 May 2026"        ||   |
|  |  +----------------------------------+|   |
|  |                                      |   |
|  |  +- Response Actions ---------------+|   |
|  |  | [    Happy to Proceed     ]      ||   |
|  |  | "This is an estimate -- the      ||   |
|  |  |  final cost may differ..."       ||   |
|  |  |                                  ||   |
|  |  | [   Message [First Name]  ]      ||   |
|  |  |                                  ||   |
|  |  | Not right for me                 ||   |
|  |  +----------------------------------+|   |
|  |                                      |   |
|  +--------------------------------------+   |
|                                             |
+---------------------------------------------+
```

**Card sizing:** `max-w-3xl` centered with `mx-auto`, padded with `p-4 sm:p-6` on the outer wrapper. Outer wrapper uses `min-h-screen bg-background py-6 sm:py-12`. Matches Phase 17.

**Content NOT shown on customer page (per D-PAGE-05):** Line item breakdown, contingency percentage, base price as separate figure.

---

## Interaction States

### Response Button Default State (D-CTA-01..07)

```
+---------------------------------------+
| [    Happy to Proceed     ]  (primary)|
| "This is an estimate -- the final     |
|  cost may differ from this            |
|  approximate figure."                 |
|                                       |
| [   Message [First Name]  ]  (outline)|
|                                       |
|        Not right for me     (text link)|
+---------------------------------------+
```

All three actions stack vertically, full-width. No side-by-side layout at any breakpoint -- the estimate page is a linear document with a single decision flow.

### "Happy to Proceed" Flow

| State | Visual | Behavior |
|-------|--------|----------|
| Default | Full-width primary button | Enabled |
| Loading | Button shows spinner, text changes to "Sending...", all actions disabled | POST request in flight |
| Success | Entire action area replaced with success message | See D-CTA-06 |
| Error | Toast notification (sonner, top-center): "Something went wrong. Please try again." Actions re-enabled. | Network/server error |

### "Message [Name]" Flow

| State | Visual | Behavior |
|-------|--------|----------|
| Default | Full-width outline button | Enabled |
| Clicked | Textarea expands inline below the button. Placeholder: "Type your message to [First Name]..." Submit button: "Send Message" (outline). | Inline expansion per D-CTA-05 |
| Message entered | Character counter appears: `{count}/2000` in `text-xs text-muted-foreground`, right-aligned | 2000 character limit |
| Loading | "Send Message" button shows spinner, text changes to "Sending...", textarea disabled | POST request in flight |
| Success | Entire action area replaced with success message | See D-CTA-06 |
| Error | Toast notification. Form remains with message preserved. | Network/server error |

### "Not right for me" Flow

| State | Visual | Behavior |
|-------|--------|----------|
| Default | Text link in `text-muted-foreground underline`, centered | Enabled |
| Clicked | Link area expands inline to show decline form. Prompt: "Let [First Name] know why -- it helps them improve." | Inline expansion per D-CTA-05 |
| Reason selection | 5 quick-tap chips in a wrapping flex grid (`flex flex-wrap gap-2`). Single-select -- tapping one deselects others. Chips use `variant="outline"` (unselected) or `bg-primary text-primary-foreground` (selected). | Required: at least one reason or freeform text |
| Freeform text | Textarea below chips: label "Anything else? (optional)", placeholder "Add a note if you'd like". 500 character limit with counter. | Optional freeform |
| Submit | "Send Feedback" button (outline), full-width | Enabled when a reason is selected OR freeform text is entered |
| Loading | "Send Feedback" shows spinner, text "Sending...", form disabled | POST request in flight |
| Success | Entire action area replaced with success message | See D-CTA-06 |
| Error | Toast notification. Form remains with selections preserved. | Network/server error |

### Decline Reason Chips

| Chip Label | API `reason` value |
|------------|-------------------|
| Too expensive | `too_expensive` |
| Going with someone else | `going_with_someone_else` |
| Decided not to do the work | `decided_not_to_do_work` |
| Just getting an idea of costs | `just_getting_idea` |
| Timing isn't right | `timing_not_right` |

### Post-Response Success State (D-CTA-06)

Replaces the entire action area after any successful response:

```
+---------------------------------------+
| (check-circle icon, success color)    |
| Thanks! [First Name] has been         |
| notified and will be in touch.        |
+---------------------------------------+
```

- Icon: `CheckCircle` from Lucide, `h-5 w-5 text-success`
- Text: `text-sm text-foreground`, centered
- Container: `py-6`, centered content
- Estimate content remains visible above

### Terminal States (D-TERM-01..03)

**Customer-triggered terminal (previously responded):**

The estimate content remains fully visible. The action area shows a read-only summary:

| Response type | Summary copy |
|---------------|-------------|
| Proceed | "You told [First Name] you're happy to proceed on [date]. They'll be in touch to arrange next steps." |
| Message | "You sent [First Name] a message on [date]. They'll be in touch." |
| Decline | "You let [First Name] know this estimate isn't right for you on [date]." |

- Style: `text-sm text-muted-foreground`, centered, `py-6`
- No active buttons

**Trader-triggered terminal (Converted / Expired / Lost / Deleted):**

Full card content is NOT shown (D-TERM-02). Instead, a centered message card:

```
+---------------------------------------+
|  (FileX icon, muted)                  |
|                                       |
|  This estimate is no longer available |
|                                       |
|  Contact [First Name] if you have     |
|  any questions.                       |
|                                       |
|  Phone: 07xxx xxx xxx                 |
|  Email: name@example.com             |
+---------------------------------------+
```

- Icon: `FileX` from Lucide, `h-12 w-12 text-muted-foreground`
- Heading: `text-lg font-semibold`
- Body: `text-sm text-muted-foreground`
- Contact details: `text-sm`, each on its own line
- Card: `max-w-md mx-auto`, `py-12` vertical padding

---

## Loading State

Skeleton matching card layout sections:

| Section | Skeleton |
|---------|----------|
| Business header | `Skeleton` h-6 w-48 (name) + `Skeleton` h-4 w-40 (contact) |
| Disclaimer card | `Skeleton` h-16 w-full rounded-lg |
| Estimate details | 3x `Skeleton` h-4 w-24 in a row |
| Scope | `Skeleton` h-4 w-full + `Skeleton` h-4 w-3/4 |
| Price range | `Skeleton` h-6 w-32 |
| Validity | `Skeleton` h-4 w-40 |
| Response buttons | `Skeleton` h-10 w-full + `Skeleton` h-10 w-full + `Skeleton` h-4 w-24 mx-auto |

Card container renders immediately with skeleton content inside. Page background visible around card. Matches Phase 17 skeleton pattern.

---

## Error States

| Condition | Display | Copy |
|-----------|---------|------|
| Invalid/unknown token | Centered error card | Heading: "Estimate not found". Body: "This estimate link is not valid. It may have been removed or the link is incorrect." |
| Expired token | Centered error card with business name | Heading: "Estimate link expired". Body: "This estimate link from {businessName} has expired. Please contact them directly." |
| Revoked token | Centered error card with business name | Heading: "Estimate no longer available". Body: "This estimate from {businessName} is no longer available. Please contact them for more information." |
| Network error | Centered error card | Heading: "Something went wrong". Body: "We couldn't load this estimate. Please check your internet connection and try again." Action button: "Reload" |
| Response already submitted (race condition) | Toast notification | "This estimate has already been responded to." Page reloads to show terminal state. |

Error card layout: centered on page (`max-w-md mx-auto`), icon (`AlertCircle` at `h-12 w-12 text-muted-foreground`), heading (`text-lg font-semibold`), body text (`text-sm text-muted-foreground`), optional action button (`variant="outline"`). Vertical padding `py-12`. Matches Phase 17 error pattern.

---

## Disclaimer Card Contract

Source: D-PAGE-02.

| Property | Value |
|----------|-------|
| Position | Below business header, before estimate content |
| Background | `bg-warning` (semantic token, amber-50 equivalent) |
| Border | `border border-warning-border rounded-lg` (semantic token, amber-200 equivalent) |
| Padding | `p-4` |
| Icon | `Info` from Lucide, `h-4 w-4 text-warning-icon shrink-0 mt-0.5` |
| Layout | `flex gap-3` (icon left, text right) |
| Text | `text-sm text-warning-foreground` (semantic token, amber-900 equivalent) |
| Copy | "This is an estimate, not a fixed price commitment. A firm quote will be provided after a site visit." |

Verbatim copy from SND-05 / Phase 44 D-LGL-02. Non-removable.

Fallback: If `--warning` semantic tokens are not yet defined in the global CSS, use raw Tailwind utilities (`bg-amber-50 border-amber-200 text-amber-900`) with a `// TODO: Replace raw amber utilities with --warning semantic tokens` comment. This is an accepted temporary exception.

---

## "Why is this a range?" Section Contract

Source: D-PAGE-04.

| Property | Value |
|----------|-------|
| Condition | Only displayed when `uncertaintyReasons` array is non-empty |
| Position | Directly below the price range value, separated by `flex flex-col gap-2` (8px) |
| Heading | "Why is this a range?" in `text-sm font-semibold text-foreground` |
| Body | "The final cost may vary depending on:" in `text-sm text-muted-foreground` |
| Reasons | Rendered as a dot-separated inline list: "[reason 1] . [reason 2] . [reason 3]" in `text-sm text-muted-foreground` |
| Dot separator | `·` (middle dot) with `mx-1` spacing |

Never shows contingency percentage. Never shows base price.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Happy to Proceed" |
| Primary CTA disclaimer | "This is an estimate -- the final cost may differ from this approximate figure." |
| Secondary CTA | "Message [First Name]" |
| Message placeholder | "Type your message to [First Name]..." |
| Message submit | "Send Message" |
| Message loading | "Sending..." |
| Tertiary CTA | "Not right for me" |
| Decline prompt | "Let [First Name] know why -- it helps them improve." |
| Decline chips | "Too expensive" / "Going with someone else" / "Decided not to do the work" / "Just getting an idea of costs" / "Timing isn't right" |
| Decline freeform label | "Anything else? (optional)" |
| Decline freeform placeholder | "Add a note if you'd like" |
| Decline submit | "Send Feedback" |
| Decline loading | "Sending..." |
| Proceed loading | "Sending..." |
| Response success | "Thanks! [First Name] has been notified and will be in touch." |
| Terminal: proceed | "You told [First Name] you're happy to proceed on [date]. They'll be in touch to arrange next steps." |
| Terminal: message | "You sent [First Name] a message on [date]. They'll be in touch." |
| Terminal: decline | "You let [First Name] know this estimate isn't right for you on [date]." |
| Terminal: trader-closed heading | "This estimate is no longer available" |
| Terminal: trader-closed body | "Contact [First Name] if you have any questions." |
| Disclaimer card | "This is an estimate, not a fixed price commitment. A firm quote will be provided after a site visit." |
| "Why is this a range?" heading | "Why is this a range?" |
| "Why is this a range?" body | "The final cost may vary depending on:" |
| Validity display | "Valid until [date]" |
| Error: not found heading | "Estimate not found" |
| Error: not found body | "This estimate link is not valid. It may have been removed or the link is incorrect." |
| Error: expired heading | "Estimate link expired" |
| Error: expired body | "This estimate link from {businessName} has expired. Please contact them directly." |
| Error: revoked heading | "Estimate no longer available" |
| Error: revoked body | "This estimate from {businessName} is no longer available. Please contact them for more information." |
| Error: network heading | "Something went wrong" |
| Error: network body | "We couldn't load this estimate. Please check your internet connection and try again." |
| Error: network action | "Reload" |
| Error: already responded (toast) | "This estimate has already been responded to." |
| Error: response failed (toast) | "Something went wrong. Please try again." |
| Message character limit | 2000 characters, counter: "{count}/2,000" |
| Decline freeform character limit | 500 characters, counter: "{count}/500" |

### Notification Email Copy (Backend)

| Element | Copy |
|---------|------|
| Proceed subject | "{customerName} responded to Estimate {estimateNumber} -- Happy to Proceed" |
| Message subject | "{customerName} responded to Estimate {estimateNumber} -- Message" |
| Decline subject | "{customerName} responded to Estimate {estimateNumber} -- Declined" |
| Email CTA button | "View Estimate" |

Source: D-NOTIFY-02.

No destructive actions in this phase. Decline is a customer choice, not a destructive action -- styled as outline/muted, not destructive red.

---

## Icon Assignments

| Element | Icon | Size | Color |
|---------|------|------|-------|
| Disclaimer card | `Info` | `h-4 w-4` | `text-warning-icon` |
| Response success | `CheckCircle` | `h-5 w-5` | `text-success` |
| Trader-closed terminal | `FileX` | `h-12 w-12` | `text-muted-foreground` |
| Error: not found / expired / revoked | `AlertCircle` | `h-12 w-12` | `text-muted-foreground` |
| "Message [Name]" button | `MessageSquare` | `h-4 w-4` | inherits button color |
| Loading spinner | `Loader2` with `animate-spin` | `h-4 w-4` | inherits button color |

Source: Claude's discretion for icon selection; Lucide library per project standard.

---

## Mobile Considerations

- Page is mobile-first. `max-w-3xl` card centered on desktop, full-width on mobile with `p-4` padding.
- All response buttons are full-width at all breakpoints (single-column decision flow).
- Touch targets: all buttons use `min-h-11` (44px) for mobile tap safety.
- Textarea inputs use `text-base md:text-sm` to prevent iOS zoom on focus (Phase 19 precedent).
- Decline reason chips use `min-h-9` for comfortable tapping.
- Toast notifications at top-center (sonner default, matches existing pattern).

---

## PECR Compliance Contract

Source: D-PAGE-06, CUST-07.

- Zero non-essential cookies.
- No analytics scripts.
- No tracking pixels.
- No third-party scripts.
- Separate `publicEstimateApi` RTK Query instance with no auth headers (no Firebase token leakage).
- No `localStorage` or `sessionStorage` usage beyond what React needs for rendering.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Card, Button, Textarea, Skeleton, Separator (all pre-installed) | not required |

No third-party registries declared. No new shadcn components need to be installed.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
