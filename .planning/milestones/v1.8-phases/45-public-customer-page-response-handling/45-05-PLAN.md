---
phase: 45-public-customer-page-response-handling
plan: 05
type: execute
wave: 4
depends_on: ["45-03", "45-04"]
files_modified: []
autonomous: false
requirements:
  - CUST-01
  - CUST-02
  - CUST-03
  - CUST-04
  - CUST-05
  - CUST-06
  - CUST-07
  - RESP-01
  - RESP-02
  - RESP-03
  - RESP-04
  - RESP-05
  - RESP-06
  - RESP-07

must_haves:
  truths:
    - "Full E2E flow works: customer opens link, views estimate, responds, trader receives notification"
    - "All three response types work end-to-end across both repos"
    - "Terminal states display correctly for both customer-triggered and trader-triggered closures"
    - "Zero non-essential cookies on the public page"
    - "CI passes in both repos"
  artifacts: []
  key_links:
    - from: "trade-flow-ui/src/features/public-estimate/api/publicEstimateApi.ts"
      to: "trade-flow-api/src/estimate/controllers/public-estimate.controller.ts"
      via: "HTTP GET/POST /v1/public/estimate/:token"
      pattern: "publicEstimateApi.*v1/public/estimate"
---

<objective>
End-to-end visual and functional verification of the complete public estimate page with both repos running. Confirms all response flows, terminal states, disclaimer display, price formatting, and PECR compliance work across the full stack.

Purpose: Gate check ensuring CUST-01..07 and RESP-01..07 all function correctly before marking the phase complete.
Output: User approval or issue list for fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-public-customer-page-response-handling/45-CONTEXT.md
@.planning/phases/45-public-customer-page-response-handling/45-03-SUMMARY.md
@.planning/phases/45-public-customer-page-response-handling/45-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full CI gate in both repos</name>
  <files></files>
  <action>
Run `npm run ci` in both trade-flow-api and trade-flow-ui to confirm all quality checks pass before presenting to the user for verification. Fix any issues that arise.
  </action>
  <verify>
    <automated>cd trade-flow-api && npm run ci && cd ../trade-flow-ui && npm run ci</automated>
  </verify>
  <done>`npm run ci` exits 0 in both trade-flow-api and trade-flow-ui.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of public estimate page</name>
  <action>
Present the page to the user for visual and functional verification. Ensure both dev servers are running. Guide the user through the verification steps listed in how-to-verify.
  </action>
  <what-built>Complete public estimate customer page at /estimate/:token with 3-action response flow, disclaimer card, price range display, terminal states, loading skeleton, and error handling. Backend API at GET/POST /v1/public/estimate/:token.</what-built>
  <how-to-verify>
1. Start both dev servers (`npm run start:dev` in trade-flow-api, `npm run dev` in trade-flow-ui)
2. Create a test estimate via the API or UI and send it (needs Phase 44 send flow -- if not available, manually create a document token pointing to an estimate)
3. Open the estimate's public link at `http://localhost:5173/estimate/{token}`
4. Verify the page shows:
   - Business name and contact details at top
   - Yellow/amber disclaimer card: "This is an estimate, not a fixed price commitment..."
   - Estimate number, date, validity
   - Scope description
   - Price range formatted as "X - Y" or "From X" (NO contingency percentage shown)
   - "Why is this a range?" section with uncertainty reasons (if any)
   - Three response actions: "Happy to Proceed" (primary), "Message [Name]" (outline), "Not right for me" (text link)
5. Test "Happy to Proceed": click the button, see "Sending..." spinner, then success confirmation
6. Test "Message [Name]" (on a fresh token): click, see textarea expand inline, type a message, submit
7. Test "Not right for me" (on a fresh token): click, see decline reasons expand as toggle group, select a reason, optionally type feedback, submit
8. Revisit a responded-to token: verify terminal state shows response summary
9. Check browser DevTools > Application > Cookies: should be empty or session-only (zero non-essential cookies)
10. Run `npm run ci` in both repos to confirm all checks pass
  </how-to-verify>
  <verify>
    <automated>cd trade-flow-api && npm run ci && cd ../trade-flow-ui && npm run ci</automated>
  </verify>
  <acceptance_criteria>
    - User confirms disclaimer card is visible with amber/warning styling
    - User confirms price range displays correctly without contingency percentage
    - User confirms all three response actions work end-to-end
    - User confirms terminal states display read-only messages
    - Browser DevTools shows zero non-essential cookies
    - `npm run ci` passes in both repos
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
  <done>User has verified the public estimate page visually and functionally. All response flows work. PECR compliance confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan -- no new code introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No new attack surface -- verifying existing mitigations from Plans 01-04 |
</threat_model>

<verification>
1. `cd trade-flow-api && npm run ci` -- full CI gate green
2. `cd trade-flow-ui && npm run ci` -- full CI gate green
3. User visual verification of all page states
4. User confirmation of E2E response flows
</verification>

<success_criteria>
- Full CI gate passes in both repos
- User approves the public estimate page visually and functionally
- All three response types work end-to-end
- Terminal states display correctly
- Zero non-essential cookies confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/45-public-customer-page-response-handling/45-05-SUMMARY.md`
</output>
