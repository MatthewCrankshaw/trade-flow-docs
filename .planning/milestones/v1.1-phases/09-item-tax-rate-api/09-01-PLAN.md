---
phase: 09-item-tax-rate-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-api/src/item/entities/item.entity.ts
  - trade-flow-api/src/item/data-transfer-objects/item.dto.ts
  - trade-flow-api/src/item/repositories/item.repository.ts
  - trade-flow-api/src/item/requests/create-item.request.ts
  - trade-flow-api/src/item/requests/update-item.request.ts
  - trade-flow-api/src/item/responses/item.response.ts
  - trade-flow-api/src/item/controllers/mappers/map-create-item-request-to-dto.utility.ts
  - trade-flow-api/src/item/controllers/mappers/map-item-to-response.utility.ts
  - trade-flow-api/src/item/controllers/mappers/merge-existing-item-with-changes.utility.ts
  - trade-flow-api/src/item/item.module.ts
  - trade-flow-api/src/item/test/repositories/item.repository.spec.ts
  - trade-flow-api/src/item/test/controllers/mappers/map-create-item-request-to-dto.utility.spec.ts
  - trade-flow-api/src/item/test/controllers/mappers/map-item-to-response.utility.spec.ts
  - trade-flow-api/src/item/test/controllers/mappers/merge-existing-item-with-changes.utility.spec.ts
autonomous: true
requirements: [ITAX-01, ITAX-04]

must_haves:
  truths:
    - "Item entity stores taxRateId (ObjectId) instead of defaultTaxRate (number)"
    - "Item DTO stores taxRateId (string) instead of defaultTaxRate (number)"
    - "Item repository correctly maps between entity taxRateId (ObjectId) and DTO taxRateId (string)"
    - "Item repository $set in update() persists taxRateId instead of defaultTaxRate"
    - "CreateItemRequest accepts taxRateId (string) instead of defaultTaxRate (number)"
    - "UpdateItemRequest accepts taxRateId (string) instead of defaultTaxRate (number)"
    - "Item API response includes taxRateId (string) instead of defaultTaxRate (number)"
    - "Bundle items still have null taxRateId via @ValidateIf conditional"
    - "ItemModule imports TaxRateModule for cross-module access"
  artifacts:
    - path: "trade-flow-api/src/item/entities/item.entity.ts"
      provides: "IItemEntity with taxRateId: ObjectId | null"
      contains: "taxRateId: ObjectId"
    - path: "trade-flow-api/src/item/data-transfer-objects/item.dto.ts"
      provides: "IItemDto with taxRateId: string | null"
      contains: "taxRateId: string"
    - path: "trade-flow-api/src/item/repositories/item.repository.ts"
      provides: "Repository with taxRateId mapping in toDto, toEntity, and $set"
      contains: "taxRateId"
    - path: "trade-flow-api/src/item/requests/create-item.request.ts"
      provides: "Request with taxRateId string field"
      contains: "taxRateId"
    - path: "trade-flow-api/src/item/responses/item.response.ts"
      provides: "Response with taxRateId string field"
      contains: "taxRateId"
  key_links:
    - from: "trade-flow-api/src/item/repositories/item.repository.ts"
      to: "trade-flow-api/src/item/entities/item.entity.ts"
      via: "toEntity maps DTO taxRateId (string) to Entity taxRateId (ObjectId)"
      pattern: "taxRateId.*new ObjectId"
    - from: "trade-flow-api/src/item/repositories/item.repository.ts"
      to: "trade-flow-api/src/item/data-transfer-objects/item.dto.ts"
      via: "toDto maps Entity taxRateId (ObjectId) to DTO taxRateId (string)"
      pattern: "taxRateId.*toString"
    - from: "trade-flow-api/src/item/controllers/mappers/map-create-item-request-to-dto.utility.ts"
      to: "trade-flow-api/src/item/data-transfer-objects/item.dto.ts"
      via: "Maps request taxRateId to DTO taxRateId"
      pattern: "taxRateId"
---

<objective>
Replace the numeric `defaultTaxRate` field with a `taxRateId` string/ObjectId reference across the entire item data layer: entity, DTO, repository, requests, response, and all controller mappers.

Purpose: This is the foundational change that all other plans depend on. Every layer of the item module must speak the same language (taxRateId) before validation, onboarding, or quote resolution can be updated.

Output: Item entity, DTO, repository, requests, response, and mappers all use `taxRateId` instead of `defaultTaxRate`. ItemModule imports TaxRateModule. All mapper tests pass.
</objective>

<execution_context>
@/Users/mcrankshaw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mcrankshaw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-item-tax-rate-api/09-CONTEXT.md
@.planning/phases/09-item-tax-rate-api/09-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/item/entities/item.entity.ts (MODIFY):
```typescript
// CURRENT: defaultTaxRate: number | null;
// CHANGE TO: taxRateId: ObjectId | null;
export interface IItemEntity extends IBaseEntity {
  businessId: ObjectId;
  type: ItemType;
  name: string;
  description: string | null;
  unit: string;
  defaultPrice: number | null;
  defaultTaxRate: number | null;  // -> taxRateId: ObjectId | null
  status: ItemStatus;
  bundleConfig: IItemBundleConfigEntity | null;
}
```

From src/item/data-transfer-objects/item.dto.ts (MODIFY):
```typescript
// CURRENT: defaultTaxRate: number | null;
// CHANGE TO: taxRateId: string | null;
export interface IItemDto extends IBaseResourceDto {
  // ... other fields ...
  defaultTaxRate: number | null;  // -> taxRateId: string | null
}
```

From src/tax-rate/tax-rate.module.ts (IMPORT):
```typescript
@Module({
  exports: [TaxRateRetrieverService, TaxRateCreatorService],
})
export class TaxRateModule {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace defaultTaxRate with taxRateId across entity, DTO, repository, and module</name>
  <files>
    trade-flow-api/src/item/entities/item.entity.ts,
    trade-flow-api/src/item/data-transfer-objects/item.dto.ts,
    trade-flow-api/src/item/repositories/item.repository.ts,
    trade-flow-api/src/item/item.module.ts,
    trade-flow-api/src/item/test/repositories/item.repository.spec.ts
  </files>
  <behavior>
    - toDto: entity with taxRateId ObjectId -> DTO with taxRateId string
    - toDto: entity with taxRateId null -> DTO with taxRateId null
    - toEntity: DTO with taxRateId string -> entity with taxRateId ObjectId
    - toEntity: DTO with taxRateId null -> entity with taxRateId null
    - update $set includes taxRateId (not defaultTaxRate)
  </behavior>
  <action>
    1. In `item.entity.ts`: Replace `defaultTaxRate: number | null` with `taxRateId: ObjectId | null`. The ObjectId import already exists.

    2. In `item.dto.ts`: Replace `defaultTaxRate: number | null` with `taxRateId: string | null`. Update the JSDoc comment to describe it as a reference to a tax rate entity (UI resolves details client-side). Remove the Money import if it was only used by defaultTaxRate (it isn't -- defaultPrice uses it).

    3. In `item.repository.ts`:
       - In `toDto()` (line ~99): Replace `defaultTaxRate: item.defaultTaxRate` with `taxRateId: item.taxRateId?.toString() ?? null`
       - In `toEntity()` (line ~128): Replace `defaultTaxRate: dto.defaultTaxRate` with `taxRateId: dto.taxRateId ? new ObjectId(dto.taxRateId) : null`
       - In `update()` $set (line ~73): Replace `defaultTaxRate: entityToUpdate.defaultTaxRate` with `taxRateId: entityToUpdate.taxRateId`

    4. In `item.module.ts`: Add `import { TaxRateModule } from "@tax-rate/tax-rate.module"` and add `TaxRateModule` to the imports array (alongside CoreModule and UserModule). This is needed for Plans 02 and 03 which inject TaxRateRetrieverService into item services.

    5. Create test file `src/item/test/repositories/item.repository.spec.ts` following the project's NestJS test pattern (see `business-creator.service.spec.ts` for structure). Test the toDto and toEntity mapping for taxRateId, including null cases. Test that update $set includes taxRateId.

    IMPORTANT: The repository methods `toDto` and `toEntity` are private. To test them, test through the public methods: `create` (exercises toEntity then toDto), `findByIdOrFail` (exercises toDto), and `update` (exercises toEntity and $set). Mock `MongoDbFetcher` and `MongoDbWriter`.
  </action>
  <verify>
    <automated>cd /Users/mcrankshaw/PersonalProjects/trade-flow-api && npm run test -- --testPathPattern="item.repository" --no-coverage</automated>
  </verify>
  <done>Entity uses taxRateId (ObjectId | null), DTO uses taxRateId (string | null), repository maps between them correctly including $set, ItemModule imports TaxRateModule, repository tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace defaultTaxRate with taxRateId in requests, response, and all controller mappers</name>
  <files>
    trade-flow-api/src/item/requests/create-item.request.ts,
    trade-flow-api/src/item/requests/update-item.request.ts,
    trade-flow-api/src/item/responses/item.response.ts,
    trade-flow-api/src/item/controllers/mappers/map-create-item-request-to-dto.utility.ts,
    trade-flow-api/src/item/controllers/mappers/map-item-to-response.utility.ts,
    trade-flow-api/src/item/controllers/mappers/merge-existing-item-with-changes.utility.ts,
    trade-flow-api/src/item/test/controllers/mappers/map-create-item-request-to-dto.utility.spec.ts,
    trade-flow-api/src/item/test/controllers/mappers/map-item-to-response.utility.spec.ts,
    trade-flow-api/src/item/test/controllers/mappers/merge-existing-item-with-changes.utility.spec.ts
  </files>
  <behavior>
    - mapCreateItemRequestToDto: request with taxRateId string -> DTO with taxRateId string
    - mapCreateItemRequestToDto: bundle request without taxRateId -> DTO with taxRateId null
    - mapItemToResponse: DTO with taxRateId string -> response with taxRateId string
    - mapItemToResponse: DTO with taxRateId null -> response with taxRateId null
    - mergeExistingItemWithChanges: update with new taxRateId -> merged item has new taxRateId
    - mergeExistingItemWithChanges: update without taxRateId -> merged item keeps existing taxRateId
    - CreateItemRequest validates taxRateId as @IsString @IsNotEmpty for non-bundles
    - UpdateItemRequest validates taxRateId as @IsString @IsNotEmpty @IsOptional for non-bundles
  </behavior>
  <action>
    1. In `create-item.request.ts`: Replace `defaultTaxRate` field with `taxRateId`:
       ```
       @ValidateIf((o) => o.type !== ItemType.BUNDLE)
       @IsString()
       @IsNotEmpty()
       taxRateId?: string;
       ```
       Remove `@IsNumber` import if no longer needed (check: `defaultPrice` still uses it, so keep it).

    2. In `update-item.request.ts`: Replace `defaultTaxRate` field with `taxRateId`:
       ```
       @IsOptional()
       @ValidateIf((o) => o.type !== ItemType.BUNDLE)
       @IsString()
       @IsNotEmpty()
       taxRateId?: string;
       ```

    3. In `item.response.ts`: Replace `defaultTaxRate: number | null` with `taxRateId: string | null`.

    4. In `map-create-item-request-to-dto.utility.ts`: Replace line `defaultTaxRate: request.defaultTaxRate === undefined ? null : request.defaultTaxRate` with `taxRateId: request.taxRateId === undefined ? null : request.taxRateId`.

    5. In `map-item-to-response.utility.ts`: Replace `defaultTaxRate: item.defaultTaxRate === null ? null : item.defaultTaxRate` with `taxRateId: item.taxRateId`. (No conversion needed -- DTO and response are both strings.)

    6. In `merge-existing-item-with-changes.utility.ts`:
       - Replace `defaultTaxRate: mergeDefaultTaxRate(...)` with `taxRateId: mergeTaxRateId(...)`
       - Rename and rewrite `mergeDefaultTaxRate` to `mergeTaxRateId`:
         ```typescript
         const mergeTaxRateId = (existingTaxRateId: string | null, newTaxRateId?: string): string | null => {
           if (newTaxRateId !== undefined) {
             return newTaxRateId;
           }
           return existingTaxRateId;
         };
         ```

    7. Create test files for all three mappers following NestJS test patterns. These are pure utility functions (no DI needed), so tests are straightforward:
       - `map-create-item-request-to-dto.utility.spec.ts`: Test taxRateId mapping for standard items and bundles
       - `map-item-to-response.utility.spec.ts`: Test taxRateId in response for non-null and null cases
       - `merge-existing-item-with-changes.utility.spec.ts`: Test taxRateId merge when provided and when omitted

    IMPORTANT: Create a shared mock generator `src/item/test/mocks/item-mock-generator.ts` that provides factory functions for creating test IItemDto instances with taxRateId. Follow the pattern from `src/business/test/mocks/business-mock-generator.ts` if it exists.
  </action>
  <verify>
    <automated>cd /Users/mcrankshaw/PersonalProjects/trade-flow-api && npm run test -- --testPathPattern="map-create-item-request|map-item-to-response|merge-existing-item" --no-coverage</automated>
  </verify>
  <done>All request classes use taxRateId (string), response uses taxRateId (string), all three mappers correctly map taxRateId, bundle items get null taxRateId. All mapper tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
cd /Users/mcrankshaw/PersonalProjects/trade-flow-api && npm run typecheck:strict
```

TypeScript compilation must pass with no errors related to defaultTaxRate (the old field should not exist anywhere in the item module).

```bash
cd /Users/mcrankshaw/PersonalProjects/trade-flow-api && npm run test -- --testPathPattern="item" --no-coverage
```

All item-related tests pass.
</verification>

<success_criteria>
- No references to `defaultTaxRate` remain in the item module (entity, DTO, repository, requests, responses, mappers)
- `taxRateId` is used consistently: ObjectId in entity, string everywhere else
- Bundle items have null taxRateId (validated via @ValidateIf)
- Repository $set updates taxRateId (not the old field name)
- ItemModule imports TaxRateModule
- All new tests pass
- TypeScript strict compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-item-tax-rate-api/09-01-SUMMARY.md`
</output>
