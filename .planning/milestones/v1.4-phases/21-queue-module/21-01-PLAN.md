---
phase: 21-queue-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-api/src/queue/queue.constant.ts
  - trade-flow-api/src/queue/queue.module.ts
  - trade-flow-api/src/queue/services/queue-producer.service.ts
  - trade-flow-api/src/queue/test/services/queue-producer.service.spec.ts
  - trade-flow-api/src/app.module.ts
autonomous: true
requirements: [QUEUE-01, QUEUE-02, QUEUE-03]

must_haves:
  truths:
    - "API boots without errors with QueueModule imported and connected to Redis"
    - "QUEUE_NAMES constant defines ECHO queue name as single source of truth"
    - "QueueProducer service can enqueue echo jobs via typed method"
  artifacts:
    - path: "trade-flow-api/src/queue/queue.constant.ts"
      provides: "Queue name constants"
      contains: "QUEUE_NAMES"
      exports: ["QUEUE_NAMES", "QueueName"]
    - path: "trade-flow-api/src/queue/queue.module.ts"
      provides: "Global queue module with BullMQ Redis connection"
      contains: "@Global()"
      exports: ["QueueModule"]
    - path: "trade-flow-api/src/queue/services/queue-producer.service.ts"
      provides: "Queue producer with typed enqueue methods"
      contains: "enqueueEcho"
      exports: ["QueueProducer"]
    - path: "trade-flow-api/src/queue/test/services/queue-producer.service.spec.ts"
      provides: "Unit tests for QueueProducer"
      contains: "QueueProducer"
    - path: "trade-flow-api/src/app.module.ts"
      provides: "AppModule with QueueModule import and REDIS_URL in config load"
      contains: "QueueModule"
  key_links:
    - from: "trade-flow-api/src/queue/queue.module.ts"
      to: "trade-flow-api/src/queue/queue.constant.ts"
      via: "QUEUE_NAMES.ECHO in BullModule.registerQueue()"
      pattern: "QUEUE_NAMES\\.ECHO"
    - from: "trade-flow-api/src/queue/services/queue-producer.service.ts"
      to: "trade-flow-api/src/queue/queue.constant.ts"
      via: "@InjectQueue(QUEUE_NAMES.ECHO)"
      pattern: "InjectQueue.*QUEUE_NAMES\\.ECHO"
    - from: "trade-flow-api/src/app.module.ts"
      to: "trade-flow-api/src/queue/queue.module.ts"
      via: "QueueModule in imports array"
      pattern: "QueueModule"
    - from: "trade-flow-api/src/queue/queue.module.ts"
      to: "ConfigService"
      via: "BullModule.forRootAsync useFactory with REDIS_URL"
      pattern: 'configService\.get.*REDIS_URL'
---

<objective>
Create the shared QueueModule that connects the NestJS API to Redis via BullMQ, with queue name constants and a QueueProducer service, then wire it into AppModule.

Purpose: Establish the queue infrastructure that both the API (producer) and future worker (consumer) will share. This is the bridge between the API and background job processing.
Output: QueueModule, QueueProducer service, queue constants, unit test, and AppModule wiring.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.4-phases/21-queue-module/21-CONTEXT.md
@.planning/milestones/v1.4-phases/21-queue-module/21-RESEARCH.md
@.planning/milestones/v1.4-phases/20-infrastructure-foundation/20-01-SUMMARY.md
@.planning/milestones/v1.4-phases/20-infrastructure-foundation/20-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From trade-flow-api/src/app.module.ts:
```typescript
// Current ConfigModule.forRoot() load array (line 27-31):
load: [
  () => ({
    MONGO_URL: process.env.MONGO_URL,
  }),
],

// Current imports array includes 16 modules ending with ScheduleModule
```

From trade-flow-api/src/core/core.module.ts (pattern to follow):
```typescript
@Global()
@Module({
  providers: [MongoConnectionService, MongoDbFetcher, MongoDbWriter, AccessControllerFactory, AuthorizedCreatorFactory],
  exports: [MongoConnectionService, MongoDbFetcher, MongoDbWriter, AccessControllerFactory, AuthorizedCreatorFactory],
})
export class CoreModule {}
```

From trade-flow-api/src/core/services/app-logger.service.ts:
```typescript
export class AppLogger { constructor(context: string) }
// Usage: private readonly logger = new AppLogger(ClassName.name);
```

From trade-flow-api/src/email/services/email-sender.service.ts (ConfigService pattern):
```typescript
constructor(private readonly configService: ConfigService) {
  const apiKey = this.configService.get<string>("RESEND_API_KEY");
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create queue constants, QueueModule, QueueProducer service, and unit test</name>
  <files>
    trade-flow-api/src/queue/queue.constant.ts,
    trade-flow-api/src/queue/queue.module.ts,
    trade-flow-api/src/queue/services/queue-producer.service.ts,
    trade-flow-api/src/queue/test/services/queue-producer.service.spec.ts
  </files>
  <read_first>
    trade-flow-api/src/core/core.module.ts,
    trade-flow-api/src/core/services/app-logger.service.ts,
    trade-flow-api/src/email/services/email-sender.service.ts,
    trade-flow-api/tsconfig.json,
    trade-flow-api/package.json
  </read_first>
  <behavior>
    - Test 1: enqueueEcho calls echoQueue.add with job name "echo" and the provided payload
    - Test 2: enqueueEcho passes the exact payload object { message: "test" } to queue.add
  </behavior>
  <action>
    Create four files in `trade-flow-api/src/queue/`:

    **1. `queue.constant.ts`** (per D-06, D-07 — QUEUE-02):
    ```typescript
    export const QUEUE_NAMES = {
      ECHO: "echo",
    } as const;

    export type QueueName = (typeof QUEUE_NAMES)[keyof typeof QUEUE_NAMES];
    ```

    **2. `queue.module.ts`** (per D-01, D-02, D-03, D-08, D-09 — QUEUE-01):
    - Import `Global, Module` from `@nestjs/common`
    - Import `BullModule` from `@nestjs/bullmq`
    - Import `ConfigService` from `@nestjs/config`
    - Import `IORedis` from `ioredis` (DEFAULT import: `import IORedis from "ioredis"`)
    - Import `QUEUE_NAMES` from `@queue/queue.constant`
    - Import `QueueProducer` from `@queue/services/queue-producer.service`
    - Decorate class with `@Global()` and `@Module({})`
    - In imports array, `BullModule.forRootAsync()` MUST come BEFORE `BullModule.registerQueue()`
    - `forRootAsync` config:
      - `useFactory: (configService: ConfigService) => { ... }`
      - `inject: [ConfigService]`
      - Inside factory: `const redisUrl = configService.get<string>("REDIS_URL");`
      - If `!redisUrl`, throw `new Error("REDIS_URL environment variable is not configured. Set REDIS_URL in your .env file.")`
      - Return `{ connection: new IORedis(redisUrl, { maxRetriesPerRequest: null }) }`
    - `BullModule.registerQueue({ name: QUEUE_NAMES.ECHO })`
    - providers: `[QueueProducer]`
    - exports: `[QueueProducer]`

    **3. `services/queue-producer.service.ts`** (per D-04, D-05 — QUEUE-02):
    - Import `Injectable` from `@nestjs/common`
    - Import `InjectQueue` from `@nestjs/bullmq`
    - Import `Queue` from `bullmq`
    - Import `QUEUE_NAMES` from `@queue/queue.constant`
    - Import `AppLogger` from `@core/services/app-logger.service`
    - Class `QueueProducer` with `@Injectable()` decorator
    - Private readonly `logger = new AppLogger(QueueProducer.name)`
    - Constructor: `@InjectQueue(QUEUE_NAMES.ECHO) private readonly echoQueue: Queue`
    - Public async method `enqueueEcho(payload: { message: string }): Promise<void>`:
      - `await this.echoQueue.add("echo", payload);`
      - `this.logger.log("Echo job enqueued", { message: payload.message });`

    **4. `test/services/queue-producer.service.spec.ts`**:
    - Import `Test, TestingModule` from `@nestjs/testing`
    - Import `getQueueToken` from `@nestjs/bullmq`
    - Import `QueueProducer` from `@queue/services/queue-producer.service`
    - Import `QUEUE_NAMES` from `@queue/queue.constant`
    - Create `mockEchoQueue = { add: jest.fn() }`
    - Use `Test.createTestingModule` with `QueueProducer` provider and mock queue via `{ provide: getQueueToken(QUEUE_NAMES.ECHO), useValue: mockEchoQueue }`
    - `afterEach(() => jest.clearAllMocks())`
    - Test: "should add a job to the echo queue" — call `enqueueEcho({ message: "test" })`, expect `mockEchoQueue.add` called with `"echo"` and `{ message: "test" }`

    Delete `trade-flow-api/src/queue/.gitkeep` after creating the real files.
  </action>
  <verify>
    <automated>cd trade-flow-api && npm test -- --testPathPattern queue-producer 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `trade-flow-api/src/queue/queue.constant.ts` contains `export const QUEUE_NAMES = {` and `ECHO: "echo"` and `} as const`
    - `trade-flow-api/src/queue/queue.constant.ts` contains `export type QueueName =`
    - `trade-flow-api/src/queue/queue.module.ts` contains `@Global()` decorator
    - `trade-flow-api/src/queue/queue.module.ts` contains `BullModule.forRootAsync(` before `BullModule.registerQueue(`
    - `trade-flow-api/src/queue/queue.module.ts` contains `maxRetriesPerRequest: null`
    - `trade-flow-api/src/queue/queue.module.ts` contains `import IORedis from "ioredis"` (default import)
    - `trade-flow-api/src/queue/queue.module.ts` contains `throw new Error("REDIS_URL environment variable is not configured`
    - `trade-flow-api/src/queue/queue.module.ts` contains `exports: [QueueProducer]`
    - `trade-flow-api/src/queue/services/queue-producer.service.ts` contains `@InjectQueue(QUEUE_NAMES.ECHO)`
    - `trade-flow-api/src/queue/services/queue-producer.service.ts` contains `async enqueueEcho(payload: { message: string }): Promise<void>`
    - `trade-flow-api/src/queue/services/queue-producer.service.ts` contains `this.echoQueue.add("echo", payload)`
    - `trade-flow-api/src/queue/test/services/queue-producer.service.spec.ts` contains `getQueueToken(QUEUE_NAMES.ECHO)`
    - `npm test -- --testPathPattern queue-producer` exits 0
    - `trade-flow-api/src/queue/.gitkeep` no longer exists
  </acceptance_criteria>
  <done>
    Queue constants define ECHO queue name, QueueModule is @Global with BullModule.forRootAsync (ConfigService + fail-fast REDIS_URL) and registerQueue, QueueProducer has typed enqueueEcho method, unit test passes proving queue.add is called correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire QueueModule into AppModule with REDIS_URL in ConfigModule load</name>
  <files>trade-flow-api/src/app.module.ts</files>
  <read_first>
    trade-flow-api/src/app.module.ts,
    trade-flow-api/src/queue/queue.module.ts
  </read_first>
  <action>
    Modify `trade-flow-api/src/app.module.ts` (per D-08, D-10 — QUEUE-03):

    **1. Add import statement** at top of file, after the ScheduleModule import:
    ```typescript
    import { QueueModule } from "@queue/queue.module";
    ```

    **2. Add REDIS_URL to ConfigModule.forRoot() load array** (line ~28-30):
    Change the load callback from:
    ```typescript
    load: [
      () => ({
        MONGO_URL: process.env.MONGO_URL,
      }),
    ],
    ```
    To:
    ```typescript
    load: [
      () => ({
        MONGO_URL: process.env.MONGO_URL,
        REDIS_URL: process.env.REDIS_URL,
      }),
    ],
    ```

    **3. Add QueueModule to imports array**, after ScheduleModule (last in the list):
    ```typescript
    ScheduleModule,
    QueueModule,
    ```
  </action>
  <verify>
    <automated>cd trade-flow-api && npm run validate 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `trade-flow-api/src/app.module.ts` contains `import { QueueModule } from "@queue/queue.module"`
    - `trade-flow-api/src/app.module.ts` contains `REDIS_URL: process.env.REDIS_URL`
    - `trade-flow-api/src/app.module.ts` contains `QueueModule` in the imports array (after ScheduleModule)
    - `npm run validate` exits 0 (typecheck + lint pass)
    - `npm test` exits 0 (full test suite including queue-producer tests)
  </acceptance_criteria>
  <done>
    AppModule imports QueueModule, REDIS_URL is loaded via ConfigModule.forRoot() load callback alongside MONGO_URL, full validate and test suite pass. API is ready to connect to Redis on startup.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run validate` passes (typecheck + lint) — confirms all imports resolve and code is clean
2. `npm test` passes (full suite including queue-producer tests) — confirms no regressions
3. Files exist: `src/queue/queue.constant.ts`, `src/queue/queue.module.ts`, `src/queue/services/queue-producer.service.ts`, `src/queue/test/services/queue-producer.service.spec.ts`
4. `src/app.module.ts` contains QueueModule import and REDIS_URL in config load
</verification>

<success_criteria>
- QueueModule is @Global and exports QueueProducer (QUEUE-01)
- QUEUE_NAMES.ECHO constant is the single source of truth for queue names (QUEUE-02)
- AppModule imports QueueModule and loads REDIS_URL via ConfigService (QUEUE-03)
- QueueProducer.enqueueEcho() is tested and passes
- `npm run validate` and `npm test` both exit 0
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.4-phases/21-queue-module/21-01-SUMMARY.md`
</output>
