---
phase: 23-developer-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-api/nodemon-worker.json
  - trade-flow-api/package.json
autonomous: true
requirements:
  - DEVX-01
  - DEVX-02

must_haves:
  truths:
    - "npm run worker:dev starts nodemon with worker-specific config and restarts on src/ changes"
    - "npm run worker:prod runs the compiled worker entry point without dev tooling"
    - "Worker debug port 9230 does not collide with API debug port 9229"
  artifacts:
    - path: "trade-flow-api/nodemon-worker.json"
      provides: "Worker hot-reload configuration"
      contains: "src/worker.ts"
    - path: "trade-flow-api/package.json"
      provides: "worker:dev and worker:prod npm scripts"
      contains: "worker:dev"
  key_links:
    - from: "package.json worker:dev"
      to: "nodemon-worker.json"
      via: "nodemon --config nodemon-worker.json"
      pattern: "nodemon --config nodemon-worker\\.json"
    - from: "nodemon-worker.json exec"
      to: "src/worker.ts"
      via: "node -r ts-node/register src/worker.ts"
      pattern: "ts-node/register src/worker\\.ts"
---

<objective>
Create the worker hot-reload development config and npm scripts so the worker process can be started independently for development and production.

Purpose: Developers need `npm run worker:dev` (hot reload) and `npm run worker:prod` (compiled) to run the worker alongside the API without Docker.
Output: `nodemon-worker.json` (new file), updated `package.json` scripts section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.4-phases/23-developer-experience/23-CONTEXT.md
@.planning/milestones/v1.4-phases/23-developer-experience/23-RESEARCH.md

@trade-flow-api/nodemon.json
@trade-flow-api/package.json
@trade-flow-api/worker-cli.json

<interfaces>
<!-- Existing nodemon.json (template for worker version): -->
```json
{
  "watch": ["src"],
  "ext": "ts",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "node --inspect -r ts-node/register src/main.ts",
  "delay": "1500",
  "signal": "SIGTERM"
}
```

<!-- Existing package.json scripts (relevant subset): -->
```json
"start:dev": "nest start --watch",
"start:prod": "node dist/main",
"build:worker": "nest build --config worker-cli.json",
"start:worker": "nest start --config worker-cli.json",
"start:worker:dev": "nest start --config worker-cli.json --watch"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create nodemon-worker.json and add worker npm scripts</name>
  <files>trade-flow-api/nodemon-worker.json, trade-flow-api/package.json</files>
  <read_first>
    - trade-flow-api/nodemon.json (template -- replicate structure exactly, changing only entry point and debug port)
    - trade-flow-api/package.json (current scripts section -- add new scripts without modifying existing ones)
    - trade-flow-api/src/worker.ts (confirm entry point path exists)
  </read_first>
  <action>
1. Create `trade-flow-api/nodemon-worker.json` with this exact content (per D-10, D-11):

```json
{
  "watch": ["src"],
  "ext": "ts",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "node --inspect=9230 -r ts-node/register src/worker.ts",
  "delay": "1500",
  "signal": "SIGTERM"
}
```

Key differences from `nodemon.json`:
- `--inspect=9230` instead of `--inspect` (port 9230 avoids collision with API on 9229)
- `src/worker.ts` instead of `src/main.ts`
- Watch full `src/` (worker imports from @core/*, @queue/*, @worker/* -- shared module changes must trigger restart)
- Same delay (1500ms) and signal (SIGTERM) as API config

2. In `trade-flow-api/package.json`, add these two scripts to the "scripts" object (per D-11, D-12):

```json
"worker:dev": "nodemon --config nodemon-worker.json",
"worker:prod": "node dist/worker"
```

Also add a convenience script for building both targets (used by Dockerfile builder stage):

```json
"build:all": "npm run build && npm run build:worker"
```

Place `worker:dev` and `worker:prod` after the existing `start:worker:dev` line. Place `build:all` after `build:worker`.

Do NOT modify or remove any existing scripts.
  </action>
  <verify>
    <automated>cd /Users/mcrankshaw/PersonalProjects/trade-flow-api && node -e "const p=require('./package.json'); const s=p.scripts; console.log('worker:dev=', s['worker:dev']); console.log('worker:prod=', s['worker:prod']); console.log('build:all=', s['build:all']);" && node -e "const n=require('./nodemon-worker.json'); console.log('exec=', n.exec); console.log('signal=', n.signal);" && npm run validate</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-api/nodemon-worker.json exists and contains `"exec": "node --inspect=9230 -r ts-node/register src/worker.ts"`
    - trade-flow-api/nodemon-worker.json contains `"watch": ["src"]`
    - trade-flow-api/nodemon-worker.json contains `"signal": "SIGTERM"`
    - trade-flow-api/nodemon-worker.json contains `"delay": "1500"`
    - trade-flow-api/package.json scripts contains `"worker:dev": "nodemon --config nodemon-worker.json"`
    - trade-flow-api/package.json scripts contains `"worker:prod": "node dist/worker"`
    - trade-flow-api/package.json scripts contains `"build:all": "npm run build && npm run build:worker"`
    - All existing scripts in package.json remain unchanged
    - `npm run validate` passes with zero errors
  </acceptance_criteria>
  <done>nodemon-worker.json created with debug port 9230 and worker entry point. package.json has worker:dev, worker:prod, and build:all scripts. Validation passes.</done>
</task>

</tasks>

<verification>
1. `cat trade-flow-api/nodemon-worker.json` shows complete worker config with `src/worker.ts` entry and `--inspect=9230`
2. `node -e "console.log(require('./trade-flow-api/package.json').scripts['worker:dev'])"` prints `nodemon --config nodemon-worker.json`
3. `node -e "console.log(require('./trade-flow-api/package.json').scripts['worker:prod'])"` prints `node dist/worker`
4. `cd trade-flow-api && npm run validate` exits 0
</verification>

<success_criteria>
- nodemon-worker.json exists as a standalone config file for worker hot reload
- worker:dev script invokes nodemon with the worker config
- worker:prod script runs compiled dist/worker.js directly
- build:all script builds both API and worker sequentially
- No regressions in existing scripts or validation
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.4-phases/23-developer-experience/23-01-SUMMARY.md`
</output>
