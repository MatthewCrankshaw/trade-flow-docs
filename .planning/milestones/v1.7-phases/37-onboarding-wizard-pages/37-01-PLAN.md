---
phase: 37-onboarding-wizard-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-ui/src/features/auth/components/OnboardingGuard.tsx
  - trade-flow-ui/src/pages/OnboardingPage.tsx
  - trade-flow-ui/src/features/onboarding/components/OnboardingWizard.tsx
  - trade-flow-ui/src/features/onboarding/components/ProfileStep.tsx
  - trade-flow-ui/src/features/onboarding/components/OnboardingProgress.tsx
  - trade-flow-ui/src/features/onboarding/constants/trades.ts
  - trade-flow-ui/src/features/onboarding/components/index.ts
  - trade-flow-ui/src/features/onboarding/hooks/useOnboardingStep.ts
  - trade-flow-ui/src/features/onboarding/hooks/index.ts
  - trade-flow-ui/src/features/onboarding/index.ts
autonomous: true
requirements:
  - ONBD-01
  - ONBD-05
  - ONBD-06
  - ONBD-07

must_haves:
  truths:
    - "New user without display name is redirected to /onboarding"
    - "User sees Step 1 form with display name input and progress bar at 50%"
    - "Completing Step 1 saves display name via PATCH /v1/user and advances to Step 2"
    - "Existing user with display name AND business bypasses onboarding entirely"
    - "Refreshing mid-wizard resumes at correct step (Step 2 if display name exists but no business)"
  artifacts:
    - path: "trade-flow-ui/src/features/auth/components/OnboardingGuard.tsx"
      provides: "Redirect logic checking displayName and business existence"
      contains: "Navigate to=\"/onboarding\""
    - path: "trade-flow-ui/src/features/onboarding/components/OnboardingWizard.tsx"
      provides: "Main wizard container managing step state"
      contains: "useState"
    - path: "trade-flow-ui/src/features/onboarding/components/ProfileStep.tsx"
      provides: "Step 1 display name form"
      contains: "useForm"
    - path: "trade-flow-ui/src/features/onboarding/components/OnboardingProgress.tsx"
      provides: "Progress bar with step text"
      contains: "role=\"progressbar\""
    - path: "trade-flow-ui/src/features/onboarding/constants/trades.ts"
      provides: "Trade enum values, labels, and icon mappings"
      contains: "TRADE_OPTIONS"
  key_links:
    - from: "OnboardingGuard.tsx"
      to: "useAuth + useCurrentBusiness"
      via: "hook calls to check user state"
      pattern: "useAuth|useCurrentBusiness"
    - from: "OnboardingWizard.tsx"
      to: "ProfileStep.tsx"
      via: "conditional rendering based on step state"
      pattern: "step.*profile"
    - from: "ProfileStep.tsx"
      to: "/v1/user"
      via: "RTK Query mutation for display name save"
      pattern: "updateUser|patchUser"
---

<objective>
Build the onboarding wizard foundation: OnboardingGuard redirect logic, wizard container with step management, ProfileStep (Step 1) form, progress indicator, and trade constants. After this plan, new users are redirected to /onboarding and can complete Step 1 (display name entry).

Purpose: Gates app access for users without completed profiles, establishing the mandatory onboarding flow (ONBD-01, ONBD-05, ONBD-06, ONBD-07).
Output: OnboardingGuard with redirect logic, OnboardingWizard container, ProfileStep form, OnboardingProgress bar, trade constants file, feature barrel exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-onboarding-wizard-pages/37-CONTEXT.md
@.planning/phases/37-onboarding-wizard-pages/37-RESEARCH.md
@.planning/phases/37-onboarding-wizard-pages/37-UI-SPEC.md
@.planning/phases/36-public-landing-page-and-route-restructure/36-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From trade-flow-ui/src/hooks/useAuth.ts:
- `useAuth()` returns `{ user }` where user has `displayName`, `email`, `uid`

From trade-flow-ui/src/hooks/useCurrentBusiness.ts:
- `useCurrentBusiness()` returns the current business object or undefined

From trade-flow-ui/src/services/userApi.ts:
- Contains RTK Query endpoints for user operations (PATCH /v1/user for display name update)

From trade-flow-ui/src/features/business/api/businessApi.ts:
- Contains RTK Query endpoints for business operations (useGetBusinessQuery)

From Phase 36 route structure (36-CONTEXT.md D-08):
- OnboardingGuard wraps both `/onboarding/*` routes AND the main app routes
- `/onboarding/*` routes sit inside the first OnboardingGuard instance
- Main app routes sit inside a second OnboardingGuard instance wrapping PaywallGuard > DashboardLayout

Phase 36 D-06: OnboardingGuard is currently a pass-through shell rendering `<Outlet />` only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create onboarding feature module with trade constants, progress component, and ProfileStep form</name>
  <files>
    trade-flow-ui/src/features/onboarding/constants/trades.ts
    trade-flow-ui/src/features/onboarding/components/OnboardingProgress.tsx
    trade-flow-ui/src/features/onboarding/components/ProfileStep.tsx
    trade-flow-ui/src/features/onboarding/components/index.ts
    trade-flow-ui/src/features/onboarding/hooks/useOnboardingStep.ts
    trade-flow-ui/src/features/onboarding/hooks/index.ts
    trade-flow-ui/src/features/onboarding/index.ts
  </files>
  <read_first>
    trade-flow-ui/src/hooks/useAuth.ts
    trade-flow-ui/src/hooks/useCurrentBusiness.ts
    trade-flow-ui/src/services/userApi.ts
    trade-flow-ui/src/features/business/api/businessApi.ts
    trade-flow-ui/src/lib/utils.ts
    trade-flow-ui/src/components/ui/card.tsx
    trade-flow-ui/src/components/ui/input.tsx
    trade-flow-ui/src/components/ui/label.tsx
    trade-flow-ui/src/components/ui/button.tsx
    trade-flow-ui/src/components/ui/badge.tsx
    trade-flow-ui/src/lib/forms/schemas/customerSchema.ts
    .planning/phases/37-onboarding-wizard-pages/37-UI-SPEC.md
    .planning/phases/37-onboarding-wizard-pages/37-CONTEXT.md
  </read_first>
  <action>
Create the onboarding feature module directory structure:

**1. `src/features/onboarding/constants/trades.ts`** (per D-10, D-11, D-12):
```typescript
import type { LucideIcon } from "lucide-react";
import {
  Wrench, Zap, Flame, Hammer, HardHat,
  PaintBucket, Grid3x3, Trees, Drill, MoreHorizontal,
} from "lucide-react";

export interface TradeOption {
  value: string;
  label: string;
  icon: LucideIcon;
}

export const TRADE_OPTIONS: TradeOption[] = [
  { value: "plumber", label: "Plumber", icon: Wrench },
  { value: "electrician", label: "Electrician", icon: Zap },
  { value: "heating_gas_engineer", label: "Heating & Gas", icon: Flame },
  { value: "carpenter_joiner", label: "Carpenter", icon: Hammer },
  { value: "builder_general_contractor", label: "Builder", icon: HardHat },
  { value: "painter_decorator", label: "Painter", icon: PaintBucket },
  { value: "tiler_flooring", label: "Tiler & Flooring", icon: Grid3x3 },
  { value: "landscaping_gardening", label: "Landscaping", icon: Trees },
  { value: "handyman", label: "Handyman", icon: Drill },
  { value: "other", label: "Other", icon: MoreHorizontal },
];
```

**2. `src/features/onboarding/components/OnboardingProgress.tsx`** (per D-04, UI-SPEC progress bar):
- Props: `interface OnboardingProgressProps { currentStep: number; totalSteps: number; }`
- Renders a container `w-full max-w-[480px] mb-6`
- Inside: flex row with `span` showing "Step {currentStep} of {totalSteps}" in `text-xs text-muted-foreground`
- Below: progress bar track `h-2 w-full bg-muted rounded-full overflow-hidden`
- Inside track: fill div `h-full bg-primary rounded-full transition-all duration-300` with width calculated as `${(currentStep / totalSteps) * 100}%`
- Add `role="progressbar"` with `aria-valuenow={currentStep}`, `aria-valuemin={0}`, `aria-valuemax={totalSteps}` on the track div

**3. `src/features/onboarding/components/ProfileStep.tsx`** (per D-08, D-09, D-17, UI-SPEC Step 1):
- Props: `interface ProfileStepProps { onComplete: () => void; }`
- Uses React Hook Form with Valibot resolver. Schema:
  ```typescript
  const profileSchema = v.object({
    displayName: v.pipe(v.string(), v.trim(), v.minLength(1, "Display name is required")),
  });
  ```
- Form renders inside CardContent area (no Card wrapper -- parent provides it):
  - `div` with `space-y-4`
  - `Label` with text "Display name" (`text-xs font-medium`)
  - `Input` with `placeholder="e.g. Dave"`, `autoFocus`, registered to form
  - Show validation error via `aria-describedby` if present
- CardFooter area: `Button` with `w-full`, text "Continue Setup", disabled when form invalid or submitting
- When submitting: button text changes to "Saving..." with `Loader2` spinner icon (`h-4 w-4 animate-spin`), input disabled
- On submit: call the existing RTK Query mutation for PATCH /v1/user with `{ displayName: formData.displayName }`. Check userApi.ts for the exact mutation name (likely `useUpdateUserMutation` or similar). If no user update mutation exists, create one in `src/services/userApi.ts` that calls `PATCH /v1/user` with body `{ displayName }` and `invalidatesTags: ["User"]`.
- On success: call `onComplete()` (parent advances to Step 2)
- On error: show sonner error toast "Something went wrong. Please try again." and re-enable form

**4. `src/features/onboarding/hooks/useOnboardingStep.ts`** (per D-26, D-27):
- Hook that determines the current wizard step based on user/business state
- Returns `{ initialStep: "profile" | "business"; isLoading: boolean; }`
- Logic: read `useAuth()` for user displayName, read `useGetBusinessQuery()` (or equivalent) for business existence
- If data is loading, return `{ initialStep: "profile", isLoading: true }`
- If user has displayName but no business: return `{ initialStep: "business", isLoading: false }`
- If neither: return `{ initialStep: "profile", isLoading: false }`
- Note: if user has BOTH displayName and business, the OnboardingGuard would not have redirected here, so this case should not occur inside the wizard

**5. Barrel exports:**
- `src/features/onboarding/components/index.ts`: export all components
- `src/features/onboarding/hooks/index.ts`: export `useOnboardingStep`
- `src/features/onboarding/index.ts`: re-export from components, hooks, constants
  </action>
  <verify>
    <automated>cd trade-flow-ui && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `trade-flow-ui/src/features/onboarding/constants/trades.ts` contains `export const TRADE_OPTIONS: TradeOption[]` with exactly 10 entries
    - `trades.ts` contains all enum values: `plumber`, `electrician`, `heating_gas_engineer`, `carpenter_joiner`, `builder_general_contractor`, `painter_decorator`, `tiler_flooring`, `landscaping_gardening`, `handyman`, `other`
    - `OnboardingProgress.tsx` contains `role="progressbar"` and `aria-valuenow`
    - `OnboardingProgress.tsx` contains `Step ${currentStep} of ${totalSteps}` or equivalent template literal
    - `OnboardingProgress.tsx` contains `bg-primary rounded-full` for the progress fill
    - `ProfileStep.tsx` contains `useForm` from react-hook-form
    - `ProfileStep.tsx` contains `valibotResolver` (or `@hookform/resolvers/valibot`)
    - `ProfileStep.tsx` contains `v.minLength(1` for display name validation
    - `ProfileStep.tsx` contains `placeholder` with value `e.g. Dave`
    - `ProfileStep.tsx` contains `Continue Setup` button text
    - `ProfileStep.tsx` contains `Saving...` submitting state text
    - `ProfileStep.tsx` contains `toast.error` or sonner error toast call
    - `ProfileStep.tsx` does NOT import from old onboarding system (`onboardingSlice`, `OnboardingDialogs`, `onboardingMiddleware`, `OnboardingProvider`, `OnboardingContext`)
    - `useOnboardingStep.ts` contains `useAuth` and business query hook call
    - `useOnboardingStep.ts` returns `initialStep` with type `"profile" | "business"`
    - `src/features/onboarding/index.ts` exists and re-exports from components and hooks
  </acceptance_criteria>
  <done>Onboarding feature module exists with trade constants (10 trades), OnboardingProgress component with accessible progress bar, ProfileStep form with React Hook Form + Valibot validation that saves display name via PATCH /v1/user, and useOnboardingStep hook that determines resume step. All files type-check.</done>
</task>

<task type="auto">
  <name>Task 2: Implement OnboardingGuard redirect logic, OnboardingWizard container, and OnboardingPage</name>
  <files>
    trade-flow-ui/src/features/auth/components/OnboardingGuard.tsx
    trade-flow-ui/src/features/onboarding/components/OnboardingWizard.tsx
    trade-flow-ui/src/pages/OnboardingPage.tsx
  </files>
  <read_first>
    trade-flow-ui/src/features/auth/components/OnboardingGuard.tsx
    trade-flow-ui/src/features/auth/components/ProtectedRoute.tsx
    trade-flow-ui/src/App.tsx
    trade-flow-ui/src/hooks/useAuth.ts
    trade-flow-ui/src/hooks/useCurrentBusiness.ts
    trade-flow-ui/src/features/business/api/businessApi.ts
    trade-flow-ui/src/features/onboarding/components/ProfileStep.tsx
    trade-flow-ui/src/features/onboarding/components/OnboardingProgress.tsx
    trade-flow-ui/src/features/onboarding/hooks/useOnboardingStep.ts
    trade-flow-ui/src/components/ui/card.tsx
    .planning/phases/37-onboarding-wizard-pages/37-UI-SPEC.md
    .planning/phases/37-onboarding-wizard-pages/37-CONTEXT.md
    .planning/phases/36-public-landing-page-and-route-restructure/36-CONTEXT.md
  </read_first>
  <action>
**1. Modify `src/features/auth/components/OnboardingGuard.tsx`** (per D-26, D-27, D-28):

Replace the pass-through shell (Phase 36) with redirect logic. The guard wraps two route groups per Phase 36 D-08:
- The `/onboarding/*` routes (guard should NOT redirect if user is already on onboarding path)
- The main app routes (guard SHOULD redirect to /onboarding if incomplete)

Implementation:
```typescript
import { Navigate, Outlet, useLocation } from "react-router-dom";
import { Loader2 } from "lucide-react";
import { useAuth } from "@/hooks/useAuth";
// Import the business query hook (check businessApi.ts for exact name)
```

- Get user from `useAuth()`, business data + loading state from business query hook
- Get current location via `useLocation()`
- While business data is loading: render centered `Loader2` spinner (`flex items-center justify-center min-h-screen`) to prevent flash redirect (Pitfall 4 from RESEARCH.md)
- Check: `const hasDisplayName = Boolean(user?.displayName?.trim())` and `const hasBusiness = Boolean(business)`
- If `hasDisplayName && hasBusiness`: render `<Outlet />` (pass-through, ONBD-07)
- If NOT complete AND current path does NOT start with `/onboarding`: render `<Navigate to="/onboarding" replace />`
- If NOT complete AND current path starts with `/onboarding`: render `<Outlet />` (let user stay on onboarding page)
- This handles the dual-wrap pattern from Phase 36 D-08

**2. Create `src/features/onboarding/components/OnboardingWizard.tsx`** (per D-01 through D-09, D-06):

The wizard container manages step state and renders the appropriate step content.

```typescript
type WizardStep = "profile" | "business" | "loading";
```

- Import `useOnboardingStep` to get `initialStep` and `isLoading`
- While `isLoading`: render the same loading spinner as OnboardingGuard
- Initialize state: `const [step, setStep] = useState<WizardStep>(initialStep)` -- this handles ONBD-06 resume
- If `step === "loading"`: render `<SetupLoadingScreen />` (imported from Plan 02 -- for now, render a placeholder `<div>Loading setup...</div>` that Plan 02 will replace)
- Otherwise render the wizard layout per UI-SPEC:
  - Outer div: `flex flex-col items-center justify-center min-h-screen px-4 py-12 bg-gradient-to-br from-background to-muted/30` (per D-02)
  - Trade Flow logo: `<span className="text-lg font-semibold text-foreground mb-8">Trade Flow</span>` (per D-03)
  - `<OnboardingProgress currentStep={step === "profile" ? 1 : 2} totalSteps={2} />`
  - `<Card className="w-full max-w-[480px]">`
    - `<CardHeader className="px-6 pt-6 pb-0">`
      - Step heading `<h1 className="text-lg font-semibold">`: "What's your name?" for profile, "Tell us about your business" for business (per D-08)
      - Subtitle `<p className="text-sm text-muted-foreground mt-1">`: "We just need a couple of things to get you started." for profile, "This helps us set everything up for you." for business (per D-09)
    - `<CardContent className="px-6 py-6">`
      - `{step === "profile" && <ProfileStep onComplete={() => setStep("business")} />}` (per D-06 no animation)
      - `{step === "business" && <BusinessStep ... />}` (placeholder div for now -- Plan 02 implements BusinessStep)
    - No CardFooter in the wizard itself -- each step component renders its own submit button

**3. Create `src/pages/OnboardingPage.tsx`**:

Simple route-level page shell:
```typescript
import { OnboardingWizard } from "@/features/onboarding";

export function OnboardingPage() {
  return <OnboardingWizard />;
}
```

Check `App.tsx` to confirm the `/onboarding` route exists and points to OnboardingPage (Phase 36 should have set this up). If not, add the route inside the OnboardingGuard wrapper per Phase 36 D-08.

**Important:** Do NOT import from old onboarding system files (`onboardingSlice.ts`, `OnboardingDialogs.tsx`, `onboardingMiddleware.ts`, `OnboardingProvider.tsx`, `OnboardingContext.ts`).
  </action>
  <verify>
    <automated>cd trade-flow-ui && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `OnboardingGuard.tsx` contains `Navigate to="/onboarding"` for redirect
    - `OnboardingGuard.tsx` contains `useAuth` hook call
    - `OnboardingGuard.tsx` contains `useLocation` to detect current path
    - `OnboardingGuard.tsx` contains `Boolean(user?.displayName` or equivalent display name check
    - `OnboardingGuard.tsx` contains `Loader2` for loading state (not flash redirect)
    - `OnboardingGuard.tsx` contains `<Outlet />` for pass-through
    - `OnboardingGuard.tsx` does NOT import from `onboardingSlice` or `OnboardingContext`
    - `OnboardingWizard.tsx` contains `useState` with type `WizardStep`
    - `OnboardingWizard.tsx` contains `useOnboardingStep` hook call
    - `OnboardingWizard.tsx` contains `bg-gradient-to-br from-background to-muted/30`
    - `OnboardingWizard.tsx` contains `Trade Flow` logo text
    - `OnboardingWizard.tsx` contains `max-w-[480px]` for card width constraint
    - `OnboardingWizard.tsx` contains `What's your name?` heading text
    - `OnboardingWizard.tsx` contains `Tell us about your business` heading text
    - `OnboardingWizard.tsx` renders `<ProfileStep` component
    - `OnboardingWizard.tsx` renders `<OnboardingProgress` component
    - `OnboardingPage.tsx` imports from `@/features/onboarding`
    - `OnboardingPage.tsx` renders `<OnboardingWizard />`
  </acceptance_criteria>
  <done>OnboardingGuard redirects incomplete users to /onboarding with loading state protection. OnboardingWizard container manages step state with resume detection, renders centered card layout with gradient background, logo, progress bar, and step content. OnboardingPage route shell exists. Full Step 1 flow works: guard redirect -> wizard -> profile form -> display name save -> advance to Step 2 placeholder. Existing users bypass guard entirely.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `cd trade-flow-ui && npx tsc --noEmit` passes
2. OnboardingGuard correctly redirects: check that it reads both user displayName and business existence before deciding
3. No imports from old onboarding system in any new file
4. Progress bar accessibility: `role="progressbar"` with aria attributes present
5. Trade constants contain all 10 enum values matching API enum
</verification>

<success_criteria>
- New users without display name are redirected to /onboarding by OnboardingGuard
- Step 1 displays "What's your name?" with display name input and "Continue Setup" button
- Submitting Step 1 calls PATCH /v1/user and advances to Step 2 (placeholder)
- Progress bar shows "Step 1 of 2" at 50% fill on Step 1
- Existing users with display name + business bypass guard (render Outlet)
- Refreshing on /onboarding with existing display name but no business resumes at Step 2
- No old onboarding system imports in any new file
</success_criteria>

<output>
After completion, create `.planning/phases/37-onboarding-wizard-pages/37-01-SUMMARY.md`
</output>
