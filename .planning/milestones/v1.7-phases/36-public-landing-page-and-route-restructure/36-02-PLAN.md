---
phase: 36-public-landing-page-and-route-restructure
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - trade-flow-ui/src/features/auth/components/OnboardingGuard.tsx
  - trade-flow-ui/src/features/auth/components/PaywallGuard.tsx
  - trade-flow-ui/src/App.tsx
  - trade-flow-ui/src/pages/LoginPage.tsx
autonomous: false
requirements:
  - LAND-01
  - LAND-05
  - LAND-06

must_haves:
  truths:
    - "App route tree has three guard layers: ProtectedRoute > OnboardingGuard > PaywallGuard"
    - "Landing page is lazy-loaded at root URL via React.lazy with Suspense fallback"
    - "LoginPage and landing page sit outside all auth guards as public routes"
    - "OnboardingGuard and PaywallGuard render only Outlet (pass-through for Phase 37/38)"
    - "Existing SubscriptionGatedLayout remains untouched"
    - "LoginPage defaults to sign-up tab when ?mode=signup query param is present"
    - "Start Free Trial CTA arrives at login page with sign-up mode active"
  artifacts:
    - path: "trade-flow-ui/src/features/auth/components/OnboardingGuard.tsx"
      provides: "Pass-through guard shell for Phase 37"
      contains: "export function OnboardingGuard"
    - path: "trade-flow-ui/src/features/auth/components/PaywallGuard.tsx"
      provides: "Pass-through guard shell for Phase 38"
      contains: "export function PaywallGuard"
    - path: "trade-flow-ui/src/App.tsx"
      provides: "Restructured route tree with nested guards"
      contains: "OnboardingGuard"
    - path: "trade-flow-ui/src/pages/LoginPage.tsx"
      provides: "Login page with mode query param support"
      contains: "useSearchParams"
  key_links:
    - from: "trade-flow-ui/src/App.tsx"
      to: "trade-flow-ui/src/pages/LandingPage.tsx"
      via: "React.lazy dynamic import"
      pattern: 'lazy.*import.*LandingPage'
    - from: "trade-flow-ui/src/App.tsx"
      to: "trade-flow-ui/src/features/auth/components/OnboardingGuard.tsx"
      via: "Route element wrapper"
      pattern: "OnboardingGuard"
    - from: "trade-flow-ui/src/App.tsx"
      to: "trade-flow-ui/src/features/auth/components/PaywallGuard.tsx"
      via: "Route element wrapper"
      pattern: "PaywallGuard"
    - from: "trade-flow-ui/src/pages/LoginPage.tsx"
      to: "useSearchParams"
      via: "query param for default mode"
      pattern: 'searchParams.*get.*mode'
---

<objective>
Restructure the App.tsx route tree to support three-tier access control (public, onboarding, authenticated+subscribed) and create pass-through guard shells for Phases 37 and 38. Update LoginPage to accept `?mode=signup` query param so the "Start Free Trial" CTA lands on the sign-up tab.

Purpose: Establish the route architecture that all remaining v1.7 phases build upon. Guard shells are empty now but provide the insertion points for onboarding redirect (Phase 37) and hard paywall blocking (Phase 38).
Output: Two new guard shell components, restructured App.tsx route tree, and updated LoginPage with mode param support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-public-landing-page-and-route-restructure/36-CONTEXT.md
@.planning/phases/36-public-landing-page-and-route-restructure/36-RESEARCH.md
@.planning/phases/36-public-landing-page-and-route-restructure/36-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create guard shells and restructure App.tsx route tree</name>
  <files>
    trade-flow-ui/src/features/auth/components/OnboardingGuard.tsx
    trade-flow-ui/src/features/auth/components/PaywallGuard.tsx
    trade-flow-ui/src/App.tsx
  </files>
  <read_first>
    trade-flow-ui/src/App.tsx
    trade-flow-ui/src/features/auth/components/ProtectedRoute.tsx
    trade-flow-ui/src/features/auth/components/index.ts
    trade-flow-ui/src/features/auth/index.ts
    trade-flow-ui/src/components/layouts/DashboardLayout.tsx
    .planning/phases/36-public-landing-page-and-route-restructure/36-CONTEXT.md
    .planning/phases/36-public-landing-page-and-route-restructure/36-RESEARCH.md
  </read_first>
  <action>
**Step 1: Create OnboardingGuard.tsx** (per D-06):
```tsx
import { Outlet } from "react-router-dom";

export function OnboardingGuard() {
  return <Outlet />;
}
```
Location: `src/features/auth/components/OnboardingGuard.tsx`

**Step 2: Create PaywallGuard.tsx** (per D-06):
```tsx
import { Outlet } from "react-router-dom";

export function PaywallGuard() {
  return <Outlet />;
}
```
Location: `src/features/auth/components/PaywallGuard.tsx`

**Step 3: Update barrel exports** in `src/features/auth/components/index.ts` and `src/features/auth/index.ts` to export both new guards. Add:
- `export { OnboardingGuard } from "./OnboardingGuard";` (in components/index.ts)
- `export { PaywallGuard } from "./PaywallGuard";` (in components/index.ts)
- Ensure auth/index.ts re-exports them if it uses barrel pattern

**Step 4: Restructure App.tsx route tree** (per D-05, D-07, D-08, D-09):

Read the CURRENT App.tsx first to understand existing route structure and imports. Then restructure:

1. Add lazy import at top: `const LandingPage = lazy(() => import("./pages/LandingPage"));`
2. Add import: `import { Suspense, lazy } from "react";` (add `Suspense` and `lazy` to existing React import)
3. Add imports for guards: `import { OnboardingGuard } from "@/features/auth";` and `import { PaywallGuard } from "@/features/auth";` (or from components path, matching existing import pattern)
4. Create a `PageSpinner` component (can be inline or separate) for the Suspense fallback:
```tsx
function PageSpinner() {
  return (
    <div className="flex items-center justify-center min-h-screen bg-background">
      <Loader2 className="size-8 animate-spin text-muted-foreground" aria-label="Loading" />
    </div>
  );
}
```
Import `Loader2` from `lucide-react`.

5. Restructure the `<Routes>` tree to match D-08:
```tsx
<Routes>
  {/* Public routes -- outside all guards */}
  <Route path="/" element={<Suspense fallback={<PageSpinner />}><LandingPage /></Suspense>} />
  <Route path="/login" element={<LoginPage />} />

  {/* Authenticated routes */}
  <Route element={<ProtectedRoute />}>
    {/* Onboarding guard wraps future onboarding routes */}
    <Route element={<OnboardingGuard />}>
      {/* Phase 37 adds onboarding routes here */}
    </Route>

    {/* Onboarding + Paywall guards wrap business routes */}
    <Route element={<OnboardingGuard />}>
      <Route element={<PaywallGuard />}>
        <Route element={<DashboardLayout />}>
          {/* MOVE all existing business routes here -- dashboard, customers, jobs, etc. */}
          {/* Keep the EXACT same route paths and elements that currently exist */}
        </Route>
      </Route>
    </Route>
  </Route>
</Routes>
```

**CRITICAL per D-07:** Do NOT modify or remove `SubscriptionGatedLayout`. If it currently wraps routes inside `DashboardLayout`, keep it exactly where it is. The existing soft paywall stays as-is. Only ADD the new guard shells around the existing structure.

**CRITICAL:** Preserve ALL existing routes, their paths, and their element components. This is a structural reorganization, not a rewrite. Every route that worked before must still work after. Support routes (if any exist at `/support/*`) should remain inside ProtectedRoute but may or may not need OnboardingGuard/PaywallGuard -- read the current App.tsx to determine placement.

If the current App.tsx has a catch-all redirect (e.g., `<Route path="*" element={<Navigate to="/dashboard" />} />`), keep it inside the ProtectedRoute block.
  </action>
  <verify>
    <automated>cd trade-flow-ui && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-ui/src/features/auth/components/OnboardingGuard.tsx contains `export function OnboardingGuard`
    - trade-flow-ui/src/features/auth/components/OnboardingGuard.tsx contains `<Outlet />`
    - trade-flow-ui/src/features/auth/components/PaywallGuard.tsx contains `export function PaywallGuard`
    - trade-flow-ui/src/features/auth/components/PaywallGuard.tsx contains `<Outlet />`
    - trade-flow-ui/src/App.tsx contains `lazy(() => import("./pages/LandingPage"))`
    - trade-flow-ui/src/App.tsx contains `<Suspense`
    - trade-flow-ui/src/App.tsx contains `<OnboardingGuard />`
    - trade-flow-ui/src/App.tsx contains `<PaywallGuard />`
    - trade-flow-ui/src/App.tsx contains `<ProtectedRoute />`
    - trade-flow-ui/src/App.tsx contains `path="/"`
    - trade-flow-ui/src/App.tsx contains `path="/login"`
    - trade-flow-ui/src/App.tsx contains `path="/dashboard"`
    - trade-flow-ui/src/App.tsx does NOT remove SubscriptionGatedLayout (if it existed before)
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>OnboardingGuard and PaywallGuard pass-through shells exist. App.tsx has three-tier nested route structure with landing page lazy-loaded at root, login as public route, and all business routes wrapped in ProtectedRoute > OnboardingGuard > PaywallGuard > DashboardLayout. All existing routes preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Update LoginPage to accept ?mode=signup query param</name>
  <files>
    trade-flow-ui/src/pages/LoginPage.tsx
  </files>
  <read_first>
    trade-flow-ui/src/pages/LoginPage.tsx
    trade-flow-ui/src/features/auth/components/AuthForm.tsx
  </read_first>
  <action>
Update LoginPage.tsx to read the `mode` query parameter and pass it to AuthForm as the default active tab (per D-12, Pitfall 3).

1. Add import: `import { useSearchParams } from "react-router-dom";` (may already have react-router-dom imports -- add useSearchParams to existing import)
2. Inside the component, read the mode param: `const [searchParams] = useSearchParams();`
3. Derive default mode: `const defaultMode = searchParams.get("mode") === "signup" ? "signup" : "login";`
4. Pass `defaultMode` to AuthForm. Read AuthForm.tsx first to understand:
   - What prop it accepts for the active tab (could be `defaultTab`, `mode`, `initialTab`, or similar)
   - If it uses a controlled or uncontrolled tab pattern
   - If AuthForm does NOT currently accept such a prop, add one:
     - Add an optional `defaultTab?: "login" | "signup"` prop to AuthForm
     - Use it as the default value for the internal tab state: `const [activeTab, setActiveTab] = useState(defaultTab ?? "login")`
     - This is a minimal change -- only add the prop and wire it to the existing state

**If AuthForm uses shadcn Tabs component:** The Tabs component accepts a `defaultValue` prop. If AuthForm passes `defaultValue="login"` to Tabs, change it to `defaultValue={defaultTab ?? "login"}` where `defaultTab` comes from props.

**If AuthForm has separate Login/Signup components toggled by state:** Pass the `defaultMode` as initial state value.

The goal: when a user clicks "Start Free Trial" on the landing page and arrives at `/login?mode=signup`, they see the sign-up form, not the login form.
  </action>
  <verify>
    <automated>cd trade-flow-ui && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - trade-flow-ui/src/pages/LoginPage.tsx contains `useSearchParams`
    - trade-flow-ui/src/pages/LoginPage.tsx contains `searchParams.get("mode")`
    - trade-flow-ui/src/pages/LoginPage.tsx contains `"signup"`
    - AuthForm component (trade-flow-ui/src/features/auth/components/AuthForm.tsx) accepts a prop that controls default tab/mode
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>LoginPage reads ?mode=signup from URL and passes it to AuthForm as the default active tab. Navigating to /login?mode=signup shows the sign-up form. Navigating to /login (no param) shows the login form.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify landing page and route structure in browser</name>
  <files>
    trade-flow-ui/src/App.tsx
  </files>
  <action>
Human verification of the complete Phase 36 deliverables in a running browser. This checkpoint covers both Plan 01 (landing page components) and Plan 02 (route restructure, guard shells, login mode param).

Steps for the human verifier:
1. Start the dev server: `cd trade-flow-ui && npm run dev`
2. Open an incognito/private browser window (no auth session)
3. Navigate to `http://localhost:5173/`
4. Verify: Landing page displays with header (Trade Flow logo, Log in button, Start Free Trial button), hero section ("Run your trade business in one place"), three feature cards (Jobs, Quotes, Scheduling), pricing card (Trade Flow Pro, GBP 6/month), and footer
5. Click "Start Free Trial" in the hero section -- verify it navigates to `/login?mode=signup` and the sign-up form is active
6. Go back, click "Log in" in the header -- verify it navigates to `/login` with the login form active
7. Log in with valid credentials
8. After login, navigate to `http://localhost:5173/` -- verify redirect to `/dashboard`
9. Verify existing app routes still work: `/dashboard`, `/customers`, `/jobs`, `/settings`
10. Run `npx vite build` and check `dist/assets/` -- LandingPage should be in its own chunk file
  </action>
  <verify>
    <automated>cd trade-flow-ui && npx vite build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Landing page renders at root URL for unauthenticated users
    - "Start Free Trial" navigates to /login?mode=signup with sign-up tab active
    - "Log in" navigates to /login with login tab active
    - Authenticated user at root URL is redirected to /dashboard
    - All existing app routes (/dashboard, /customers, /jobs, /settings) still work
    - vite build produces a separate chunk for LandingPage
  </acceptance_criteria>
  <done>Human has verified the landing page renders correctly, CTAs navigate properly, auth redirect works, existing routes are preserved, and bundle isolation is confirmed via build output.</done>
</task>

</tasks>

<verification>
1. `cd trade-flow-ui && npx tsc --noEmit` -- zero TypeScript errors
2. `cd trade-flow-ui && npx eslint .` -- no new lint errors
3. `cd trade-flow-ui && npx vite build` -- builds successfully, LandingPage in separate chunk
4. Manual browser verification of all routes and navigation flows
5. Grep App.tsx for OnboardingGuard, PaywallGuard, ProtectedRoute -- all present
6. Grep LoginPage.tsx for useSearchParams -- present
</verification>

<success_criteria>
- OnboardingGuard and PaywallGuard shell components exist and render Outlet only
- App.tsx route tree matches D-08 structure with three nested guard layers
- Landing page lazy-loaded at root URL, login page as public route
- All existing routes preserved and functional
- LoginPage reads ?mode=signup and defaults to sign-up tab
- Bundle isolation confirmed via vite build chunk inspection
- Existing SubscriptionGatedLayout untouched (per D-07)
</success_criteria>

<output>
After completion, create `.planning/phases/36-public-landing-page-and-route-restructure/36-02-SUMMARY.md`
</output>
