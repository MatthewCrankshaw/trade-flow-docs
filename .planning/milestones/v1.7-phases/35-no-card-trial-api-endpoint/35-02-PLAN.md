---
phase: 35-no-card-trial-api-endpoint
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-api/src/worker/processors/stripe-webhook.processor.ts
  - trade-flow-api/src/subscription/test/processors/stripe-webhook.processor.spec.ts
autonomous: true
requirements: [TRIAL-01, TRIAL-04]

must_haves:
  truths:
    - "customer.subscription.created webhook upserts a local subscription record by stripeCustomerId"
    - "checkout.session.completed webhook uses upsert instead of insert-only, supporting users who had a prior trial"
    - "The webhook processor handles both Checkout-originated and API-originated subscriptions"
  artifacts:
    - path: "trade-flow-api/src/worker/processors/stripe-webhook.processor.ts"
      provides: "customer.subscription.created handler and upserted checkout handler"
      contains: "customer.subscription.created"
    - path: "trade-flow-api/src/subscription/test/processors/stripe-webhook.processor.spec.ts"
      provides: "Unit tests for new and updated webhook handlers"
      contains: "customer.subscription.created"
  key_links:
    - from: "trade-flow-api/src/worker/processors/stripe-webhook.processor.ts"
      to: "SubscriptionRepository.upsertByStripeCustomerId"
      via: "DI injection"
      pattern: "upsertByStripeCustomerId"
---

<objective>
Add customer.subscription.created webhook handler and update checkout.session.completed to use upsert, ensuring the webhook pipeline handles both Checkout-originated and API-originated (trial) subscriptions.

Purpose: Ensures belt-and-suspenders reliability -- the trial endpoint writes synchronously, and the webhook reconciles. Also makes Checkout resilient to pre-existing records (e.g., user starts trial, cancels, then subscribes via Checkout).
Output: Updated webhook processor with tests covering new handler and upsert behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-no-card-trial-api-endpoint/35-CONTEXT.md
@.planning/phases/35-no-card-trial-api-endpoint/35-RESEARCH.md
@.planning/milestones/v1.6-phases/30-stripe-checkout-and-webhooks/30-CONTEXT.md

<interfaces>
<!-- Existing webhook processor structure from Phase 30-02 summary -->

StripeWebhookProcessor (trade-flow-api/src/worker/processors/stripe-webhook.processor.ts):
- @Processor(QUEUE_NAMES.STRIPE_WEBHOOKS)
- Handles 5 event types via switch: checkout.session.completed, customer.subscription.updated, customer.subscription.deleted, invoice.payment_succeeded, invoice.payment_failed
- Injects SubscriptionRepository
- Uses AppLogger

SubscriptionRepository methods available:
- upsertByStripeCustomerId(dto: ISubscriptionDto): Promise<ISubscriptionDto>
- upsertByStripeSubscriptionId(dto: ISubscriptionDto): Promise<ISubscriptionDto>
- findByStripeCustomerId(id: string): Promise<ISubscriptionDto | null>

SubscriptionStatus enum:
- TRIALING = "trialing", ACTIVE = "active", PAST_DUE = "past_due", CANCELED = "canceled", INCOMPLETE = "incomplete"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add customer.subscription.created handler and update checkout.session.completed to upsert</name>
  <files>
    trade-flow-api/src/worker/processors/stripe-webhook.processor.ts,
    trade-flow-api/src/subscription/test/processors/stripe-webhook.processor.spec.ts
  </files>
  <read_first>
    trade-flow-api/src/worker/processors/stripe-webhook.processor.ts,
    trade-flow-api/src/subscription/repositories/subscription.repository.ts,
    trade-flow-api/src/subscription/data-transfer-objects/subscription.dto.ts,
    trade-flow-api/src/subscription/enums/subscription-status.enum.ts
  </read_first>
  <behavior>
    - Test 1: customer.subscription.created event calls upsertByStripeCustomerId with stripeCustomerId, stripeSubscriptionId, status mapped from subscription.status, currentPeriodEnd converted from unix seconds, trialEnd converted from unix seconds (when present), cancelAtPeriodEnd
    - Test 2: customer.subscription.created event with no trial_end sets trialEnd to undefined
    - Test 3: customer.subscription.created extracts stripeCustomerId correctly whether subscription.customer is a string or an object with .id
    - Test 4: checkout.session.completed event uses upsertByStripeCustomerId (not insert-only) so pre-existing records are updated rather than duplicated (per D-07)
    - Test 5: Unrecognized event types are logged and ignored (existing behavior preserved)
  </behavior>
  <action>
    1. Add `customer.subscription.created` case to the event type switch in StripeWebhookProcessor:
       ```typescript
       case "customer.subscription.created": {
         const subscription = event.data.object as Stripe.Subscription;
         const stripeCustomerId = typeof subscription.customer === "string"
           ? subscription.customer
           : subscription.customer.id;

         await this.subscriptionRepository.upsertByStripeCustomerId({
           stripeCustomerId,
           stripeSubscriptionId: subscription.id,
           status: subscription.status as SubscriptionStatus,
           currentPeriodEnd: new Date(subscription.current_period_end * 1000),
           trialEnd: subscription.trial_end
             ? new Date(subscription.trial_end * 1000)
             : undefined,
           cancelAtPeriodEnd: subscription.cancel_at_period_end,
         });
         this.logger.log(`Subscription created for customer ${stripeCustomerId}`);
         break;
       }
       ```

    2. Update the existing `checkout.session.completed` handler (per D-07):
       - Change the repository call from any insert-only method to `upsertByStripeCustomerId`
       - The handler should use `upsertByStripeCustomerId` with the fields it currently writes (stripeSubscriptionId, stripeLatestCheckoutSessionId, status)
       - Preserve all existing field assignments -- only change the repository method to upsert
       - Use `$set` semantics (which upsertByStripeCustomerId already implements via findOneAndUpdate with upsert: true)
       - IMPORTANT: Read the current checkout.session.completed handler carefully to understand what fields it writes and what repository method it calls, then change only the method to upsert

    3. Do NOT add `customer.subscription.trial_will_end` -- explicitly excluded per D-08.

    4. Create or update test file at `src/subscription/test/processors/stripe-webhook.processor.spec.ts`:
       - Check if this file already exists. If it does, add new test cases. If not, create it following existing test patterns.
       - Mock SubscriptionRepository with upsertByStripeCustomerId stub
       - Create mock Stripe events for customer.subscription.created (with trial, without trial, customer as string vs object)
       - Create mock Stripe event for checkout.session.completed to verify upsert is called
       - Test all 5 behaviors listed above
  </action>
  <verify>
    <automated>cd trade-flow-api && npx jest --testPathPattern=stripe-webhook --no-coverage -x && npx jest --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - File `trade-flow-api/src/worker/processors/stripe-webhook.processor.ts` contains `"customer.subscription.created"` as a case in the switch
    - File contains `upsertByStripeCustomerId` called in the customer.subscription.created handler
    - File contains `new Date(subscription.current_period_end * 1000)` in the customer.subscription.created handler
    - File contains `subscription.trial_end` with conditional check (ternary or if) for undefined
    - File does NOT contain `customer.subscription.trial_will_end` (per D-08)
    - The checkout.session.completed handler calls `upsertByStripeCustomerId` (not an insert-only method)
    - Test file exists at `trade-flow-api/src/subscription/test/processors/stripe-webhook.processor.spec.ts` and contains test cases for "customer.subscription.created"
    - `cd trade-flow-api && npx jest --no-coverage` exits 0 with no test failures
  </acceptance_criteria>
  <done>StripeWebhookProcessor handles customer.subscription.created via upsert, checkout.session.completed uses upsert for idempotency, all tests pass including new webhook handler tests and full suite.</done>
</task>

</tasks>

<verification>
1. `cd trade-flow-api && npx jest --no-coverage` -- full suite passes with no regressions
2. `cd trade-flow-api && npx tsc --noEmit` -- TypeScript compilation succeeds
3. grep confirms `customer.subscription.created` is handled and `customer.subscription.trial_will_end` is NOT present
</verification>

<success_criteria>
- customer.subscription.created webhook handler upserts local record by stripeCustomerId with correct field mapping
- checkout.session.completed handler uses upsert (not insert-only) for idempotency
- No customer.subscription.trial_will_end handler exists (deferred)
- All unit tests pass, full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/35-no-card-trial-api-endpoint/35-02-SUMMARY.md`
</output>
