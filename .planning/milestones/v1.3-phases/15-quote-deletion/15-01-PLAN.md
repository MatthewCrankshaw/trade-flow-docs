---
phase: 15-quote-deletion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-api/src/quote/enums/quote-status.enum.ts
  - trade-flow-api/src/quote/enums/quote-transitions.ts
  - trade-flow-api/src/quote/services/quote-transition.service.ts
  - trade-flow-api/src/quote/repositories/quote.repository.ts
  - trade-flow-api/src/quote/dtos/quote.dto.ts
  - trade-flow-api/src/quote/entities/quote.entity.ts
autonomous: true
requirements:
  - QMGT-01

must_haves:
  truths:
    - "DRAFT quotes can transition to DELETED status"
    - "Non-DRAFT quotes cannot transition to DELETED status"
    - "Deleted quotes do not appear in the quote list API response"
    - "deletedAt timestamp is recorded when a quote is deleted"
  artifacts:
    - path: "trade-flow-api/src/quote/enums/quote-status.enum.ts"
      provides: "DELETED enum value"
      contains: "DELETED"
    - path: "trade-flow-api/src/quote/enums/quote-transitions.ts"
      provides: "DRAFT -> DELETED transition"
      contains: "QuoteStatus.DELETED"
    - path: "trade-flow-api/src/quote/repositories/quote.repository.ts"
      provides: "Filter deleted quotes from list"
      contains: "$ne"
  key_links:
    - from: "trade-flow-api/src/quote/enums/quote-transitions.ts"
      to: "trade-flow-api/src/quote/services/quote-transition.service.ts"
      via: "ALLOWED_TRANSITIONS map consumed by transition service"
      pattern: "isValidTransition"
    - from: "trade-flow-api/src/quote/services/quote-transition.service.ts"
      to: "trade-flow-api/src/quote/repositories/quote.repository.ts"
      via: "Service sets deletedAt, repository persists"
      pattern: "deletedAt"
---

<objective>
Add soft-delete support to the quote API by adding DELETED to QuoteStatus enum, allowing DRAFT -> DELETED transition, setting deletedAt timestamp, and filtering deleted quotes from list queries.

Purpose: Backend foundation for quote deletion feature — all UI work depends on these API changes.
Output: Updated enum, transition map, transition service, repository query filter, and DTO/entity with deletedAt field.
</objective>

<execution_context>
@/Users/mcrankshaw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mcrankshaw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-quote-deletion/15-CONTEXT.md
@.planning/phases/15-quote-deletion/15-RESEARCH.md

@trade-flow-api/src/quote/enums/quote-status.enum.ts
@trade-flow-api/src/quote/enums/quote-transitions.ts
@trade-flow-api/src/quote/services/quote-transition.service.ts
@trade-flow-api/src/quote/repositories/quote.repository.ts
@trade-flow-api/src/quote/dtos/quote.dto.ts
@trade-flow-api/src/quote/entities/quote.entity.ts

<interfaces>
<!-- Current QuoteStatus enum (API) -->
From trade-flow-api/src/quote/enums/quote-status.enum.ts:
```typescript
export enum QuoteStatus {
  DRAFT = "draft",
  SENT = "sent",
  ACCEPTED = "accepted",
  REJECTED = "rejected",
  EXPIRED = "expired",
}
```

From trade-flow-api/src/quote/enums/quote-transitions.ts:
```typescript
export const ALLOWED_TRANSITIONS: ReadonlyMap<QuoteStatus, readonly QuoteStatus[]> = new Map([
  [QuoteStatus.DRAFT, [QuoteStatus.SENT]],
  [QuoteStatus.SENT, [QuoteStatus.ACCEPTED, QuoteStatus.REJECTED, QuoteStatus.SENT]],
  [QuoteStatus.ACCEPTED, []],
  [QuoteStatus.REJECTED, []],
  [QuoteStatus.EXPIRED, []],
]);
export const isValidTransition = (from: QuoteStatus, to: QuoteStatus): boolean => { ... };
export const getValidTransitions = (from: QuoteStatus): readonly QuoteStatus[] => { ... };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DELETED status and DRAFT->DELETED transition with tests</name>
  <files>
    trade-flow-api/src/quote/enums/quote-status.enum.ts,
    trade-flow-api/src/quote/enums/quote-transitions.ts,
    trade-flow-api/src/quote/enums/quote-transitions.spec.ts
  </files>
  <behavior>
    - isValidTransition(DRAFT, DELETED) returns true
    - isValidTransition(SENT, DELETED) returns false
    - isValidTransition(ACCEPTED, DELETED) returns false
    - isValidTransition(REJECTED, DELETED) returns false
    - isValidTransition(EXPIRED, DELETED) returns false
    - getValidTransitions(DRAFT) includes DELETED and SENT
    - DELETED has no outgoing transitions (getValidTransitions(DELETED) is empty)
  </behavior>
  <action>
    1. Add `DELETED = "deleted"` to `QuoteStatus` enum in `quote-status.enum.ts`
    2. In `quote-transitions.ts`, add `QuoteStatus.DELETED` to the DRAFT transitions array: `[QuoteStatus.DRAFT, [QuoteStatus.SENT, QuoteStatus.DELETED]]`
    3. Add `[QuoteStatus.DELETED, []]` entry to the map (no outgoing transitions from DELETED)
    4. Create `quote-transitions.spec.ts` with tests for the behaviors listed above. Follow existing test patterns in the trade-flow-api project (Jest).
  </action>
  <verify>
    <automated>cd /Users/mcrankshaw/PersonalProjects/trade-flow-api && npm run test -- --testPathPattern=quote-transitions</automated>
  </verify>
  <done>DELETED enum value exists, DRAFT->DELETED transition is valid, all other statuses cannot transition to DELETED, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add deletedAt field and filter deleted quotes from list</name>
  <files>
    trade-flow-api/src/quote/entities/quote.entity.ts,
    trade-flow-api/src/quote/dtos/quote.dto.ts,
    trade-flow-api/src/quote/services/quote-transition.service.ts,
    trade-flow-api/src/quote/repositories/quote.repository.ts
  </files>
  <action>
    1. In `quote.entity.ts`: Add optional `deletedAt?: Date` field to the quote entity
    2. In `quote.dto.ts`: Add optional `deletedAt?: string` field (ISO string) to the quote DTO. Ensure entity-to-DTO and DTO-to-entity mappings handle the field.
    3. In `quote-transition.service.ts`: In the `transition()` method, add a branch for DELETED target status that sets `deletedAt` to `DateTime.now()` (Luxon). Follow the same pattern used for `sentAt`, `acceptedAt`, `rejectedAt` timestamps.
    4. In `quote.repository.ts`: In `findQuotesByBusinessId()`, add `status: { $ne: QuoteStatus.DELETED }` to the MongoDB filter to exclude deleted quotes from list results. Import QuoteStatus if not already imported.
    5. Run full API validation to confirm no regressions.
  </action>
  <verify>
    <automated>cd /Users/mcrankshaw/PersonalProjects/trade-flow-api && npm run validate</automated>
  </verify>
  <done>deletedAt field exists on entity and DTO, transition service sets deletedAt on DELETED transition, deleted quotes excluded from list query, API validates clean</done>
</task>

</tasks>

<verification>
cd /Users/mcrankshaw/PersonalProjects/trade-flow-api && npm run validate && npm run test -- --testPathPattern=quote
</verification>

<success_criteria>
- QuoteStatus enum includes DELETED
- DRAFT -> DELETED transition allowed, all other -> DELETED transitions rejected
- deletedAt timestamp set when transitioning to DELETED
- Deleted quotes excluded from findQuotesByBusinessId results
- All existing tests pass, new transition tests pass
- `npm run validate` passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/15-quote-deletion/15-01-SUMMARY.md`
</output>
