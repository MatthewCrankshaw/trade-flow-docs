---
phase: 03-schedule-data-model-and-create-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-api/src/schedule/entities/schedule.entity.ts
  - trade-flow-api/src/schedule/data-transfer-objects/schedule.dto.ts
  - trade-flow-api/src/schedule/enum/schedule-status.enum.ts
  - trade-flow-api/src/schedule/requests/create-schedule.request.ts
  - trade-flow-api/src/schedule/responses/schedule.response.ts
  - trade-flow-api/src/schedule/controllers/mappers/map-create-schedule-request-to-dto.utility.ts
  - trade-flow-api/src/schedule/controllers/mappers/map-schedule-to-response.utility.ts
  - trade-flow-api/src/schedule/repositories/schedule.repository.ts
  - trade-flow-api/src/schedule/policies/schedule.policy.ts
  - trade-flow-api/src/schedule/schedule.module.ts
  - trade-flow-api/src/schedule/test/mocks/schedule-mock-generator.ts
  - trade-flow-api/src/app.module.ts
  - trade-flow-api/src/job/job.module.ts
  - trade-flow-api/src/visit-type/visit-type.module.ts
  - trade-flow-api/src/core/errors/error-codes.enum.ts
  - trade-flow-api/src/core/errors/errors-map.constant.ts
  - trade-flow-api/tsconfig.json
  - trade-flow-api/package.json
  - trade-flow-api/src/migration/migrations/20260301000000-create-schedules-indexes.migration.ts
autonomous: true
requirements:
  - SCHED-06
  - SCHED-07

must_haves:
  truths:
    - "A schedule entity can represent a visit with job reference, visit type reference, date, start time, duration, notes, status, and assignee"
    - "The schedule data model uses separate date (YYYY-MM-DD) and startTime (HH:mm) fields matching how tradespeople think"
    - "Duration is stored as integer minutes with 15-minute increment validation (min 15, max 1440)"
    - "The schedule status enum defines all five statuses (scheduled, confirmed, completed, canceled, no_show) with scheduled as the initial value"
    - "The request-to-DTO mapper defaults assigneeId to the authenticated user when omitted (SCHED-06)"
    - "The request-to-DTO mapper defaults durationMinutes to 60 when not provided (SCHED-07)"
    - "The schedules collection has a compound index on (jobId, date, startTime) for chronological queries"
  artifacts:
    - path: "trade-flow-api/src/schedule/entities/schedule.entity.ts"
      provides: "MongoDB document structure for schedules"
      contains: "IScheduleEntity"
    - path: "trade-flow-api/src/schedule/data-transfer-objects/schedule.dto.ts"
      provides: "Inter-layer data transfer contract for schedules"
      contains: "IScheduleDto"
    - path: "trade-flow-api/src/schedule/enum/schedule-status.enum.ts"
      provides: "Status enum with all five values for Phase 4 scaffolding"
      contains: "ScheduleStatus"
    - path: "trade-flow-api/src/schedule/requests/create-schedule.request.ts"
      provides: "Validated request body for schedule creation"
      contains: "CreateScheduleRequest"
    - path: "trade-flow-api/src/schedule/responses/schedule.response.ts"
      provides: "API response shape for schedule data"
      contains: "IScheduleResponse"
    - path: "trade-flow-api/src/schedule/repositories/schedule.repository.ts"
      provides: "MongoDB data access for schedules"
      contains: "ScheduleRepository"
    - path: "trade-flow-api/src/schedule/policies/schedule.policy.ts"
      provides: "Business membership authorization for schedules"
      contains: "SchedulePolicy"
    - path: "trade-flow-api/src/schedule/schedule.module.ts"
      provides: "NestJS module with cross-module imports"
      contains: "ScheduleModule"
    - path: "trade-flow-api/src/schedule/controllers/mappers/map-create-schedule-request-to-dto.utility.ts"
      provides: "Request-to-DTO mapping with smart defaults (SCHED-06, SCHED-07)"
      contains: "mapCreateScheduleRequestToDto"
    - path: "trade-flow-api/src/schedule/controllers/mappers/map-schedule-to-response.utility.ts"
      provides: "DTO-to-response mapping for API output"
      contains: "mapScheduleToResponse"
  key_links:
    - from: "trade-flow-api/src/app.module.ts"
      to: "trade-flow-api/src/schedule/schedule.module.ts"
      via: "NestJS module imports"
      pattern: "ScheduleModule"
    - from: "trade-flow-api/src/schedule/schedule.module.ts"
      to: "trade-flow-api/src/job/job.module.ts"
      via: "forwardRef module import"
      pattern: "JobModule"
    - from: "trade-flow-api/src/schedule/schedule.module.ts"
      to: "trade-flow-api/src/visit-type/visit-type.module.ts"
      via: "forwardRef module import"
      pattern: "VisitTypeModule"
    - from: "trade-flow-api/src/job/job.module.ts"
      to: "trade-flow-api/src/job/services/job-retriever.service.ts"
      via: "exports array"
      pattern: "JobRetrieverService"
    - from: "trade-flow-api/src/visit-type/visit-type.module.ts"
      to: "trade-flow-api/src/visit-type/services/visit-type-retriever.service.ts"
      via: "exports array"
      pattern: "VisitTypeRetrieverService"
---

<objective>
Create the complete schedule module foundation: data model, repository, policy, request/response contracts, mappers with smart defaults, configuration, and MongoDB indexes. This plan builds every layer EXCEPT the creator service and controller (Plan 02).

Purpose: Establishes the schedule as a first-class data type with all plumbing in place. The request-to-DTO mapper implements SCHED-06 (assignee defaults to auth user) and SCHED-07 (duration defaults to 1 hour) at the mapping layer. Cross-module dependencies (JobModule, VisitTypeModule exports) are resolved so Plan 02 can wire the service and controller without configuration blockers.

Output: A compiling `src/schedule/` module skeleton registered in AppModule, with all data contracts, repository, policy, mappers, mock generator, migration, path aliases, and error codes ready for Plan 02's service and controller.
</objective>

<execution_context>
@/Users/mcrankshaw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mcrankshaw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-schedule-data-model-and-create-api/03-RESEARCH.md
@.planning/phases/03-schedule-data-model-and-create-api/03-CONTEXT.md

# Pattern sources to clone from:
@trade-flow-api/src/visit-type/entities/visit-type.entity.ts
@trade-flow-api/src/visit-type/data-transfer-objects/visit-type.dto.ts
@trade-flow-api/src/visit-type/enum/visit-type-status.enum.ts
@trade-flow-api/src/visit-type/requests/create-visit-type.request.ts
@trade-flow-api/src/visit-type/responses/visit-type.response.ts
@trade-flow-api/src/visit-type/controllers/mappers/map-create-visit-type-request-to-dto.utility.ts
@trade-flow-api/src/visit-type/controllers/mappers/map-visit-type-to-response.utility.ts
@trade-flow-api/src/visit-type/repositories/visit-type.repository.ts
@trade-flow-api/src/visit-type/policies/visit-type.policy.ts
@trade-flow-api/src/visit-type/visit-type.module.ts
@trade-flow-api/src/visit-type/test/mocks/visit-type-mock-generator.ts
@trade-flow-api/src/job/job.module.ts
@trade-flow-api/src/app.module.ts
@trade-flow-api/src/core/errors/error-codes.enum.ts
@trade-flow-api/src/core/errors/errors-map.constant.ts
@trade-flow-api/tsconfig.json
@trade-flow-api/package.json
@trade-flow-api/src/migration/migrations/20260222120000-create-visit-types-indexes.migration.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create schedule data model, enum, request, response, and mappers with smart defaults</name>
  <files>
    trade-flow-api/src/schedule/entities/schedule.entity.ts
    trade-flow-api/src/schedule/data-transfer-objects/schedule.dto.ts
    trade-flow-api/src/schedule/enum/schedule-status.enum.ts
    trade-flow-api/src/schedule/requests/create-schedule.request.ts
    trade-flow-api/src/schedule/responses/schedule.response.ts
    trade-flow-api/src/schedule/controllers/mappers/map-create-schedule-request-to-dto.utility.ts
    trade-flow-api/src/schedule/controllers/mappers/map-schedule-to-response.utility.ts
  </files>
  <action>
    Create the schedule data contracts and mappers. Follow the visit-type module pattern exactly for structure, adapting fields per CONTEXT.md decisions.

    **ScheduleStatus enum (`enum/schedule-status.enum.ts`):**
    - Define all five values from VSTAT-01 now (scaffolding for Phase 4):
      - `SCHEDULED = "scheduled"`, `CONFIRMED = "confirmed"`, `COMPLETED = "completed"`, `CANCELED = "canceled"`, `NO_SHOW = "no_show"`
    - Only `SCHEDULED` is used as the initial value in Phase 3

    **Entity (`entities/schedule.entity.ts`) -- `IScheduleEntity extends IBaseEntity`:**
    - Fields: `businessId` (ObjectId), `jobId` (ObjectId), `visitTypeId` (ObjectId | null), `assigneeId` (ObjectId), `date` (string), `startTime` (string), `durationMinutes` (number), `notes` (string | null), `status` (ScheduleStatus)
    - Import `ObjectId` from `mongodb`, `IBaseEntity` from `@core/entities/base.entity`, `ScheduleStatus` from `@schedule/enum/schedule-status.enum`

    **DTO (`data-transfer-objects/schedule.dto.ts`) -- `IScheduleDto extends IBaseResourceDto`:**
    - Same fields as entity but `id: string`, `businessId: string`, `jobId: string`, `visitTypeId: string | null`, `assigneeId: string`
    - Import `IBaseResourceDto` from `@core/data-transfer-objects/base-resource.dto`
    - Add JSDoc comments following the visit-type DTO pattern

    **CreateScheduleRequest (`requests/create-schedule.request.ts`):**
    - Fields with class-validator decorators:
      - `date`: `@IsString() @IsNotEmpty() @Matches(/^\d{4}-\d{2}-\d{2}$/, { message: "date must be in YYYY-MM-DD format" })` -- required
      - `startTime`: `@IsString() @IsNotEmpty() @IsMilitaryTime({ message: "startTime must be in HH:mm format" })` -- required
      - `durationMinutes?`: `@IsOptional() @IsInt({ message: "durationMinutes must be a whole number" }) @Min(15, { message: "durationMinutes must be at least 15" }) @Max(1440, { message: "durationMinutes must be at most 1440" }) @IsDivisibleBy(15, { message: "durationMinutes must be in 15-minute increments" })` -- optional, defaults to 60 in mapper
      - `visitTypeId?`: `@IsOptional() @IsString() @IsNotEmpty() @Length(24, 24)` -- optional
      - `assigneeId?`: `@IsOptional() @IsString() @IsNotEmpty() @Length(24, 24)` -- optional, defaults to auth user in mapper
      - `notes?`: `@IsOptional() @IsString() @MaxLength(2000)` -- optional
    - Import all validators from `class-validator`

    **Response (`responses/schedule.response.ts`) -- `IScheduleResponse`:**
    - Fields: `id: string`, `jobId: string`, `visitTypeId: string | null`, `assigneeId: string`, `date: string`, `startTime: string`, `durationMinutes: number`, `notes: string | null`, `status: string`
    - Independent interface -- does NOT extend DTO (codebase convention)

    **Request-to-DTO mapper (`controllers/mappers/map-create-schedule-request-to-dto.utility.ts`):**
    - Function signature: `mapCreateScheduleRequestToDto(requestBody: CreateScheduleRequest, jobId: string, businessId: string, authUserId: string): IScheduleDto`
    - **SCHED-06:** `assigneeId: requestBody.assigneeId ?? authUserId` -- defaults to the authenticated user
    - **SCHED-07:** `durationMinutes: requestBody.durationMinutes ?? 60` -- defaults to 1 hour
    - Other defaults: `visitTypeId: requestBody.visitTypeId ?? null`, `notes: requestBody.notes ?? null`, `status: ScheduleStatus.SCHEDULED`
    - Generate `id: new ObjectId().toString()`
    - Import `ObjectId` from `mongodb`

    **DTO-to-response mapper (`controllers/mappers/map-schedule-to-response.utility.ts`):**
    - Function signature: `mapScheduleToResponse(schedule: IScheduleDto): IScheduleResponse`
    - Map all fields from DTO to response (simple field copy, same as visit-type pattern)

    Use `@schedule/*` path aliases for all internal imports.
  </action>
  <verify>
    All 7 files exist under `trade-flow-api/src/schedule/`. The `CreateScheduleRequest` has `@IsMilitaryTime` on startTime, `@Matches` regex on date, and `@IsDivisibleBy(15)` on durationMinutes. The request-to-DTO mapper uses `?? authUserId` for assigneeId and `?? 60` for durationMinutes. The DTO-to-response mapper returns `IScheduleResponse`. The enum has all 5 status values.
  </verify>
  <done>
    Schedule data model layer complete. Entity uses ObjectId references, DTO uses string IDs. Request validates date (YYYY-MM-DD regex), startTime (military time), durationMinutes (optional, 15-min increments, 15-1440 range). Request-to-DTO mapper applies SCHED-06 (assignee default) and SCHED-07 (duration default). Response is an independent interface. Status enum scaffolds all 5 values for Phase 4.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create repository, policy, module, mock generator, migration, and all configuration wiring</name>
  <files>
    trade-flow-api/src/schedule/repositories/schedule.repository.ts
    trade-flow-api/src/schedule/policies/schedule.policy.ts
    trade-flow-api/src/schedule/schedule.module.ts
    trade-flow-api/src/schedule/test/mocks/schedule-mock-generator.ts
    trade-flow-api/src/app.module.ts
    trade-flow-api/src/job/job.module.ts
    trade-flow-api/src/visit-type/visit-type.module.ts
    trade-flow-api/src/core/errors/error-codes.enum.ts
    trade-flow-api/src/core/errors/errors-map.constant.ts
    trade-flow-api/tsconfig.json
    trade-flow-api/package.json
    trade-flow-api/src/migration/migrations/20260301000000-create-schedules-indexes.migration.ts
  </files>
  <action>
    Build the data access, authorization, module wiring, test infrastructure, and configuration.

    **ScheduleRepository (`repositories/schedule.repository.ts`):**
    - Clone from `VisitTypeRepository` pattern
    - Collection name: `"schedules"`
    - Inject `MongoDbFetcher`, `MongoDbWriter`, `MongoConnectionService`
    - Implements `IByIdRetrieverRepository, ICreatorRepository`
    - Methods:
      - `create(dto: IScheduleDto): Promise<IScheduleDto>` -- convert to entity, insertOne, return DTO
      - `findByIdOrFail(id: string): Promise<IScheduleDto>` -- findOne by _id, throw `ResourceNotFoundError` if not found
    - Private conversion methods:
      - `toDto(entity: IScheduleEntity): IScheduleDto` -- convert ObjectId fields to strings. Handle `visitTypeId` nullability: `entity.visitTypeId?.toString() ?? null`
      - `toEntity(dto: IScheduleDto): IScheduleEntity` -- convert string IDs to ObjectId. Handle `visitTypeId` nullability: `dto.visitTypeId ? new ObjectId(dto.visitTypeId) : null`. Include `...createAuditFields()`
    - NOTE: No list/update/delete methods yet -- those are Phase 4+ scope. Only `create` and `findByIdOrFail` for Phase 3.

    **SchedulePolicy (`policies/schedule.policy.ts`):**
    - Clone from `VisitTypePolicy` -- extends `BasePolicy<IScheduleDto>`
    - `canCreate`: Check `authUser.businessIds.includes(resource.businessId)` or `isSupportUser(authUser)`
    - `canRead`: Same business membership check
    - `canUpdate`: Same business membership check
    - `canDelete`: Return `false`
    - Add logging in each method matching the visit-type pattern

    **ScheduleModule (`schedule.module.ts`):**
    - Imports: `CoreModule`, `forwardRef(() => UserModule)`, `forwardRef(() => JobModule)`, `forwardRef(() => VisitTypeModule)`
    - Controllers: Empty array `[]` for now (controller added in Plan 02)
    - Providers: `ScheduleRepository`, `SchedulePolicy`
    - Exports: Empty array `[]` for now
    - NOTE: `ScheduleCreatorService` is NOT registered here yet -- that's Plan 02

    **ScheduleMockGenerator (`test/mocks/schedule-mock-generator.ts`):**
    - Clone from `VisitTypeMockGenerator` pattern
    - Methods:
      - `createScheduleDto(overrides)`: Returns IScheduleDto with sensible defaults -- `date: "2026-03-15"`, `startTime: "09:00"`, `durationMinutes: 60`, `visitTypeId: null`, `notes: null`, `status: ScheduleStatus.SCHEDULED`
      - `createScheduleResponse(overrides)`: Returns IScheduleResponse with matching defaults
      - `createCreateScheduleRequest(overrides)`: Returns `new CreateScheduleRequest()` with `date` and `startTime` set, other fields optional
      - `createUserDto(overrides)`: Same pattern as VisitTypeMockGenerator -- includes `businessIds: []`, `supportRoleIds: []`, etc.
    - Import `ObjectId` from `mongodb` for generating IDs

    **Error codes -- MODIFY `error-codes.enum.ts`:**
    - Add to the `ErrorCodes` enum under a `// Schedules` comment:
      - `SCHEDULE_JOB_NOT_FOUND = "SCHEDULE_0"`
      - `SCHEDULE_VISIT_TYPE_NOT_FOUND = "SCHEDULE_1"`
      - `SCHEDULE_ASSIGNEE_NOT_IN_BUSINESS = "SCHEDULE_2"`

    **Error messages -- MODIFY `errors-map.constant.ts`:**
    - Add 3 entries to the `ERRORS_MAP` under a `// Schedules` comment:
      - `SCHEDULE_JOB_NOT_FOUND` -> `"Job not found for schedule creation"`
      - `SCHEDULE_VISIT_TYPE_NOT_FOUND` -> `"Visit type not found for schedule creation"`
      - `SCHEDULE_ASSIGNEE_NOT_IN_BUSINESS` -> `"Assignee does not belong to this business"`

    **Cross-module exports -- MODIFY `job.module.ts`:**
    - Add `JobRetrieverService` to the `exports` array (currently only exports `JobTypeCreatorService`)
    - This is required so ScheduleCreatorService (Plan 02) can inject JobRetrieverService to validate job existence

    **Cross-module exports -- MODIFY `visit-type.module.ts`:**
    - Add `VisitTypeRetrieverService` to the `exports` array (currently only exports `VisitTypeCreatorService`)
    - This is required so ScheduleCreatorService (Plan 02) can inject VisitTypeRetrieverService to validate visit type existence

    **Path aliases -- MODIFY `tsconfig.json`:**
    - Add to `paths`: `"@schedule/*": ["./src/schedule/*"]` and `"@schedule-test/*": ["./src/schedule/test/*"]`
    - Place after the `@visit-type-test/*` entry to maintain alphabetical-ish ordering

    **Jest moduleNameMapper -- MODIFY `package.json`:**
    - Add to `moduleNameMapper`: `"^@schedule/(.*)$": "<rootDir>/schedule/$1"` and `"^@schedule-test/(.*)$": "<rootDir>/schedule/test/$1"`
    - Place after the `@visit-type-test` entry

    **AppModule -- MODIFY `app.module.ts`:**
    - Import `ScheduleModule` from `@schedule/schedule.module`
    - Add `ScheduleModule` to the `imports` array (after `VisitTypeModule`)

    **Migration (`migrations/20260301000000-create-schedules-indexes.migration.ts`):**
    - Clone from `20260222120000-create-visit-types-indexes.migration.ts`
    - Class: `CreateSchedulesIndexesMigration` implementing `IMigration`
    - `migrationName`: `"20260301000000-create-schedules-indexes.migration"`
    - `up(db)` creates 1 index:
      - Compound index on `{ jobId: 1, date: 1, startTime: 1 }` -- supports Phase 6 (SCHED-02) chronological schedule listing by job
    - `down(db)` drops the index: `dropIndex("jobId_1_date_1_startTime_1")`
    - `isBootstrap()` returns `false`
  </action>
  <verify>
    Run `cd trade-flow-api && npx tsc --noEmit` -- compiles without errors. Verify `ScheduleModule` is imported in `app.module.ts`. Verify `@schedule/*` and `@schedule-test/*` path aliases exist in both `tsconfig.json` and `package.json`. Verify `errors-map.constant.ts` contains 3 new schedule error codes. Verify `job.module.ts` exports `JobRetrieverService`. Verify `visit-type.module.ts` exports `VisitTypeRetrieverService`. Verify migration file creates index on `{ jobId: 1, date: 1, startTime: 1 }`. Run `cd trade-flow-api && npm test -- --passWithNoTests` to confirm no existing tests break.
  </verify>
  <done>
    Schedule module foundation is complete and compiles. Repository handles create and findByIdOrFail with proper ObjectId/string conversion. Policy authorizes via business membership. Module imports CoreModule, UserModule, JobModule, and VisitTypeModule. Mock generator provides test fixtures. Error codes registered for cross-module validation failures. JobModule now exports JobRetrieverService, VisitTypeModule now exports VisitTypeRetrieverService. Path aliases work in TypeScript and Jest. Migration creates compound index for chronological queries. No existing tests broken.
  </done>
</task>

</tasks>

<verification>
1. `cd trade-flow-api && npx tsc --noEmit` compiles without errors
2. `cd trade-flow-api && npm test -- --passWithNoTests` passes (no existing test breakage)
3. `IScheduleEntity` has all 9 domain fields (businessId, jobId, visitTypeId, assigneeId, date, startTime, durationMinutes, notes, status)
4. `CreateScheduleRequest` validates date with regex, startTime with IsMilitaryTime, durationMinutes with IsInt+IsDivisibleBy(15)+Min(15)+Max(1440)
5. `mapCreateScheduleRequestToDto` uses `?? authUserId` for assigneeId (SCHED-06) and `?? 60` for durationMinutes (SCHED-07)
6. `ScheduleStatus` enum has 5 values: SCHEDULED, CONFIRMED, COMPLETED, CANCELED, NO_SHOW
7. `ScheduleModule` is imported in `app.module.ts`
8. `@schedule/*` and `@schedule-test/*` aliases registered in `tsconfig.json` AND `package.json` jest config
9. `job.module.ts` exports `JobRetrieverService` alongside `JobTypeCreatorService`
10. `visit-type.module.ts` exports `VisitTypeRetrieverService` alongside `VisitTypeCreatorService`
11. 3 new error codes in `error-codes.enum.ts`: SCHEDULE_0, SCHEDULE_1, SCHEDULE_2
12. Migration creates compound index on `{ jobId: 1, date: 1, startTime: 1 }` in `schedules` collection
</verification>

<success_criteria>
- The schedule module skeleton compiles and is registered in the NestJS application
- Data contracts (entity, DTO, request, response) correctly model the schedule domain per CONTEXT.md decisions
- Smart defaults implemented in the mapper: assignee defaults to auth user (SCHED-06), duration defaults to 60 minutes (SCHED-07)
- Cross-module dependencies resolved: JobRetrieverService and VisitTypeRetrieverService are exported for Plan 02
- Path aliases, error codes, migration, and mock generator all in place
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-schedule-data-model-and-create-api/03-01-SUMMARY.md`
</output>
