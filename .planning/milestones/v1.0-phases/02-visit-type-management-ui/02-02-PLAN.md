---
phase: 02-visit-type-management-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - trade-flow-ui/src/features/visit-types/components/VisitTypesSection.tsx
  - trade-flow-ui/src/features/visit-types/components/VisitTypesTable.tsx
  - trade-flow-ui/src/features/visit-types/components/VisitTypesCardList.tsx
  - trade-flow-ui/src/features/visit-types/components/VisitTypesCardSkeleton.tsx
  - trade-flow-ui/src/features/visit-types/components/VisitTypesStatusFilter.tsx
  - trade-flow-ui/src/features/visit-types/components/ColorPicker.tsx
  - trade-flow-ui/src/features/visit-types/components/VisitTypeFormDialog.tsx
  - trade-flow-ui/src/features/visit-types/components/index.ts
  - trade-flow-ui/src/features/visit-types/hooks/useVisitTypesList.ts
  - trade-flow-ui/src/features/visit-types/hooks/useVisitTypeActions.ts
  - trade-flow-ui/src/features/visit-types/hooks/index.ts
  - trade-flow-ui/src/features/visit-types/index.ts
  - trade-flow-ui/src/features/business/components/BusinessDetails.tsx
autonomous: false
requirements:
  - VTYPE-03
  - VTYPE-04

must_haves:
  truths:
    - "User can navigate to Scheduling tab and see all visit types for their business"
    - "User can create a new custom visit type with name, description, and color via dialog form"
    - "Default visit types display with a Default badge and primary-tinted background"
    - "User can switch between active and inactive views using a status filter"
    - "User can edit description and color of an existing visit type (name is locked)"
    - "User can deactivate/activate visit types from row action menu and dialog action menu"
    - "Attempting to deactivate the last active visit type shows a specific error message"
    - "Table view on desktop (768px+), card list on mobile"
  artifacts:
    - path: "trade-flow-ui/src/features/visit-types/components/VisitTypesSection.tsx"
      provides: "Main orchestrator component with status filter and dialog management"
      exports: ["VisitTypesSection"]
    - path: "trade-flow-ui/src/features/visit-types/components/VisitTypesTable.tsx"
      provides: "Desktop table view with name, description, action menu columns"
      exports: ["VisitTypesTable"]
    - path: "trade-flow-ui/src/features/visit-types/components/VisitTypesCardList.tsx"
      provides: "Mobile card list view"
      exports: ["VisitTypesCardList"]
    - path: "trade-flow-ui/src/features/visit-types/components/VisitTypeFormDialog.tsx"
      provides: "Create/edit dialog with color picker and action menu in edit mode"
      exports: ["VisitTypeFormDialog"]
    - path: "trade-flow-ui/src/features/visit-types/components/ColorPicker.tsx"
      provides: "Preset color palette selector (8 colors)"
      exports: ["ColorPicker", "VISIT_TYPE_COLORS"]
    - path: "trade-flow-ui/src/features/visit-types/hooks/useVisitTypesList.ts"
      provides: "List fetching with client-side status filtering"
      exports: ["useVisitTypesList"]
    - path: "trade-flow-ui/src/features/visit-types/hooks/useVisitTypeActions.ts"
      provides: "Create, update, and toggle status mutations"
      exports: ["useVisitTypeActions"]
  key_links:
    - from: "trade-flow-ui/src/features/visit-types/components/VisitTypesSection.tsx"
      to: "trade-flow-ui/src/features/visit-types/hooks/useVisitTypesList.ts"
      via: "Hook provides filtered visit types and status filter state"
      pattern: "useVisitTypesList"
    - from: "trade-flow-ui/src/features/visit-types/components/VisitTypeFormDialog.tsx"
      to: "trade-flow-ui/src/features/visit-types/hooks/useVisitTypeActions.ts"
      via: "Hook provides create and update mutations"
      pattern: "useVisitTypeActions"
    - from: "trade-flow-ui/src/features/business/components/BusinessDetails.tsx"
      to: "trade-flow-ui/src/features/visit-types/components/VisitTypesSection.tsx"
      via: "Scheduling tab content imports VisitTypesSection"
      pattern: "VisitTypesSection.*businessId"
---

<objective>
Build the complete visit type management UI: responsive list views (table/card), form dialog for create/edit, color picker, status filter, skeleton/empty/error states, deactivate/activate actions, and wire into the Scheduling tab.

Purpose: Deliver the user-facing VTYPE-03 (create custom visit types) and VTYPE-04 (view and manage visit types) requirements as a fully functional management interface.

Output: Complete visit type management UI within the Scheduling tab of Business Details, with responsive views, form dialog, status filter, and action menus.
</objective>

<execution_context>
@/Users/mcrankshaw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mcrankshaw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visit-type-management-ui/02-CONTEXT.md
@.planning/phases/02-visit-type-management-ui/02-RESEARCH.md
@.planning/phases/02-visit-type-management-ui/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build visit types list view with hooks, table, cards, skeleton, status filter, and empty state</name>
  <files>
    trade-flow-ui/src/features/visit-types/hooks/useVisitTypesList.ts
    trade-flow-ui/src/features/visit-types/hooks/useVisitTypeActions.ts
    trade-flow-ui/src/features/visit-types/hooks/index.ts
    trade-flow-ui/src/features/visit-types/components/VisitTypesSection.tsx
    trade-flow-ui/src/features/visit-types/components/VisitTypesTable.tsx
    trade-flow-ui/src/features/visit-types/components/VisitTypesCardList.tsx
    trade-flow-ui/src/features/visit-types/components/VisitTypesCardSkeleton.tsx
    trade-flow-ui/src/features/visit-types/components/VisitTypesStatusFilter.tsx
    trade-flow-ui/src/features/visit-types/components/ColorPicker.tsx
    trade-flow-ui/src/features/visit-types/components/index.ts
    trade-flow-ui/src/features/visit-types/index.ts
  </files>
  <action>
    Build the visit types list view by cloning the JobTypesSection + Items status filter patterns. Reference files:
    - `src/features/job-types/components/JobTypesSection.tsx` (main section structure)
    - `src/features/job-types/components/JobTypesCardList.tsx` (mobile card pattern)
    - `src/features/job-types/components/JobTypesCardSkeleton.tsx` (skeleton pattern)
    - `src/features/items/hooks/useItemsList.ts` (status filter pattern)
    - `src/features/items/hooks/useItemActions.ts` (mutation pattern)

    **useVisitTypesList.ts** -- Clone from `useItemsList.ts` but simpler (no search, no type tabs):
    - Fetch via `useGetVisitTypesQuery(businessId, { skip: !businessId })`
    - State: `statusFilter` ("active" | "inactive"), default "active"
    - `statusFilteredTypes` = filter all visit types by statusFilter
    - `inactiveCount` = count of inactive types (for filter indicator)
    - `isEmpty` = no types at all (regardless of filter)
    - Return: `{ visitTypes, allVisitTypes, isLoading, isEmpty, statusFilter, setStatusFilter, inactiveCount }`
    - List sorted by creation order (no sorting -- rely on backend/MongoDB natural order per CONTEXT.md decision)

    **useVisitTypeActions.ts** -- Clone from items `useItemActions.ts`:
    - Use `useCreateVisitTypeMutation` and `useUpdateVisitTypeMutation` from the API
    - `handleCreate(data: CreateVisitTypeRequest)` -- calls create mutation, shows success/error toast
    - `handleUpdate(visitTypeId: string, data: UpdateVisitTypeRequest)` -- calls update mutation, shows success/error toast
    - `handleToggleStatus(visitType: VisitType)` -- toggles between active/inactive via update mutation
      - On deactivate error: parse the API error response for the last-active guard error and show specific message "Cannot deactivate the last active visit type" (see RESEARCH.md Pitfall 8)
    - Return: `{ createVisitType, updateVisitType, toggleStatus, isCreating, isUpdating }`

    **hooks/index.ts** barrel: export both hooks

    **VisitTypesStatusFilter.tsx** -- Simple filter matching the Items page pattern:
    - Props: `statusFilter`, `onStatusFilterChange`, `inactiveCount`
    - Render a segmented control or button group: "Active" / "Inactive (N)" where N = inactiveCount
    - Use shadcn Button components with variant toggle (outline for inactive selection, default for active)
    - Only show the inactive count badge if inactiveCount > 0

    **ColorPicker.tsx** -- New component (Claude's discretion for implementation):
    - Define `VISIT_TYPE_COLORS` constant array of 8 preset colors:
      ```
      Blue (#3B82F6), Green (#10B981), Amber (#F59E0B), Red (#EF4444),
      Purple (#8B5CF6), Pink (#EC4899), Cyan (#06B6D4), Orange (#F97316)
      ```
    - Props: `value: string`, `onChange: (color: string) => void`
    - Render a grid of circular color swatches (e.g., `h-8 w-8 rounded-full`)
    - Selected swatch has a check icon overlay or ring indicator
    - Use `cn()` for conditional classes
    - Export both `ColorPicker` component and `VISIT_TYPE_COLORS` constant

    **VisitTypesTable.tsx** -- Desktop table view, clone from `JobTypesSection`'s inline JobTypesTable:
    - Props: `visitTypes: VisitType[]`, `onEditVisitType`, `onToggleStatus`
    - Columns: Name, Description, Actions (3-dot menu)
    - Per CONTEXT.md: "Each item shows name and description only (no status display in the row)"
    - Name column: show name text + color swatch (small circle showing the visit type color) + "Default" badge if `isDefault` is true
    - Default types: subtle primary-tinted background on the row (`bg-primary/5` per CONTEXT.md, matching job types)
    - Row is clickable (opens edit dialog via `onEditVisitType`)
    - Action menu (MoreHorizontal dropdown): Edit, separator, Deactivate/Activate (toggle based on current status)
    - Stop propagation on action menu button click to prevent row click

    **VisitTypesCardList.tsx** -- Mobile card view, clone from `JobTypesCardList.tsx`:
    - Props: same as table
    - Each card shows: color swatch, name (with Default badge if applicable), description, action menu
    - Default cards get primary-tinted border/background
    - Cards are clickable to open edit dialog
    - Action menu same as table

    **VisitTypesCardSkeleton.tsx** -- Clone from `JobTypesCardSkeleton.tsx`:
    - Show 3-4 skeleton cards matching the card layout

    **VisitTypesSection.tsx** -- Main orchestrator, clone from `JobTypesSection`:
    - Props: `businessId: string`
    - Uses `useVisitTypesList({ businessId })` for data and filtering
    - Uses `useVisitTypeActions({ businessId })` for mutations
    - Uses `useMediaQuery("(min-width: 768px)")` for responsive switching
    - State: `dialogOpen`, `dialogMode` ("create" | "edit"), `selectedVisitType`
    - Layout:
      - Section header: "Visit Types" title with Calendar icon, description text, + "Create Visit Type" button (top right)
      - Status filter below header (VisitTypesStatusFilter)
      - DataView section: loading ? skeleton : empty ? empty state : desktop ? table : card list
    - Empty state logic (per CONTEXT.md and RESEARCH.md Pitfall 6):
      - If `isEmpty` (no types at all): show illustration + "No visit types yet" + prominent "Create Visit Type" button
      - If not `isEmpty` but filtered list is empty: show "No [active/inactive] visit types" without create button
    - Error state: if the query has an error, show Alert with destructive variant "Failed to load visit types"
    - The form dialog is rendered but NOT created in this task -- use a placeholder comment. Plan 02 Task 2 creates it.
      Actually, to avoid a partial build that can't compile, include a minimal `VisitTypeFormDialog` stub that accepts the right props and renders an empty Dialog. Task 2 will flesh it out.

    **components/index.ts** barrel: export VisitTypesSection and all components

    **Update features/visit-types/index.ts**: add exports for hooks and components:
    ```typescript
    export * from "./api";
    export * from "./hooks";
    export * from "./components";
    ```

    After changes, run: `cd trade-flow-ui && npm run typecheck` to confirm.
  </action>
  <verify>
    <automated>cd /Users/mcrankshaw/PersonalProjects/trade-flow-ui && npm run typecheck</automated>
    <manual>Verify all component files exist and barrel exports are complete</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    - useVisitTypesList hook fetches all types and filters by status client-side
    - useVisitTypeActions hook provides create/update/toggleStatus with toast notifications
    - VisitTypesSection renders header, status filter, and responsive data view
    - VisitTypesTable shows name (with color swatch + Default badge), description, action menu
    - VisitTypesCardList shows equivalent mobile layout
    - VisitTypesCardSkeleton shows loading placeholders
    - VisitTypesStatusFilter switches between active/inactive views
    - ColorPicker renders 8 preset color swatches with selection indicator
    - Empty state differentiates "no types at all" vs "no types matching filter"
    - Default types have primary-tinted row/card background
    - TypeScript compiles with 0 errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Build form dialog, wire into BusinessDetails Scheduling tab, and complete integration</name>
  <files>
    trade-flow-ui/src/features/visit-types/components/VisitTypeFormDialog.tsx
    trade-flow-ui/src/features/business/components/BusinessDetails.tsx
  </files>
  <action>
    Build the full form dialog and wire VisitTypesSection into the Scheduling tab, completing the feature.

    **VisitTypeFormDialog.tsx** -- Clone from `JobTypeFormDialog.tsx` pattern:
    - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `businessId: string`, `visitType: VisitType | null`, `mode: "create" | "edit"`
    - Uses react-hook-form with Valibot resolver (`visitTypeFormSchema` from `@/lib/forms/schemas/visit-type.schema.ts`)
    - Uses `useVisitTypeActions({ businessId })` for mutations

    **Create mode:**
    - Dialog title: "Create Visit Type"
    - Fields: Name (Input, required), Description (Input or Textarea, optional), Color (ColorPicker component)
    - Default color: first color in VISIT_TYPE_COLORS palette (#3B82F6 blue)
    - On submit: call `createVisitType({ name, description, color, icon: "calendar" })` -- icon defaults to "calendar" since CONTEXT.md does not mention icon selection (per RESEARCH.md Pitfall 3)
    - On success: close dialog, toast.success
    - On error: show inline form error if it's a name uniqueness error from API, or toast.error for other failures
    - Server-side uniqueness validation only -- do NOT pre-validate name uniqueness on the client (per CONTEXT.md)

    **Edit mode:**
    - Dialog title: "Edit Visit Type"
    - `useEffect` to reset form when visitType changes (populate form with current values)
    - Name field: visible but **disabled** with helper text "Name cannot be changed after creation" (per CONTEXT.md and RESEARCH.md Pitfall 5)
    - Description and color fields: editable
    - Action menu (MoreHorizontal) in dialog header top-right (per CONTEXT.md -- deactivate/activate available from dialog):
      - "Deactivate" / "Activate" option based on current status
      - Calls `toggleStatus(visitType)` from useVisitTypeActions
    - On submit: call `updateVisitType(visitTypeId, { description, color })` -- only send changed fields
    - On success: close dialog, toast.success
    - On error: toast.error

    **Form structure** (use FormProvider pattern from react-hook-form):
    ```tsx
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent>
        <DialogHeader>
          <div className="flex items-center justify-between">
            <DialogTitle>{mode === "create" ? "Create Visit Type" : "Edit Visit Type"}</DialogTitle>
            {mode === "edit" && visitType && (
              <DropdownMenu>
                <DropdownMenuTrigger asChild>
                  <Button variant="ghost" size="icon"><MoreHorizontal /></Button>
                </DropdownMenuTrigger>
                <DropdownMenuContent>
                  <DropdownMenuItem onClick={() => toggleStatus(visitType)}>
                    {visitType.status === "active" ? "Deactivate" : "Activate"}
                  </DropdownMenuItem>
                </DropdownMenuContent>
              </DropdownMenu>
            )}
          </div>
        </DialogHeader>
        <form onSubmit={handleSubmit(onSubmit)}>
          {/* Name field */}
          {/* Description field */}
          {/* Color picker */}
          <DialogFooter>
            <Button type="button" variant="outline" onClick={() => onOpenChange(false)}>Cancel</Button>
            <Button type="submit" disabled={isSubmitting}>{mode === "create" ? "Create" : "Save"}</Button>
          </DialogFooter>
        </form>
      </DialogContent>
    </Dialog>
    ```

    **Wire into BusinessDetails.tsx:**
    - Replace the Plan 01 placeholder Scheduling tab content with the real component
    - Add import: `import { VisitTypesSection } from "@/features/visit-types";`
    - Replace the entire placeholder `<TabsContent value="scheduling">...</TabsContent>` block with:
    ```tsx
    <TabsContent value="scheduling">
      <VisitTypesSection businessId={businessId} />
    </TabsContent>
    ```
    - Remove the Card, CardHeader, CardContent, CardTitle, CardDescription imports IF they are no longer used elsewhere in the file (they ARE used by BusinessCard/LocationCard so keep them)
    - The Calendar import from lucide-react can remain since it's used in the TabsTrigger

    After all changes, run: `cd trade-flow-ui && npm run lint && npm run typecheck`
  </action>
  <verify>
    <automated>cd /Users/mcrankshaw/PersonalProjects/trade-flow-ui && npm run lint && npm run typecheck</automated>
    <manual>Verify the form dialog renders in create and edit modes, and the Scheduling tab shows VisitTypesSection</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    - VisitTypeFormDialog renders create mode with name, description, color fields
    - VisitTypeFormDialog renders edit mode with name disabled + helper text, description/color editable
    - Edit mode has action menu in dialog header with deactivate/activate option
    - Default icon "calendar" sent with create requests
    - Server-side uniqueness errors surface in the form
    - Last-active guard errors show specific message
    - BusinessDetails Scheduling tab renders VisitTypesSection instead of placeholder
    - Lint passes and TypeScript compiles with 0 errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify visit type management UI end-to-end</name>
  <files>trade-flow-ui/src/features/visit-types/components/VisitTypesSection.tsx</files>
  <action>
    Human verification checkpoint. No code changes -- verify the complete visit type management UI built in Tasks 1-2.

    What was built:
    - Responsive list views (table on desktop, cards on mobile)
    - Status filter for active/inactive views
    - Default visit types with primary-tinted styling and "Default" badge
    - Create dialog with name, description, and color picker
    - Edit dialog with name locked and description/color editable
    - Deactivate/activate from row action menu and dialog action menu
    - Error handling for last-active guard and name uniqueness
    - Skeleton loading, empty state, and error states

    How to verify (prerequisites: both servers running -- `npm run start:dev` in trade-flow-api, `npm run dev` in trade-flow-ui):

    1. Navigate to My Business page, verify "Scheduling" tab appears with Calendar icon
    2. Click Scheduling tab -- default visit types (Quote, Job, Follow-up, Inspection) should appear with "Default" badge, primary-tinted background, color swatches, name and description
    3. Click "Create Visit Type" button -- dialog opens with name (required), description (optional), color picker (8 swatches, blue default)
    4. Create a visit type (e.g., "Emergency Callout") -- appears in list without Default badge
    5. Try creating a duplicate name -- server-side error displays
    6. Click an existing visit type -- edit dialog with name disabled + helper text, description/color editable, action menu in header
    7. Edit description and color, save -- changes reflected in list
    8. From action menu (row or dialog), deactivate a visit type -- toast success
    9. Switch to "Inactive" filter -- deactivated type appears
    10. From inactive view, activate the type -- returns to active view
    11. Try deactivating all until one remains -- specific error "Cannot deactivate the last active visit type"
    12. Resize browser below 768px -- cards replace table

    Resume signal: Type "approved" to complete Phase 2, or describe any issues found.
  </action>
  <verify>
    <automated>cd /Users/mcrankshaw/PersonalProjects/trade-flow-ui && npm run lint && npm run typecheck</automated>
    <manual>Full end-to-end manual verification of all 12 steps above</manual>
  </verify>
  <done>
    - All 12 verification steps pass
    - User approves the visit type management UI
  </done>
</task>

</tasks>

<verification>
1. Frontend: `cd trade-flow-ui && npm run lint && npm run typecheck` passes with 0 errors
2. Backend: `cd trade-flow-api && npm run typecheck:strict` passes with 0 errors
3. VisitTypesSection renders within Scheduling tab of BusinessDetails
4. Create dialog produces new visit types via POST API
5. Edit dialog updates visit types via PATCH API with name locked
6. Status filter switches between active/inactive views client-side
7. Default types show badge and tinted background
8. Last-active guard error surfaced specifically
9. Responsive: table on 768px+, cards below
</verification>

<success_criteria>
- All visit type CRUD operations work end-to-end through the UI
- Responsive layout matches existing JobType/Items patterns
- Status filter works for active/inactive views
- Default types visually distinguished from custom types
- Form validation (client + server) works correctly
- Error messages are specific and helpful
- No TypeScript errors, no lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-visit-type-management-ui/02-02-SUMMARY.md`
</output>
