---
status: diagnosed
phase: 04-schedule-status-and-crud-api
source: 04-03-SUMMARY.md
started: 2026-03-01T18:00:00Z
updated: 2026-03-01T18:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Structured Filter - Status with IN operator
expected: GET /v1/business/:businessId/schedule?filter:status:in=Scheduled,Confirmed returns only schedules with status "Scheduled" or "Confirmed". Other statuses are excluded from results.
result: issue
reported: "false"
severity: major

### 2. Case-Insensitive Status Filtering
expected: GET with ?filter:status:eq=scheduled (lowercase) returns the same results as ?filter:status:eq=Scheduled (title case). The filter is case-insensitive.
result: pass

### 3. Structured Filter - Date Range with BT operator
expected: GET /v1/business/:businessId/schedule?filter:startDateTime:bt=2026-03-01,2026-03-31 returns only schedules with startDateTime between those dates (inclusive).
result: pass

### 4. Combined Filters
expected: GET with both ?filter:status:in=Scheduled&filter:startDateTime:bt=2026-03-01,2026-03-31 returns only schedules matching BOTH conditions (status AND date range).
result: pass

### 5. Invalid Filter Silently Ignored
expected: GET with ?filter:status:xx=Scheduled (invalid operator "xx") or ?filter:::=value (malformed) does not cause an error — endpoint returns results as if no filter was applied.
result: pass

## Summary

total: 5
passed: 4
issues: 1
pending: 0
skipped: 0

## Gaps

- truth: "List endpoints filter by status using structured filter:status:in= query parameter"
  status: failed
  reason: "User reported: false"
  severity: major
  test: 1
  root_cause: "No code defect found. The filter chain (parser → controller → repository) correctly handles filter:status:in=Scheduled,Confirmed end-to-end. Most likely cause: stale build (app running pre-04-03 compiled code), test data mismatch, or HTTP client sending repeated query params instead of comma-separated values. Latent design weakness: if client sends ?filter:status:in=Scheduled&filter:status:in=Confirmed (repeated key), parser line 34 discards all but first array element."
  artifacts:
    - path: "trade-flow-api/src/core/filters/parse-structured-filters.utility.ts"
      issue: "Line 34 Array.isArray check discards repeated query param values - robustness improvement for in operator"
  missing:
    - "Array-join improvement: Array.isArray(rawValue) ? rawValue.join(',') : rawValue to handle both formats"
  debug_session: ".planning/debug/status-in-filter-broken.md"
