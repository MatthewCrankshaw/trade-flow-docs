---
phase: 14-quote-detail-and-line-items
plan: 05
type: execute
wave: 1
depends_on: ["14-04"]
files_modified:
  - trade-flow-ui/src/features/quotes/components/QuoteLineItemsTable.tsx
  - trade-flow-ui/src/features/quotes/components/QuoteLineItemsCardList.tsx
  - trade-flow-ui/src/types/quote.ts
autonomous: true
requirements: [QLIT-03, QLIT-04]
gap_closure: true

must_haves:
  truths:
    - "Bundle component rows visually align with parent table columns in desktop view"
    - "Line total column shows tax-inclusive amounts for all line items"
    - "Component rows within expanded bundles show tax-inclusive line totals"
  artifacts:
    - path: "trade-flow-ui/src/features/quotes/components/QuoteLineItemsTable.tsx"
      provides: "Aligned component rows using table sub-rows, tax-inclusive line totals"
    - path: "trade-flow-ui/src/features/quotes/components/QuoteLineItemsCardList.tsx"
      provides: "Tax-inclusive line totals in mobile card view"
  key_links:
    - from: "QuoteLineItemsTable.tsx"
      to: "QuoteLineItem.lineTotal + taxRate"
      via: "inline computation: lineTotal * (1 + taxRate/100)"
      pattern: "lineTotal.*taxRate"
---

<objective>
Fix bundle component column alignment in desktop table view and display tax-inclusive line totals across both desktop and mobile views.

Purpose: UAT reported two display issues: (1) component rows don't align with parent table columns, and (2) line totals show pre-tax amounts when users expect tax-inclusive totals.
Output: Corrected QuoteLineItemsTable with aligned component sub-rows and tax-inclusive line total display in both Table and CardList views.
</objective>

<execution_context>
@/Users/mcrankshaw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mcrankshaw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-quote-detail-and-line-items/14-04-SUMMARY.md

<interfaces>
<!-- Key types the executor needs -->

From trade-flow-ui/src/types/quote.ts:
```typescript
export interface QuoteLineItem {
  id: string;
  itemId: string;
  quantity: number;
  unit: string;
  unitPrice: number;
  originalUnitPrice: number;
  lineTotal: number;        // pre-tax (qty * unitPrice)
  originalLineTotal: number;
  discountAmount: number;
  taxRate: number;           // percentage, e.g. 20 for 20%
  type: ItemType;
  status: string;
  components?: QuoteLineItem[];
}
```

From trade-flow-ui/src/hooks/useCurrency.ts:
```typescript
formatDecimal(value: number): string  // formats number to locale currency string
```

Current QuoteLineItemsTable component rows for bundles (lines 266-317): Uses a single `<TableRow>` with full-width `<TableCell colSpan={7|8}>` containing a `<div className="ml-6">` with CSS grid children. The grid has `gridTemplateColumns: "1fr auto 5rem auto 7rem auto auto"` which does not match the parent table's column widths.

The fix approach: Replace the CSS grid inside colSpan with actual `<TableRow>` elements for each component. This uses the browser's native table layout algorithm so columns automatically align with the header.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix bundle component alignment with table sub-rows and add tax-inclusive line totals</name>
  <files>trade-flow-ui/src/features/quotes/components/QuoteLineItemsTable.tsx, trade-flow-ui/src/features/quotes/components/QuoteLineItemsCardList.tsx</files>
  <action>
**QuoteLineItemsTable.tsx — Component alignment fix:**

Replace the current expanded bundle rendering (lines 266-317) which uses a single `<TableRow>` with a colSpan cell containing CSS grid divs. Instead, render each component as its own `<TableRow>` with proper `<TableCell>` elements matching the parent header columns (Item, Type, Qty, Unit, Unit Price, Tax %, Line Total, [action]).

For the component rows:
- First component row: Prepend a "Components (N)" label row using a `<TableRow className="bg-muted/30">` with a `<TableCell colSpan={isEditable ? 8 : 7}>` containing the "Components (N)" header text with `pl-10` indent.
- Each component: Render as `<TableRow className="bg-muted/30 hover:bg-muted/30">` with:
  - Item cell: `<TableCell className="pl-10 font-medium">{componentName}</TableCell>` (pl-10 gives visual indent without breaking grid)
  - Type cell: Badge with typeColors/typeLabels
  - Qty cell: `component.quantity` (read-only, no Input)
  - Unit cell: `component.unit`
  - Unit Price cell: `formatDecimal(component.unitPrice)`
  - Tax % cell: `{component.taxRate}%`
  - Line Total cell: `formatDecimal(component.lineTotal * (1 + component.taxRate / 100))` right-aligned
  - Action cell (if isEditable): empty `<TableCell />`

Remove the old CSS grid approach entirely (the `<div className="ml-6 space-y-2">` block with `gridTemplateColumns`).

**QuoteLineItemsTable.tsx — Tax-inclusive line totals for parent rows:**

On line 248, change `formatDecimal(lineItem.lineTotal)` to `formatDecimal(lineItem.lineTotal * (1 + lineItem.taxRate / 100))`.

Create a helper at the top of the component function:
```typescript
const lineTotalIncTax = (item: QuoteLineItem) =>
  item.lineTotal * (1 + item.taxRate / 100);
```

Use `formatDecimal(lineTotalIncTax(lineItem))` for parent row line totals and `formatDecimal(lineTotalIncTax(component))` for component row line totals.

**Update the Line Total column header** from "Line Total" to "Total (inc. tax)" to make it clear.

**QuoteLineItemsCardList.tsx — Tax-inclusive line totals:**

Add the same `lineTotalIncTax` helper. Update:
- Line 241: `formatDecimal(lineItem.lineTotal)` to `formatDecimal(lineTotalIncTax(lineItem))`
- Line 292: `formatDecimal(component.lineTotal)` to `formatDecimal(lineTotalIncTax(component))`
  </action>
  <verify>
    <automated>cd /Users/mcrankshaw/PersonalProjects/trade-flow-ui && npm run lint && npm run typecheck</automated>
  </verify>
  <done>Bundle component rows render as proper table rows aligned with parent columns. All line total values display tax-inclusive amounts. No CSS grid workaround remains for component rows.</done>
</task>

</tasks>

<verification>
- `cd trade-flow-ui && npm run lint && npm run typecheck` passes
- No remaining `gridTemplateColumns` usage for component rows in QuoteLineItemsTable.tsx
- All `lineTotal` display sites use the `lineTotalIncTax` helper
</verification>

<success_criteria>
- Bundle component rows align with parent table header columns using native table layout
- Line Total column shows tax-inclusive amounts throughout (desktop table and mobile cards)
- Column header reads "Total (inc. tax)" to avoid ambiguity
</success_criteria>

<output>
After completion, create `.planning/phases/14-quote-detail-and-line-items/14-05-SUMMARY.md`
</output>
