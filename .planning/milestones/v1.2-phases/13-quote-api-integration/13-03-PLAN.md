---
phase: 13-quote-api-integration
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - trade-flow-ui/src/features/quotes/components/CreateQuoteDialog.tsx
  - trade-flow-ui/src/features/quotes/components/QuoteActionStrip.tsx
  - trade-flow-ui/src/features/quotes/components/index.ts
  - trade-flow-ui/src/features/quotes/index.ts
  - trade-flow-ui/src/pages/QuoteDetailPage.tsx
  - trade-flow-ui/src/pages/QuotesPage.tsx
  - trade-flow-ui/src/features/jobs/components/JobDetailTabs.tsx
  - trade-flow-ui/src/pages/JobDetailPage.tsx
  - trade-flow-ui/src/App.tsx
autonomous: true
requirements:
  - QUOT-01
  - QUOT-04

must_haves:
  truths:
    - "User can create a new quote linked to a job and customer via a creation dialog"
    - "User can create a quote from the Quotes page (select customer then job) or from Job detail (pre-filled)"
    - "User can view a quote detail page showing header info and status"
    - "User can transition a quote through Draft->Sent->Accepted/Rejected via action buttons"
    - "Confirmation dialogs appear for irreversible transitions (Accept, Reject) but not for Send"
    - "Quotes in Sent status show a modified-since-last-sent indicator when updatedAt > sentAt"
  artifacts:
    - path: "trade-flow-ui/src/features/quotes/components/CreateQuoteDialog.tsx"
      provides: "Multi-context quote creation dialog"
      contains: "CreateQuoteDialog"
    - path: "trade-flow-ui/src/features/quotes/components/QuoteActionStrip.tsx"
      provides: "Contextual status transition buttons with modified indicator"
      contains: "QuoteActionStrip"
    - path: "trade-flow-ui/src/pages/QuoteDetailPage.tsx"
      provides: "Full quote detail page with header and action strip"
      contains: "QuoteDetailPage"
    - path: "trade-flow-ui/src/App.tsx"
      provides: "Route for /quotes/:quoteId"
      contains: "quoteId"
  key_links:
    - from: "trade-flow-ui/src/features/quotes/components/CreateQuoteDialog.tsx"
      to: "trade-flow-ui/src/features/quotes/api/quoteApi.ts"
      via: "useCreateQuoteMutation"
      pattern: "useCreateQuoteMutation"
    - from: "trade-flow-ui/src/features/quotes/components/QuoteActionStrip.tsx"
      to: "trade-flow-ui/src/features/quotes/api/quoteApi.ts"
      via: "useTransitionQuoteMutation"
      pattern: "useTransitionQuoteMutation"
    - from: "trade-flow-ui/src/pages/QuoteDetailPage.tsx"
      to: "trade-flow-ui/src/features/quotes/api/quoteApi.ts"
      via: "useGetQuoteQuery"
      pattern: "useGetQuoteQuery"
---

<objective>
Build the CreateQuoteDialog for quote creation from both contexts (Quotes page and Job detail), the QuoteDetailPage with QuoteActionStrip for status transitions, and wire routing. Includes "modified since last sent" indicator per user decision.

Purpose: Completes the quote creation and status management user flows. Users need to create quotes linked to jobs and transition them through their lifecycle.
Output: Working CreateQuoteDialog, QuoteDetailPage with action strip and status transitions, route wired in App.tsx.
</objective>

<execution_context>
@/Users/mcrankshaw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mcrankshaw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-quote-api-integration/13-CONTEXT.md
@.planning/phases/13-quote-api-integration/13-RESEARCH.md
@.planning/phases/13-quote-api-integration/13-01-SUMMARY.md
@.planning/phases/13-quote-api-integration/13-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 API and Plan 02 types -->

Quote type (from @/types/quote.ts, created in Plan 02):
```typescript
enum QuoteStatus { DRAFT = "draft", SENT = "sent", ACCEPTED = "accepted", REJECTED = "rejected", EXPIRED = "expired" }
interface Quote {
  id: string; businessId: string; customerId: string; jobId: string;
  number: string; title: string; notes?: string; status: QuoteStatus;
  quoteDate: string; sentAt?: string; acceptedAt?: string; rejectedAt?: string;
  updatedAt: string;
  customerName: string; jobTitle: string;
  totals: { subTotal: number; taxTotal: number; total: number; };
}
interface CreateQuoteRequest { customerId: string; jobId: string; quoteDate: string; title?: string; notes?: string; }
```

RTK Query hooks (from quoteApi.ts, created in Plan 02):
```typescript
useGetQuoteQuery(quoteId: string)
useCreateQuoteMutation()
useTransitionQuoteMutation()
```

Existing patterns:
```typescript
// CreateJobDialog pattern: Dialog + Form + Command/Popover pickers
// JobActionStrip pattern: contextual buttons based on status
// useCurrentBusiness(): { businessId, hasBusiness, user, isLoading }
// useBusinessCurrency(): { formatAmount(amount) }
// useGetJobsQuery(businessId): { data: Job[] }
// useGetCustomersQuery or useCustomersList({ businessId }): customer list
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the CreateQuoteDialog component and wire to both pages</name>
  <files>
    trade-flow-ui/src/features/quotes/components/CreateQuoteDialog.tsx,
    trade-flow-ui/src/features/quotes/components/index.ts,
    trade-flow-ui/src/features/quotes/index.ts,
    trade-flow-ui/src/pages/QuotesPage.tsx,
    trade-flow-ui/src/features/jobs/components/JobDetailTabs.tsx,
    trade-flow-ui/src/pages/JobDetailPage.tsx
  </files>
  <action>
  1. **CreateQuoteDialog.tsx** (NEW):
     - Props interface: `{ open: boolean; onOpenChange: (open: boolean) => void; prefilledJobId?: string; prefilledCustomerId?: string; prefilledJobTitle?: string; prefilledCustomerName?: string; businessId: string; }`.
     - When `prefilledJobId` is provided (created from Job detail): skip customer/job selection, show only reference number (auto-generated, editable), quote date (defaults to today via `DateTime.now().toISODate()`), and notes textarea.
     - When `prefilledJobId` is NOT provided (created from Quotes page): show a multi-step flow in a single dialog:
       a. **Customer picker**: Use Command+Popover pattern (like SearchableItemPicker). Fetch customers via `useCustomersList({ businessId })` or the customers RTK Query hook. Show searchable dropdown of active customers. On select, store customerId and customerName.
       b. **Job picker**: Once customer selected, fetch jobs via `useGetJobsQuery(businessId)` and filter client-side to that customer's jobs (`job.customerId === selectedCustomerId`). Show searchable dropdown. Include an "Add new job" option at the bottom that opens CreateJobDialog inline (import from `@/features/jobs`). On select or create, store jobId and jobTitle.
       c. **Details fields**: Reference number (auto-generated placeholder -- leave empty, server generates it; OR show "Auto-generated" disabled input), quote date (date input, defaults to today), notes (optional textarea).
     - Form validation with Valibot schema: `customerId` required string, `jobId` required string, `quoteDate` required string (ISO date format).
     - On submit: call `createQuote({ businessId, data: { customerId, jobId, quoteDate, notes } })` via `useCreateQuoteMutation`.
     - On success: show toast "Quote created successfully", close dialog, navigate to `/quotes/${result.id}` using `useNavigate`.
     - On error: show toast with error message.
     - Use React Hook Form + Valibot resolver following established dialog pattern.
     - Layout: shadcn Dialog with DialogHeader, DialogContent, DialogFooter. Cancel and Create buttons.

  2. **Component barrel** (index.ts): Export `CreateQuoteDialog`.

  3. **Feature barrel** (features/quotes/index.ts): Ensure CreateQuoteDialog is re-exported.

  4. **QuotesPage.tsx**: Wire the "New Quote" button to open CreateQuoteDialog.
     - Add state: `const [createOpen, setCreateOpen] = useState(false)`.
     - Button onClick: `() => setCreateOpen(true)`.
     - Render `<CreateQuoteDialog open={createOpen} onOpenChange={setCreateOpen} businessId={businessId!} />` (no prefilled props = Quotes page context).

  5. **JobDetailPage.tsx**: Wire the quotes tab "Create Quote" action to open CreateQuoteDialog.
     - Add state: `const [createQuoteOpen, setCreateQuoteOpen] = useState(false)`.
     - Replace the `handleCreateQuote` placeholder with `() => setCreateQuoteOpen(true)`.
     - Pass `onCreateQuote={() => setCreateQuoteOpen(true)}` to `<JobDetailTabs>`.
     - Render `<CreateQuoteDialog open={createQuoteOpen} onOpenChange={setCreateQuoteOpen} businessId={businessId!} prefilledJobId={jobId!} prefilledCustomerId={job?.customerId} prefilledJobTitle={job?.title} prefilledCustomerName={/* from job data or mock customer */} />`.

  6. **JobDetailTabs.tsx**: The `onCreateQuote` prop was added in Plan 02 Task 3 -- no structural changes needed. Just confirm it's wired to the TabHeader "Add" button and empty state CTA.

  Per CONTEXT.md locked decision: "Inline job creation within the quote creation dialog should use the same pattern as the existing CreateJobDialog." Import and render `CreateJobDialog` when the user clicks "Add new job" in the job picker step.
  </action>
  <verify>
    <automated>cd /Users/mcrankshaw/PersonalProjects/trade-flow-ui && npm run lint && npm run typecheck</automated>
  </verify>
  <done>
    - CreateQuoteDialog renders with customer/job pickers when opened from Quotes page
    - CreateQuoteDialog shows pre-filled fields when opened from Job detail
    - Quote date defaults to today, is editable
    - Submit calls createQuote mutation and navigates to new quote detail page
    - "New Quote" button on QuotesPage opens the dialog
    - Job detail quotes tab Add button opens the dialog with pre-filled job/customer
    - lint and typecheck pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create QuoteDetailPage with QuoteActionStrip, modified indicator, and route</name>
  <files>
    trade-flow-ui/src/features/quotes/components/QuoteActionStrip.tsx,
    trade-flow-ui/src/features/quotes/components/index.ts,
    trade-flow-ui/src/pages/QuoteDetailPage.tsx,
    trade-flow-ui/src/App.tsx
  </files>
  <action>
  1. **QuoteActionStrip.tsx** (NEW):
     - Props: `{ quote: Quote; businessId: string; }`.
     - Uses `useTransitionQuoteMutation` from quoteApi.
     - Renders contextual buttons based on `quote.status`:
       a. **Draft**: "Send" button (Send icon from lucide-react). On click, immediately call `transitionQuote({ businessId, quoteId: quote.id, status: QuoteStatus.SENT })`. No confirmation dialog (per CONTEXT.md: "Send is immediate"). Show success toast "Quote sent".
       b. **Sent**: "Mark Accepted" button (Check icon) + "Mark Rejected" button (X icon). Both require confirmation dialog (per CONTEXT.md: "Confirmation dialogs only for irreversible transitions"). Use AlertDialog from shadcn. "Mark Accepted" dialog: "Are you sure you want to mark this quote as accepted? This cannot be undone." "Mark Rejected" dialog: "Are you sure you want to mark this quote as rejected? This cannot be undone." Also show "Re-send" button (Send icon) for re-sending -- this is immediate (no confirmation since it's reversible in the sense you can re-send again).
       c. **Accepted/Rejected**: No action buttons (terminal states). Optionally show a status message like "This quote has been {status}."
     - **"Modified since last sent" indicator** (per user decision): When quote is in Sent status and both `quote.sentAt` and `quote.updatedAt` exist, compare the timestamps: if `new Date(quote.updatedAt) > new Date(quote.sentAt)`, show a warning badge/text such as "Modified since last sent" with an amber/yellow info icon. This indicates the quote content has changed since it was last sent to the customer, prompting the user to re-send. When the user clicks "Re-send", the transition sets a new sentAt which clears the indicator on next fetch.
     - Layout: horizontal button strip, matching JobActionStrip pattern. Use `flex items-center gap-2`.

  2. **Component barrel** (index.ts): Export `QuoteActionStrip`.

  3. **QuoteDetailPage.tsx** (NEW: trade-flow-ui/src/pages/QuoteDetailPage.tsx):
     - Route param: `const { quoteId } = useParams<{ quoteId: string }>()`.
     - Fetch quote: `const { data: quote, isLoading, error } = useGetQuoteQuery(quoteId!)`.
     - Wrap in DashboardLayout (same pattern as all pages).
     - Content:
       a. **Back link**: Link to `/quotes` with left arrow icon ("Back to Quotes").
       b. **Header section**: Quote number as page title (e.g., "Q-2026-001"), status badge next to title. Below: customer name as clickable link (navigate to customer page if exists, or just display), job name as clickable link (`/jobs/${quote.jobId}`), quote date formatted.
       c. **QuoteActionStrip**: Rendered below the header. Shows contextual buttons and modified-since-sent indicator.
       d. **Content area**: Placeholder card for Phase 14 line items. Show a card with "Line items will appear here" or similar placeholder content. Include the totals display: subtotal, tax, total using `useBusinessCurrency().formatAmount()`.
     - Loading state: skeleton placeholder.
     - Error state: error message with back link.
     - Not found: if quote is undefined after loading, show "Quote not found".

  4. **App.tsx**: Add route `<Route path="/quotes/:quoteId" element={<QuoteDetailPage />} />` inside the authenticated routes, after the `/quotes` route. Import `QuoteDetailPage` from `@/pages/QuoteDetailPage`.

  Per CONTEXT.md: "Action strip at top of quote detail page with contextual buttons based on current status." The QuoteActionStrip goes between the header and content area.

  Per CONTEXT.md: "Quote detail page shows job name as a clickable link to the job detail page." Render job name as `<Link to={`/jobs/${quote.jobId}`}>{quote.jobTitle}</Link>`.

  Per CONTEXT.md locked decision: "Quotes can be edited after being sent -- they stay in Sent status but show a 'modified since last sent' indicator in the UI." Implemented via updatedAt > sentAt comparison in QuoteActionStrip.
  </action>
  <verify>
    <automated>cd /Users/mcrankshaw/PersonalProjects/trade-flow-ui && npm run lint && npm run typecheck</automated>
  </verify>
  <done>
    - QuoteDetailPage renders at /quotes/:quoteId with quote header info
    - QuoteActionStrip shows "Send" for Draft, "Mark Accepted"/"Mark Rejected"/"Re-send" for Sent
    - Send transition is immediate (no confirmation)
    - Accept/Reject transitions show AlertDialog confirmation
    - "Modified since last sent" indicator appears when updatedAt > sentAt on Sent quotes
    - Job name is a clickable link to /jobs/:jobId
    - Route registered in App.tsx
    - Phase 14 line items area has placeholder
    - lint and typecheck pass
  </done>
</task>

</tasks>

<verification>
- `cd trade-flow-ui && npm run lint && npm run typecheck` passes
- CreateQuoteDialog can be opened from Quotes page and Job detail page
- QuoteDetailPage accessible at /quotes/:quoteId
- Status transitions work: Draft->Sent (immediate), Sent->Accepted (confirmed), Sent->Rejected (confirmed)
- "Modified since last sent" indicator shows when quote content changed after sending
</verification>

<success_criteria>
- User can create a quote from Quotes page by selecting customer then job, with auto-generated reference number
- User can create a quote from Job detail page with customer/job pre-filled
- User can view quote detail page showing reference number, customer, job (linked), status, date, totals
- User can transition Draft->Sent with one click, Sent->Accepted/Rejected with confirmation dialog
- Sent quotes that have been modified show a "modified since last sent" indicator
- All routes and navigation work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/13-quote-api-integration/13-03-SUMMARY.md`
</output>
