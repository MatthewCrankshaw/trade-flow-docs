---
phase: quick
plan: 260407-rp3
type: execute
wave: 1
depends_on: []
files_modified:
  # API
  - trade-flow-api/package.json
  - trade-flow-api/src/quote/test/services/quote-email-sender.service.spec.ts
  # UI
  - trade-flow-ui/package.json
  - trade-flow-ui/.prettierrc
  - trade-flow-ui/src/**/*.{ts,tsx}
  # Docs
  - CLAUDE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "All API unit tests pass (386/386)"
    - "All UI unit tests pass (69/69)"
    - "API eslint runs without crashes and reports zero errors"
    - "UI eslint runs without errors"
    - "API prettier check passes on all files"
    - "UI prettier check passes on all files"
    - "Both repos have a npm run ci script that gates deployment"
    - "CLAUDE.md documents CI gate requirement for all phases/milestones"
  artifacts:
    - path: "trade-flow-api/package.json"
      provides: "Fixed ajv override, added format:check and ci scripts"
    - path: "trade-flow-ui/package.json"
      provides: "Added prettier, format scripts, and ci script"
    - path: "trade-flow-ui/.prettierrc"
      provides: "Prettier config matching API conventions"
    - path: "CLAUDE.md"
      provides: "CI gate documentation"
  key_links:
    - from: "trade-flow-api/package.json ci script"
      to: "Railway build"
      via: "npm run ci && npm run build"
      pattern: "ci.*test.*lint.*format.*typecheck"
---

<objective>
Fix all failing checks (tests, linting, formatting) in both trade-flow-api and trade-flow-ui, add CI gate scripts to both repos, and document the deployment gate policy in CLAUDE.md.

Purpose: Ensure code quality gates are enforced before any deployment, preventing broken code from reaching production.
Output: Both repos passing all checks, CI gate scripts, updated CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@trade-flow-api/package.json
@trade-flow-ui/package.json
@trade-flow-api/eslint.config.mjs
@trade-flow-api/.prettierrc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix API checks -- failing tests, broken eslint, prettier formatting</name>
  <files>
    trade-flow-api/package.json
    trade-flow-api/src/quote/test/services/quote-email-sender.service.spec.ts
    trade-flow-api/src/**/*.ts (prettier formatting -- 10 files)
  </files>
  <action>
Fix three categories of issues in trade-flow-api:

**1. Fix failing tests (6 failures in quote-email-sender.service.spec.ts):**
The `QuoteEmailSender` service now has an `OnboardingProgressUpdater` dependency at constructor index [10] that the test module does not provide. Open `trade-flow-api/src/quote/test/services/quote-email-sender.service.spec.ts` and add a mock provider for `OnboardingProgressUpdater` to the `Test.createTestingModule` providers array, following the same pattern as other mocked services in that file (use `{ provide: OnboardingProgressUpdater, useValue: { update: jest.fn() } }` or similar based on the methods called).

**2. Fix ESLint crash (ajv version conflict):**
The `"ajv": "8.18.0"` entry in `package.json` `overrides` forces ajv v8 globally, but `@eslint/eslintrc` requires ajv v6. Remove the `"ajv": "8.18.0"` line from the `overrides` object in `package.json`. Then run `npm install` to restore the correct ajv version for eslint. Verify `npm run lint:check` runs without crashing.

**3. Fix prettier formatting (10 files):**
Run `npm run format` (which runs `prettier --write "src/**/*.ts" "test/**/*.ts"`) to auto-fix all 10 files with formatting issues.

**4. Add CI gate scripts to package.json:**
Add these scripts to `package.json`:
- `"format:check": "prettier --check \"src/**/*.ts\" \"test/**/*.ts\""`
- `"ci": "npm run test -- --passWithNoTests && npm run lint:check && npm run format:check && npm run typecheck"`

The `ci` script runs all quality gates in sequence: tests, lint, format check, typecheck.
  </action>
  <verify>
    <automated>cd trade-flow-api && npm run ci</automated>
  </verify>
  <done>
    - All 386 tests pass (0 failures)
    - ESLint runs without crashes and reports 0 errors
    - Prettier check passes on all files
    - Typecheck passes
    - `npm run ci` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix UI checks -- install prettier, fix lint errors, format all files</name>
  <files>
    trade-flow-ui/package.json
    trade-flow-ui/.prettierrc
    trade-flow-ui/src/**/*.{ts,tsx} (prettier formatting -- 102 files)
    trade-flow-ui/src/features/onboarding/components/SetupLoadingScreen.tsx
    trade-flow-ui/src/lib/__tests__/utils.test.ts
  </files>
  <action>
Fix all quality check issues in trade-flow-ui:

**1. Install prettier and add config:**
Run `cd trade-flow-ui && npm install --save-dev prettier`. Create `.prettierrc` matching the API config:
```json
{
  "semi": true,
  "trailingComma": "all",
  "singleQuote": false,
  "printWidth": 125,
  "tabWidth": 2,
  "useTabs": false,
  "bracketSpacing": true,
  "arrowParens": "always",
  "endOfLine": "lf",
  "quoteProps": "as-needed"
}
```

**2. Add format scripts to package.json:**
Add these scripts:
- `"format": "prettier --write \"src/**/*.{ts,tsx}\""`
- `"format:check": "prettier --check \"src/**/*.{ts,tsx}\""`
- `"ci": "npm run test && npm run lint && npm run format:check && npm run typecheck"`

**3. Fix 6 lint errors:**
- `src/features/onboarding/components/SetupLoadingScreen.tsx` (line 94): The `react-hooks/set-state-in-effect` error. The `runSetup()` function calls setState within a useEffect. Refactor: move the `runSetup` call pattern to avoid the lint rule violation -- wrap in a self-invoking async IIFE or extract to a separate function that is not flagged, or restructure the effect to not directly call a function that synchronously sets state. Look at the actual code to determine the best fix. If the pattern is genuinely needed (async setup with loading states), the most likely fix is to ensure the setState calls happen in async callbacks (after awaits), which should satisfy the rule.
- `src/lib/__tests__/utils.test.ts` (line 17): `no-constant-binary-expression` error -- a `&&` expression has a constant truthy left-hand side. Fix the test assertion to not use a constant truthiness check.
- Fix any remaining warnings that are classified as errors.

**4. Format all 102 files:**
Run `npm run format` to auto-fix all prettier formatting issues.

**5. Verify all checks pass:**
Run `npm run ci` to confirm tests + lint + format + typecheck all pass.
  </action>
  <verify>
    <automated>cd trade-flow-ui && npm run ci</automated>
  </verify>
  <done>
    - All 69 tests pass
    - ESLint reports 0 errors
    - Prettier check passes on all files
    - Typecheck passes
    - `npm run ci` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Document CI gate policy in CLAUDE.md and update Railway build commands</name>
  <files>
    CLAUDE.md
  </files>
  <action>
**1. Add CI Gate Policy section to the root CLAUDE.md** (the docs repo CLAUDE.md, not the sub-repo ones).

Add a new section after the "Conventions" section (or at an appropriate location near the end of existing content) titled `## CI Gate Policy`. Content:

```markdown
## CI Gate Policy

All phases of development and milestones MUST have all quality checks passing before they can be deployed. This is enforced via the `npm run ci` script in both repositories.

### Quality Gates (both repos)

Every deployment must pass these checks in order:

1. **Unit tests** -- all tests must pass with zero failures
2. **Linting** -- ESLint must report zero errors
3. **Formatting** -- Prettier must report zero formatting issues
4. **Type checking** -- TypeScript compiler must report zero errors

### Commands

| Repository | CI Gate Command | Description |
|------------|----------------|-------------|
| trade-flow-api | `npm run ci` | Runs tests, lint:check, format:check, typecheck |
| trade-flow-ui | `npm run ci` | Runs tests, lint, format:check, typecheck |

### Enforcement Rules

- **Before merging any feature branch:** `npm run ci` must pass in both affected repos
- **Before deploying to Railway:** The build process should run `npm run ci` before `npm run build`
- **During phase execution:** Each plan's verification step must confirm `npm run ci` passes
- **Quick tasks:** Must not leave any repo with failing CI checks
- **No suppression:** Do not use `eslint-disable`, `@ts-ignore`, `@ts-expect-error`, or `@ts-nocheck` to bypass checks -- fix the root cause
```

**2. Update the trade-flow-api CLAUDE.md and trade-flow-ui CLAUDE.md** to add `npm run ci` to their Commands tables (if not already present).

In `trade-flow-api/CLAUDE.md`, add to the Commands table:
- `npm run ci` | Run all quality gates (test + lint + format + typecheck)
- `npm run format:check` | Check formatting without fixing

In `trade-flow-ui/CLAUDE.md`, add to the Commands table:
- `npm run ci` | Run all quality gates (test + lint + format + typecheck)
- `npm run format` | Auto-fix formatting
- `npm run format:check` | Check formatting without fixing
  </action>
  <verify>
    <automated>grep -q "CI Gate Policy" CLAUDE.md && grep -q "npm run ci" trade-flow-api/CLAUDE.md && grep -q "npm run ci" trade-flow-ui/CLAUDE.md && echo "PASS"</automated>
  </verify>
  <done>
    - Root CLAUDE.md contains CI Gate Policy section with enforcement rules
    - Both sub-repo CLAUDE.md files document `npm run ci` command
    - Policy clearly states all phases/milestones must pass CI before deployment
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- this is a developer tooling / CI configuration change only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | T (Tampering) | CI gate scripts | accept | Scripts run locally and in CI; no external attack surface. Developer must run `npm run ci` before deploy. |
</threat_model>

<verification>
1. `cd trade-flow-api && npm run ci` exits 0
2. `cd trade-flow-ui && npm run ci` exits 0
3. Root CLAUDE.md contains "CI Gate Policy" section
4. Both sub-repo CLAUDE.md files document `npm run ci`
</verification>

<success_criteria>
- Zero test failures across both repos (386 API + 69 UI = 455 total)
- Zero lint errors across both repos
- Zero prettier formatting issues across both repos
- Zero typecheck errors across both repos
- `npm run ci` script exists and passes in both repos
- CLAUDE.md documents that all development phases and milestones must pass CI before deployment
</success_criteria>

<output>
After completion, create `.planning/quick/260407-rp3-ensure-api-and-ui-have-passing-unit-test/260407-rp3-SUMMARY.md`
</output>
