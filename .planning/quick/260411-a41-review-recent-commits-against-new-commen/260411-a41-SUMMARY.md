---
id: 260411-a41
name: Review recent commits against new comments code standard and refactor
mode: quick
created: 2026-04-11
completed: 2026-04-11
---

# Summary: Apply new comments standard to recent commits

## Context

In the previous session, the user codified a comments code standard into every
`CLAUDE.md` (root + both sub-repos) and asked to audit the last few commits of each
project against it.

## Standard applied

- Prefer self-documenting code over comments; rename/extract before commenting.
- Acceptable: legal headers, `TODO` with tickets, non-obvious consequences, intent
  clarification.
- Never commit commented-out code.
- Never write comments that restate what the code does.

## Comments removed

### trade-flow-api

- **`auth.guard.ts`**: the 3-line null-name fallback explanation collapsed to 1 line.
  Kept because the null sentinel drives a non-obvious UI branch (onboarding step gating).
- **`is-mongo-duplicate-key-error.utility.ts`**: dropped the opening sentence that
  restated the function name and the closing paragraph that speculated about future
  evolution. Kept a short paragraph explaining when services should use it.
- **`subscription-trial-creator.service.ts`**:
  - 14-line JSDoc block collapsed to 4 lines. Kept the essential non-obvious behavior
    (idempotent, race-safe, orphan Stripe cleanup).
  - Dropped "A concurrent call won the race..." — redundant with the adjacent
    `logger.warn("Duplicate subscription row detected...")`.
  - Dropped the 6-line orphan Stripe customer cleanup rationale — the method name
    `deleteOrphanStripeCustomer` and its internal `.error` log ("manual cleanup
    required") already carry the intent.
  - Dropped "Extremely unlikely: 11000 was thrown but no matching row..." — the
    `InternalServerError` message next to it says exactly that.
- **`subscription-trial-creator.service.spec.ts`**: dropped the assertion-explanation
  comment above `expect(mockStripe.customers.del)` — the test name and the assertion
  are self-evident.

### trade-flow-ui

- **`SetupLoadingScreen.tsx`**:
  - Module-scope `executeSetup` rationale: 5 lines → 2 lines. Kept the essential warning
    (inlining as a useCallback breaks the `set-state-in-effect` lint rule).
  - Dropped "Check if business already exists (for retry after partial completion per
    D-18)" — `existingBusiness` on the next line is a clear variable name.
  - 422 defensive fallback rationale: 4 lines → 2 lines.
  - "Wait for User and Business caches" race explanation: 6 lines → 4 lines, keeping
    the framework-gotcha essence (`isLoading` vs `isFetching`).
  - Dropped "Backend said the business was created but the user record still has no
    businessRole..." — restated the `if (!hasBusinessRole)` branch directly below.
  - `hasRunRef` StrictMode rationale: 6 lines → 3 lines.
- **`SetupLoadingScreen.test.tsx`**:
  - Dropped the `// Mock react-router-dom navigate` and `// Mock toast` one-liners that
    restated the following `vi.mock` blocks.
  - Dropped the 8-line body comment in the StrictMode regression test — its test name
    already carries "regression: duplicate business creation".

## Comments intentionally kept

- `// Stripe v21+: current_period_end is at item level, not subscription level` —
  pre-existing and a legitimate third-party quirk warning.
- The Arrange/Act/Assert comments in the spec file — project-wide convention, would
  introduce inconsistency if removed from one file.
- The chained-mock "first returns null, second returns the row" comments in the spec
  file — legitimately useful for reading chained-mock tests.
- Pre-existing comments in `auth.guard.ts` that were not part of the recent commit
  under review (stayed in scope of "last few commits").

## Quality gates

**trade-flow-api `npm run ci`**: PASSED
- 390 tests, 0 errors, 24 pre-existing warnings, prettier/typecheck clean.

**trade-flow-ui `npm run ci`**: PASSED
- 72 tests, 0 errors, 1 pre-existing BusinessStep warning, prettier/typecheck clean.

## Files touched

- `trade-flow-api/src/auth/auth.guard.ts`
- `trade-flow-api/src/core/errors/is-mongo-duplicate-key-error.utility.ts`
- `trade-flow-api/src/subscription/services/subscription-trial-creator.service.ts`
- `trade-flow-api/src/subscription/test/services/subscription-trial-creator.service.spec.ts`
- `trade-flow-ui/src/features/onboarding/components/SetupLoadingScreen.tsx`
- `trade-flow-ui/src/features/onboarding/components/__tests__/SetupLoadingScreen.test.tsx`
- `.planning/quick/260411-a41-review-recent-commits-against-new-commen/260411-a41-PLAN.md`
- `.planning/quick/260411-a41-review-recent-commits-against-new-commen/260411-a41-SUMMARY.md` (this file)

## Commit strategy

One comment-tightening commit per code repo (API, UI) plus one docs commit in
trade-flow-docs. Pure comment refactor — no behavioral changes.
