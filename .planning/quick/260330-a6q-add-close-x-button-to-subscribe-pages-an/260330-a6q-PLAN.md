---
phase: quick
plan: 260330-a6q
type: execute
wave: 1
depends_on: []
files_modified:
  - trade-flow-ui/src/pages/SubscribePage.tsx
  - trade-flow-ui/src/pages/SubscribeSuccessPage.tsx
  - trade-flow-ui/src/pages/SubscribeCancelPage.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "All three subscribe pages show a close X button in the top right"
    - "Clicking the X button navigates back to the Trade Flow dashboard"
    - "Page headings are visually centered and aligned cleanly"
  artifacts:
    - path: "trade-flow-ui/src/pages/SubscribePage.tsx"
      provides: "Subscribe page with close button and centered heading"
    - path: "trade-flow-ui/src/pages/SubscribeSuccessPage.tsx"
      provides: "Success page with close button"
    - path: "trade-flow-ui/src/pages/SubscribeCancelPage.tsx"
      provides: "Cancel page with close button"
  key_links:
    - from: "X button"
      to: "/dashboard"
      via: "react-router-dom Link"
      pattern: "Link.*to.*dashboard"
---

<objective>
Add a close X button to all three subscribe pages (SubscribePage, SubscribeSuccessPage, SubscribeCancelPage) and fix heading alignment so content is clean and centered.

Purpose: These pages currently have no way to navigate back to the app without using the browser back button. A close button provides clear UX for returning to the dashboard.
Output: Three updated page components with consistent close button and aligned headings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@trade-flow-ui/src/pages/SubscribePage.tsx
@trade-flow-ui/src/pages/SubscribeSuccessPage.tsx
@trade-flow-ui/src/pages/SubscribeCancelPage.tsx
@trade-flow-ui/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add close X button and fix heading alignment on all three subscribe pages</name>
  <files>
    trade-flow-ui/src/pages/SubscribePage.tsx,
    trade-flow-ui/src/pages/SubscribeSuccessPage.tsx,
    trade-flow-ui/src/pages/SubscribeCancelPage.tsx
  </files>
  <action>
Add a close X button to the top-right corner of all three subscribe pages that navigates to `/dashboard`. Use the `X` icon from `lucide-react` (already imported in SubscribeCancelPage -- note: the cancel page uses X as its status icon, so use a different approach there or rename the import).

For each page, wrap the existing content in a `relative` container and add an absolutely-positioned close button in the top-right:

```tsx
<Link to="/dashboard" className="absolute right-0 top-0 rounded-sm opacity-70 ring-offset-background transition-opacity hover:opacity-100 focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2">
  <X className="h-5 w-5" />
  <span className="sr-only">Close</span>
</Link>
```

Specific changes per page:

**SubscribePage.tsx:**
- Add `import { X } from "lucide-react"` and `import { Link } from "react-router-dom"`
- Wrap the outer div with `relative` class
- Add the close X button as first child
- Center the heading block: change outer div to `flex min-h-[60vh] flex-col items-center justify-center` so the content is vertically and horizontally centered (matching the success/cancel page pattern)
- Keep the heading text left-aligned within its block but center the block itself using `text-center`

**SubscribeSuccessPage.tsx:**
- Import `X` from `lucide-react` (add to existing import)
- Add `relative` to the outermost container divs for each state (isPolling, isVerified, isTimedOut, initial loading)
- Add the close X button to the `isVerified` and `isTimedOut` states (not the loading/polling states -- user should wait for those)

**SubscribeCancelPage.tsx:**
- Already imports `X` from `lucide-react` as the cancel status icon. Rename that usage: `import { X as XIcon, X as CloseIcon } from "lucide-react"` -- actually just use `X` for both since it is the same icon. Keep the existing `X` import.
- Add `relative` to the outer container div
- Add the close X button (same `X` icon, different context -- close button is top-right, status icon is centered in content)
  </action>
  <verify>
    <automated>cd /Users/mattc/Documents/projects/agent/trade-flow-docs/trade-flow-ui && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - All three subscribe pages have a close X button in the top-right corner
    - Close button navigates to /dashboard via Link
    - SubscribePage content is vertically and horizontally centered
    - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Close X buttons on all subscribe pages and centered heading alignment on SubscribePage</what-built>
  <how-to-verify>
    1. Run `cd trade-flow-ui && npm run dev`
    2. Log in and navigate to /subscribe
    3. Verify: X button visible in top-right, heading is centered, clicking X goes to /dashboard
    4. Navigate to /subscribe/cancel
    5. Verify: X button in top-right (separate from the cancel status icon), clicking X goes to /dashboard
    6. Navigate to /subscribe/success (without session_id it will show loading, but check layout)
    7. Confirm consistent styling across all pages
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd trade-flow-ui && npx tsc --noEmit` passes
- `cd trade-flow-ui && npm run lint` passes
- All three pages render with close button
</verification>

<success_criteria>
- Close X button visible on all three subscribe pages
- Clicking X navigates to /dashboard
- SubscribePage heading is centered and clean
- No TypeScript or lint errors
</success_criteria>

<output>
After completion, create `.planning/quick/260330-a6q-add-close-x-button-to-subscribe-pages-an/260330-a6q-SUMMARY.md`
</output>
